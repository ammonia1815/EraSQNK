@SET_CONFIGURE
LOCAL:207 = FLAG:207
LOCAL:239 = FLAG:239
CALL SET_CONFIGURE_MAIN
;컨피그に関する설정로드
IF FLAG:207 && FLAG:207 != LOCAL:207
	FOR LOCAL, 1, CHARANUM
		CALL SETFLAG, "음모と겨털", LOCAL
	NEXT
ENDIF
IF CONFIG("３사이즈") && FLAG:239 != LOCAL:239
	;거유の컵の定義を変化した時
	IF GETBITS(FLAG:239, 1, 3) != GETBITS(LOCAL:239, 1, 3)
		CALL SETFLAG, "바스트測りなおし"
	ENDIF
	FOR LOCAL, 1, CHARANUM
		CALL SET_3SIZE, LOCAL
	NEXT
ENDIF


@SET_CONFIGURE_MAIN
PRINTL 
PRINTFORML 현재의 설정은 다음과 같습니다. 변경하고 싶은 내용을 선택해주세요
PRINTL 
PRINTFORML  [ 0] 조교신 전반에 깊게 관련된 설정
PRINTFORML  [ 1] 시스템 및 이벤트 관련 설정
PRINTFORML  [ 2] 임신 시스템과 관련된 설정（임신패치 사용상황：\@ CONFIG("임신패치") ? ○ # －\@）
IF CONFIG("ユニコード")
	PRINTFORML  [ 3] 확장 UNICODE 사용 설정：%UNI("드래곤")% ⬅ 드래곤 마크가 정상적으로 표시됩니다.
ELSE
	PRINTFORML  [ 3] 확장 UNICODE 사용 설정：%UNI("드래곤")% ⬅ 드래곤 마크 대신 한자를 사용합니다
ENDIF
IF CONFIG("EmueraTunes")
	PRINTFORM  [ 4] 보이스설정：
	IF FLAG:500 == 1
		PRINTFORML 로리
	ELSEIF FLAG:500 == 2
		PRINTFORML 성년
	ELSEIF FLAG:500 == 3
		PRINTFORML 상대에 맞춰서 자동설정
	ELSE
		PRINTFORML 표준
	ENDIF
ENDIF
PRINTFORML  [10] 컨피그 세이브
;グローバル変数の読み込み
LOADGLOBAL
SIF GLOBAL:99 > 0
	PRINTFORML  [20] 컨피그 로드（기록일:{GLOBAL:99/10000}년 {(GLOBAL:99%10000)/100}월 {GLOBAL:99%100}일）
PRINTFORML  [30] 이름이나 주인과 이야기 할 때의 1인칭, 2인칭의 변경.

SIF CHARANUM <= 3
	SETCOLOR DEF_COLOR("회색")
PRINTFORML  [40] 정렬
RESETCOLOR

;たった一度の変更いろいろ
SIF GETBIT(FLAG:11, 1) == 0
	PRINTFORML  [90] 주인의 외견연령 기준치를 변경한다 (한번뿐입니다)
SIF GETBIT(FLAG:11, 2) == 0
	PRINTFORML  [91] 모든 사람의 유두 상세를 변경한다 (한번뿐입니다)

PRINTFORML [100] 돌아간다

$INPUT_LOOP
INPUT

IF RESULT == 100
	RETURN 0
ELSEIF RESULT == 0
	CALL SETCONFIG, "조교全般"
ELSEIF RESULT == 1
	CALL SETCONFIG, "システム＆이벤트全般"
ELSEIF RESULT == 2
	CALL SETCONFIG, "임신"
ELSEIF RESULT == 3
	INVERTBIT FLAG:279, 0
ELSEIF RESULT == 4 && CONFIG("EmueraTunes")
	CALL SETCONFIG, "ボイス"
ELSEIF RESULT == 10
	CALL SAVE_CONFIGURE
ELSEIF RESULT == 20 && GLOBAL:99 > 0
	CALL LOAD_CONFIGURE
ELSEIF RESULT == 30
	CALL NAME_CONVERT, 0, 0
ELSEIF RESULT == 40
	IF CHARANUM <= 3
		PRINTFORMW 정렬할 수 있는 캐릭터가 없습니다
	ELSE
		CALL CHARA_SORT
	ENDIF
ELSEIF RESULT == 90
	IF GETBIT(FLAG:11, 1) == 0
		PRINTFORML 외견연령의 사양이 변경됨에 따라, 한 번뿐이지만 주인의 것을 변경할 수 있습니다.
		IF TALENT:MASTER:오토코노코
			CALL PRINT_STR, "황색_다만, 오토코노코는[ 2]나 [ 3]을 선택해도, 오토코노코인 한 외견은 소년입니다_L"
			CALL PRINT_STR, "황색_물론 [ 0]을 선택하면 외모가 어린 오토코노코의 완성입니다_L"
		ENDIF
		PRINTFORML ……어느 것으로 하시겠습니까？
		PRINTL 
		PRINTFORML  [ 0] 아이
		PRINTFORML  [ 1] \@ TALENT:MASTER:남자 ? 소년 # 소녀 \@
		PRINTFORML  [ 2] \@ TALENT:MASTER:남자 ? 청년 # 누님 \@
		PRINTFORML  [ 3] \@ TALENT:MASTER:남자 ? 어른 # 숙녀\@
		PRINTFORML [100] 다시 생각한다

		CALL INPUT_SELECT, 4, 100

		SELECTCASE RESULT
		CASE 100
		CASEELSE
			SELECTCASE RESULT
			CASE 0
				PRINTFORMW %CALLNAME:MASTER%의 외견연령을 [아이]로 합니다
			CASE 1
				PRINTFORMW %CALLNAME:MASTER%의 외견연령을 [\@ TALENT:MASTER:남자 ? 소년]으로 # 소녀]로 \@ 합니다
			CASE 2
				PRINTFORMW %CALLNAME:MASTER%의 외견연령을 [\@ TALENT:MASTER:남자 ? 청년 # 누님 \@]으로 합니다
			CASE 3
				PRINTFORMW %CALLNAME:MASTER%의 외견연령을 [\@ TALENT:MASTER:남자 ? 어른]으로 # 숙녀]로 \@ 합니다
			ENDSELECT
			TALENT:MASTER:외견연령 = RESULT + 1
			SETBIT FLAG:11, 1
		ENDSELECT
	ENDIF
ELSEIF RESULT == 91
	IF GETBIT(FLAG:11, 2) == 0
		PRINTFORML 유두의 상세 묘사법의 추가에 따라 한 번뿐이지만 모두 변경할 수 있습니다
		;1ページ目を表示
		CALL RESELECT_ERECT, 0, 0
	ENDIF
ELSE
	GOTO INPUT_LOOP
ENDIF

RESTART

@CONFIG(ARGS)
#FUNCTION
SELECTCASE ARGS
CASE "ＳＨＯＰ画面비표시"
	RETURNF FLAG:2

CASE "自動購入"
	RETURNF FLAG:9

CASE "조수への지시"
	RETURNF GETBIT(FLAG:45, 0)
CASE "조수の탐색능력표시"
	RETURNF GETBIT(FLAG:45, 1)
CASE "탐색동행자선택中"
	RETURNF GETBIT(FLAG:45, 2)
CASE "능력표시で３사이즈閲覧"
	RETURNF FLAG:46
CASE "조교대상변경で애널라이즈"
	RETURNF GETBIT(FLAG:47, 0)
CASE "촉수설정"
	RETURNF FLAG:80 + FLAG:87
CASE "촉수は주인"
	RETURNF FLAG:87
CASE "사정箇所の변경"
	RETURNF FLAG:91
CASE "사정後に抜かない"
	RETURNF GETBIT(FLAG:91, 1)
CASE "画面표시"
	RETURNF FLAG:98
CASE "画面표시：カラーバー"
	RETURNF GETBIT(FLAG:98, 0)
CASE "画面표시：未実行커맨드の色"
	RETURNF GETBIT(FLAG:98, 1)
CASE "画面표시：파라미터４列"
	RETURNF GETBIT(FLAG:98, 2)
CASE "画面표시：조교커맨드４列"
	RETURNF GETBIT(FLAG:98, 3)
CASE "画面표시：バー背景없음"
	RETURNF GETBIT(FLAG:98, 4)
CASE "画面상성：バーのグラフ표시"
	RETURNF GETBIT(FLAG:98, 5)
CASE "画面표시：요의게이지"
	RETURNF GETBIT(FLAG:98, 6)
CASE "画面표시：배설욕게이지"
	RETURNF GETBIT(FLAG:98, 7)

CASE "갈아입히기설정"
	RETURNF FLAG:200+FLAG:201+FLAG:202+FLAG:203+FLAG:204
CASE "조교종료時の복장"
	RETURNF FLAG:200
CASE "신발やグローブの표기"
	RETURNF FLAG:201
CASE "室内や屋外での신발の扱い"
	RETURNF FLAG:202
CASE "팬티を몽마の好きにさせる"
	RETURNF FLAG:203
CASE "衣装の自動脱ぎの금지"
	RETURNF GETBIT(FLAG:204, 0)
CASE "衣装の細かい色표시の금지"
	RETURNF GETBIT(FLAG:204, 1)
CASE "下着が見えない服装時の표시の제한"
	RETURNF GETBIT(FLAG:204, 2)
CASE "팬티と브래지어を合わせる"
	RETURNF GETBIT(FLAG:204, 3)

CASE "일상"
	RETURNF FLAG:205
CASE "自動喘ぎ"
	RETURNF FLAG:206
CASE "음모と겨털", "음모"
	RETURNF FLAG:207
CASE "겨털"
	RETURNF GETBIT(FLAG:207, 10)
CASE "조교필터"
	RETURNF FLAG:209
CASE "Ｖ조교필터"
	RETURNF GETBIT(FLAG:209, 0)
CASE "Ａ조교필터"
	RETURNF GETBIT(FLAG:209, 1)

CASE "실행치필터"
	RETURNF GETBIT(FLAG:209, 4)
CASE "脱동정필터"
	RETURNF GETBIT(FLAG:209, 5)
CASE "脱처녀필터"
	RETURNF GETBIT(FLAG:209, 6)
CASE "페니스밴드필터"
	RETURNF GETBIT(FLAG:209, 7)

;조교필터関連
CASE "애무系비표시"
	RETURNF GETBIT(FLAG:210, 0)
CASE "도구系비표시"
	RETURNF GETBIT(FLAG:210, 1)
CASE "Ｖ섹스系비표시"
	RETURNF GETBIT(FLAG:210, 2)
CASE "Ａ섹스系비표시"
	RETURNF GETBIT(FLAG:210, 3)
CASE "특수섹스系비표시"
	RETURNF GETBIT(FLAG:210, 4)
CASE "봉사系비표시"
	RETURNF GETBIT(FLAG:210, 5)
CASE "조교자に삽입系비표시"
	RETURNF GETBIT(FLAG:210, 6)
CASE "ＳＭ・하드코어系비표시"
	RETURNF GETBIT(FLAG:210, 7)
CASE "조수系비표시"
	RETURNF GETBIT(FLAG:210, 8)
CASE "특수系비표시"
	RETURNF GETBIT(FLAG:210, 9)
CASE "촉수系비표시"
	RETURNF GETBIT(FLAG:210, 10)
CASE "아르카나系비표시"
	RETURNF GETBIT(FLAG:210, 11)
CASE "소비아이템系비표시"
	RETURNF GETBIT(FLAG:210, 12)
CASE "その他비표시"
	RETURNF GETBIT(FLAG:210, 13)

CASE "조교컨피그모드"
	RETURNF GETBIT(FLAG:210, 20)
CASE "비표시커맨드の표시"
	RETURNF GETBIT(FLAG:210, 21)

CASE "항목표시필터：Ｂ"
	RETURNF GETBIT(FLAG:210, 30)
CASE "항목표시필터：Ｃ"
	RETURNF GETBIT(FLAG:210, 31)

CASE "조교컨피그모드：비표시커맨드を숨김"
	SIF CONFIG("조교컨피그모드") == 0
		RETURNF 0
	RETURNF GETBIT(FLAG:210, 40)
CASE "조교컨피그모드：금지커맨드を숨김"
	SIF CONFIG("조교컨피그모드") == 0
		RETURNF 0
	RETURNF GETBIT(FLAG:210, 41)

CASE "조교컨피그모드：全커맨드표시"
	SIF CONFIG("조교컨피그모드") == 0
		RETURNF 0
	RETURNF GETBIT(FLAG:210, 45)

CASE "조교필터경험플래그"
	RETURNF GETBIT(FLAG:210, 60)

CASE "임신패치"
	RETURNF FLAG:211
CASE "임신패치：주인の精子のみ"
	RETURNF GETBIT(FLAG:211, 1)
CASE "임신패치：주인임신가능"
	RETURNF GETBIT(FLAG:211, 2)
CASE "임신패치：조교以外で임신가능"
	RETURNF GETBIT(FLAG:211, 3)
CASE "임신패치：딸の가슴の성장あり"
	RETURNF GETBIT(FLAG:211, 4)
CASE "임신패치：슬라임分裂"
	RETURNF GETBIT(FLAG:211, 5)
CASE "임신패치：受精확률표시"
	RETURNF GETBIT(FLAG:211, 6)

CASE "내부데이터の표시"
	RETURNF FLAG:217
CASE "조수상대でも연모適用"
	RETURNF FLAG:218
CASE "난이도"
	RETURNF FLAG:220
CASE "ＥＡＳＹ"
	SIF FLAG:220 == 1
		RETURNF 1
CASE "ＮＯＲＭＡＬ"
	SIF FLAG:220 == 0
		RETURNF 1
CASE "ＨＡＲＤ"
	SIF FLAG:220 == 2
		RETURNF 1
CASE "소환사고"
	RETURNF FLAG:221
CASE "소환사고：유니크"
	RETURNF GETBIT(FLAG:221, 1)
CASE "소환사고：후타나리"
	RETURNF GETBIT(FLAG:221, 2)
CASE "소환사고：오토코노코"
	RETURNF GETBIT(FLAG:221, 3)
CASE "우르즈군없이도대량사정"
	RETURNF FLAG:222
CASE "황홀상태"
	RETURNF FLAG:223
CASE "표시人数"
	RETURNF FLAG:225
CASE "대상の몽마化"
	RETURNF GETBIT(FLAG:226, 0)
CASE "대상の몽마化：男"
	RETURNF GETBIT(FLAG:226, 1)
CASE "대상の몽마化：여체화"
	RETURNF GETBIT(FLAG:226, 2)
CASE "주인・조수の몽마化"
	RETURNF GETBIT(FLAG:227, 0)
CASE "주인・조수の몽마化：男"
	RETURNF GETBIT(FLAG:227, 1)
CASE "주인・조수の몽마化：여체화"
	RETURNF GETBIT(FLAG:227, 2)
CASE "주인・조수の몽마化：캔슬可"
	RETURNF GETBIT(FLAG:227, 3)
CASE "비디오매각の선택"
	RETURNF FLAG:261
CASE "세대別표시"
	RETURNF FLAG:237
CASE "３사이즈"
	RETURNF FLAG:239
CASE "３사이즈：표시"
	RETURNF GETBIT(FLAG:239, 0)
CASE "거유보정"
	IF GETBIT(FLAG:239, 3)
		RETURNF 5
	ELSEIF GETBIT(FLAG:239, 2)
		RETURNF 2
	ELSE
		RETURNF 0
	ENDIF
CASE "거유보정：컵"
	IF GETBIT(FLAG:239, 3)
		RETURNF 2
	ELSEIF GETBIT(FLAG:239, 2)
		RETURNF 1
	ELSE
		RETURNF 0
	ENDIF
CASE "３사이즈：Ｆ"
	RETURNF GETBIT(FLAG:239, 1)
CASE "３사이즈：Ｇ"
	RETURNF GETBIT(FLAG:239, 2)
CASE "３사이즈：Ｈ"
	RETURNF GETBIT(FLAG:239, 3)
CASE "상사상애"
	RETURNF FLAG:241
CASE "상사상애：인수제한없음", "상사상애：인원수제한없음", "상사상애：인数제한없음"
	RETURNF GETBIT(FLAG:241, 0)
CASE "상사상애：요바이제한"
	RETURNF GETBIT(FLAG:241, 1)
CASE "몽마化時の特徴変化"
	RETURNF FLAG:242
CASE "몽마化時の特徴変化：外見のみ"
	SIF FLAG:242 == 1
		RETURNF 1
CASE "몽마化時の特徴変化：全部"
	SIF FLAG:242 == 2
		RETURNF 1
CASE "탐색ボス"
	RETURNF FLAG:245

CASE "레벨리미터"
	RETURNF FLAG:247
CASE "레벨리미터：－10ＬＶ"
	RETURNF GETBIT(FLAG:247, 11)
CASE "레벨리미터：－５ＬＶ"
	RETURNF GETBIT(FLAG:247, 12)
CASE "레벨리미터：상대に合わせる"
	RETURNF GETBIT(FLAG:247, 13)
CASE "레벨리미터：＋５ＬＶ"
	RETURNF GETBIT(FLAG:247, 14)
CASE "레벨리미터：＋10ＬＶ"
	RETURNF GETBIT(FLAG:247, 15)

CASE "경험표시詰め"
	RETURNF FLAG:250
CASE "연모と음란の任意획득"
	RETURNF FLAG:251
CASE "조교상대변경불가"
	RETURNF FLAG:252
CASE "성기상세표시"
	RETURNF GETBIT(FLAG:254, 0)
CASE "ＶＡ상세표시：수동ＬＶＵＰ"
	RETURNF GETBIT(FLAG:254, 1)

CASE "다방描写"
	SIF FLAG:258 == 0
		RETURNF 1
CASE "다방"
	RETURNF FLAG:258
CASE "寝取られ"
	RETURNF FLAG:262
CASE "페니스상세설정"
	RETURNF FLAG:263

CASE "제한시간철폐"
	RETURNF FLAG:265
CASE "날짜고정이벤트재현"
	RETURNF FLAG:266
CASE "니플퍽"
	RETURNF FLAG:267
CASE "정액관장"
	RETURNF FLAG:268
CASE "유니크캐릭터의다중소환"
	RETURNF FLAG:269
CASE "요바이아침기습금지"
	RETURNF FLAG:270

CASE "대사の名前비표시"
	RETURNF FLAG:273
CASE "하룻밤개방の이벤트全표시"
	RETURNF FLAG:274
CASE "온천여행에서의바람기"
	RETURNF FLAG:275
CASE "요도を확장하기쉬움"
	RETURNF FLAG:276
CASE "쇼타메이드"
	RETURNF FLAG:277
CASE "匂いの표기변경"
	RETURNF FLAG:278
CASE "ユニコード"
	RETURNF FLAG:279
CASE "성희롱금지"
	RETURNF FLAG:281
CASE "성희롱許可"
	SIF FLAG:281 == 0
	RETURNF 1
CASE "蓄精"
	RETURNF FLAG:282

CASE "유두の상세설정"
	RETURNF FLAG:283
CASE "유두비대"
	RETURNF GETBIT(FLAG:283, 0)
CASE "함몰유두"
	RETURNF GETBIT(FLAG:283, 1)
CASE "거유륜"
	RETURNF GETBIT(FLAG:283, 2)

CASE "地の文ＯＦＦ"
	SIF (FLAG:6 == 0 || TARGET && TCVAR:24)
		RETURNF 1
CASE "구상による地の文ＯＦＦ"
	SIF TARGET && TCVAR:24
		RETURNF 1

;이하は항목ONOFFボタン。キャラメイク中は例外的に全部表示させる
CASE "항목：搾精ランキング"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 0)
CASE "항목：조수のお仕事지시"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 1)
CASE "항목：출연作品詳細"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 2)
CASE "항목：身嗜み"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 3)
CASE "항목：服装詳細"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 4)
CASE "항목：体型"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 5)
CASE "항목：성벽"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 6)
CASE "항목：성감대"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 7)
CASE "항목：속성"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 8)
CASE "항목：宝구슬"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 9)
CASE "항목：경험"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 10)
CASE "항목：이상경험"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 11)
CASE "항목：능력"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 12)
CASE "항목：각인"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 13)
CASE "항목：소질"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 14)
CASE "항목：가족구성"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 15)
CASE "항목：상성"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 16)
CASE "항목：음모"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 17)
CASE "항목：악세사리"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 18)
CASE "항목：사정ランキング"
	SIF COND("キャラメイク中")
		RETURNF 0
	RETURNF GETBIT(FLAG:3, 19)

CASE "ボイス"
	RETURNF FLAG:500
CASE "EmueraTunes"
	RETURNF FLAG:501
CASEELSE
	THROW %GAMEBASE_YEAR%버젼 BUG : "%ARGS%" 를 찾을 수 없습니다
ENDSELECT
RETURNF 0

@SETCONFIG, ARGS
#DIM LCOUNT
#DIM MEMO_LINECOUNT
#DIM NEXT_LINE
#DIM MEMO, 10

MEMO_LINECOUNT = LINECOUNT
PRINTL 
SELECTCASE ARGS
CASE "조교全般"
	PRINTFORML  [ 0] \@ CONFIG("사정箇所の변경") ? ○ # －\@ 사정 장소의 변경
	PRINTFORML  [ 1] \@ CONFIG("우르즈군없이도대량사정") ? ○ # －\@ 우르즈군 없이도 초대량 사정
	PRINTFORML  [ 2] \@ CONFIG("황홀상태") ? ○ # －\@ 황홀상태일 때는 몽마가 원하는 대로 한다
	PRINTFORML  [ 3] \@ CONFIG("갈아입히기설정") ? ○ # －\@ 갈아입히기설정
	PRINTFORM  [ 4] \@ CONFIG("３사이즈") ? ○ # －\@ ３사이즈와 신장의 표시＆거유의 상한 설정：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("３사이즈：표시")
		PRINT 표시한다＆
		IF CONFIG("３사이즈：Ｈ")
			PRINTL Ｈ
		ELSEIF CONFIG("３사이즈：Ｇ")
			PRINTL Ｇ
		ELSE
			PRINTL Ｆ
		ENDIF
	ELSE
		PRINTL 표시하지 않는다
	ENDIF
	RESETCOLOR
	PRINTFORM  [ 5] \@ CONFIG("음모と겨털") ? ○ # －\@ 음모와 %TEXTS("겨드랑이")%털의 설정：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("음모と겨털")
		IF GETBIT(FLAG:207, 3)
			PRINT 어른만 난다
		ELSEIF GETBIT(FLAG:207, 2)
			PRINT 청년부터 난다
		ELSEIF GETBIT(FLAG:207, 1)
			PRINT 아이 이외에는 모두 난다
		ELSEIF GETBIT(FLAG:207, 0)
			PRINT 모두 난다
		ENDIF
		PRINTFORML ＆%TEXTS("겨드랑이")%털\@ CONFIG("겨털") ? 있음 # 없음 \@
	ELSE
		PRINTL 표시하지 않는다
	ENDIF
	RESETCOLOR
	PRINTFORML  [ 6] \@ CONFIG("페니스상세설정") ? ○ # －\@ 페니스의 상세 설정
	PRINTFORML  [ 7] \@ CONFIG("조수상대でも연모適用") ? ○ # －\@ %TEXTS("조수の명칭")% 조교중에 연모 구상을 사용한다
	PRINTFORML  [ 8] \@ CONFIG("寝取られ") ? ○ # －\@ 상사상애인 상대를 빼앗기는 이벤트의 발생
	PRINTFORML  [ 9] \@ CONFIG("촉수설정") ? ○ # －\@ 촉수설정
	PRINTFORML  [10] \@ CONFIG("내부데이터の표시") ? ○ # －\@ 카운터 확률 등 몇 가지 내부 데이터의 표시
	PRINTFORML  [11] \@ CONFIG("自動喘ぎ") ? ○ # －\@ 구상이 없는 캐릭터에 자동으로 신음 소리를 생성
	PRINTFORML  [12] \@ CONFIG("제한시간철폐") ? ○ # －\@ 선택지의 제한 시간 철폐
	PRINTFORML  [13] \@ CONFIG("니플퍽") ? ○ # －\@ 니플퍽이 가능하게 한다
	PRINTFORML  [14] \@ CONFIG("정액관장") ? ○ # －\@ 애널에 잔뜩 사정하면 관장과 같은 효과가 나오도록 한다.
	PRINTFORML  [15] \@ CONFIG("요도を확장하기쉬움") ? ○ # －\@ 요도의 확장 난이도를 낮춘다
	PRINTFORM  [16] \@ CONFIG("匂いの표기변경") ? ○ # －\@ 「匂い」라는 단어의 표기를 변경한다.。현재：
	CALL PRINTL_COLORTEXT, TEXTS("匂い"), DEF_COLOR("황색")
	PRINTFORM  [17] \@ CONFIG("유두の상세설정") ? ○ # －\@ 유두의 상세 설정
	IF CONFIG("유두の상세설정")
		PRINTFORM ：
		SETCOLOR DEF_COLOR("暗황색")
		PRINTFORM \@ CONFIG("유두비대") ? ○ # ✕\@ 유두비대 
		PRINTFORM \@ CONFIG("함몰유두") ? ○ # ✕\@ 함몰유두 
		PRINTFORM \@ CONFIG("거유륜") ? ○ # ✕\@ 거유륜
		RESETCOLOR
	ENDIF
	PRINTL 
	PRINTFORML [100] 돌아간다
	CALL INPUT_SELECT, 18, 100
	SELECTCASE RESULT
	CASE 100
		RETURN 0
	CASE 0
		CALL SETCONFIG, "사정箇所の변경"
	CASE 1
		CALL SETCONFIG, "우르즈군없이도대량사정"
	CASE 2
		CALL SETCONFIG, "황홀상태"
	CASE 3
		CALL SETCONFIG, "갈아입히기설정"
	CASE 4
		CALL SETCONFIG, "３사이즈"
	CASE 5
		CALL SETCONFIG, "음모と겨털"
	CASE 6
		CALL SETCONFIG, "페니스상세설정"
	CASE 7
		CALL SETCONFIG, "조수상대でも연모適用"
	CASE 8
		CALL SETCONFIG, "寝取られ"
	CASE 9
		CALL SETCONFIG, "촉수설정"
	CASE 10
		CALL SETCONFIG, "내부데이터の표시"
	CASE 11
		CALL SETCONFIG, "自動喘ぎ"
	CASE 12
		CALL SETCONFIG, "제한시간철폐"
	CASE 13
		CALL SETCONFIG, "니플퍽"
	CASE 14
		CALL SETCONFIG, "정액관장"
	CASE 15
		CALL SETCONFIG, "요도を확장하기쉬움"
	CASE 16
		CALL SETCONFIG, "匂いの표기변경"
	CASE 17
		CALL SETCONFIG, "유두の상세설정"

	ENDSELECT
	ARGS = 조교全般
	RESTART
CASE "システム＆이벤트全般"
	PRINTFORM  [ 0] \@ CONFIG("난이도") ? ○ # －\@ 현재의 난이도：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("ＨＡＲＤ")
		PRINTL HARD
	ELSEIF CONFIG("ＥＡＳＹ")
		PRINTL EASY
	ELSE
		PRINTL NORMAL
	ENDIF
	RESETCOLOR
	PRINTFORML  [ 1] \@ CONFIG("연모と음란の任意획득") ? ○ # －\@ 연모・음란의 임의 획득
	PRINTFORM  [ 2] \@ CONFIG("상사상애") ? ○ # －\@ 소질：상사상애 설정：
	SETCOLOR DEF_COLOR("暗황색")
	PRINTFORM \@ CONFIG("상사상애：인수제한없음") ? 몇명이라도 # 한명만 \@
	PRINTFORML ＆\@ CONFIG("상사상애：요바이제한") ? 밤덮치기 제한있음 # 밤덮치기 제한없음 \@
	RESETCOLOR
	PRINTFORML  [ 3] \@ CONFIG("경험표시詰め") ? ○ # －\@ 능력표시에서, 갖고있지 않은 경험을 표시하지 않는다
	PRINTFORM  [ 4] \@ CONFIG("성기상세표시") ? ○ # －\@ V와 A등 성기의 능력의 상세 표시：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("성기상세표시")
		PRINTFORML 이후＆ＶＡ확장 ＬＶ의 변화는\@ CONFIG("ＶＡ상세표시：수동ＬＶＵＰ") ? 수동 # 자동 \@으로 진행
	ELSE
		PRINTFORML 하지 않는다
	ENDIF
	RESETCOLOR
	PRINTFORM  [ 5] \@ CONFIG("소환사고") ? ○ # －\@ 소환사고：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("소환사고")
		LOCALS = 유니크\@ CONFIG("소환사고：유니크") ? ○ # ✕ \@
		LOCALS = %LOCALS% 후타나리\@ CONFIG("소환사고：후타나리") ? ○ # ✕ \@
		LOCALS = %LOCALS% 오토코노코\@ CONFIG("소환사고：오토코노코") ? ○ # ✕ \@
		PRINTFORML 발생한다（%LOCALS%）
	ELSE
		PRINTFORML 발생하지않는다
	ENDIF
	RESETCOLOR
	PRINTFORM  [ 6] \@ CONFIG("대상の몽마化") ? ○ # －\@ 조교상대의 몽마변화：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("대상の몽마化")
		LOCALS = 남자\@ CONFIG("대상の몽마化：男") ? ○ # ✕ \@
		LOCALS = %LOCALS% 여체화\@ CONFIG("대상の몽마化：여체화") ? ○ # ✕ \@
		PRINTFORML 발생한다（%LOCALS%）
	ELSE
		PRINTFORML 발생하지 않는다
	ENDIF
	RESETCOLOR
	PRINTFORM  [ 7] \@ CONFIG("주인・조수の몽마化") ? ○ # －\@ 주인・%TEXTS("조수の명칭")%의 몽마화：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("주인・조수の몽마化")
		LOCALS = 남자\@ CONFIG("주인・조수の몽마化：男") ? ○ # ✕ \@
		LOCALS = %LOCALS% 여체화\@ CONFIG("주인・조수の몽마化：여체화") ? ○ # ✕ \@
		LOCALS = %LOCALS% 캔슬\@ CONFIG("주인・조수の몽마化：캔슬可") ? ○ # ✕ \@
		PRINTFORML 발생한다（%LOCALS%）
	ELSE
		PRINTFORML 발생하지 않는다
	ENDIF
	RESETCOLOR
	PRINTFORM  [ 8] \@ CONFIG("몽마化時の特徴変化") ? ○ # －\@ 몽마화시의 특징변화：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("몽마化時の特徴変化") == 0
		PRINTL 특징을 계승하지 않는다
	ELSEIF CONFIG("몽마化時の特徴変化：外見のみ")
		PRINTL 외관의 특징을 계승한다
	ELSEIF CONFIG("몽마化時の特徴変化：全部")
		PRINTL 외견＆내면의 특징을 계승한다
	ENDIF
	RESETCOLOR
	PRINTFORM  [ 9] \@ CONFIG("표시人数") ? ○ # －\@ 캐릭터 리스트의 표시인원 수 설정：
	SETCOLOR DEF_COLOR("暗황색")
	PRINTFORML \@ CONFIG("표시人数") ? １Ｐ{CONFIG("표시人数"), 2}명 표시 # 디폴트 \@\@ CONFIG("세대別표시") ? ＆세대별 # \@
	RESETCOLOR
	PRINTFORM  [10] \@ CONFIG("일상") ? ○ # －\@ 몽마들의 일상발생빈도：
	SETCOLOR DEF_COLOR("暗황색")
	IF FLAG:205 == 0
		PRINTL 휴식때만
	ELSEIF FLAG:205 == 1
		PRINTL 시간경과시 항상
	ELSEIF FLAG:205 == 2
		PRINTL 발생하지 않는다
	ENDIF
	RESETCOLOR
	PRINTFORML  [11] \@ CONFIG("自動購入") ? ○ # －\@ 소모품 자동구입
	PRINTFORML  [12] \@ CONFIG("탐색ボス") ? ○ # －\@ 탐색 커맨드에서, 이미 소환 된 보스캐릭터도 인카운트 된다
	PRINTFORML  [13] \@ CONFIG("비디오매각の선택") ? ○ # －\@ 비디오 매각의 선택
	PRINTFORML  [14] \@ CONFIG("날짜고정이벤트재현") ? ○ # －\@ 현실의 날짜 고정 이벤트를 게임 내의 특정한 날짜에서도 재현한다.
	PRINTFORML  [15] \@ CONFIG("유니크캐릭터의다중소환") ? ○ # －\@ 유니크 캐릭터의 다중 소환
	PRINTFORML  [16] \@ CONFIG("요바이아침기습금지") ? ○ # －\@ 요바이와 아침봉사를 금지한다
	PRINTFORML  [17] \@ CONFIG("대사の名前비표시") ? ○ # －\@ 조교 외 이벤트의 대사에 첨부되는 이름을 표시하지 않는다.
	PRINTFORML  [18] \@ CONFIG("하룻밤개방の이벤트全표시") ? ○ # －\@ 하룻밤 개방 이벤트를 모든 캐릭터 표시한다
	PRINTFORML  [19] \@ CONFIG("온천여행에서의바람기") ? ○ # －\@ 온천여행에서 바람기 이벤트의 해금
	PRINTFORML  [20] \@ CONFIG("쇼타메이드") ? ○ # －\@ 쇼타도 다방에서 일 시킨다
	PRINTFORML  [21] \@ CONFIG("성희롱금지") ? ○ # －\@ 주인이 선택지 없이 성희롱 등을 하는 것을 금지한다
	PRINTFORML  [22] \@ CONFIG("蓄精") ? ○ # －\@ 하룻밤 개방 이벤트 등으로 보온병에 정액을 축적할 수 있도록 한다.
	PRINTFORML [100] 돌아간다
	CALL INPUT_SELECT, 23, 100
	SELECTCASE RESULT
	CASE 100
		RETURN 0
	CASE 0
		CALL SETCONFIG, "난이도"
	CASE 1
		CALL SETCONFIG, "연모と음란の任意획득"
	CASE 2
		CALL SETCONFIG, "상사상애"
	CASE 3
		CALL SETCONFIG, "경험표시詰め"
	CASE 4
		CALL SETCONFIG, "성기상세표시"
	CASE 5
		CALL SETCONFIG, "소환사고"
	CASE 6
		CALL SETCONFIG, "대상の몽마化"
	CASE 7
		CALL SETCONFIG, "주인・조수の몽마化"
	CASE 8
		CALL SETCONFIG, "몽마化時の特徴変化"
	CASE 9
		CALL SETCONFIG, "표시人数"
	CASE 10
		CALL SETCONFIG, "일상"
	CASE 11
		CALL SETCONFIG, "自動購入"
	CASE 12
		CALL SETCONFIG, "탐색ボス"
	CASE 13
		CALL SETCONFIG, "비디오매각の선택"
	CASE 14
		CALL SETCONFIG, "날짜고정이벤트재현"
	CASE 15
		CALL SETCONFIG, "유니크캐릭터의다중소환"
	CASE 16
		CALL SETCONFIG, "요바이아침기습금지"
	CASE 17
		CALL SETCONFIG, "대사の名前비표시"
	CASE 18
		CALL SETCONFIG, "하룻밤개방の이벤트全표시"
	CASE 19
		CALL SETCONFIG, "온천여행에서의바람기"
	CASE 20
		CALL SETCONFIG, "쇼타메이드"
	CASE 21
		CALL SETCONFIG, "성희롱금지"
	CASE 22
		CALL SETCONFIG, "蓄精"
	ENDSELECT
	ARGS = システム＆이벤트全般
	RESTART
CASE "성벽"
	REDRAW 0
	PRINTL 성벽을 설정합니다. 다음 중에서 원하는 만큼 선택해주세요
	FOR LCOUNT, 0, NUM("속성")
		PRINTFORMLC 　[{LCOUNT, 2}] \@GETBIT(TALENT:MASTER:성벽, LCOUNT) ? ○ # － \@ %NAMES("속성", LCOUNT)%
		SIF LCOUNT%3 == 2
			PRINTL 
	NEXT
	PRINTL 
	;PRINTL   [99] 랜덤하게 １～３개 선택한다
	PRINTL  [100] 돌아간다
	CALL INPUT_SELECT, NUM("속성"), 100
	IF RESULT == 100
		REDRAW 1
		RETURN 0
	;ELSEIF RESULT == 99
	;	TALENT:MASTER:성벽 = 0
	;	SETBIT TALENT:MASTER:성벽, RAND:NUM("속성")
	;	SIF RAND:2
	;		SETBIT TALENT:MASTER:성벽, RAND:NUM("속성")
	;	SIF RAND:2
	;		SETBIT TALENT:MASTER:성벽, RAND:NUM("속성")
	ELSEIF RESULT >= 0 && RESULT <= NUM("속성") - 1
		;IF GETBIT(TALENT:MASTER:성벽, RESULT) == 0 && NUM_BIT(TALENT:MASTER:성벽) == 3
		;	PRINTW 持てる성벽は３つまでです. どれか一つを減らしてください
		;ELSE
			INVERTBIT TALENT:MASTER:성벽, RESULT
		;ENDIF
	ENDIF
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "성감대"
	REDRAW 0
	PRINTL 성감대를 설정합니다. 다음 중에서 원하는 만큼 선택해주세요
	FOR LCOUNT, 0, NUM("성감대")
		PRINTFORMLC 　[{LCOUNT, 2}] \@GETBIT(TALENT:MASTER:성감대, LCOUNT) ? ○ # － \@ %NAMES("성감대", LCOUNT)%
		SIF LCOUNT%3 == 2
			PRINTL 
	NEXT
	PRINTL 
	PRINTL   [99] 전부 선택한다
	PRINTL  [100] 돌아간다
	CALL INPUT_SELECT, NUM("성감대"), 99, 100
	IF RESULT == 100
		REDRAW 1
		RETURN 0
	ELSEIF RESULT == 99
		FOR LCOUNT, 0, NUM("성감대")
			SETBIT TALENT:MASTER:성감대, LCOUNT
		NEXT
	ELSEIF RESULT >= 0 && RESULT <= NUM("속성") - 1
		INVERTBIT TALENT:MASTER:성감대, RESULT
	ENDIF
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "自動購入"
	REDRAW 0
	PRINTL  ～소모품 자동구입 설정상황 일람～
	PRINTL  변경할 항목을 택해주세요
	PRINTFORML  [ 0] \@ GETBIT(FLAG:9, 0) ? ○ # － \@ 비디오테이프
	PRINTFORML  [ 1] \@ GETBIT(FLAG:9, 1) ? ○ # － \@ 로션
	PRINTFORML  [ 2] \@ GETBIT(FLAG:9, 2) ? ○ # － \@ 미약
	PRINTFORML  [ 3] \@ GETBIT(FLAG:9, 3) ? ○ # － \@ 이뇨제
	PRINTFORML  [ 4] \@ GETBIT(FLAG:9, 4) ? ○ # － \@ 진정제
	SIF CONFIG("임신패치")
		PRINTFORML  [ 5] \@ GETBIT(FLAG:9, 5) ? ○ # － \@ 배란유발제
	PRINTFORML  [ 6] \@ GETBIT(FLAG:9, 6) ? ○ # － \@ 재스민
	PRINTFORML  [ 7] \@ GETBIT(FLAG:9, 7) ? ○ # － \@ 라벤더
	PRINTFORML  [ 8] \@ GETBIT(FLAG:9, 8) ? ○ # － \@ 머스크
	PRINTFORML  [ 9] \@ GETBIT(FLAG:9, 9) ? ○ # － \@ 샌들우드
	PRINTFORML  [10] \@ GETBIT(FLAG:9, 10) ? ○ # － \@ 로즈마리
	PRINTFORML  [11] \@ GETBIT(FLAG:9, 11) ? ○ # － \@ 입욕제
	PRINTFORML  [12] \@ GETBIT(FLAG:9, 12) ? ○ # － \@ 근육이완제
	PRINTFORML [100] 이 설정으로 좋다

	CALL INPUT_SELECT, 13, 100
	IF RESULT == 100
		REDRAW 1
		RETURN 0
	ELSEIF RESULT == 5 && CONFIG("임신패치") == 0
	ELSEIF RESULT >= 0 && RESULT <= 12
		INVERTBIT FLAG:9, RESULT
	ENDIF
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "사정箇所の변경"
	PRINTFORML 　～사정 시에 자동 선택하는 사정 장소 일람～
	PRINTFORML 　변경할 것을 선택해주세요.
	PRINTFORML [ 0] %"입이나 가슴으로 사정시키는 조교：", 50, LEFT%%TEXTS("사정箇所の표시", 92)%
	PRINTFORML [ 1] %"손로 사정시키는 조교：", 50, LEFT%%TEXTS("사정箇所の표시", 93)%
	PRINTFORML [ 2] %"질로 사정시키는 조교：", 50, LEFT%%TEXTS("사정箇所の표시", 94)%
	PRINTFORML [ 3] %"애널로 사정시키는 조교：", 50, LEFT%%TEXTS("사정箇所の표시", 95)%
	PRINTFORML [ 4] %"다리나 스마타같이 신체로 사정시키는 조교：", 50, LEFT%%TEXTS("사정箇所の표시", 96)%
	PRINTFORML [ 7] 사정 장소를 전부 "그대로 사정"으로 한다
	PRINTFORML [ 8] %"사정 후의 페니스 삽입 해제 빈도：", 50, LEFT%\@ GETBIT(FLAG:91, 1) ? 뽑지 않는다 # 가끔 뽑는다 \@
	PRINTFORML [ 9] 이 설정으로 한다

	CALL ONEINPUT_SELECT, 5, 7, 8, 9

	SELECTCASE RESULT
	CASE 9
		IF ALLSAMES(20, FLAG:92, FLAG:93, FLAG:94, FLAG:95, FLAG:96)
			CLEARBIT FLAG:91, 0
			PRINTW 항상 그대로 사정하도록 했습니다
		ELSE
			SETBIT FLAG:91, 0
		ENDIF
		RETURN 1
	CASE 7
		FOR LCOUNT, 92, 97
			FLAG:LCOUNT = 20
		NEXT
	CASE 8
		INVERTBIT FLAG:91, 1
	CASE 0 TO 4
		LOCAL = RESULT + 92
		PRINTL 사정 시에 자동으로 선택할 사정 장소를 선택해주세요
		PRINTL [ 0] 그때마다 선택
		PRINTL [ 1] 머리카락
		PRINTL [ 2] 얼굴
		PRINTL [ 3] 입
		PRINTL [ 4] 가슴
		PRINTL [ 5] 가랑이
		PRINTL [ 6] 배와 엉덩이
		PRINTL [20] 그대로 사정
		PRINTL [30] 랜덤
		PRINTL [40] 여유가 있으면 선택
		IF ITEM:보온병
			PRINTL [90] 보온병에 정액을 모은다
			CALL INPUT_SELECT, 7, 20, 30, 40, 90
		ELSE
			CALL INPUT_SELECT, 7, 20, 30, 40
		ENDIF
		IF RESULT == 0
			PRINTW ―사정할때마다 선택하도록 설정했습니다―
			FLAG:LOCAL = 0
		ELSEIF RESULT == 1
			PRINTW ―사정장소를 머리카락으로 고정했습니다―
			FLAG:LOCAL = 1
		ELSEIF RESULT == 2
			PRINTW ―사정장소를 얼굴로 고정했습니다―
			FLAG:LOCAL = 2
		ELSEIF RESULT == 3
			PRINTW ―사정장소를 입으로 고정했습니다―
			FLAG:LOCAL = 3
		ELSEIF RESULT == 4
			PRINTW ―사정장소를 가슴으로 고정했습니다―
			FLAG:LOCAL = 4
		ELSEIF RESULT == 5
			PRINTW ―사정장소를 가랑이로 고정했습니다―
			FLAG:LOCAL = 5
		ELSEIF RESULT == 6
			PRINTW ―사정장소를 배와 엉덩이로 고정했습니다―
			FLAG:LOCAL = 6
		ELSEIF RESULT == 20
			PRINTW ―그대로 사정하도록 했습니다―
			FLAG:LOCAL = 20
		ELSEIF RESULT == 30
			PRINTW ―사정장소를 무작위로 했습니다―
			FLAG:LOCAL = 30
		ELSEIF RESULT == 40
			PRINTW ―여유가 있을 때 선택하도록 설정했습니다―
			FLAG:LOCAL = 40
		ELSEIF RESULT == 90 && ITEM:보온병
			PRINTW ―보온병에 모으도록 설정했습니다―
			FLAG:LOCAL = 90
		ENDIF
	ENDSELECT
	RESTART
CASE "촉수설정"
	PRINTFORML 현재 설정은 다음과 같이 되어있습니다. 변경하고 싶은 내용을 선택하십시오
	PRINTL 
	PRINTFORM  [ 0] 촉수삽입　　　　　　　　　　　　　　
	IF GETBIT(FLAG:80, 0)
		PRINTL 뽑는다
	ELSEIF GETBIT(FLAG:80, 1)
		PRINTL 랜덤
	ELSE
		PRINTL 삽입한채로 놔둔다
	ENDIF
	PRINTFORM  [ 1] 애널촉수　　　　　　　　　　　　　
	IF GETBIT(FLAG:80, 2)
		PRINTL 뽑는다
	ELSEIF GETBIT(FLAG:80, 3)
		PRINTL 랜덤
	ELSE
		PRINTL 삽입한채로 놔둔다
	ENDIF
	PRINTFORM  [ 2] 촉수클리자극　　　　　　　　　　　　
	IF GETBIT(FLAG:80, 4)
		PRINTL 자극을 그만둔다
	ELSEIF GETBIT(FLAG:80, 5)
		PRINTL 랜덤
	ELSE
		PRINTL 계속 자극한다
	ENDIF
	PRINTFORM  [ 3] 촉수유두자극　　　　　　　　　　　　
	IF GETBIT(FLAG:80, 6)
		PRINTL 자극을 그만둔다
	ELSEIF GETBIT(FLAG:80, 7)
		PRINTL 랜덤
	ELSE
		PRINTL 계속 자극한다
	ENDIF
	PRINTFORM  [ 4] 촉수착유　　　　　　　　　　　　　　
	IF GETBIT(FLAG:80, 8)
		PRINTL 자극을 그만둔다
	ELSEIF GETBIT(FLAG:80, 9)
		PRINTL 랜덤
	ELSE
		PRINTL 계속 자극한다
	ENDIF
	PRINTFORM  [ 5] 촉수페니스자극　　　　　　　　　　　
	IF GETBIT(FLAG:80, 10)
		PRINTL 자극을 그만둔다
	ELSEIF GETBIT(FLAG:80, 11)
		PRINTL 랜덤
	ELSE
		PRINTL 계속 자극한다
	ENDIF
	PRINTFORM  [ 6] 촉수구욕　　　　　　　　　　　　　　
	IF GETBIT(FLAG:80, 12)
		PRINTL 뽑는다
	ELSEIF GETBIT(FLAG:80, 13)
		PRINTL 랜덤
	ELSE
		PRINTL 삽입한채로 놔둔다
	ENDIF
	PRINTFORM  [ 7] 소환된 촉수는…　　　　　　　　　
	IF CONFIG("촉수は주인")
		PRINTL 당신
	ELSE
		PRINTL 불러낸 것
	ENDIF
	PRINTFORML [100] 취소

	CALL INPUT_SELECT, 8, 100

	IF RESULT == 100
		RETURN 0
	ELSEIF RESULT >= 0 && RESULT <= 6
		LOCAL = RESULT
		CLEARBIT FLAG:80, LOCAL*2
		CLEARBIT FLAG:80, LOCAL*2 + 1
		PRINTL [0] 디폴트
		PRINTL [1] 턴 마지막에 개방한다
		PRINTL [2] 랜덤
		CALL INPUT_SELECT, 3
		IF RESULT == 1
			SETBIT FLAG:80, LOCAL*2
		ELSEIF RESULT == 2
			SETBIT FLAG:80, LOCAL*2 + 1
		ENDIF
	ELSEIF RESULT == 7
		PRINTFORML [0] 어디선가 나타난 이형의 촉수
		PRINTFORML [1] %CALLNAME:MASTER%
		CALL INPUT_SELECT, 2
		IF RESULT == 1
			PRINTW 촉수로 변할 수 있게 되었습니다! 
			FLAG:87 = 1
		ELSEIF RESULT == 0
			PRINTW 이형의 촉수를 소환하도록 했습니다
			FLAG:87 = 0
		ENDIF
	ENDIF
	RESTART
CASE "갈아입히기설정"
	REDRAW 0
	PRINTFORML 현재의 설정은 다음과 같습니다(○마크는 디폴트에서 변경된 설정입니다)
	PRINTFORML 변경하고 싶은 내용을 선택해주세요
	PRINTL 
	PRINTFORM  [ 0] \@ FLAG:200 ? ○ # － \@ %TEXT_LJ("조교종료시의 복장", 30)%
	SELECTCASE FLAG:200
	CASE 0
		PRINTL 본인의 옷으로 돌아간다
	CASE 1
		PRINTL 종료 시 그대로
	CASE 2
		PRINTL 지정된 마음에 드는 옷을 입게한다
	CASE 3
		PRINTL 마음에 드는 옷 중 하나를 입게한다
	CASE 4
		PRINTL 옷이 얇으면 입게 한다
	ENDSELECT
	PRINTFORM  [ 1] \@ FLAG:201 ? ○ # － \@ %TEXT_LJ("신발과 글러브의 표기", 30)%
	SELECTCASE FLAG:201
	CASE 0
		PRINTL 항상 표시
	CASE 1
		PRINTL 표시하지 않는다
	CASE 2
		PRINTL 거의 알몸이면 표시
	ENDSELECT
	PRINTFORM  [ 2] \@ FLAG:202 ? ○ # － \@ %TEXT_LJ("실내와 야외에서의 신발 취급", 30)%
	SELECTCASE FLAG:202
	CASE 0
		PRINTL 벗지 않는다
	CASE 1
		PRINTL 실내에서는 일단 벗게한다
	ENDSELECT
	PRINTFORML  [ 3] \@ FLAG:203 ? ○ # － \@ %TEXT_LJ("팬티를 몽마 마음대로 한다", 30)%
	PRINTFORML  [ 4] \@ GETBIT(FLAG:204, 0) ? ○ # － \@ %TEXT_LJ("의상 자동탈의 금지", 30)%
	PRINTFORML  [ 5] \@ GETBIT(FLAG:204, 1) ? ○ # － \@ %TEXT_LJ("의상의 세세한 색 표시 금지", 30)%
	PRINTFORML  [ 6] \@ GETBIT(FLAG:204, 2) ? ○ # － \@ %TEXT_LJ("속옷이 보이지않는 복장시의 표시제한", 30)%
	PRINTFORML  [ 7] \@ GETBIT(FLAG:204, 3) ? ○ # － \@ %TEXT_LJ("팬티를 입을때 브래지어도 세트로", 30)%

	PRINTFORML [100] 돌아간다

	CALL INPUT_SELECT, 8, 100

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 0
		PRINTL 조교종료시의 복장을 어떻게 하겠습니까?
		PRINTL [ 0] 본인의 옷으로 되돌린다
		PRINTL [ 1] 종료시의 복장 그대로로 한다
		PRINTL [ 2] 지정된 마음에 드는 복장을 입힌다
		PRINTL [ 3] 마음에 드는 복장 중에서 랜덤으로 입힌다
		PRINTL [ 4] 옷이나 보디슈츠를 입지 않았을 때에 본인의 옷으로 되돌린다
		CALL INPUT_SELECT, 5
		SELECTCASE RESULT
		CASE 2
			PRINTFORML 낮, 밤의 시간대에 지정되어있는 복장이 있으면 그것을. 
			PRINTFORMW 없으면 마음에 드는 복장의 ０번 의상을 입게할 수 있습니다. 
		ENDSELECT
		FLAG:200 = RESULT
	CASE 1
		PRINTL 　　신발과 글러브의 표기는 어떻게 합니까?
		PRINTL [ 0] 항상 표시
		PRINTL [ 1] 표시하지 않는다
		PRINTL [ 2] 신발, 글러브, 삭스 이외의 것을 몸에 걸치고 있지 않으면 표시
		CALL INPUT_SELECT, 3
		FLAG:201 = RESULT
	CASE 2
		PRINTL 　　 실내나 실외에서 신발을 벗거나 신거나 하도록 하시겠습니까?
		PRINTL [ 0] 특별히 지시하지 않는다
		PRINTL [ 1] 조교 개시시와, 야외에서 돌아올 때에 벗도록 한다
		CALL INPUT_SELECT, 2
		FLAG:202 = RESULT
	CASE 3
		INVERTBIT FLAG:203, 0
	CASE 4
		INVERTBIT FLAG:204, 0
	CASE 5
		INVERTBIT FLAG:204, 1
	CASE 6
		INVERTBIT FLAG:204, 2
	CASE 7
		INVERTBIT FLAG:204, 3
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "일상"
	PRINTL 　　발생빈도를 어떻게 설정합니까？
	PRINTL [0] 휴식시에만
	PRINTL [1] 시간경과시 항상
	PRINTL [2] 발생하지 않는다
	CALL INPUT_SELECT, 3
	FLAG:205 = RESULT
	SELECTCASE FLAG:205
	CASE 0
		PRINTW 휴식때에만 설정한다
	CASE 1
		PRINTW 시간이 경과할때마다 발생하도록 설정한다
	CASE 2
		PRINTW 발생하지 않도록 설정합니다
	ENDSELECT
CASE "自動喘ぎ"
	INVERTBIT FLAG:206, 0
	IF FLAG:206
		PRINTW 자동 신음 엔진을 적용하도록 설정합니다
	ELSE
		PRINTW 구상이 준비되어있지 않은 경우에는 말하지 않습니다
	ENDIF
CASE "음모と겨털"
	REDRAW 0
	PRINTFORML 음모와 %TEXTS("겨드랑이")%털의 설정을 합니다
	PRINTFORML 어느 외견 연령에서 자라게 하는 지 설정해주세요
	PRINTFORML 단, %TEXTS("겨드랑이")%털은 음모가 자라는 캐릭터 한정입니다
	PRINTFORML （주의：OFF로 설정하면, 모든 음모＆%TEXTS("겨드랑이")%털 플래그가 초기화됩니다! ）
	PRINTFORM  음모의 설정：
	SETCOLOR DEF_COLOR("暗황색")
	IF CONFIG("음모と겨털") == 0
		PRINTL ＯＦＦ
	ELSE
		IF GETBIT(FLAG:207, 3)
			PRINT ：어른만 자란다
		ELSEIF GETBIT(FLAG:207, 2)
			PRINT ：청년부터 자란다
		ELSEIF GETBIT(FLAG:207, 1)
			PRINT ：아이가 아니면 자란다
		ELSEIF GETBIT(FLAG:207, 0)
			PRINT ：모두 자란다
		ENDIF
		PRINTFORML ＆%TEXTS("겨드랑이")%털\@ CONFIG("겨털") ? 있음 # 없음 \@
	ENDIF
	RESETCOLOR
	PRINTFORML  [ 0] 모두 자란다
	PRINTFORML  [ 1] 아이：✕　소녀：○　누님：○　어른：○
	PRINTFORML  [ 2] 아이：✕　소녀：✕　누님：○　어른：○
	PRINTFORML  [ 3] 아이：✕　소녀：✕　누님：✕　어른：○
	SIF CONFIG("음모と겨털")
		PRINTFORML  [10] \@ CONFIG("겨털") ? ○ # － \@ %TEXTS("겨드랑이")%털의 유무
	PRINTFORML  [99] OFF
	PRINTFORML [100] 돌아간다

	CALL INPUT_SELECT, 4, 10, 99, 100

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 99
		LOCAL = 0
		FOR LCOUNT, 1, CHARANUM
			SIF BASE:LCOUNT:음모 == 0
				CONTINUE
			LOCAL = 1
			BREAK
		NEXT
		IF LOCAL
			PRINTFORML 음모와 %TEXTS("겨드랑이")%털의 상태를 정말로 초기화시켜도 됩니까？
			PRINTFORML [ 0] 예
			PRINTFORML [ 1] 역시 그만둔다
			CALL INPUT_SELECT, 2
			SIF RESULT == 1
				RESTART
		ENDIF
		PRINTFORMW 음모와 %TEXTS("겨드랑이")%털의 설정을 OFF했습니다
		FLAG:207 = 0
		CALL SETFLAG, "음모と겨털の설정初期化"
		REDRAW 1
		RETURN 0
	CASE 0 TO 3
		CLEARBIT FLAG:207, 0
		CLEARBIT FLAG:207, 1
		CLEARBIT FLAG:207, 2
		CLEARBIT FLAG:207, 3
		SETBIT FLAG:207, RESULT
	CASE 10
		SIF CONFIG("음모と겨털")
			INVERTBIT FLAG:207, 10
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART

CASE "임신"
	REDRAW 0
	PRINTFORML ～임신패치 설정상태일람～
	PRINTFORML 변경하고 싶은 내용을 선택해주세요
	PRINTFORML （주의：임신패치 자체를 OFF시키면, 임신에 관련된 플래그가 고정됩니다! ）
	PRINTFORML  [ 0] \@ GETBIT(FLAG:211, 0) ? ○ # － \@ 임신패치
	PRINTFORML  [ 1] \@ GETBIT(FLAG:211, 1) ? ○ # － \@ MASTER의 정자로만 임신가능
	PRINTFORML  [ 2] \@ GETBIT(FLAG:211, 2) ? ○ # － \@ MASTER도 임신가능
	PRINTFORML  [ 3] \@ GETBIT(FLAG:211, 3) ? ○ # － \@ 요바이 등 조교이외의 씬에서 임신가능
	PRINTFORML  [ 4] \@ GETBIT(FLAG:211, 4) ? ○ # － \@ 딸의 가슴성장（필요：가슴에의 자극）
	PRINTFORML  [ 5] \@ GETBIT(FLAG:211, 5) ? ○ # － \@ 슬라임은 임신발각과 동시에 분열
	PRINTFORML  [ 6] \@ GETBIT(FLAG:211, 6) ? ○ # － \@ 질내사정 시에 수정 확률이 변화하면 표시
	PRINTFORML [100] 이 설정으로 좋다

	CALL INPUT_SELECT, 7, 100

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 1
	CASE 0
		IF FLAG:211
			FLAG:211 = 0
		ELSE
			FLAG:211 = 1
		ENDIF
	CASE 1
		INVERTBIT FLAG:211, 1
		SIF GETBIT(FLAG:211, 1)
			CLEARBIT FLAG:211, 2
	CASE 2
		INVERTBIT FLAG:211, 2
		SIF GETBIT(FLAG:211, 2)
			CLEARBIT FLAG:211, 1
	CASEELSE
		INVERTBIT FLAG:211, RESULT
	ENDSELECT
	SIF FLAG:211
		SETBIT FLAG:211, 0
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "내부데이터の표시"
	INVERTBIT FLAG:217, 0
	IF FLAG:217
		PRINTW 자세한 수치를 볼 수 있습니다
	ELSE
		PRINTW 수치를 숨깁니다
	ENDIF
CASE "조수상대でも연모適用"
	INVERTBIT FLAG:218, 0
	IF FLAG:218
		PRINTFORMW %조사처리(TEXTS("조수の명칭"), "가")% 조교시에도 연모의 구상을 표시합니다
	ELSE
		PRINTFORMW %조사처리(TEXTS("조수の명칭"), "가")% 조교하고 있을 때에는 연모를 잃도록 합니다
	ENDIF
CASE "난이도"
	PRINTL 　　어느 난이도로 바꿉니까?
	PRINTL [ 0] NORMAL
	PRINTL [ 1] EASY
	PRINTL [ 2] HARD？
	CALL INPUT_SELECT, 3
	IF RESULT == 0
		PRINTW 보통 난이도입니다
	ELSEIF RESULT == 1
		PRINTW 능력의 LVUP과 연모의 조건은 물론, 그밖에도 이것저것 간단하게 됩니다
	ELSEIF RESULT == 2
		PRINTFORMW 몽마들의 기교 등 일부 능력이 자동적으로 레벨업하도록 하고, 그밖에도 여러가지 난이도가 올라갑니다
		PRINTFORMW 그 대신, %NAME:MASTER%의 정력과 회복량 등이 상승합니다
	ENDIF
	FLAG:220 = RESULT
CASE "소환사고"
	REDRAW 0
	LOCAL = FLAG:221
	PRINTFORML 현재의 설정은 다음과 같습니다. 세부사항을 설정해주세요. 
	PRINTFORML  [ 0] \@ CONFIG("소환사고") ? ○ # － \@ 소환사고 발생
	PRINTFORML  [ 1] \@ CONFIG("소환사고：유니크") ? ○ # － \@ 유니크 캐릭터의 소환사고 발생
	PRINTFORML  [ 2] \@ CONFIG("소환사고：후타나리") ? ○ # － \@ 여성 캐릭터의 후타나리화
	PRINTFORML  [ 3] \@ CONFIG("소환사고：오토코노코") ? ○ # － \@ 여성캐릭터의 오토코노코화（다만, 외모가 소녀까지）
	PRINTFORML [100] 이걸로 좋다
	CALL INPUT_SELECT, 4, 100
	SELECTCASE RESULT
	CASE 100
		IF CONFIG("소환사고") == 0
			PRINTFORML 소환사고가 일어나지않습니다
		ELSE
			IF CONFIG("소환사고：유니크")
				PRINTFORML 어떤 캐릭터라도 소환사고가 일어날 수 있게합니다
				PRINTFORML 베테랑 설정의 캐릭터가 처녀가 되더라도 불평하지 말아주세요
			ELSE
				PRINTFORML 유니크 캐릭터 이외에 한해서, 소환사고가 발생하게 되었습니다
			ENDIF
			LOCALS = \@ CONFIG("소환사고：후타나리") ? 후타나리 # \@
			SIF CONFIG("소환사고：오토코노코")
				LOCALS = %LOCALS%\@ CONFIG("소환사고：후타나리") ? 및 # \@오토코노코
			IF LOCALS != ""
				PRINTFORML 또한, 여성 캐릭터의 %LOCALS%화가 발생합니다
				PRINTFORML 위화감이 발생하더라도 용서해주세요
			ENDIF
		ENDIF
		WAIT
		REDRAW 1
		RETURN 0
	CASE 0 TO 3
		INVERTBIT FLAG:221, RESULT
	ENDSELECT
	IF GETBIT(LOCAL, 0) && GETBIT(FLAG:221, 0) == 0
		FLAG:221 = 0
	ELSEIF GETBIT(LOCAL, 0) == 0 && FLAG:221
		SETBIT FLAG:221, 0
	ENDIF
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "우르즈군없이도대량사정"
	INVERTBIT FLAG:222, 0
	IF FLAG:222
		PRINTW 우르즈군 없이도 잔뜩 사정하도록 했습니다
	ELSE
		PRINTW 우르즈군 없이는 그렇게까진 못합니다
	ENDIF
CASE "황홀상태"
	PRINTL 　　황홀 시에는 몽마 마음대로 하게 합니까?
	PRINTL 　　（구체적으로는, 황홀하게 되어버리면, 커맨드를 몽마가 멋대로 선택합니다.）
	PRINTL [0] 예
	PRINTL [1] 아니오
	CALL INPUT_SELECT, 2
	IF RESULT == 0
		PRINTW 지나치게 쥐어짜지는 것에 주의해주세요
		FLAG:223 = 1
	ELSEIF RESULT == 1
		FLAG:223 = 0
	ENDIF
CASE "표시人数"
	REDRAW 0
	PRINTFORML 다음 중에서, 원하는 표시인수와 세대별 표시기능의 ON/OFF를 선택해주세요. 
	PRINTFORML 
	PRINTFORM  [ 0] \@ CONFIG("표시人数") ? ○ # － \@ 표시인수 변경
	SIF CONFIG("표시人数")
		PRINTFORM ：{CONFIG("표시人数")}명
	PRINTL 
	PRINTFORML  [ 1] \@ CONFIG("세대別표시") ? ○ # － \@ 세대별 표시기능
	PRINTFORML [100] 변경종료

	CALL INPUT_SELECT, 2, 100

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 0
		PRINTL １페이지 당 몇명을 표시합니까？
		PRINTL 　 [ 0] 전부 표시한다
		PRINTL [1～99] １페이지 당 입력한 인원수를 표시한다
		PRINTL 　[100] 돌아간다

		CALL INPUT_SELECT, 101

		SELECTCASE RESULT
		CASE 0
			FLAG:225 = 0
			PRINTFORMW 전원을 한번에 표시합니다
		CASE 1 TO 99
			FLAG:225 = RESULT
			PRINTFORMW １페이지 당 {FLAG:225}명씩 표시합니다
		ENDSELECT
	CASE 1
		INVERTBIT FLAG:237, 0
		SIF CONFIG("세대別표시")
			PRINTW 아이만 표시하거나, 부모만 표시하는 것이 가능해졌습니다
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "대상の몽마化"
	REDRAW 0
	PRINTFORML 대상의 몽마화 조건을 설정합니다
	PRINTFORML 현재의 설정은 다음과 같습니다. 세부사항을 선택해주세요.
	PRINTFORML  [ 0] \@ CONFIG("대상の몽마化") ? ○ # － \@ 몽마화 이벤트 발생
	PRINTFORML  [ 1] \@ CONFIG("대상の몽마化：男") ? ○ # － \@ 남자 캐릭터의 몽마화
	PRINTFORML  [ 2] \@ CONFIG("대상の몽마化：여체화") ? ○ # － \@ 몽마화에 따른 여체화
	PRINTFORML [100] 이걸로 좋다
	CALL INPUT_SELECT, 3, 100
	SELECTCASE RESULT
	CASE 100
		IF CONFIG("대상の몽마化")
			PRINTFORMW 플레이어가 몽마일 경우에, 몽마가 아닌 대상의 체력 또는 정력을 ０으로 만들면 몽마가 됩니다
			PRINTFORMW ※대상의 몽마화\@ CONFIG("대상の몽마化：여체화") ?  및 여체화 # \@는 취소할 수 없습니다. 　조교는 계획적으로 
		ELSE
			PRINTFORMW 몽마화가 일어나지 않도록 했습니다
		ENDIF
		REDRAW 1
		RETURN 0
	CASE 0
		INVERTBIT FLAG:226, RESULT
		SIF CONFIG("대상の몽마化") == 0
			FLAG:226 = 0
	CASE 1
		INVERTBIT FLAG:226, RESULT
		IF CONFIG("대상の몽마化：男")
			SETBIT FLAG:226, 0
		ELSE
			CLEARBIT FLAG:226, 2
		ENDIF
	CASE 2
		INVERTBIT FLAG:226, RESULT
		IF CONFIG("대상の몽마化：여체화")
			SETBIT FLAG:226, 0
			SETBIT FLAG:226, 1
		ENDIF
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "주인・조수の몽마化"
	REDRAW 0
	PRINTFORML 주인 및 %TEXTS("조수の명칭")%의 몽마화 조건을 설정합니다.
	PRINTFORML 현재의 설정은 다음과 같습니다. 세부사항을 설정해주세요. 
	PRINTFORML  [ 0] \@ CONFIG("주인・조수の몽마化") ? ○ # － \@ 몽마화 이벤트 발생
	PRINTFORML  [ 1] \@ CONFIG("주인・조수の몽마化：男") ? ○ # － \@ 남자 캐릭터의 몽마화
	PRINTFORML  [ 2] \@ CONFIG("주인・조수の몽마化：여체화") ? ○ # － \@ 몽마화에 따른 여체화
	PRINTFORML  [ 3] \@ CONFIG("주인・조수の몽마化：캔슬可") ? ○ # － \@ 몽마화의 취소 가능
	PRINTFORML [100] 이걸로 좋다
	CALL INPUT_SELECT, 4, 100
	SELECTCASE RESULT
	CASE 100
		IF CONFIG("주인・조수の몽마化")
			PRINTFORMW 대상이 몽마의 경우에, 몽마가 아닌 주인이나 %조사처리(TEXTS("조수の명칭"), "가")% 정력 0이 되면 몽마가 됩니다
			PRINTFORM ※주인・%TEXTS("조수の명칭")%의 몽마화\@ CONFIG("주인・조수の몽마化：여체화") ? 및 여체화 # \@는 취소
			IF CONFIG("주인・조수の몽마化：캔슬可")
				PRINTW 가능합니다
			ELSE
				PRINTL 못합니다!
				PRINTW (역)조교는 계획적으로
			ENDIF
		ELSE
			PRINTFORMW 주인이나 %TEXTS("조수の명칭")%의 몽마화는 일어나지 않도록 합니다
		ENDIF
		REDRAW 1
		RETURN 0
	CASE 0
		INVERTBIT FLAG:227, RESULT
		SIF CONFIG("주인・조수の몽마化") == 0
			FLAG:227 = 0
	CASE 1
		INVERTBIT FLAG:227, RESULT
		IF CONFIG("주인・조수の몽마化：男")
			SETBIT FLAG:227, 0
		ELSE
			CLEARBIT FLAG:227, 2
		ENDIF
	CASE 2
		INVERTBIT FLAG:227, RESULT
		IF CONFIG("주인・조수の몽마化：여체화")
			SETBIT FLAG:227, 0
			SETBIT FLAG:227, 1
		ENDIF
	CASE 3
		INVERTBIT FLAG:227, RESULT
		SIF CONFIG("주인・조수の몽마化：캔슬可")
			SETBIT FLAG:227, 0
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "몽마化時の特徴変化"
	PRINTL 몽마화 했을 때의 변화를 설정합니다
	PRINTFORML 외견특징은 일부를 제외하고 「원래 몽마가 아닌 캐릭터」만 계승합니다
	PRINTL [0] 변화하지 않는다
	PRINTL [1] 외견만 영향을 받는다
	PRINTL [2] 외견과 더불어 성격도 영향을 받는다
	CALL INPUT_SELECT, 3
	FLAG:242 = RESULT
	IF RESULT == 0
		PRINTW 몽마화 해도 특징은 변화하지 않습니다
	ELSEIF RESULT == 1
		PRINTW 몽마화하면, 외견에 영향을 줍니다
	ELSEIF RESULT == 2
		PRINTW 몽마화하면, 겉도 속도 영향을 줍니다
	ENDIF
CASE "비디오매각の선택"
	PRINTL 비디오를 판매할 때 선택지를 봅니까?
	PRINTL 선택지를 표시할 경우, 비디오를 판매하지 않는다를 선택하면, 
	PRINTL 특정 몽마로 비디오를 누군가에게 보낼 수 있습니다
	PRINTL [0] 예
	PRINTL [1] 아니오
	CALL INPUT_SELECT, 2
	FLAG:261 = !RESULT
	PRINTFORMW 비디오\@ FLAG:261 ?  매각시에 선택지를 표시합 # 는 자동적으로 매각됩 \@니다
CASE "３사이즈"
	REDRAW 0
	PRINTFORML ３사이즈와 신장 및 거유의 상한을 설정합니다. 
	PRINTFORML 거유의 상한 쪽은 ３사이즈가 전제되는 컨피그이므로 [ 0]이 OFF로 기능하지않습니다. 
	PRINTFORML 현재의 설정은 다음과 같습니다. 세부사항을 설정해주세요. 
	PRINTFORML  [ 0] \@ CONFIG("３사이즈：표시") ? ○ # － \@ ３사이즈와 신장의 표시
	SIF CONFIG("３사이즈：표시") == 0
		SETCOLOR DEF_COLOR("회색")
	PRINTFORML  [\@ CONFIG("３사이즈：표시") ? {1, 2} # × \@] \@ CONFIG("３사이즈：Ｆ") ? ○ # － \@ Ｅ～Ｆ컵이 거유
	PRINTFORML  [\@ CONFIG("３사이즈：표시") ? {2, 2} # × \@] \@ CONFIG("３사이즈：Ｇ") ? ○ # － \@ Ｅ～Ｇ컵이 거유
	PRINTFORML  [\@ CONFIG("３사이즈：표시") ? {3, 2} # × \@] \@ CONFIG("３사이즈：Ｈ") ? ○ # － \@ Ｅ～Ｈ컵이 거유
	RESETCOLOR
	PRINTFORML [100] 돌아간다

	IF CONFIG("３사이즈：표시")
		CALL INPUT_SELECT, 4, 100
	ELSE
		CALL INPUT_SELECT, 1, 100
	ENDIF

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 0
		INVERTBIT FLAG:239, 0
		IF CONFIG("３사이즈：표시")
			SETBIT FLAG:239, 1
			PRINTFORML 계속해서 거유의 상한을 설정해도 좋습니다
			PRINTFORMW 초기설정은 Ｅ～Ｆ컵이 거유입니다
		ELSE
			FLAG:239 = 0
		ENDIF
	CASE 1 TO 3
		FLAG:239 = 1
		SETBIT FLAG:239, RESULT
	ENDSELECT

	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "상사상애"
	REDRAW 0
	PRINTFORML 소질：『상사상애』의 취급을 결정합니다
	PRINTFORML 변경할 항목을 선택해주세요：
	PRINTFORML 
	PRINTFORM  [ 0] 상사상애가 된 인원수 \@ITEM:서약의반지 >= 2 ?（변경불가!）# %BL(14)% \@　　　　　　　
	IF CONFIG("상사상애：인수제한없음")
		PRINTFORML 사랑에 한계는 없다
	ELSE
		PRINTFORML 한명만
	ENDIF
	PRINTFORM  [ 1] 상사상애가 된 캐릭터가 있을 때의 밤 이벤트 등의 취급　
	IF CONFIG("상사상애：요바이제한")
		PRINTFORML 상사상애를 가진 캐릭터만
	ELSE
		PRINTFORML 항상 하던대로
	ENDIF
	PRINTFORML [100] 종료
	CALL INPUT_SELECT, 2, 100
	IF RESULT == 100
		REDRAW 1
		RETURN 0
	ELSEIF RESULT == 0 && ITEM:서약의반지 < 2
		INVERTBIT FLAG:241, 0
	ELSEIF RESULT == 1
		INVERTBIT FLAG:241, 1
	ENDIF
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "탐색ボス"
	INVERTBIT FLAG:245, 0
	IF CONFIG(ARGS)
		PRINTFORMW 세계에는 닮은 인간 셋쯤은 있지요
	ELSE
		PRINTFORMW 도플갱어가 나타나지 않게 했습니다
	ENDIF
CASE "경험표시詰め"
	INVERTBIT FLAG:250, 0
	IF CONFIG(ARGS)
		PRINTFORMW 표시를 채웁니다
	ELSE
		PRINTFORMW 경험의 표시방식을 원래대로 되돌립니다
	ENDIF
CASE "연모と음란の任意획득"
	INVERTBIT FLAG:251, 0
	IF CONFIG(ARGS)
		PRINTFORMW 조건을 만족했을 때 능력치 업 화면에서 획득하도록 합니다
	ELSE
		PRINTFORMW 사랑은 언제나 허리케인!
	ENDIF
CASE "성기상세표시"
	REDRAW 0
	PRINTFORML 보지나 애널 등의 확장성 및 명기도 상세 표시 등을 설정합니다 
	PRINTFORML 현재의 설정은 다음과 같이 되어 있습니다. 상세를 설정하세요. 
	IF GETBIT(FLAG:11, 0) == 0
		PRINTFORM （[ 1]번에서 수동화를 택한 때, 
		CALL PRINT_COLORTEXT, "한번만", DEF_COLOR("황색")
		PRINTFORML 전캐릭터의 ＶＡ확장ＬＶ초기화 가능합니다）
	ENDIF
	PRINTFORML  [ 0] \@ CONFIG("성기상세표시") ? ○ # － \@ 상세히 표시시킨다(각 캐릭터의 능력표시 화면에 나타납니다)
	PRINTFORML  [\@ CONFIG("성기상세표시") ? {1, 2} # × \@] \@ CONFIG("ＶＡ상세표시：수동ＬＶＵＰ") ? ○ # － \@ Ｖ확장도와Ａ확장도의ＬＶＵＰ를 수동으로 실시하는（상세표시가 전제입니다）
	PRINTFORML [100] 돌아간다

	IF CONFIG("성기상세표시")
		CALL INPUT_SELECT, 2, 100
	ELSE
		CALL INPUT_SELECT, 1, 100
	ENDIF

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 0
		INVERTBIT FLAG:254, 0
		SIF CONFIG("성기상세표시") == 0
			FLAG:254 = 0
	CASE 1
		INVERTBIT FLAG:254, 1
		;たった一度の初期化
		IF CONFIG("ＶＡ상세표시：수동ＬＶＵＰ") && GETBIT(FLAG:11, 0) == 0
			PRINTFORML 모든 캐릭터의 Ｖ확장도와 Ａ확장도를 초기화합니까？
			PRINTFORML 정말 한번밖에 없으니, 잘 생각하고 선택하세요 
			PRINTFORML [ 0] 초기화한다
			PRINTFORML [ 1] 초기화하지 않는다
			CALL INPUT_SELECT, 2
			IF RESULT == 0
				FOR LCOUNT, 1, CHARANUM
					ABL:LCOUNT:Ｖ확장 = 0
					ABL:LCOUNT:Ａ확장 = 0
					CALL GET_EXABL2, LCOUNT
				NEXT
				SETBIT FLAG:11, 0
			ENDIF
		;自動ＬＶを選んだなら早速ＬＶＵＰ
		ELSEIF CONFIG("ＶＡ상세표시：수동ＬＶＵＰ") == 0
			FOR LCOUNT, 1, CHARANUM
				ABL:LCOUNT:Ｖ확장 = CALCF("Ｖ확장の상한", LCOUNT)
				ABL:LCOUNT:Ａ확장 = CALCF("Ａ확장の상한", LCOUNT)
			NEXT
		ENDIF
	ENDSELECT

	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "다방"
	INVERTBIT FLAG:258, 0
	IF CONFIG(ARGS)
		PRINTW 이벤트가 발생하지 않도록 했습니다
	ELSE
		PRINTW 시간이 경과할때마다 발생하도록 설정한다
	ENDIF
CASE "寝取られ"
	INVERTBIT FLAG:262, 0
	IF CONFIG(ARGS)
		PRINTFORMW 드래곤에게 너무 범해지면, 네토라레당할지도 모릅니다
		PRINTFORMW 하지만, 네토라레당해도 노력하면 되돌릴 수 있습니다
	ELSE
		PRINTFORMW 네토라레따윈 일어나지 않습니다!
		PRINTFORMW 그러나, 이미 일어나버린 마음의 변화는 제거되지 않기때문에 주의를…
	ENDIF
CASE "페니스상세설정"
	PRINTL 페니스에 대해 상세하게 설정합니다
	CALL SETCONFIG, "페니스상세설정：범용"
	IF FLAG:263 == 0
		PRINTFORMW 자세하지 않기 떄문에 좋은 점도 있지요
		RETURN 0
	ENDIF
	SIF TALENT:MASTER:페니스 == 0
		CALL SETCONFIG, "페니스상세설정：주인"
	;実際の数値を決定
	CALL SETFLAG, "페니스상세설정"
	PRINTFORMW 그럼 즐겨주세요
CASE "페니스상세설정：범용"
	REDRAW 0
	PRINTFORML 다음의 성격의 캐릭터에 대해, 경향을 설정하십시오
	PRINTFORML 난수요소가 들어가지만, 선택한 경향이 될 확률이 높아집니다
	PRINTFORML 다만, 아들 등 성숙하지 않은 몸을 가진 캐릭터는 작고 포경이 안 되어있을 가능성이 높아집니다
	PRINTFORML （주의：크다고 다 좋은 것은 아닙니다! ）
	FOR LCOUNT, 0, 3
		SELECTCASE LCOUNT
		CASE 0
			PRINT 　후타나리：
		CASE 1
			PRINT 　　　남자：
		CASE 2
			PRINT 오토코노코：
		ENDSELECT

		LOCAL = LCOUNT * 10

		PRINTPLAINFORM 크기…
		SETCOLOR DEF_COLOR("暗황색")
		IF GETBIT(FLAG:263, LOCAL + 3)
			PRINTPLAIN 말잦이
		ELSEIF GETBIT(FLAG:263, LOCAL + 2)
			PRINTPLAIN 거근　
		ELSEIF GETBIT(FLAG:263, LOCAL + 1)
			PRINTPLAIN 왜소　
		ELSE
			PRINTPLAIN 보통　
		ENDIF
		RESETCOLOR
		PRINTFORML 　[{LOCAL, 2}]보통　[{LOCAL + 1, 2}]작음　[{LOCAL + 2, 2}]거근　[{LOCAL + 3, 2}]말잦이
		PRINTFORM 　　　　　　포경…
		SETCOLOR DEF_COLOR("暗황색")
		IF GETBIT(FLAG:263, LOCAL + 7)
			PRINTPLAIN 진성　
		ELSEIF GETBIT(FLAG:263, LOCAL + 6)
			PRINTPLAIN 가성　
		ELSE
			PRINTPLAIN 없음　
		ENDIF
		RESETCOLOR
		PRINTFORML 　[{LOCAL + 5, 2}]껍데기없음　　　[{LOCAL + 6, 2}]가성　[{LOCAL + 7, 2}]진성
	NEXT
	PRINTFORML  [99] 역시 상세한 설정은 사용하지 않는다
	PRINTFORML [100] 이걸로 좋다\@ (TALENT:MASTER:페니스 == 0 && FLAG:5) ? （계속해서 %NAME:MASTER%의 페니스 설정으로 갑니다）# \@
	CALL INPUT_SELECT, 101
	SELECTCASE RESULT
	CASE 100
		SETBIT FLAG:263, 0
		REDRAW 1
		RETURN 0
	CASE 99
		FLAG:263 = 0
		REDRAW 1
		RETURN 0
	CASE 0 TO 3
		FLAG:263 = CLEARBITS(FLAG:263, 1, 3)
		SETBIT FLAG:263, RESULT
	CASE 5 TO 7
		FLAG:263 = CLEARBITS(FLAG:263, 6, 7)
		SETBIT FLAG:263, RESULT
	CASE 10 TO 13
		FLAG:263 = CLEARBITS(FLAG:263, 11, 13)
		SETBIT FLAG:263, RESULT
	CASE 15 TO 17
		FLAG:263 = CLEARBITS(FLAG:263, 16, 17)
		SETBIT FLAG:263, RESULT
	CASE 20 TO 23
		FLAG:263 = CLEARBITS(FLAG:263, 21, 23)
		SETBIT FLAG:263, RESULT
	CASE 25 TO 27
		FLAG:263 = CLEARBITS(FLAG:263, 26, 27)
		SETBIT FLAG:263, RESULT
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "페니스상세설정：주인"
	REDRAW 0
	TALENT:MASTER:페니스 = MAX(TALENT:MASTER:페니스, 1)
	PRINTFORML %NAME:MASTER%의 페니스의 성질을 결정해주세요
	SIF PENIS(MASTER) == 0
		PRINTFORML （현재는 없지만, 만약 있다는 가정하에 대답）
	PRINTFORM 크기…
	SETCOLOR DEF_COLOR("暗황색")
	IF GETBIT(TALENT:MASTER:페니스, 3)
		PRINTPLAIN 말잦이
	ELSEIF GETBIT(TALENT:MASTER:페니스, 2)
		PRINTPLAIN 거근　
	ELSEIF GETBIT(TALENT:MASTER:페니스, 1)
		PRINTPLAIN 왜소　
	ELSEIF GETBIT(TALENT:MASTER:페니스, 0)
		PRINTPLAIN 보통　
	ENDIF
	RESETCOLOR
	PRINTFORML 　[ 0]보통　[ 1]작음　[ 2]거근　[ 3]말잦이
	PRINTFORM 포경…
	SETCOLOR DEF_COLOR("暗황색")
	IF GETBIT(TALENT:MASTER:페니스, 12)
		PRINTPLAIN 진성　
	ELSEIF GETBIT(TALENT:MASTER:페니스, 11)
		PRINTPLAIN 가성　
	ELSE
		PRINTPLAIN 없음　
	ENDIF
	RESETCOLOR
	PRINTFORML 　[10]껍데기없음　　　[11]가성　[12]진성
	PRINTFORML [100] 이걸로 좋다
	CALL INPUT_SELECT, 101
	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 0 TO 3
		TALENT:MASTER:페니스 = CLEARBITS(TALENT:MASTER:페니스, 0, 3)
		SETBIT TALENT:MASTER:페니스, RESULT
	CASE 10 TO 12
		TALENT:MASTER:페니스 = CLEARBITS(TALENT:MASTER:페니스, 11, 12)
		SIF RESULT > 10
			SETBIT TALENT:MASTER:페니스, RESULT
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "제한시간철폐"
	INVERTBIT FLAG:265, 0
	IF CONFIG(ARGS)
		PRINTW 이제 넘어뜨려지는 일이 많아도 안심입니다
	ELSE
		PRINTW 스릴 넘치는 것도 좋지요
	ENDIF
CASE "날짜고정이벤트재현"
	INVERTBIT FLAG:266, 0
	IF CONFIG(ARGS)
		PRINTL 1년이 120일이므로 같은 날짜가 돌아오진 않지만 양해바랍니다
		PRINTW （달력은 삼분의 일로 압축되어있습니다）
	ELSE
		PRINTW 날짜고정 이벤트는 현재의 날짜만 참조하게되었습니다
	ENDIF
CASE "니플퍽"
	INVERTBIT FLAG:267, 0
	IF CONFIG(ARGS)
		PRINTW 가슴이 거유이상으로 큰 사람이, 슬라임이나 음유라면 니플퍽이 가능하게 했습니다
	ELSE
		PRINTW 유두에는 삽입가능한 구멍따윈 없습니다. 그게 보통이지요
	ENDIF
CASE "정액관장"
	INVERTBIT FLAG:268, 0
	IF CONFIG(ARGS)
		PRINTL 관장에 비해서는 약합니다만, 배설물이 쌓여있는 사람의 안에 사정하고 자극을 하는 것이 지나치면…
		PRINTW 애널섹스 등의 전에는 뱃속을 깨끗하게 해야할지도 모릅니다
		SIF CONFIG("画面표시：배설욕게이지") == 0
			PRINTW 아울러 화면표시의 배설욕게이지의 표시도 ON으로 하는 것을 추천합니다.
	ELSE
		PRINTW 안에 사정했을 시의 배설촉진작용을 없앴습니다
	ENDIF
CASE "유니크캐릭터의다중소환"
	INVERTBIT FLAG:269, 0
	IF CONFIG(ARGS)
		PRINTL 유니크캐릭터라도 자유롭게 소환가능하게 했습니다. 
		PRINTW 이상한 일이 생길지도 모릅니다만… 그것도 또 재미있을지도 모릅니다
	ELSE
		PRINTW 유니크캐릭터는 단 한명뿐입니다
	ENDIF
CASE "요바이아침기습금지"
	INVERTBIT FLAG:270, 0
	IF CONFIG(ARGS)
		PRINTL 주인의 방문에 자물쇠를 달고, 벽을 통과하지 못하도록 세공을 합니다
		PRINTW 아침 저녁으로 습격당할일은 없겠지만, 욕구불만을 품은 몽마가 늘어날지도 모릅니다…
	ENDIF
CASE "대사の名前비표시"
	INVERTBIT FLAG:273, 0
	IF CONFIG(ARGS)
		PRINTW 다방이나 한마디 이벤트 등의 발언에서 대사의 이름 표시를 그만둡니다.
	ELSE
		PRINTL 조교 이외의 발언으로 누구의 대사인가를 표시하도록 반려했습니다
		PRINTW ……네？ 같은 이름의 캐릭터가 있다구요？! 
	ENDIF
CASE "하룻밤개방の이벤트全표시"
	INVERTBIT FLAG:274, 0
	IF CONFIG(ARGS)
		PRINTL 깊은밤의 외출 허가한 모든 캐릭터를 표시합니다.
		PRINTW 캐릭터 수가 많은 경우 길어집니다.
	ELSE
		PRINTW 외출 허가한 캐릭터 대표 한명만 표시하도록 합니다.
	ENDIF
CASE "온천여행에서의바람기"
	INVERTBIT FLAG:275, 0
	IF CONFIG(ARGS)
		PRINTW 성욕을 주체 하지 못하면, 주인과 여행 중에도 남자 고기잡이를 시작해 버릴지도 모릅니다.
	ELSE
		PRINTW 여행 중에 남자를 찾고 다니지 않습니다.
	ENDIF
CASE "요도を확장하기쉬움"
	INVERTBIT FLAG:276, 0
	IF CONFIG(ARGS)
		PRINTW 상당히 순조롭게 요도가 넓어지게 됩니다.
	ELSE
		PRINTW 좀처럼 넓어지지 않는다. 그것 또한 즐거움
	ENDIF
CASE "쇼타메이드"
	INVERTBIT FLAG:277, 0
	IF CONFIG(ARGS)
		PRINT 오토코노코 이외에 쇼타 (남자이면서 외견이 어림...…능력 표시의 속성에
		CALL PRINT_COLORTEXT, "[쇼타]", DEF_COLOR("노랑")
		PRINTW 로 표시됩니다)를 다방에서 일할 수 있도록 합니다
	ELSE
		PRINTW 다방에서 일할 수 있는 남자를 오토코노코로만 하겠습니다.
	ENDIF
CASE "匂いの표기변경"
	IF FLAG:278 < 100
		PRINTFORML 문장에 등장하는 「匂い」라는 단어의 표기의 일부를 당신 기호로 변경할 수 있습니다.
		PRINTFORM 구체적으로는、
		CALL PRINTL_COLORTEXT, "일반적으로 좋다고 해도 불쾌하다고는 단정하기 어려운 냄새가 나는 냄새", DEF_COLOR("황색")
		CALL PRINT_COLORTEXT, "……예를 들면, 좋아하는 상대의 성기 냄새나 체취입니다.", DEF_COLOR("황색")
		PRINTFORML 의 표기를 골라 주세요.
		PRINTFORML （다만, 머리카락의 냄새, 요리의 냄새 등의 일반적으로 좋은 향기의 것은,「내음」로 고정,
		PRINTFORMW 두피의 냄새, 배설물의 냄새 등의 일반적으로 좋지 않은 향기는「냄새」로 고정합니다）
		PRINTFORML 
	ENDIF
	PRINTFORML 그럼 다음 중에 선택해주세요
	PRINTL 
	PRINTFORML [ 0] 내음
	PRINTFORML [ 1] 냄새
	PRINTFORML [ 2] 향내
	PRINTFORML [ 3] 향기

	CALL INPUT_SELECT, 4

	FLAG:278 = RESULT

	PRINTFORMW %조사선택(TEXTS("匂い"), "를")% 선택했습니다. 다음 문장은 예문입니다.
	PRINTL 
	PRINTFORMW 팬티 너머 보지의 %TEXTS("匂い")%……
	PRINTFORMW 촉촉히 밴 땀의 %TEXTS("匂い")%……
	PRINTFORMW 달아오른 육체에서 풍기는 야한 %TEXTS("匂い")%……
	PRINTFORMW 질구를 꽉 조이거나 크게 열게 하여 태내의 %조사선택(TEXTS("匂い"), "을")% 풍긴다……
	PRINTFORMW 코앞에 들이밀어진 페니스의 %TEXTS("匂い")%……
	PRINTFORMW 땀 섞인 음란한 %TEXTS("匂い")%……
	PRINTFORMW 오래된 당신의 셔츠의 %TEXTS("匂い")%……
	PRINTL 
	PRINTFORML ……%조사선택(TEXTS("匂い"), "로")% 하시겠습니까？
	PRINTFORML [ 0] 예
	PRINTFORML [ 1] 다시 생각한다

	CALL INPUT_SELECT, 2

	IF RESULT == 0
		PRINTFORMW 그럼 계속 즐겨주세요
	ELSE
		FLAG:278 = 100
		RESTART
	ENDIF
CASE "성희롱금지"
	INVERTBIT FLAG:281, 0
	IF CONFIG(ARGS)
		PRINTFORML 선택지 없이 성희롱을 삼가도록 합니다
		PRINTFORMW （기본적으로는 발생 이벤트의 종류가 줄어 듭니다.）
	ELSE
		PRINTFORMW %마스터가()% 선택지 없이 손버릇이 나쁜 짓을 하도록 합니다.
	ENDIF
CASE "蓄精"
	INVERTBIT FLAG:282, 0
	IF CONFIG(ARGS)
		PRINTFORMW 하루밤 개방등의 일부 이벤트에서 보온병에 정액을 모으는 선택지가 나오게 됩니다.
	ELSE
		PRINTFORMW 조교 중에만 보온병에 정액을 모으는 선택지가 등장합니다.
	ENDIF

CASE "유두の상세설정"
	REDRAW 0

	CALL PRINT_STR, "유두의 상세… 구체적으로는, _황색_유두비대_와 _황색_함몰유두_와 _황색_거유륜_의 존재를 ON/OFF합니다_L"
	PRINTFORML ON하는 것에 〇를、OFF하는 것에 ✕를 해주세요.
	PRINTL
	PRINTFORML  [ 0] \@ CONFIG("유두비대") ? ○ # ✕\@ 유두비대
	PRINTFORML  [ 1] \@ CONFIG("함몰유두") ? ○ # ✕\@ 함몰유두
	PRINTFORML  [ 2] \@ CONFIG("거유륜") ? ○ # ✕\@ 거유륜
	PRINTFORML [100] 이걸로 좋다

	CALL INPUT_SELECT, 3, 100

	SELECTCASE RESULT
	CASE 100
		REDRAW 1
		RETURN 0
	CASE 0 TO 3
		INVERTBIT FLAG:283, RESULT
	ENDSELECT
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
CASE "ボイス"
	PRINTFORM 현재의 설정：
	IF FLAG:500 == 1
		PRINTFORML 로리
	ELSEIF FLAG:500 == 2
		PRINTFORML 성인
	ELSEIF FLAG:500 == 3
		PRINTFORML 상대에 맞춰서 자동선택
	ELSE
		PRINTFORML 표준
	ENDIF
	PRINTL 음성을 설정합니다
	PRINTL [0] 표준
	PRINTL [1] 로리
	PRINTL [2] 성인
	PRINTL [3] 상대에 맞춰서 자동선택
	CALL INPUT_SELECT, 4
	FLAG:500 = RESULT
	IF RESULT == 0
		LASTWORD = [표준]
	ELSEIF RESULT == 1
		LASTWORD = [로리]
	ELSEIF RESULT == 2
		LASTWORD = [성인]
	ELSEIF RESULT == 3
		LASTWORD = [상대에 맞춰서 자동선택]
	ENDIF
	PRINTFORMW %LASTWORD로()% 설정합니다
ENDSELECT

@NAME_CONVERT, P, M
#DIM LCOUNT

CALL SHOW_CHARA_LIST_PRE, "名前변경"
CALL SHOW_CHARA_LIST, "名前변경", P, M

$INPUT_LOOP
INPUT
IF RESULT == 100
	RETURN 0
ELSEIF RESULT == 2000 && CONFIG("세대別표시")
	M = 0
	P = 0
	RESTART
ELSEIF RESULT == 2001 && CONFIG("세대別표시")
	M += 1
	P = 0
	RESTART
ELSEIF RESULT == 2002 && CONFIG("세대別표시")
	M -= 1
	P = 0
	RESTART
ELSEIF RESULT >= 1001 && RESULT <= 1000+Q && CONFIG("표시人数") && Q >= 2 && P != RESULT - 1001
	P = RESULT - 1001
	RESTART
ELSEIF RESULT < MASTER || RESULT >= CHARANUM
	GOTO INPUT_LOOP
ENDIF

CALL NAME_CONVERT_MAIN, RESULT

RESTART

;選んだキャラはARGに格納されている
@NAME_CONVERT_MAIN, ARG
#DIM CHOICE
#DIMS MEMO_NAME
#DIMS MEMO_CALLNAME
#DIMS MEMO_CNAME
#DIMS MEMO_CNAME_KANA


;名前と呼び名を記録
MEMO_NAME = %NAME:ARG%
MEMO_CALLNAME = %CALLNAME:ARG%

PRINTL 
PRINTFORML %NAME:ARG%의 어느 것을 변경하시겠습니까?
PRINTFORML  [ 0] %"이름（"+NAME:ARG+"）을 변경", 30, LEFT%（능력표시 등）
PRINTFORML  [ 1] %"호칭（"+CALLNAME:ARG+"）을 변경", 30, LEFT%（99％이상은 이쪽）
;PRINTFORML  [ 2] 닉네임을 변경（일부 캐릭터를 제외하고 설정되지 않습니다. 쓸데도 없음）
IF ARG != MASTER
	PRINTFORM  [10] 주인과 대화할 때의 1인칭（%CNAMET(ARG, "一人称")%、독음：%CNAMET(ARG, "一人称", "かな")%）
	PRINTFORML  %조사만처리(CNAMET(ARG, "一人称", "かな"),"을")% 변경
	PRINTFORM  [11] 주인과 대화할 때의 2인칭（%CNAMET(ARG, "二人称")%、독음：%CNAMET(ARG, "二人称", "かな")%）
	PRINTFORML  %조사만처리(CNAMET(ARG, "二人称", "かな"),"을")% 변경
ENDIF
PRINTFORML [100] 변경하지 않는다

IF ARG != MASTER
	CALL INPUT_SELECT, 2, 10, 11, 100
ELSE
	CALL INPUT_SELECT, 2, 100
ENDIF

CHOICE = RESULT

SELECTCASE CHOICE
CASE 100
	PRINTFORMW 캐릭터 선택화면으로 돌아옵니다.
	RETURN 0
CASE 0
	PRINTFORML %NAME : ARG%의 이름을 변경합니다.
	PRINTFORML (전각8자(반각이면 16자)이내로 입력해주세요)
	PRINTFORML 아무것도 입력하지 않는 경우에는 변경하지 않습니다.
	CALL INPUTS_SELECT, 16
CASE 1
	PRINTFORML %NAME:ARG%의 호칭%조사선택("「"+CALLNAME:ARG+"」", "을")% 변경합니다.
	PRINTFORML (전각8자(반각이면 16자)이내로 입력해주세요)
	PRINTFORML 아무것도 입력하지 않는 경우에는 변경하지 않습니다.
	CALL INPUTS_SELECT, 16
CASE 10
	PRINTFORML %조사선택(NAME:ARG, "가")% 주인과 이야기할 때의 일인칭을 변경합니다
	PRINTFORML 전각 15문자(반각이면 30문자) 이내로 입력해주세요
	PRINTFORML 덧붙여, "%CALLNAME:TARGET% 님"이라고 부르고 싶은 경우에는 "CALLNAME_TARGET 님"이라고 입력해 주세요.
	PRINTFORML 아무것도 입력하지 않는 경우에는 변경하지 않습니다.
	CALL INPUTS_SELECT, 30
CASE 11
	PRINTFORML %조사선택(NAME:ARG,"가")%、주인과 이야기할 때의 이인칭을 변경합니다
	PRINTFORML 전각 15문자(반각이면 30문자) 이내로 입력해주세요
	PRINTFORML 덧붙여, "%CALLNAME:MASTER% 님"이라고 부르고 싶은 경우에는 "CALLNAME_MASTER 님"이라고 입력해 주세요.
	PRINTFORML 아무것도 입력하지 않는 경우에는 변경하지 않습니다.
	CALL INPUTS_SELECT, 30
ENDSELECT

PRINTL 
SELECTCASE RESULTS
CASE ""
	PRINTFORMW 변경하지 않았습니다
	RESTART
CASEELSE
	SELECTCASE CHOICE
	CASE 0
		IF RESULTS == MEMO_NAME
			PRINTFORMW 변경하지 않았습니다
			RESTART
		ENDIF
		NAME:ARG = %RESULTS%
		PRINTFORMW 이름을 %조사선택(RESULTS, "로")% 변경했습니다
	CASE 1
		IF RESULTS == MEMO_CALLNAME
			PRINTFORMW 변경하지 않았습니다
			RESTART
		ENDIF
		CALLNAME:ARG = %RESULTS%
		PRINTFORMW 호칭을 %조사선택(RESULTS, "로")% 변경했습니다
	CASE 10
		IF RESULTS == CEVENTS("一人称주인", ARG)
			PRINTFORMW 변경하지 않았습니다
			RESTART
		ENDIF

		MEMO_CNAME = 一人称주인：%RESULTS%
		MEMO_CNAME_KANA = 一人称주인かな：%RESULTS%

		;呼び名フラグリセット
		CALL DEL_CEVENT_GROUP_EX, "一人称주인", ARG
		CALL SET_CEVENT, MEMO_CNAME, ARG
		PRINTFORMW 주인과 이야기 할 때의 1인칭을 %조사선택(CNAMET(ARG, "一人称"), "로")% 변경했습니다.
		PRINTL 
		PRINTFORML 이어서, 이 1인칭의 독음을 입력합니다.
		PRINTFORML （아무것도 입력하지 않으면 %조사선택(CNAMET(ARG, "一人称"), "가")% 독음이 됩니다）
		PRINTL 
		PRINTFORML (전각30문자 (반각이면 60문자) 이내로 입력해주세요)
		PRINTFORML 일인칭에, "%CALLNAME:TARGET%様"이라고 입력했다면, 마찬가지로 "CALLNAME_TARGET님"라고 입력해 주세요.

		CALL INPUTS_SELECT, 60

		PRINTL 
		SIF RESULTS != ""
			MEMO_CNAME_KANA = 一人称주인かな：%RESULTS%

		CALL SET_CEVENT, MEMO_CNAME_KANA, ARG

		PRINTFORML 주인과 이야기 할 때의 1인칭을 %CNAMET(ARG, "一人称")%, 
		PRINTFORMW 그 독음을 %조사선택(CNAMET(ARG, "一人称", "かな"), "로")% 변경했습니다.

		RESTART
	CASE 11
		IF RESULTS == CEVENTS("二人称주인", ARG)
			PRINTFORMW 변경하지 않았습니다
			RESTART
		ENDIF

		MEMO_CNAME = 二人称주인：%RESULTS%
		MEMO_CNAME_KANA = 二人称주인かな：%RESULTS%

		;呼び名フラグリセット
		CALL DEL_CEVENT_GROUP_EX, "二人称주인", ARG
		CALL SET_CEVENT, MEMO_CNAME, ARG
		PRINTFORMW 주인과 이야기 할 때의 2인칭을 %조사선택(CNAMET(ARG, "二人称"), "로")% 변경했습니다.
		PRINTL 
		PRINTFORML 이어서, 이 2인칭의 독음을 입력합니다.
		PRINTFORML （아무것도 입력하지 않으면 %조사선택(CNAMET(ARG, "二人称"), "가")% 독음이 됩니다）
		PRINTL 
		PRINTFORML (전각30문자 (반각이면 60문자) 이내로 입력해주세요)
		PRINTFORML 2인칭에, "%CALLNAME:MASTER%様"이라고 입력했다면, 마찬가지로 "CALLNAME_MASTER님"라고 입력해 주세요.

		CALL INPUTS_SELECT, 60

		PRINTL 
		SIF RESULTS != ""
			MEMO_CNAME_KANA = 二人称주인かな：%RESULTS%

		CALL SET_CEVENT, MEMO_CNAME_KANA, ARG

		PRINTFORML 주인과 이야기 할 때의 2인칭을 %CNAMET(ARG, "二人称")%, 
		PRINTFORMW 그 독음을 %조사선택(CNAMET(ARG, "二人称", "かな"), "로")% 변경했습니다.

		RESTART
	ENDSELECT
ENDSELECT

;名前を変えた
IF NAME:ARG != MEMO_NAME && CALLNAME:ARG != NAME:ARG
	PRINTFORML 계속해서 호칭（%CALLNAME:ARG%）도 변경하나요?
	PRINTFORML [ 0] 예
	PRINTFORML [ 1] 아니오
	PRINTFORML [ 2] 호칭을 이름（%NAME:ARG%）과 같게한다
	CALL INPUT_SELECT, 3
	IF RESULT == 0
		PRINTFORML %NAME:ARG%의 호칭%조사처리("「"+CALLNAME:ARG+"」", "를")% 변경합니다.
		PRINTFORML 호칭을 입력해주세요. 아무것도 입력하지 않으면 변경하지 않습니다. 

		CALL INPUTS_SELECT, 16

		PRINTL 
		SELECTCASE RESULTS
		CASE "", MEMO_CALLNAME
			PRINTFORMW 변경하지않았습니다
		CASEELSE
			CALLNAME:ARG = %RESULTS%
			PRINTFORMW 호칭을 %조사선택(RESULTS, "로")% 변경했습니다
		ENDSELECT
	ELSEIF RESULT == 2
		CALLNAME:ARG = %NAME:ARG%
		PRINTFORMW 호칭을 이름과 같은 %조사선택("「"+CALLNAME:ARG+"」", "로")% 합니다
	ENDIF
ENDIF

;呼び名を変えた場合にはニックネームも変更
IF NICKNAME:ARG != "" && CALLNAME:ARG != MEMO_CALLNAME
	IF STRFIND(CALLNAME:ARG, "・") > 0 || STRFIND(CALLNAME:ARG, "＝") > 0
		NICKNAME:ARG = %SUBSTRING(CALLNAME:ARG, 0, MAX(STRFIND(CALLNAME:ARG, "・"), STRFIND(CALLNAME:ARG, "＝") ) - 1)%
	ELSEIF STRFIND(CALLNAME:ARG, " ") > 0
		NICKNAME:ARG = %SUBSTRING(CALLNAME:ARG, STRFIND(CALLNAME:ARG, " ") + 1, -1)%
	ELSEIF STRFIND(CALLNAME:ARG, " ") > 0 || STRFIND(CALLNAME:ARG, "　") > 0
		NICKNAME:ARG = %SUBSTRING(CALLNAME:ARG, STRFIND(CALLNAME:ARG, "　") + 2, -1)%
	ELSE
		NICKNAME:ARG = %CALLNAME:ARG%
	ENDIF
	PRINTFORMW 호칭이 %조사선택("「"+CALLNAME:ARG+"」", "로")% 변경되었으므로, 별명을 %조사선택(NICKNAME:ARG,"로")% 했습니다
ENDIF

RESTART

;-------------------------------------------------
;キャラクターの並び替えを行う
;合わせて@SORT_CHARA, ARGSも参照すること
;-------------------------------------------------
@CHARA_SORT
#DIM LCOUNT
#DIM MEMO_LINECOUNT
#DIM CHOICE, 3
#DIMS CHOICES, 2

MEMO_LINECOUNT = LINECOUNT

REDRAW 0
PRINTL 
PRINTFORML 캐릭터 정렬을 실행합니다
PRINTFORML 둘의 차례를 바꾸고 싶은 경우에는 계속해서 둘을 선택해주세요
PRINTFORML 또, 호감도나 캐릭터번호 등으로 한번에 정렬하고 싶은 경우에는 [200]을 선택해주세요

FOR LCOUNT, 2, CHARANUM
	CALL PRINT_NUM_NAME, "체력", LCOUNT
NEXT
PRINTL  [100]돌아간다　[200]일괄 정렬

$INPUT_LOOP
CALL INPUT_SELECT, CHARANUM, 100, 200

SELECTCASE RESULT
CASE 0, 1
	GOTO INPUT_LOOP
CASE 100
	REDRAW 1
	RETURN 0
CASE 200
	PRINTL 
	PRINTFORML 어느 요소를 기준으로 정렬합니까？
	PRINTFORML  [ 0] 가입순서　　　　　　　　（이것의 오름차순이 초기 정렬순입니다）
	PRINTFORML  [ 1] 캐릭터번호　　　　　　　（소환할 때에 표시되어 있는 번호순입니다）
	PRINTFORML  [ 2] 호감도
	PRINTFORML  [ 3] 마지막으로 상대한 날짜　（최근 상대한 캐릭터일수록 값이 큽니다）
	PRINTFORML  [ 4] ＬＶ
	PRINTFORML  [ 5] 평가포인트　　　　　　　（소위 매각금액 순서입니다）
	PRINTFORML  [ 6] 신장
	PRINTFORML  [ 7] 가슴 크기
	PRINTFORML [100] 돌아간다

	CALL INPUT_SELECT, 8, 100

	CHOICE = RESULT

	SELECTCASE CHOICE
	CASE 100
		CLEARLINE LINECOUNT - MEMO_LINECOUNT
	CASEELSE
		SELECTCASE CHOICE
		CASE 0
			CHOICES = 가입순
		CASE 1
			CHOICES = 캐릭터번호
		CASE 2
			CHOICES = 호감도
		CASE 3
			CHOICES = 마지막으로 상대한 날짜
		CASE 4
			CHOICES = ＬＶ
		CASE 5
			CHOICES = 평가포인트
		CASE 6
			CHOICES = 신장
		CASE 7
			CHOICES = 가슴 크기
		ENDSELECT

		PRINTL 선택된 요소가 큰 쪽과 작은 쪽, 어느쪽을 위로 놓습니까？
		PRINTL [ 0] 작은 쪽（오름차순）
		PRINTL [ 1] 큰 쪽　（내림차순）

		CALL INPUT_SELECT, 2

		SELECTCASE RESULT
		CASE 0
			CHOICES:1 = 오름차순
		CASE 1
			CHOICES:1 = 내림차순
		ENDSELECT

		CALL SORT_CHARA, CHOICES, CHOICES:1

		PRINTFORMW 캐릭터 전원을 [%CHOICES%]의 [%CHOICES:1%]으로 정렬했습니다
		CLEARLINE LINECOUNT - MEMO_LINECOUNT
		;履歴を残してみる
		PRINTFORML 캐릭터 전원을 [%CHOICES%]의 [%CHOICES:1%]으로 정렬했습니다
	ENDSELECT

	RESTART
ENDSELECT

CHOICE:1 = RESULT
CLEARLINE LINECOUNT - MEMO_LINECOUNT
MEMO_LINECOUNT = LINECOUNT

PRINTL 
PRINTFORML 순서를 교체할 첫번째 [{CHOICE:1, 2}]%조사처리(CALLNAME:(CHOICE:1),"를")% 선택했습니다
PRINTL 
PRINTFORML 한명 더 선택해주세요

FOR LCOUNT, 2, CHARANUM
	SIF LCOUNT == CHOICE:1
		SETCOLOR DEF_COLOR("황색")

	CALL PRINT_NUM_NAME, "체력", LCOUNT

	RESETCOLOR
NEXT
PRINTL  [100]돌아간다

$INPUT_LOOP_2
CALL INPUT_SELECT, CHARANUM, 100

SELECTCASE RESULT
CASE 0, 1, CHOICE:1
	GOTO INPUT_LOOP_2
CASE 100
	CLEARLINE LINECOUNT - MEMO_LINECOUNT
	RESTART
ENDSELECT

CHOICE:2 = RESULT

PRINTFORMW [{CHOICE:1, 2}]%조사처리(CALLNAME:(CHOICE:1),"과")% [{CHOICE:2, 2}]%CALLNAME:(CHOICE:2)%의 순서를 바꿨습니다

CLEARLINE LINECOUNT - MEMO_LINECOUNT
;履歴を残してみる
PRINTFORML [{CHOICE:1, 2}]%조사처리(CALLNAME:(CHOICE:1),"과")% [{CHOICE:2, 2}]%CALLNAME:(CHOICE:2)%의 순서를 바꿨습니다

CALL SWAP_CHARA, CHOICE:1, CHOICE:2

RESTART

