@COM303
;조른다
;実際には고통があるためもっと체력기력をもっていかれる

PRINTL 조른다

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 조른다

RETURN 1

@FLAG_COM303, ARG

@SOURCE_COM303, ARG
VARSET LOCAL

LOCAL:20 += 120
LOCAL:21 += 120

LOCAL:3 = 600
LOCAL:13 = 800
LOCAL:14 = 1000

;PALAM:고통をみる
SELECTCASE PALAM:고통
CASE IS < PALAMLV:1
	LOCAL:6 = 4000
CASE IS < PALAMLV:2
	LOCAL:6 = 4400
CASE IS < PALAMLV:3
	LOCAL:6 = 4800
CASE IS < PALAMLV:4
	LOCAL:6 = 5200
CASE IS >= PALAMLV:4
	LOCAL:6 = 5600
ENDSELECT

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM303
PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% %CALLNAME:ASSI%에게 명령해 %조사처리(CALLNAME:TARGET,"를")% 조르게 했다…
PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ASSI%의 긴 꼬리에 휘감겨, 전신을 꽉 졸랐다…
