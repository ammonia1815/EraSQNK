@COM306
;하드스킨
;스투나専用技. 効果시간を無限にしました. 
IF STATE("하드스킨", PLAYER)
	PRINTL 하드스킨해제
ELSE
	PRINTL 하드스킨
ENDIF

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 하드스킨

IF STATE("하드스킨", PLAYER)
	CALL REMOVE_STATE, MASTER, "하드스킨"
	SIF ASSI
		CALL REMOVE_STATE, ASSI, "하드스킨"
ELSE
	CALL ADD_STATE, MASTER, "하드스킨"
	SIF ASSI
		CALL ADD_STATE, ASSI, "하드스킨"
	CALL KOJO_MESSAGE_COM
	;Ｗ넘어뜨리기等での구상処理
	CALL KOJO_MESSAGE_COM_MULTI
ENDIF

PREVCOM = 306

RETURN 0

@TRAIN_MESSAGE_COM306
IF STATE("하드스킨", PLAYER)
	IF NO:PLAYER == 34
		PRINTFORM %조사처리(CALLNAME:PLAYER,"는")% 
	ELSEIF NO:TARGET == 34
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	ELSE
		PRINTFORM %조사처리(CALLNAME:ASSI,"는")% 
	ENDIF
	PRINTFORMW 하드스킨의 효과를 해제했다…
ELSE
	IF NO:PLAYER == 34
		PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 피부를 돌처럼 단단하게 했다! 
	ELSEIF NO:TARGET == 34
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 돌처럼 단단하게 했다! 
	ELSE
		PRINTFORMW %조사처리(CALLNAME:ASSI,"는")% 피부를 돌처럼 단단하게 했다! 
	ENDIF
	PRINTFORMW \@ ASSI ? %CALLNAME:PLAYER%들은 # %조사처리(CALLNAME:PLAYER,"는")% \@ 쾌감을 얻기 힘들어졌다! 
	IF PENIS(MASTER)
		PRINTFORM %CALLNAME:MASTER%
		IF ASSI
			SIF PENIS(ASSI)
				PRINT 들
		ENDIF
		PRINTFORM 의 페니스가 한층 더 딱딱해
		IF INSERT("인서트")
			PRINTFORMW 져, %CALLNAME:TARGET%의 질벽을 그 모양대로 벌린다…! 
		ELSE
			PRINTFORMW 졌다! 
		ENDIF
	ELSEIF ASSI
		IF PENIS(ASSI)
			PRINTFORM %CALLNAME:ASSI%의 페니스가 한층 더 딱딱해
			IF INSERT("인서트")
				PRINTFORMW 져, %CALLNAME:TARGET%의 질벽을 그 모양대로 벌린다…! 
			ELSE
				PRINTFORMW 졌다! 
			ENDIF
		ENDIF
	ENDIF
ENDIF


@EQUIP_COM306
RETURN 1

@EQUIP_COM306_2
PRINTL ＜하드스킨효과 발휘중＞

RETURN 1
