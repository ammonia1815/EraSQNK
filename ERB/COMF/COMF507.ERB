@COM507
;음주(TARGET)

PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 몹시 취해있다! 

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 음주

RETURN 1

@FLAG_COM507, ARG

@SOURCE_COM507, ARG
VARSET LOCAL

LOCAL:20 += 0
LOCAL:21 += 50

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM507
IF RAND:4 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%에게 안겨왔다! 
	IF TALENT:거유
		PRINTFORMW 커다란 가슴의 감촉이 정말 견딜 수 없다…! 
	ELSE
		PRINTFORMW 그리고, 몸을 비비기 시작했다…
	ENDIF
ELSEIF RAND:3 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 키스를 하려고 이쪽에 몸을 내밀었다……
ELSEIF RAND:2 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:PLAYER,"를")% 넋을 잃고 보고있다……
ELSEIF RAND:5 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 잠자고 싶어졌다…
ELSEIF RAND:4 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 두서없이 %CALLNAME:PLAYER%에게 말을 걸고있다……
ELSEIF RAND:3 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 우스꽝스럽게 웃고있다……
ELSEIF RAND:2 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%에게 술을 더 달라고 조르고 있다……
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 약간 속이 안 좋아진 것같다……
ENDIF
