@COM16
;음경맞대기
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = %STR:2%
;-------------------------------------------------
;사정체크
;-------------------------------------------------
CALL SAMEN_CHECK

RETURN 1

@FLAG_COM16, ARG
SIF CHECK_CLO("Ｖ노출") == 0
	CALL SETFLAG, "바텀을노출：텍스트없음", TARGET
;조교자のＰ⇔대상のＰの汚れが移動
CALL EQ_STAIN, TARGET, PLAYER, 2, 2


@SOURCE_COM16, ARG
VARSET LOCAL

LOCAL:20 += 50
LOCAL:21 += 100

LOCAL:3  = 500
LOCAL:8 = COM_ORDER_STAIN("Ｐ", "Ｐ")*50 + 60
LOCAL:11  = 800
LOCAL:12  = 600
LOCAL:13 = 500
LOCAL:14 = 50

;ABL:ＢＬ끼をみる
SELECTCASE ABL:ＢＬ끼
CASE 0
	LOCAL:4 = 200
	LOCAL:5 = 100
CASE 1
	LOCAL:4 = 250
	LOCAL:5 = 180
CASE 2
	LOCAL:4 = 300
	LOCAL:5 = 250
CASE 3
	LOCAL:4 = 350
	LOCAL:5 = 350
CASE 4
	LOCAL:4 = 400
	LOCAL:5 = 500
CASEELSE
	LOCAL:4 = 450
	LOCAL:5 = 800
ENDSELECT

;남자か후타나리
IF PENIS(TARGET)
	;ABL:Ｃ감각をみる
	LOCAL:0 = CALCF("Ｃ刺激", 1)
	LOCAL:0 = SOURCE_EXABL(LOCAL:0, 33, PLAYER)
ENDIF

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM16
IF SELECTCOM == PREVCOM && COND("前Ｔ조교자") == PLAYER
	PRINTFORMW %조사처리(CALLNAME:PLAYER,"와")%%CALLNAME:TARGET%하,%TEXTS("키스継続")%서로의 페니스를 계속 맞대고 비비고 있다…
ELSE
	PRINTFORMW %조사처리(CALLNAME:PLAYER,"와")%%CALLNAME:TARGET%하%TEXTS("키스継続")%서로의 페니스를 맞대고 비볐다…
ENDIF
