@COM210
;촉수피스톤
;촉수は侵入した後は好き勝손に蠢いてますが, 動きを強力な突き入れで統制する. 

PRINTL 촉수피스톤

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 촉수피스톤

RETURN 1

@FLAG_COM210, ARG
SIF TEQUIP:바이브
	CALL FLAG_COM8, 100
SIF TEQUIP:애널바이브
	CALL FLAG_COM2, 100
SIF TEQUIP:촉수구욕
	CALL FLAG_COM140, 100

@SOURCE_COM210, ARG
SIF TEQUIP:바이브
	CALL SOURCE_COM8, 100
SIF TEQUIP:애널바이브
	CALL SOURCE_COM2, 100
SIF TEQUIP:촉수구욕
	CALL SOURCE_COM140, 100

VARSET LOCAL

LOCAL:20 += 50
LOCAL:21 += 50

LOCAL:9 = 1000
LOCAL:14 = 1000

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM210
IF PREVCOM == SELECTCOM
	PRINTFORMW \@ CONFIG("촉수は주인") ? %조사처리(CALLNAME:MASTER,"는")% # 촉수는 \@ %CALLNAME:TARGET%의 몸으로 쾌락을 계속 탐\@FLAG:87 ? 했다 # 하고 있다\@…
	RETURN 1
ENDIF
PRINTFORM %CALLNAME:TARGET%
VARSET LOCAL
SIF TEQUIP:바이브
	LOCAL += 1
SIF TEQUIP:애널바이브
	LOCAL += 1
SIF TEQUIP:촉수구욕
	LOCAL += 1
IF TEQUIP:구속
	IF LOCAL == 3
		PRINT 의 몸의 모든 구멍을 꿰뚫고 있는 
	ELSEIF LOCAL == 2
		PRINT 의 꼼짝 못하는 몸을 찔러올리고 있는 
	ELSEIF TEQUIP:바이브
		PRINT 의 질에 비집고 들어가 안까지 닿아 있는 
	ELSEIF TEQUIP:애널바이브
		PRINTFORM %조사만처리(CALLNAME:TARGET,"가")% 쾌락에 정신줄을 놓을 정도로 안까지 비집고 들어가 있는 
	ELSE
		PRINTFORM %조사만처리(CALLNAME:TARGET,"를")% 일방적으로 괴롭히고 있는 
	ENDIF
ELSE
	IF LOCAL == 3
		PRINT 의 구멍이라는 구멍은 전부 메우고 있는 
	ELSEIF LOCAL == 2
		PRINTFORMW %조사만처리(CALLNAME:TARGET,"를")% 범하고 있는 
	ELSEIF TEQUIP:촉수구욕
		PRINT 의 입에 침입하고 있는 
	ELSEIF TEQUIP:바이브
		PRINT 의 질에서 꿈틀거리고 있는 
	ELSEIF TEQUIP:애널바이브
		PRINT 의 직장에서 또아리를 틀고 있는 
	ENDIF
ENDIF
PRINT 촉수가, 
IF TEQUIP:촉수구욕 == 2 && RANDIF(2, TEQUIP:바이브, TEQUIP:애널바이브)
	PRINT 목구멍을 안쪽까지 깊숙하게 문지르면서 
ELSEIF TEQUIP:바이브 && RANDIF(2, TEQUIP:애널바이브)
	IF SIZE("바이브", TARGET) >= 3 && RAND:2
		PRINT 살의 동굴을 넓히면서 
	ELSE
		PRINT 육벽을 문지르면서 
	ENDIF
ELSEIF TEQUIP:애널바이브 && RAND:2
	PRINT 내장을 밀어올리면서 
ENDIF

IF LOCAL >= 2
	IF RAND:2
		PRINT 일제히 
	ELSE
		PRINT 협력해서 
	ENDIF
ENDIF
IF SIZE("바이브", TARGET) >= 3 && RAND:3 == 0
	PRINT 복부를 튀어오르게 할 정도의 
ELSEIF SIZE("Ａ바이브", TARGET) >= 3 && RAND:3 == 0
	PRINT , 덜덜 떨릴 정도의 긴 스트로크로 
ELSEIF TEQUIP:바이브 + TEQUIP:애널바이브 && RAND:3 == 0
	PRINT 몸을 뒤흔드는 
ELSEIF RAND:2
	PRINT 격렬한 
ELSE
	PRINT 흉악한 
ENDIF
PRINTFORMW 피스톤 운동을 시작했다…

