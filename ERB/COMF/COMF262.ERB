@COM262
;이뇨제

PRINTL 이뇨제

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 이뇨제

SIF TEQUIP:촉수 == 0
	ITEM:이뇨제 -= 1

TEQUIP:이뇨제 = 1

RETURN 1

@FLAG_COM262, ARG
BASE:요의 += 5000

@SOURCE_COM262, ARG
VARSET LOCAL

;조합지식があれば소비を抑える
IF TALENT:MASTER:조합지식 || (ASSI && TALENT:ASSI:조합지식)
	LOCAL:20 += 50
	LOCAL:21 += 50
ELSE
	LOCAL:20 += 120
	LOCAL:21 += 120
ENDIF

LOCAL:14 = 2000
;이뇨제効果で軽く変動
LOCAL:15 = 150

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM262
IF TEQUIP:촉수
	PRINTFORML 아주 가늘은 촉수가, 이뇨작용이 있는 체액을 요도를 통해 흘려 넣었다…
ELSE
	PRINTFORM %CALLNAME:TARGET%에게 
	SIF ABL:PLAYER:기교 >= 3
		PRINT 입을 통해서 
	SIF ABL:순종 <= 2
		PRINT 억지로 
	PRINTFORML 이뇨제를 먹였다
ENDIF
PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 거친 숨을 쉬고, 
SIF CHECK_COM("강제방뇨") && TALENT:겁쟁이 + TALENT:내성적
	PRINT 눈물을 머금으며 
PRINTFORMW 오줌이 마려운 것을 참고있다…
