@COM139
;아무것도하지않는다
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = %STR:2%

RETURN 1


@FLAG_COM139, ARG
;全員の절정게이지が3000か최대치の半分ぐらいか下がる
BASE:절정 = LIMIT(BASE:절정 - MAX(MAXBASE:절정/2, 3000), 0, BASE:절정*3/4)
BASE:MASTER:절정 = LIMIT(BASE:MASTER:절정 - MAX(MAXBASE:MASTER:절정/2, 3000), 0, BASE:MASTER:절정*3/4)
SIF ASSI
	BASE:ASSI:절정 = LIMIT(BASE:ASSI:절정 - MAX(MAXBASE:ASSI:절정/2, 3000), 0, BASE:ASSI:절정*3/4)
IF INSERT("인서트")

ELSEIF INSERT("삽입中")

ELSEIF TEQUIP:구속 + TEQUIP:아이마스크

ELSEIF TEQUIP:목욕탕플레이
	;汚れを初期化
	CALL RESET_STAIN_EX, "全部", MASTER
	CALL RESET_STAIN_EX, "全部", TARGET
	;조수がいる場合は, 조수もきれいに
	SIF ASSI
		CALL RESET_STAIN_EX, "全部", ASSI
ENDIF

@SOURCE_COM139, ARG
VARSET LOCAL

LOCAL:20 -= 30
LOCAL:21 -= 30

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM139
PRINTFORM %조사처리(CALLNAME:PLAYER,"는")% 
IF INSERT("인서트")
	;기승위中
	IF INSERT("기승위")
		PRINTFORMW 위에 올라타서 허리를 흔들고있는 %조사처리(CALLNAME:TARGET,"를")% 바라보면서 휴식했다
		CALL TEXT, "추가地の文：기승위"
	ELSE
		PRINTFORMW 허리 움직임을 멈추고, 휴식을 하기로 했다
	ENDIF
ELSEIF INSERT("삽입中")
	IF INSERT("기승위", PLAYER)
		PRINTFORMW %조사처리(CALLNAME:TARGET,"를")% 자빠뜨린 자세로 허리를 멈추고, 얼굴을 내려다보면서 휴식했다
	ELSE
		PRINTFORMW 몸에서 힘을 빼고, %CALLNAME:TARGET%에게 범해지던 중이었지만 휴식을 제안해 봤다
	ENDIF
ELSEIF TEQUIP:구속 + TEQUIP:아이마스크
	PRINTFORMW 자유롭게 움직일 수 없는 %조사처리(CALLNAME:TARGET,"를")% %TEXTR("무시하고", "바라보면서")% 휴식했다
ELSEIF TEQUIP:목욕탕플레이
	PRINTFORMW %조사처리(CALLNAME:TARGET,"과")% 같이 욕탕에 들어가서 휴식했다
ELSE
	PRINTFORMW 아무것도 안하고 휴식했다
ENDIF
