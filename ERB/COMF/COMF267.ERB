@COM267
PRINTL 근육이완제

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 근육이완제

RETURN 1

@FLAG_COM267, ARG

@SOURCE_COM267, ARG
VARSET LOCAL

;일탈と공포
LOCAL:9 = 2000
LOCAL:14 = 1000

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM267
PRINTFORML %CALLNAME:TARGET%의 어느 곳에 근육이완제를 주사합니까? 
PRINTFORML (주의：극약이므로, 한번의 조교에서는 한 곳만 투여할 수 있습니다)
PRINTFORML  [ 0] 애널
SIF TALENT:남자 == 0
	PRINTFORML  [ 1] 요도주변
SIF TALENT:남자 == 0
	PRINTFORML  [ 2] 보지
SIF TALENT:남자 == 0 && TALENT:처녀 == 0 && TALENT:임신 == 0
	PRINTFORML  [ 3] 자궁구
PRINTFORML [100] 돌아간다

IF TALENT:남자 == 0 && TALENT:처녀 == 0 && TALENT:임신 == 0
	CALL INPUT_SELECT, 4, 100
ELSEIF TALENT:남자 == 0
	CALL INPUT_SELECT, 3, 100
ELSE
	CALL INPUT_SELECT, 1, 100
ENDIF

SELECTCASE RESULT
CASE 100
	TFLAG:14 = 1
	RETURN 0
CASEELSE
	TFLAG:16 = RESULT
	SETBIT TEQUIP:근육이완제, RESULT

	SELECTCASE RESULT
	CASE 0
		PRINTFORMW 근육이완제를 엄밀하게 계량해서 사용해, %CALLNAME:TARGET%의 항문 주변의 근육의 야무짐을 느슨하게 했습니다
		IF TALENT:약독내성 || TALENT:요정
			ABL:Ａ확장 += 1
			PRINTFORMW Ａ확장레벨＋１
		ELSE
			ABL:Ａ확장 += 2
			PRINTFORMW Ａ확장레벨＋２
		ENDIF
		CALL GET_EXABL_A, TARGET
		ABL:Ａ명기도 = ABL_A(TARGET)
	CASE 1
		PRINTFORMW 근육이완제를 엄밀하게 계량해서 사용해, %CALLNAME:TARGET%의 요도괄약근의 야무짐을 느슨하게 했습니다
	CASE 2
		PRINTFORMW 근육이완제를 엄밀하게 계량해서 사용해, %CALLNAME:TARGET%의 질의 야무짐을 느슨하게 했습니다
		IF TALENT:약독내성 || TALENT:요정
			ABL:Ｖ확장 += 1
			PRINTFORMW Ｖ확장레벨＋１
		ELSE
			ABL:Ｖ확장 += 2
			PRINTFORMW Ｖ확장레벨＋２
		ENDIF
		CALL GET_EXABL_V, TARGET
		ABL:Ｖ명기도 = ABL_V(TARGET)
	CASE 3
		PRINTFORMW 근육이완제를 엄밀하게 계량해서 사용해, %CALLNAME:TARGET%의 자궁구를 부드럽게 했습니다
	ENDSELECT
ENDSELECT

ITEM:근육이완제 -= 1

