@COM35
;니플캡
IF TEQUIP:촉수
	PRINTL 촉수유두자극
ELSE
	PRINTL 니플캡
ENDIF

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

IF TEQUIP:촉수
	STR:0 = 촉수유두자극
ELSE
	STR:0 = 니플캡
ENDIF

RETURN 1

@FLAG_COM35, ARG

@SOURCE_COM35, ARG
VARSET LOCAL

LOCAL:20 += 10
LOCAL:21 += 20

LOCAL:14 = 100

LOCAL:17 = CALCF("Ｂ刺激", 0, 40)
SIF TALENT:니플피어스
	TIMES LOCAL:17, 2.00

CALL CALC, "조교소스", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM35
IF TEQUIP:니플캡
	CALL COM_ABLE34
	IF RESULT && TEQUIP:클리캡
		PRINTFORML 클리캡도 뗍니까？
		PRINTFORML [ 0] 예
		PRINTFORML [ 1] 아니오
		CALL INPUT_SELECT, 2
		IF RESULT == 0
			PRINTFORMW %CALLNAME:TARGET%에게서 클리캡과 니플캡을 떼어내었다…
			TEQUIP:클리캡 = 0
			TEQUIP:니플캡 = 0
			RETURN 1
		ENDIF
	ENDIF
	PRINTFORMW %CALLNAME:TARGET%의 유두에서 니플캡을 떼어냈다…
	TEQUIP:니플캡 = 0
ELSE
	CALL COM_ABLE34
	IF RESULT && TEQUIP:클리캡 == 0
		PRINTFORML 클리캡도 답니까？
		PRINTFORML [ 0] 예
		PRINTFORML [ 1] 아니오
		CALL INPUT_SELECT, 2
		IF RESULT == 0
			PRINTFORMW %CALLNAME:TARGET%에게 클리캡과 니플캡을 달아주었다
			PRINTFORMW 두종류의 음구는 %CALLNAME:TARGET%의 민감한 장소를 집요하게 자극해 쾌락을 주고 있다…
			TEQUIP:클리캡 = 1
			TEQUIP:니플캡 = 1
			RETURN 1
		ENDIF
	ENDIF
	PRINTFORMW %CALLNAME:TARGET%의 유두에 잘게 진동하는 캡을 달아주었다
	PRINTFORMW 얼핏 니플리스처럼 보이는 그것은 피부에 딱 달라붙어, %CALLNAME:TARGET%의 유두에 달콤한 쾌락을 주고 있다…
	TEQUIP:니플캡 = 1
ENDIF

@TRAIN_MESSAGE_COM204
IF TEQUIP:니플캡
	PRINTFORMW 촉수는 %CALLNAME:TARGET%의 유두에서 떨어졌다…
	TEQUIP:니플캡 = 0
ELSE
	IF RAND:2 == 0 && CALCF("Ｂ발정")
		PRINTFORMW 세밀하게 꿈틀대는 촉수가 %CALLNAME:TARGET%의 유두를 희롱하기 시작했다…
	ELSE
		PRINTFORMW 커다랗게 입을 연 촉수가 %CALLNAME:TARGET%의 유두에 달라붙었다…
	ENDIF
	TEQUIP:니플캡 = 1
	CALL ADD_STAIN, TARGET, "Ｂ", "촉수"
ENDIF

@EQUIP_COM15
;니플캡装備中
CALL SOURCE_COM35, 100
CALL FLAG_COM35, 100
RETURN 1

@EQUIP_COM15_2
;니플캡装備中
IF TEQUIP:촉수
	PRINTL ＜촉수 유두자극중＞
ELSE
	PRINTL ＜니플캡 장착중＞
ENDIF

RETURN 1
