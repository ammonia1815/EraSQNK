@GET_TREASURE_EXPLORER
#DIM LCOUNT
VARSET LOCAL
;아이템を持てるか체크
FOR LCOUNT, 0, 50
	SIF LCOUNT <= 29 && ITEM:LCOUNT
		CONTINUE
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 96, 100
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 300, 330
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT

SIF ITEM:음마의물방울 + I:78 < 999
	LOCAL:78 = 1


CALL SET_PALAM_EXPLORER, "財宝期待値", RAND:100
SELECTCASE PALAM_EXPLORER("아이템레벨")
CASE 1
	IF PALAM_EXPLORER("財宝期待値") >= 61 && PALAM_EXPLORER("財宝期待値") <= 70 && LOCAL:40
		CALL PRINT_COLORTEXT, "재스민", DEF_COLOR("황색")
		PRINTFORML 을 땄습니다. 
		I:40 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 71 && PALAM_EXPLORER("財宝期待値") <= 80 && LOCAL:41
		CALL PRINT_COLORTEXT, "라벤더", DEF_COLOR("황색")
		PRINTFORML 를 땄습니다. 
		I:41 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 81 && PALAM_EXPLORER("財宝期待値") <= 85 && LOCAL:43
		CALL PRINT_COLORTEXT, "샌들우드", DEF_COLOR("황색")
		PRINTFORML 를 땄습니다. 
		I:43 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 86 && PALAM_EXPLORER("財宝期待値") <= 90 && LOCAL:44
		CALL PRINT_COLORTEXT, "로즈마리", DEF_COLOR("황색")
		PRINTFORML 를 땄습니다. 
		I:44 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") <= 90
		MONEY:5 = PALAM_EXPLORER("財宝期待値")
		CALL PRINT_COLORTEXT, MONEY_C(MONEY:5), DEF_COLOR("황색")
		PRINTFORML 를 주웠습니다. 
		MONEY:6 += MONEY:5
	ELSE
		CALL TREASURE_BOX_EXPLORER
	ENDIF
CASEELSE
	IF PALAM_EXPLORER("財宝期待値") >= 11 && PALAM_EXPLORER("財宝期待値") <= 90
		CALL TREASURE_BOX_EXPLORER
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 91 && PALAM_EXPLORER("財宝期待値") <= 92 && LOCAL:40
		CALL PRINT_COLORTEXT, "재스민", DEF_COLOR("황색")
		PRINTFORML 을 땄습니다. 
		I:40 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 93 && PALAM_EXPLORER("財宝期待値") <= 94 && LOCAL:41
		CALL PRINT_COLORTEXT, "라벤더", DEF_COLOR("황색")
		PRINTFORML 를 땄습니다. 
		I:41 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 95 && PALAM_EXPLORER("財宝期待値") <= 96 && LOCAL:43
		CALL PRINT_COLORTEXT, "샌들우드", DEF_COLOR("황색")
		PRINTFORML 를 땄습니다. 
		I:43 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 97 && LOCAL:44
		CALL PRINT_COLORTEXT, "로즈마리", DEF_COLOR("황색")
		PRINTFORML 를 땄습니다. 
		I:44 += 1
	ELSE
		MONEY:5 = PALAM_EXPLORER("財宝期待値") + 100
		CALL PRINT_COLORTEXT, MONEY_C(MONEY:5), DEF_COLOR("황색")
		PRINTFORML 를 주웠습니다. 
		MONEY:6 += MONEY:5
	ENDIF
ENDSELECT
WAIT

;宝箱
@TREASURE_BOX_EXPLORER

;歌声のトラップ
IF TEXTS("탐색先") == "안쇼스海" && RAND:5 == 0
	PRINT 어디선가 노랫소리가 들려온다. 이것은…

	SELECTCASE RAND:5
	CASE 3, 4
		PRINT 매혹의 노래다! 
		IF RAND:100 < 80
			IF RAND:2
				PRINTFORML …고혹적인 노랫소리가 %CALLNAME:PLAYER%의 정력을 잠식해간다…
				BASE:PLAYER:정력 -= RAND:6 + 6
			ELSE
				PRINTFORML …%조사처리(CALLNAME:PLAYER,"는")% 그 유혹에 거역할 수 없었다…
				BASE:PLAYER:정력 -= RAND:4 + 3
			ENDIF
		ELSE
			PRINTFORML …그러나 %조사처리(CALLNAME:PLAYER,"는")% 그 유혹을 뿌리쳤다
		ENDIF
	CASE 1, 2
		IF RAND:10 == 0 && PALAM_EXPLORER("플로어") < PALAM_EXPLORER("最深플로어")
			PRINTFORML 유혹의 노래다!…아름다운 노랫소리가 %조사처리(CALLNAME:PLAYER,"를")% 안쪽으로 이끈다…
			CALL SET_PALAM_EXPLORER, "플로어", 1
			PRINTFORM {PALAM_EXPLORER("플로어")}Ｆ 탐색개시…
		ELSE
			PRINT 미혹의 노래다! 
			IF RAND:5 >= 2 && PALAM_EXPLORER("플로어") > 1
				PRINTFORML …%조사처리(CALLNAME:PLAYER,"는")% 노랫소리에 혹해 길을 되돌아갔다…
				CALL SET_PALAM_EXPLORER, "플로어", -1
				PRINTFORM 다시 {PALAM_EXPLORER("플로어")}Ｆ 탐색개시…
			ELSEIF RAND:4 == 0 && PALAM_EXPLORER("플로어") > 4
				PRINTFORML 
				PRINTFORML ……
				PRINTFORML ………
				PRINTFORML 정신을 차리자, %조사처리(CALLNAME:PLAYER,"는")% 저택 앞이었다…
				COUNT:99 = 100
			ELSE
				PRINTFORML …그러나 %조사처리(CALLNAME:PLAYER,"는")% 유혹당하지않고, 다시 안쪽을 향해 걷기시작했다…
			ENDIF
		ENDIF
	CASEELSE
		PRINT 평온의 노래다! 
		IF RAND:2
			PRINTFORML …아름다운 노랫소리는 %CALLNAME:PLAYER%의 몸에 평온함을 가져왔다…
			BASE:PLAYER:정력 = MIN(BASE:PLAYER:정력 + 4, MAXBASE:PLAYER:정력)
		ELSE
			PRINTFORML …노랫소리에 감춰진 마력으로 %CALLNAME:PLAYER%의 정력이 회복되었다…
			BASE:PLAYER:정력 = MIN(BASE:PLAYER:정력 + 2, MAXBASE:PLAYER:정력)
		ENDIF
	ENDSELECT
	RETURN 1
ENDIF


PRINTFORM 보물상자를 발견했다. 
;トラップ発動。通常は20％。罠解除持ちだと５％。誘発持ちだと50％
IF (ASSI <= 0 && RAND:5 == 0) || (ASSI && RAND:(5 + COND("탐색：罠解除", ASSI) - COND("탐색：罠誘発", ASSI) ) == 0)
	PRINT 트랩
	IF RAND:5 == 0
		PRINTFORML ? 리난시의 함정이다!…%CALLNAME:PLAYER%에게 행운이 내려왔다! 
		CALL SET_PALAM_EXPLORER, "幸運", RAND:3 + 2
		CALL SET_PALAM_EXPLORER, "아이템発見率", 50
		CALL SET_PALAM_EXPLORER, "아이템레벨", 3
	ELSEIF PALAM_EXPLORER("아이템레벨") >= 5 && RAND:6 == 0
		PRINTFORML ! 릴리스의 함정이다! %조사처리(CALLNAME:PLAYER,"는")% ……굉장히 기분좋았다. 
		BASE:PLAYER:정력 -= RAND:6 + 5
	ELSEIF PALAM_EXPLORER("아이템레벨") >= 3 && RAND:5 == 0
		PRINTFORML ! 이스케이프의 함정이다! %조사처리(CALLNAME:PLAYER,"는")% 조금 먼 곳으로 날아가버렸다! 
		COUNT:99 += RAND:5 + 1
	ELSEIF RAND:4 == 0
		PRINTFORML ! 독함정이다! %조사처리(CALLNAME:PLAYER,"는")% 느끼기 쉽게되었다! 
		IF COND("탐색：상태異常無効持ちが居る")
			PRINTFORML …하지만, 곧바로치료했다.
		ELSE
			CALL SET_PALAM_EXPLORER, "센시브", RAND:4 + 3
		ENDIF
	ELSEIF RAND:3 == 0
		PRINTFORML ! 아르카나의 함정이다! %조사처리(CALLNAME:PLAYER,"는")% 굉장히 서툴러지게 되었다! 
		IF COND("탐색：상태異常無効持ちが居る")
			PRINTFORML …하지만, 곧바로 치료했다.
		ELSE
			CALL SET_PALAM_EXPLORER, "クラムジ", RAND:4 + 3
		ENDIF
	ELSEIF RAND:2 == 0
		PRINTFORML ! 하렘의 함정이다! 몽마가 일제히 이쪽으로 온다! 
		LOCAL = RAND:4 + 3
		CALL SET_PALAM_EXPLORER, "ハーレム", LOCAL
		CALL SET_PALAM_EXPLORER, "遭遇率", LOCAL*10
	ELSE
		PRINTFORML !서큐버스의 함정이다! %조사처리(CALLNAME:PLAYER,"는")% ……기분좋았다. 
		BASE:PLAYER:정력 -= RAND:6 + 1
	ENDIF
	RETURN 0
ENDIF

CALL TREASURE_BOX_EXPLORER_B



;-------------------------------------------------
;宝箱の中身決定関数
;ARGは戦闘戦利品のときのみ, 상대のTARGETが代入される
;-------------------------------------------------
@TREASURE_BOX_EXPLORER_B, ARG
#DIM LCOUNT
VARSET LOCAL
;아이템を持てるか체크
FOR LCOUNT, 0, 50
	SIF LCOUNT <= 29 && ITEM:LCOUNT
		CONTINUE
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 96, 100
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 300, 330
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
SIF ITEM:음마의물방울 + I:350 < 999
	LOCAL:350 = 1

SELECTCASE RAND:5
CASE 0
	IF LOCAL:99 && RAND:3
		PRINTFORM 안에는 
		CALL PRINT_COLORTEXT, "문드롭스", DEF_COLOR("황색")
		PRINTFORML 가 들어있었다. 
		IF PLAYER != MASTER && COND("몽마", PLAYER)
			PRINTFORM %조사처리(CALLNAME:PLAYER,"는")% 크게 만족했다. 
			CFLAG:PLAYER:2 += 50
			BASE:PLAYER:정력 = MAXBASE:PLAYER:정력
		ELSE
			I:99 += 1
		ENDIF
		RETURN 0
	ELSEIF LOCAL:312 && RANDIF(1, LOCAL:96, LOCAL:311)
		PRINTFORM 안에서 
		CALL PRINT_COLORTEXT, "배추흰나비", DEF_COLOR("황색")
		PRINTFORML 가 팔랑팔랑 날아왔다. 
		I:312 += 1
		RETURN 0
	ELSEIF LOCAL:311 && RANDIF(1, LOCAL:96)
		PRINTFORM 안에는 
		CALL PRINT_COLORTEXT, "사고를태우는불꽃", DEF_COLOR("황색")
		PRINTFORML 이 들어있었다. 
		I:311 += 1
		RETURN 0
	ELSEIF LOCAL:96
		PRINTFORM 안에는 
		CALL PRINT_COLORTEXT, "사고연마의돌", DEF_COLOR("황색")
		PRINTFORML 이 들어있었다. 
		I:96 += 1
		RETURN 0
	ENDIF
;악세사리
CASE 1
	PRINTFORM 안에는 

	;특수な악세사리
	IF ARG > 0
		SELECTCASE CALLNAME:ARG
		CASE "뷔이브"
			CALL PRINT_COLORTEXT, "아련한보석", DEF_COLOR("황색")
			I:325 += 1
			PRINTFORML 이 들어있었다. 
			RETURN 0
		ENDSELECT
	ENDIF

	SELECTCASE RAND:9
	CASE 0 TO 1
		CALL PRINT_COLORTEXT, "아련한팔찌", DEF_COLOR("황색")
		PRINTL 가 들어있었다. 
		I:320 += 1
	CASE 2 TO 3
		CALL PRINT_COLORTEXT, "아련한소매장식", DEF_COLOR("황색")
		PRINTL 이 들어있었다. 
		I:321 += 1
	CASE 4 TO 5
		CALL PRINT_COLORTEXT, "아련한장갑", DEF_COLOR("황색")
		PRINTL 이 들어있었다. 
		I:322 += 1
	CASE 6 TO 7
		CALL PRINT_COLORTEXT, "아련한장식띠", DEF_COLOR("황색")
		PRINTL 가 들어있었다. 
		I:323 += 1
	CASEELSE
		CALL PRINT_COLORTEXT, "아련한반지", DEF_COLOR("황색")
		PRINTL 가 들어있었다. 
		I:324 += 1
	ENDSELECT
	RETURN 0
ENDSELECT

LOCAL:101 = 1
FOR LOCAL:102, 0, PALAM_EXPLORER("아이템레벨")+1
	SIF PALAM_EXPLORER("아이템発見率") > RAND:100
		LOCAL:101 += 1
NEXT

CALL SET_PALAM_EXPLORER, "財宝", RAND:(LOCAL:101)

MONEY:5 = 0
;로즈닐의시집
IF PALAM_EXPLORER("財宝") <= 3 && RAND:(3 + (ITEM:로즈닐의시집 + I:310 > 0)*7) == 0 && LOCAL:315
	CALL PRINT_COLORTEXT, "로즈닐의 시집", DEF_COLOR("황색")
	PRINTFORML 을 손에넣었다. 
	I:310 += 1
	RETURN 0
ENDIF

SELECTCASE PALAM_EXPLORER("財宝")
;お금
CASE 0
	MONEY:5 = PALAM_EXPLORER("財宝期待値")*PALAM_EXPLORER("아이템레벨")*3 + RAND:100
;LV1 安い아이템($500,装備品は半額程度)
CASE 1
	IF RAND:10 == 0 && LOCAL:300
		CALL PRINT_COLORTEXT, "도자기스푼", DEF_COLOR("황색")
		I:300 += 1
	ELSEIF LOCAL:30 && RANDIF(2, LOCAL:40, LOCAL:32)
		CALL PRINT_COLORTEXT, "로션", DEF_COLOR("황색")
		I:30 += 1
	ELSEIF LOCAL:32 && RANDIF(2, LOCAL:40)
		CALL PRINT_COLORTEXT, "이뇨제", DEF_COLOR("황색")
		I:32 += 1
	ELSEIF LOCAL:40 && RAND:2
		CALL PRINT_COLORTEXT, "재스민", DEF_COLOR("황색")
		I:40 += 1
	ELSE
		MONEY:5 = 250 + RAND:100
	ENDIF
;LV2 安い아이템2($1000)
CASE 2
	IF RAND:5 == 0 && LOCAL:300
		CALL PRINT_COLORTEXT, "도자기스푼", DEF_COLOR("황색")
		I:300 += 1
	ELSEIF LOCAL:1 && RANDIF(2, LOCAL:41, LOCAL:33, LOCAL:0, LOCAL:10)
		CALL PRINT_COLORTEXT, "바이브", DEF_COLOR("황색")
		I:1 += 1
	ELSEIF LOCAL:10 && RANDIF(2, LOCAL:41, LOCAL:33, LOCAL:0)
		CALL PRINT_COLORTEXT, "채찍", DEF_COLOR("황색")
		I:10 += 1
	ELSEIF LOCAL:0 && RANDIF(2, LOCAL:41, LOCAL:33)
		CALL PRINT_COLORTEXT, "로터", DEF_COLOR("황색")
		I:0 += 1
	ELSEIF LOCAL:33 && RANDIF(2, LOCAL:41, LOCAL:33)
		CALL PRINT_COLORTEXT, "비디오테이프", DEF_COLOR("황색")
		I:33 += 1
	ELSEIF LOCAL:41 && RAND:2
		CALL PRINT_COLORTEXT, "라벤더", DEF_COLOR("황색")
		I:41 += 1
	ELSE
		MONEY:5 = 400 + RAND:200
	ENDIF
;LV3 程ほどの値段の아이템($2000)
CASE 3
	IF RAND:5 == 0 && LOCAL:301
		CALL PRINT_COLORTEXT, "도자기찻잔", DEF_COLOR("황색")
		I:301 += 1
	ELSEIF LOCAL:9 && RANDIF(2, LOCAL:43, LOCAL:31)
		CALL PRINT_COLORTEXT, "재갈세트", DEF_COLOR("황색")
		I:9 += 1
	ELSEIF LOCAL:31 && RANDIF(2, LOCAL:43)
		CALL PRINT_COLORTEXT, "미약", DEF_COLOR("황색")
		I:31 += 1
	ELSEIF LOCAL:43 && RAND:2
		CALL PRINT_COLORTEXT, "샌들우드", DEF_COLOR("황색")
		I:43 += 1
	ELSE
		MONEY:5 = 800 + RAND:500
	ENDIF
;LV4 程ほどの値段の아이템2($4000)
CASE 4
	IF RAND:10 == 0 && LOCAL:305
		CALL PRINT_COLORTEXT, "은스푼", DEF_COLOR("황색")
		I:302 += 1
	ELSEIF RAND:5 == 0 && LOCAL:301
		CALL PRINT_COLORTEXT, "도자기찻잔", DEF_COLOR("황색")
		I:301 += 1
	ELSEIF LOCAL:4 && RANDIF(2, LOCAL:34, LOCAL:44, LOCAL:17)
		CALL PRINT_COLORTEXT, "페니스밴드", DEF_COLOR("황색")
		I:4 += 1
	ELSEIF LOCAL:17 && RANDIF(2, LOCAL:34, LOCAL:44)
		CALL PRINT_COLORTEXT, "착유기", DEF_COLOR("황색")
		I:17 += 1
	ELSEIF LOCAL:44 && RAND:2
		CALL PRINT_COLORTEXT, "로즈마리", DEF_COLOR("황색")
		I:44 += 1
	ELSEIF LOCAL:34 && RAND:2
		CALL PRINT_COLORTEXT, "진정제", DEF_COLOR("황색")
		I:34 += 1
	ELSE
		MONEY:5 = 1700 + RAND:1000
	ENDIF
;LV5 程ほどの値段の아이템3($6000)
CASE 5
	IF RAND:5 == 0 && LOCAL:305
		CALL PRINT_COLORTEXT, "은스푼", DEF_COLOR("황색")
		I:302 += 1
	ELSEIF LOCAL:8 && RANDIF(2, LOCAL:42, LOCAL:14, LOCAL:22)
		CALL PRINT_COLORTEXT, "니플캡", DEF_COLOR("황색")
		I:8 += 1
	ELSEIF LOCAL:22 && RANDIF(2, LOCAL:42, LOCAL:14)
		CALL PRINT_COLORTEXT, "공기펌프", DEF_COLOR("황색")
		I:22 += 1
	ELSEIF LOCAL:14 && RANDIF(2, LOCAL:42)
		CALL PRINT_COLORTEXT, "구속도구", DEF_COLOR("황색")
		I:14 += 1
	ELSEIF LOCAL:42 && RAND:2
		CALL PRINT_COLORTEXT, "머스크", DEF_COLOR("황색")
		I:42 += 1
	ELSE
		MONEY:5 = 2500 + RAND:1000
	ENDIF
;LV6 高価な아이템($8000)
CASE 6
	IF RAND:10 == 0 && LOCAL:306
		CALL PRINT_COLORTEXT, "은찻잔", DEF_COLOR("황색")
		I:303 += 1
	ELSEIF LOCAL:5 && RANDIF(2, LOCAL:49, LOCAL:12)
		CALL PRINT_COLORTEXT, "아이마스크", DEF_COLOR("황색")
		I:5 += 1
	ELSEIF LOCAL:12 && RANDIF(2, LOCAL:49)
		CALL PRINT_COLORTEXT, "오나홀", DEF_COLOR("황색")
		I:12 += 1
	ELSEIF LOCAL:49 && RAND:2
		CALL PRINT_COLORTEXT, "프랭킨센스", DEF_COLOR("황색")
		I:49 += 1
	ELSE
		MONEY:5 = 3500 + RAND:1000
	ENDIF
;LV7 高価な아이템2($10000)
CASE 7
	IF RAND:5 == 0 && LOCAL:306
		CALL PRINT_COLORTEXT, "은찻잔", DEF_COLOR("황색")
		I:303 += 1
	ELSEIF LOCAL:2 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25, LOCAL:18, LOCAL:13, LOCAL:3)
		CALL PRINT_COLORTEXT, "전기안마기", DEF_COLOR("황색")
		I:2 += 1
	ELSEIF LOCAL:3 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25, LOCAL:18, LOCAL:13)
		CALL PRINT_COLORTEXT, "애널바이브", DEF_COLOR("황색")
		I:3 += 1
	ELSEIF LOCAL:13 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25, LOCAL:18)
		CALL PRINT_COLORTEXT, "플레이매트", DEF_COLOR("황색")
		I:13 += 1
	ELSEIF LOCAL:18 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25)
		CALL PRINT_COLORTEXT, "개목걸이", DEF_COLOR("황색")
		I:18 += 1
	ELSEIF LOCAL:25 && RANDIF(2, LOCAL:49, LOCAL:26)
		CALL PRINT_COLORTEXT, "애널비즈", DEF_COLOR("황색")
		I:25 += 1
	ELSEIF LOCAL:26 && RANDIF(2, LOCAL:49)
		CALL PRINT_COLORTEXT, "보온병", DEF_COLOR("황색")
		I:26 += 1
	ELSEIF LOCAL:49 && RAND:2
		CALL PRINT_COLORTEXT, "프랭킨센스", DEF_COLOR("황색")
		I:49 += 1
	ELSE
		MONEY:5 = 4500 + RAND:1000
	ENDIF
;LV8 レア아이템($16000)
CASEELSE
	IF RAND:10 == 0 && LOCAL:311
		CALL PRINT_COLORTEXT, "미스릴찻잔", DEF_COLOR("황색")
		I:305 += 1
	ELSEIF RAND:5 == 0 && LOCAL:310
		CALL PRINT_COLORTEXT, "미스릴스푼", DEF_COLOR("황색")
		I:304 += 1
	ELSEIF CONFIG("임신패치") && LOCAL:35 && RANDIF(2, LOCAL:49, LOCAL:6, LOCAL:15, LOCAL:23)
		CALL PRINT_COLORTEXT, "배란유발제", DEF_COLOR("황색")
		I:35 += 1
	ELSEIF LOCAL:23 && RANDIF(2, LOCAL:49, LOCAL:6, LOCAL:15)
		CALL PRINT_COLORTEXT, "확장벌룬", DEF_COLOR("황색")
		I:23 += 1
	ELSEIF LOCAL:15 && RANDIF(2, LOCAL:49, LOCAL:6)
		CALL PRINT_COLORTEXT, "관장기＋플러그", DEF_COLOR("황색")
		I:15 += 1
	ELSEIF LOCAL:6 && RANDIF(2, LOCAL:49)
		CALL PRINT_COLORTEXT, "비디오카메라", DEF_COLOR("황색")
		I:6 += 1
	ELSEIF LOCAL:49 && RAND:2
		CALL PRINT_COLORTEXT, "프랭킨센스", DEF_COLOR("황색")
		I:49 += 1
	ELSE
		MONEY:5 = 7500 + RAND:1000
	ENDIF
ENDSELECT
IF MONEY:5
	CALL PRINT_COLORTEXT, MONEY_C(MONEY:5), DEF_COLOR("황색")
	PRINTFORML %조사만처리(LASTWORD,"를")% 획득했다. 
	MONEY:6 += MONEY:5
ELSE
	PRINTFORML %조사만처리(LASTWORD,"를")% 손에 넣었다. 
ENDIF
