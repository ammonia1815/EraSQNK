;-------------------------------------------------
;다방
;#eraSQ製作スレＩＲＣメンバー合作! ｗ
;-------------------------------------------------
@SHOP_EX
#DIM LCOUNT
;FLAG:400 店舗数
;FLAG:401 제복の情報
;SAVESTR:1 各店舗の名前　デフォはDream Goblet

PRINTL 
PRINTL 길들인 몽마를 점원으로 해서, 귀여운 제복을 내세운 다방을 운영합니다
PRINTL 수입은 일요일 아침에 일주일분을 한꺼번에 얻습니다
PRINTL 점포의 통로는 넓기 때문에, 라미아등의 신체의 큰 몽마에서도 문제 없게 일할 수 있고,
PRINTL 일하기 힘든 모습의 몽마에게는, 형태를 인간형으로 바꾸는 마법을 건다거나, 걸지 않는다거나 합니다
PRINTL 또한, 남자지만 보기에 아름다운 용모라면 일을 시킵니다만, 손님의 꿈을 부수지 않게 배려해, 
PRINTL 중성적인 외모를 가진 사람에 한해, 여성스탭과 같은 복장으로 웨이트리스로 일하게 합니다
PRINTFORML

VARSET LOCAL
MONEY:8 = 0

;다방が稼動しているとき
IF NUM("運営店舗")
	FOR LCOUNT, 2, CHARANUM
		IF COND("플로어점원", LCOUNT)
			CALL SETFLAG, "매각価格", LCOUNT
			MONEY:8 += CALCF("다방の給금", LCOUNT)
		ENDIF
	NEXT
	LOCAL = (MONEY:8 * 14 * NUM("運営店舗") ) / CHARA_NUM("플로어점원")/1000*1000
	PRINTFORML 점원수：{CHARA_NUM("플로어점원")}명 운영가능점포수：{NUM("運営店舗")} 예상수입:%MONEY_C(LOCAL)%/주
ELSEIF NUM("所持店舗")
	PRINTFORML 점원수：{CHARA_NUM("플로어점원")}명…점원이 부족합니다
ELSE
	PRINTFORM 점원수：{CHARA_NUM("バイト가능")}명 운영가능점포수：{NUM("運営가능店舗")} 
	FLAG:400 += 1
	IF CHARA_NUM("플로어점원") > 0
		FOR LCOUNT, 2, CHARANUM
			IF COND("플로어점원", LCOUNT)
				CALL SETFLAG, "매각価格", LCOUNT
				MONEY:8 += CALCF("다방の給금", LCOUNT)
			ENDIF
		NEXT
		LOCAL = (MONEY:8 * 14 * NUM("運営店舗") ) / CHARA_NUM("플로어점원")/1000*1000
	ENDIF
	FLAG:400 -= 1
	PRINTFORML 예상수입:%MONEY_C(LOCAL)%/주
ENDIF
PRINTL 

SIF NUM("所持店舗") < NUM("運営가능店舗")
	PRINTFORML  [ 0] 새 가게를 차린다 （$10,000） 소지금:%MONEY_C(MONEY)%
IF NUM("所持店舗")
	PRINTFORML  [ 1] 가게이름을 변경   현재：%NAMES("다방")%
	PRINTFORML  [ 2] 제복을 변경   현재：%TEXTS("제복")%
	;営業方바늘はとりあえず凍結
	;PRINTFORML  [ 3] 영업 방침을 변경　 현재：%TEXTS("영업 방침")%
	PRINTFORML  [98] 이벤트 빈도 변경 현재：\@ CONFIG("다방描写") ?  시간 경과 때에 발생 # 발생하지 않는다 \@
ENDIF
SIF CHARA_NUM("バイト가능")
	PRINTL  [99] 점원 일람＆지시
PRINTL [100] 돌아온다

$INPUT_LOOP
INPUT

IF RESULT == 100
	RETURN 0
ELSEIF RESULT == 98 && FLAG:400
	CALL SETCONFIG, "다방"
ELSEIF RESULT == 99 && CHARA_NUM("バイト가능")
	CALL CAFE_LIST, 0, 0
ELSEIF RESULT == 0 && NUM("所持店舗") < NUM("運営가능店舗")
	IF MONEY >= 10000
		IF NUM("所持店舗") == 0
			SAVESTR:1 = Dream Goblet
			FLAG:401 = 1p0
			PRINTFORML 초기설정에서는, 가게이름은 %NAMES("다방")%, 제복은 %TEXTS("제복")%으로 되어있습니다
			PRINTFORMW 원하는 내용을 변경해서 즐겨주세요
		ELSE
			PRINTFORMW 점포를 한개 추가했습니다
		ENDIF
		MONEY -= 10000
		FLAG:400 += 1
	ELSE
		PRINTFORMW 금액이 부족합니다. 
	ENDIF
ELSEIF RESULT == 1 && NUM("所持店舗")
	PRINTFORML 다방의 이름을 「%NAMES("다방")%」에서 변경합니다
	PRINTFORML 이름을 입력해주세요（아무 것도 입력하지 않으면 변경하지 않습니다）
	CALL INPUTS_SELECT, 24
	IF RESULTS != "" && SAVESTR:1 != RESULTS
		SAVESTR:1 = %RESULTS%
		PRINTFORMW 가게 이름을 %NAMES("다방")% 개명했습니다
	ENDIF
ELSEIF RESULT == 2 && NUM("所持店舗")
	CALL SETFLAG, "다방の복장"
ELSE
	GOTO INPUT_LOOP
ENDIF

RESTART

@CAFE
#DIM LCOUNT
#DIM MEMO_TARGET
#DIM MEMO_LINECOUNT, 2

;ここではTARGETを変更させるので保存
MEMO_TARGET = TARGET
TARGET = 0

;同じ이벤트は起こさない
VARSET TFLAG
;勤労플래그初期化(一일二回あるので)
FOR LCOUNT, 1, CHARANUM
	CLEARBIT CFLAG:LCOUNT:95, 1
NEXT

IF CONFIG("다방描写")
	PRINTL 
	CUSTOMDRAWLINE ☆

	;挨拶。LINECOUNTを記録して、キャラが喋ったかを判別させる
	MEMO_LINECOUNT = LINECOUNT

	;喋る캐릭터의選別
	FOR LCOUNT, 1, MAX(4, NUM("運営店舗") + 1)
		;挨拶していない人から選出
		TARGET = FIND_COND("출연가능な점원", "다방挨拶まだ")

		MEMO_LINECOUNT:1 = LINECOUNT
		;台詞有る場合に, 台詞の名前表示をする？
		SIF CONFIG("대사の名前비표시") == 0
			PRINTFORM %TEXT_LJ(CALLNAME:TARGET, 16)%

		CALL SELF_KOJO, "다방"

		;挨拶フラグ
		SETBIT CFLAG:95, 6

		;台詞が無いなら辻褄あわせ
		IF LINECOUNT == MEMO_LINECOUNT:1
			PRINTL 
			CLEARLINE 1
		ENDIF
	NEXT
	IF LINECOUNT > MEMO_LINECOUNT
		WAIT
		PRINTL 
	ENDIF
ENDIF

FOR LCOUNT, 1, NUM("運営店舗") + 1
	;FIND_CONDはTARGET以外から探すので一旦リセット
	TARGET = 0
	;挨拶済みを優遇
	TARGET = FIND_COND("출연가능な점원", "다방挨拶済み")
	SIF TARGET == 0
		TARGET = FIND_COND("출연가능な점원")

	;収入get
	CALL SETFLAG, "매각価格", TARGET
	MONEY:7 += CALCF("다방の給금", TARGET)

	;이벤트描写플래그ON
	SETBIT CFLAG:95, 1
	IF CONFIG("다방描写")
		SIF LCOUNT >= 2
			PRINTL 

		CALL EVENT_CAFE, LCOUNT
	ENDIF
NEXT

IF CONFIG("다방描写")
	CUSTOMDRAWLINE ☆
	PRINTL 
ENDIF

TARGET = MEMO_TARGET

;-------------------------------------------------
;다방で起きた이벤트の名称
;-------------------------------------------------
@EVENT_NAMES_CAFE(ARG)
#FUNCTIONS

SELECTCASE ARG
CASE 1
	LOCALS = パンチラ
CASE 2
	LOCALS = セクハラ
CASE 3
	LOCALS = オムライス
CASE 4
	LOCALS = 스커트めくり
CASE 5
	LOCALS = こけてパンもろ
CASE 6
	LOCALS = 자위중독
CASE 7
	LOCALS = 身体が화照る
CASE 8
	LOCALS = テーブル拭き１
CASE 9
	LOCALS = クリーム拭き取り
CASE 10
	LOCALS = あ～ん
CASE 11
	LOCALS = 노팬티接손님
CASE 12
	LOCALS = 飲み物溢してつまみ食い
CASE 13
	LOCALS = トイレでつまみ食い
CASE 14
	LOCALS = テーブル拭き２
CASE 15
	LOCALS = チップ
CASE 16
	LOCALS = 財布忘れた상대をつまみ食い
CASE 17
	LOCALS = 暇なときの시간潰し
CASE 18
	LOCALS = 휴식
CASE 19
	LOCALS = 査察

CASE 100
	LOCALS = 눈에띄고싶어함
CASE 101
	LOCALS = 레즈끼
CASE 102
	LOCALS = 不遜な接손님
CASE 103
	LOCALS = 모유
CASE 104
	LOCALS = 아이
CASE 105
	LOCALS = 폭유
CASE 106
	LOCALS = 팬티見せつけ
CASE 107
	LOCALS = 내성적
CASE 108
	LOCALS = 양기
CASE 109
	LOCALS = 음기
CASE 110
	LOCALS = 쿨
CASE 111
	LOCALS = 요정
CASE 112
	LOCALS = 라미아
CASE 113
	LOCALS = 머메이드
CASE 114
	LOCALS = カオス料理
CASE 115
	LOCALS = 盗撮
;CASE 116
;	LOCALS = 꼬리
CASE 117
	LOCALS = 가슴の谷間
CASE 118
	LOCALS = 치료の名目でつまみ食い
CASE 119
	LOCALS = ハーブティー
CASE 120
	LOCALS = 주인がトイレで
CASE 121
	LOCALS = お茶を淹れる

CASE 200
	LOCALS = コスプレ
ENDSELECT
RETURNF LOCALS

;-------------------------------------------------
;다방ARG号店の이벤트
;-------------------------------------------------
@EVENT_CAFE, ARG
#DIM LCOUNT
#DIM MEMO_LINECOUNT
#DIM WAITRESS
#DIMS EVENT_NAMES

PRINTFORML ~%NAMES("다방")%\@ NUM("所持店舗") >= 2 ?  %BL(1)%{ARG}호점 # \@~

;大別すると, 対손님系(これが一番多い), 経営系（一番少ない）, 勤務態度系. 
;종족限定이벤트, 계절이벤트も追加したいところですな. BY□ボ

VARSET LOCAL

;소질の関係ない이벤트(1～99)と소질이벤트(100～)で番号をわける. 
;라미아や하피とかはどうしようかな…

CALL CALC, "랜덤선택"
;パンチラや에로な仕草サービス
SIF TFLAG:1 == 0 && ABL:노출벽 >= 3 && COND("제복：ロング") == 0 && COND("다방：パンチラ") && COND("身嗜み：노팬티") == 0
	CALL CALC, "랜덤선택", 1
;セクハラ
SIF TFLAG:2 == 0
	CALL CALC, "랜덤선택", 2
;オムライスにお絵かき
SIF TFLAG:3 == 0
	CALL CALC, "랜덤선택", 3
;스커트めくり
SIF TFLAG:4 == 0 && COND("제복：스커트") && COND("제복：ロング") == 0 && COND("身嗜み：노팬티") == 0
	CALL CALC, "랜덤선택", 4
;미니스커트でこけてパンもろorＭ字開다리披露
SIF TFLAG:5 == 0 && COND("제복：미니스커트") + COND("제복：바니") && COND("身嗜み：노팬티") == 0 && COND("다리 사용") && TALENT:요정 == 0
	CALL CALC, "랜덤선택", 5
;자위중독だと自然に손가락が…
SIF TFLAG:6 == 0 && ABL:자위중독 >= 3 && TALENT:남자 == 0
	CALL CALC, "랜덤선택", 6
;상대したばかり+음란
SIF TFLAG:7 == 0 && TALENT:호색 + TALENT:음란 && COND("다방：섹스")
	CALL CALC, "랜덤선택", 7
;テーブルを拭く時に가슴が…
SIF TFLAG:8 == 0 && TALENT:남자 == 0 && TALENT:요정 == 0
	CALL CALC, "랜덤선택", 8
;頬をクリームで汚したお손님にサービス
SIF TFLAG:9 == 0 && ABL:봉사정신 >= 3
	CALL CALC, "랜덤선택", 9
;伝説の「あ～ん」ktkr! 
SIF TFLAG:10 == 0 && ABL:봉사정신 >= 3
	CALL CALC, "랜덤선택", 10
;どきどき노팬티or파스너開けっ放し接손님
SIF TFLAG:11 == 0 && ABL:욕망 >= 4 && ABL:노출벽 >= 4 && TALENT:남자 == 0 && COND("제복：스커트") + COND("제복：파스너")
	CALL CALC, "랜덤선택", 11
;ちょっと暇な시간帯で, ジュースをこぼして拭き拭き
SIF TFLAG:12 == 0 && MIN(ABL:기교 + ABL:혀놀림, ABL:정액중독) >= 3 && COND("다방：펠라")
	CALL CALC, "랜덤선택", 12
;溜まっていると, トイレで摘み食い
SIF TFLAG:13 == 0 && COND("다방：섹스")
	CALL CALC, "랜덤선택", 13
;テーブル拭き その２
SIF TFLAG:14 == 0 && ABL:봉사정신 >= 3 && TALENT:남자 == 0 && TALENT:요정 == 0
	CALL CALC, "랜덤선택", 14
;いっぱい稼ぐ人はチップをいっぱい貰う
SIF TFLAG:15 == 0 && CALCF("다방の給금", TARGET) >= 5000 && TALENT:남자 == 0
	CALL CALC, "랜덤선택", 15
;定番のお皿洗いとおもいきや
SIF TFLAG:16 == 0 && COND("다방：섹스") && TALENT:남자 == 0
	CALL CALC, "랜덤선택", 16
;いつになく暇な店内
SIF TFLAG:17 == 0
	CALL CALC, "랜덤선택", 17
;휴식
SIF TFLAG:18 == 0
	CALL CALC, "랜덤선택", 18
;MASTERの査察
SIF TFLAG:19 == 0 && COND("다방출연끝", MASTER) == 0
	CALL CALC, "랜덤선택", 19

;눈에띄고싶어함（とーかお嬢様みたいなイメージ？）
SIF TFLAG:100 == 0 && TALENT:눈에띄고싶어함
	CALL CALC, "랜덤선택", 100
;여자손님をガン見
SIF TFLAG:101 == 0 && TALENT:바이 && ABL:레즈끼 >= 3
	CALL CALC, "랜덤선택", 101
;不遜な接손님
SIF TFLAG:102 == 0 && (TALENT:교만 || TALENT:건방짐)
	CALL CALC, "랜덤선택", 102
;ミルク系(거유系？……거유はミルクと別にしようか考え中)
SIF TFLAG:103 == 0 && TALENT:거유 && TALENT:모유체질
	CALL CALC, "랜덤선택", 103
;유치で小柄で미련함
SIF TFLAG:104 == 0 && TALENT:미련함 && TALENT:작은몸집 && TALENT:유치
	CALL CALC, "랜덤선택", 104
;폭유
SIF TFLAG:105 == 0 && TALENT:거유 >= 2 && TALENT:요정 == 0
	CALL CALC, "랜덤선택", 105
;팬티見せつけ
SIF TFLAG:106 == 0 && (TALENT:호색 || ABL:노출벽 >= 3) && COND("제복：미니스커트") + COND("제복：바니") && TALENT:요정 == 0 && COND("다방：パンチラ") && TALENT:남자 == 0
	CALL CALC, "랜덤선택", 106
;내성적（おどおど웨이트리스は萌える）
SIF TFLAG:107 == 0 && TALENT:내성적
	CALL CALC, "랜덤선택", 107
;양기, 미련함（おバカ）
SIF TFLAG:108 == 0 && TALENT:양기 + TALENT:미련함
	CALL CALC, "랜덤선택", 108
;음기(店内が妙に暗くなる感じで)
SIF TFLAG:109 == 0 && TALENT:음기
	CALL CALC, "랜덤선택", 109
;쿨系（と見られてしまう이벤트, ルリコンホイホイ）
SIF TFLAG:110 == 0 && TALENT:냉정 + TALENT:무관심 + TALENT:감정결핍
	CALL CALC, "랜덤선택", 110
;요정さん
SIF TFLAG:111 == 0 && TALENT:요정
	CALL CALC, "랜덤선택", 111
;라미아さん
SIF TFLAG:112 == 0 && TALENT:라미아
	CALL CALC, "랜덤선택", 112
;머메이드さん…ちょっと乾いていないか？이벤트
SIF TFLAG:113 == 0 && TALENT:머메이드
	CALL CALC, "랜덤선택", 113
;カオス料理…호기심, 조합지식, 광기, 금단의지식, 마법사
SIF TFLAG:114 == 0 && TALENT:호기심 + TALENT:조합지식 + TALENT:광기 + TALENT:금단의지식 + TALENT:마법사
	CALL CALC, "랜덤선택", 114
;盗撮（매력or인기）
SIF TFLAG:115 == 0 && TALENT:인기 + TALENT:매력 && COND("제복：ロング") == 0 && TALENT:요정 == 0
	CALL CALC, "랜덤선택", 115
;꼬리が可愛い! 
;SIF TFLAG:116 == 0 && TALENT:꼬리
;	CALL CALC, "랜덤선택", 116
;가슴の谷間
SIF TFLAG:117 == 0 && TALENT:거유 && TALENT:양기 + TALENT:부끄럼없음 && TALENT:부끄럼쟁이 == 0
	CALL CALC, "랜덤선택", 117
;치료の名目で摘み食い(조합지식)
SIF TFLAG:118 == 0 && TALENT:조합지식 && COND("다방：펠라") && TALENT:남자 == 0
	CALL CALC, "랜덤선택", 118
;ハーブティー作ったり
SIF TFLAG:119 == 0 && TALENT:조합지식
	CALL CALC, "랜덤선택", 119
;MASTERがトイレを覗くと…
SIF TFLAG:120 == 0 && COND("다방출연끝", MASTER) == 0 && MIN(ABL:기교, ABL:레즈끼) >= 3 && COND("다방：펠라") && FIND_COND("출연가능な점원", "조합지식")
	CALL CALC, "랜덤선택", 120
;お茶を淹れるぞー
SIF TFLAG:121 == 0 && TALENT:헌신 + TALENT:조합지식 + TALENT:기품
	CALL CALC, "랜덤선택", 121


;200番台は時節이벤트
;コスプレ기모노
;SIF TFLAG:200 == 0 && FLAG:873 == 0 && DAY%7 == 0
;	CALL CALC, "랜덤선택", 200

;既読이벤트の記録
TFLAG:RESULT = 1

VARSET LOCALS

WAITRESS = 0

EVENT_NAMES = %EVENT_NAMES_CAFE(RESULT)%

;EVENT_NAMES = パンチラ
;EVENT_NAMES = 飲み物溢してつまみ食い
;EVENT_NAMES = トイレでつまみ食い
;EVENT_NAMES = 치료の名目でつまみ食い

;이하, 이벤트内容の記述
;소질の関係ない이벤트群
SELECTCASE EVENT_NAMES
CASE "パンチラ"
	PRINTFORM %CALLNAME:TARGET%는, 손님에게 자연스럽게 
	IF COND("제복：바니")
		PRINTFORMW 야한 행동을 대놓고 보여주면서 즐겁게 일하고있습니다. 
	ELSE
		PRINTFORMW 팬티를 슬쩍 보여주면서 즐겁게 일하고 있습니다. 
	ENDIF

	CALL SELF_KOJO_L, EVENT_NAMES

	IF TALENT:부끄럼쟁이 && RAND:2
		PRINTFORMW 꽤나 부끄러운 듯 하지만, 보여지는 쾌감에는 이길 수는 없는 것 같습니다… 
	ELSEIF TALENT:심술쟁이 + TALENT:소악마 && RAND:2
		PRINTFORM 아무래도, 
		IF COND("제복：바니")
			IF TALENT:남자 == 0 && TALENT:빈유 == 0 && RAND:3 == 0
				IF RAND:2
					PRINTFORM 인사하는 
				ELSE
					PRINTFORM 몸을 굽히는 
				ENDIF
				PRINTFORM 김에 가슴 골짜기를 강조해 보여줄 때
			ELSEIF RAND:2
				PRINTFORM 발밑의 물건을 주울 때에 다리를 뻗은 채 몸을 앞으로 굽히고 
				IF RAND:2
					PRINTFORM 엉덩이
				ELSE
					PRINTFORM 각선미
				ENDIF
				PRINTFORM 를 과시할 때
			ELSE
				SIF TALENT:호색 + TALENT:소악마
					PRINTFORM 곁눈질을 보내면서 
				IF RAND:2
					;PRINTFORM 머리카락을 쓸어 올려 
					PRINTFORM 머리 모양을 정돈하면서 
				ELSE
					PRINTFORM 팔을 머리의  뒤로 넘겨 
				ENDIF
				PRINTFORM %TEXTS("겨드랑이")%를 보여줄 때
			ENDIF
		ELSE
			IF RAND:2
				PRINTFORM 팬티가 보일 때
			ELSE
				PRINTFORM 스커트 안을 보여줄 때
			ENDIF
		ENDIF
		IF RAND:2
			PRINTFORMW 의 손님의 행복해하는 모습이 재미있어서 어쩔 수 없나봅니다… 
		ELSE
			PRINTFORMW 에 손님이 \@ RAND:2 ?  얼굴을 붉히는 # 몸을 앞으로 숙이는 \@ 것이 재미있어서 어쩔 수 없나봅니다… 
		ENDIF
	ELSE
		LOCAL = 100 + RAND:8
		IF COND("제복：바니")
			PRINTFORM 몸의 라인이 드러나는 에로틱한 슈트
			SELECTCASE AGE(TARGET)
			CASE IS >= 4
				PRINTFORM 로 감싸인 어른의 매력
			CASE 3
				PRINTFORM 로 끌어내어진 %CALLNAME:TARGET%의 매력
			CASE 2
				PRINTFORM 로 발돋움하는 듯한 귀여운 소녀의 매력
			CASE 1
				PRINTFORM 와 어린 겉모습의 언밸런스한 매력
			ENDSELECT
		ELSEIF RAND:2
			PRINTFORM 힐끔힐끔 보이는 %CALLNAME:TARGET%의 
			SIF CHECK_CLO("팬티스타킹노출")
				PRINTFORM 팬티스타킹 너머로
			IF (AGE(TARGET) >= 3 || ABL:노출벽 >= 3 || TALENT:호색 + TALENT:음란) && RAND:2
				IF AGE(TARGET) <= 2 && RAND:4
					PRINTFORM 얌전한 
				ELSEIF RAND:2
					PRINTFORM 아슬아슬한 
				ELSE
					PRINTFORM 과격한 
				ENDIF
			ELSEIF RAND:2 || AGE(TARGET) >= 3
				IF RAND:3 == 0
					PRINTFORM 실크제의 
				ELSEIF RAND:2
					PRINTFORM 순백의 
				ELSE
					PRINTFORM 청초한 레이스의 
				ENDIF
			ELSEIF RAND:2
				IF RAND:2 || AGE(TARGET) >= 2
					PRINTFORM 어른스러운 
				ELSE
					PRINTFORM 어린애같은 
				ENDIF
			ENDIF
			PRINTFORM 팬티
		ELSEIF LOCAL == 100
			PRINTFORM 슬쩍슬쩍 보이는 줄무늬팬티
		ELSEIF LOCAL == 101
			PRINTFORM 살짝 보이는 성숙한 검은 팬티
		ELSEIF LOCAL == 102
			PRINTFORM 귀염스런 프릴이 가득한 팬티
		ELSEIF LOCAL == 103
			PRINTFORM 「풀릴지도」라고 기대하게 만드는 사이드의 끈
		ELSEIF LOCAL == 104
			PRINTFORM 완전히 죄다 보이는 토실토실한 엉덩이
		ELSEIF LOCAL == 105
			IF COND("음모") >= 7
				PRINTFORM 팬티에서 삐져나온 부끄러운 털
			ELSE
				IF AGE(TARGET) >= 3
					PRINTFORM \@ COND("오토코노코") ? 막대기 끝 # 균열 \@이 비쳐보일 것같은 얇은 천으로 만들어진 팬티
				ELSE
					PRINTFORM 배가 다 보이는 작은 팬티
				ENDIF
			ENDIF
		ELSEIF LOCAL == 106
			IF RAND:2
				PRINTFORM 다 비치는 뒤쪽으로 보이는 
				IF AGE(TARGET) >= 3
					PRINTFORM 엉덩이 계곡
				ELSE
					PRINTFORM 작은 엉덩이
				ENDIF
			ELSE
				PRINTFORM 희미하게 비쳐보이는 
				IF COND("음모") == 0 || RAND:3 == 0
					PRINTFORM 살결
				ELSEIF COND("음모") <= 3
					PRINTFORM 무모의 언덕
				ELSEIF COND("음모") <= 6
					PRINTFORM 음모
				ELSE
					PRINTFORM 밀림
				ENDIF
			ENDIF
		ELSEIF LOCAL == 107 || CHECK_PANTIES("하이레그")
			PRINTFORM 자그마한 팬티가 먹혀든 
			IF (AGE(TARGET) >= 3 && RAND:2) || COND("오토코노코")
				PRINTFORM 부푼 곳
			ELSE
				PRINTFORM 보드라워 보이는 언덕
			ENDIF
		ELSE
			PRINTFORM 가드가 허술한 %CALLNAME:TARGET%의 스커트로 엿보이는 광경
		ENDIF
		PRINTFORMW 에, 손님의 시선은 못박힌듯 합니다. 
		IF RAND:2 && COND("오토코노코")
			PRINTFORMW 사실은 남자지만…귀여우니까 알고있는 손님도 모르는 손님도, 모두가 행복해보입니다…
		ELSE
			PRINTFORMW 에스컬레이트 하지 않으면, 모두가 행복할테죠…
		ENDIF
	ENDIF
CASE "성희롱"
	PRINTFORM %CALLNAME:TARGET%는 
	IF RAND:3 == 0
		PRINTFORM 테이블을 걸레질하는 중에, 
		LOCALS = 걸레질
	ELSEIF RAND:2
		PRINTFORM 요리를 옮기던 중에, 
		LOCALS = 상차림
	ELSE
		PRINTFORM 주문을 받던 중에, 
		LOCALS = 주문
	ENDIF
	PRINTFORM 손님에게 
	LOCAL = 0
	IF RAND:3 == 0 && LOCALS != "상차림"
		PRINTFORMW 가랑이를 쓰다듬어졌습니다. 
		LOCAL = COND("오토코노코")
	ELSEIF RAND:3 == 0 && LOCALS != "주문"
		PRINTFORMW 엉덩이를 만져졌습니다. 
	ELSEIF RAND:2
		PRINTFORMW 허벅지를 쓰다듬어졌습니다. 
	ELSE
		PRINTFORMW 야한 장난을 당했습니다. 
	ENDIF
	;露骨過ぎるので, ちょっと확률低い
	IF TALENT:음란 && TALENT:부끄럼쟁이 == 0 && COND("다방：おさわり") && RANDIF(3, TALENT:냉정 + TALENT:감정결핍 + TALENT:음란, TALENT:강기)
		PRINTFORM …만, %CALLNAME:TARGET%는 요염한 미소를 띄우며, 역으로 손님
		IF RAND:2 == 0
			PRINTFORM 의 손을
			IF COND("제복：바니") || COND("身嗜み：노팬티")
				IF RAND:2
					PRINTFORM 허벅지와 가랑이이로 만들어진 삼각형의 틈새로
				ELSE
					PRINTFORM 만져지면 기분 좋아져버려 장소로 
				ENDIF
			ELSE
				PRINTFORM 팬티 안에 있는 \@ COND("오토코노코") ? 페니스 # 비부 \@로
			ENDIF
			PRINTFORMW 가져와 즐겼습니다.
		ELSE
			PRINTFORMW 에게 몸을 부비기 시작했습니다. 
		ENDIF
		IF RAND:2 == 0
			PRINTFORMW 가벼운 장난으로 손을 댄 것일 터였을 손님은, 예상외의 전개에 굳어져 버렸다…
		ELSE
			WAITRESS = FIND_COND("출연가능な점원")
			SETBIT CFLAG:WAITRESS:95, 1
			PRINTFORM 거기만 공기가 바뀌어 버렸으므로, %CALLNAME:WAITRESS%하지만
			IF TALENT:WAITRESS:호색 + TALENT:WAITRESS:소악마
				PRINTFORMW 히죽 웃으며 수습하기 위해 향했습니다…
			ELSE
				PRINTFORMW 당황하며 수습하러 나섰습니다…
			ENDIF
		ENDIF
	ELSEIF TALENT:부끄럼쟁이 && RANDIF(1, TALENT:냉정 + TALENT:감정결핍 + TALENT:음란, TALENT:강기)
		PRINTFORMW %CALLNAME:TARGET%는 갑작스런 해프닝에 「\@ COND("오토코노코") ? 왓 # 히야앙 \@!」하고 귀여운 비명을 질러버리고, 매우 부끄러운 듯이, 손님을 타일렀습니다. 
		PRINTFORMW 손님은 맘에 든 여자아이를 못된 장난으로 울려버린것 같은 표정으로, 당황하며 사과하고 있습니다…
	ELSEIF TALENT:강기 && RANDIF(1, TALENT:냉정 + TALENT:감정결핍 + TALENT:호색 + TALENT:음란)
		PRINTFORM %CALLNAME:TARGET%는 반사적으로 
		IF RAND:3 == 0 && COND("다리が無い") == 0
			PRINTFORM 걷어차버리고 말았습니다만, 
		ELSEIF RAND:2
			PRINTFORM 털어내버리고 말았습니다만, 
		ELSE
			PRINTFORM 때려버렸습니다만, 
		ENDIF
		PRINTFORMW 주위에선 어째서인지 박수가 일고 있습니다…
	ELSEIF TALENT:냉정 + TALENT:감정결핍 + TALENT:음란
		IF (CHECK_PANTIES("Ｈ") || RAND:3 == 0) && ABL:봉사정신 >= 3 && ABL:노출벽 && LOCALS != "상차림" && COND("다방：おさわり")
			PRINTFORM …주변에 손님이 없는 것을 확인한 %CALLNAME:TARGET%는 
			IF LOCALS == "걸레질"
				PRINTFORM 어깨너머로 손님을 슬쩍 보고는, 다리를 어깨넓이로 벌려 걸레질 하는 동안만 
			ELSE
				PRINTFORM 손님을 슬쩍 보고, 
				IF COND("제복：바니") && COND("제복：파스너") && COND("다방：Ｖ섹스") && TALENT:여자
					PRINTFORM 못된 장난을 치고 있는 손을 가랑이 부분의 지퍼 손잡이로 유도해 주문동안만 
					LOCALS:2 = 파스너
				ELSEIF RAND:2 || COND("제복：바니")
					PRINTFORM 다리를 조금 벌리고 주문동안만 
				ELSE
					PRINTFORM 한손으로 스커트를 들어올리고 주문받는 동안만 
				ENDIF
			ENDIF
			PRINTFORMW 장난치는 것을 묵인해준것 같습니다. 
			PRINTFORM 손님은 
			IF COND("제복：바니")
				PRINTFORM 생각치 못한 전개에 마음 속으로 환성을 지르면서,
				IF LOCALS:2 == "파스너"
					PRINTFORMW %CALLNAME:TARGET%의 질내에 손가락을 넣고 찰나의 유희에 열중하고 있습니다…
				ELSE
					PRINTFORMW 환기성이 나쁜 의상 아래의 습기 찬 다리 사이와 촉감이 좋은 그물 타이츠 너머의 엉덩이, 그리고 허벅지 안쪽을 손바닥 전체로 즐겼습니다…
				ENDIF
			ELSE
				LOCALS:1 = 
				IF LOCALS == "걸레질"
					IF (ABL:노출벽 >= 3 && RAND:2) || CHECK_PANTIES("Ｔ백")
						IF RAND:2
							LOCALS:1 = Ｔ백을 밀어내고 \@ RAND:2 ? 엄지손가락의 배부분으로 애널을 희롱하 # %CALLNAME:TARGET%의 애널을 바라보 \@면서
						ELSE
							LOCALS:1 = 끝같이 가늘은 Ｔ백으로는 감출 수 없는 항문의 주름과 색을 바라보면서
						ENDIF
					ELSEIF ABL:노출벽 >= 3 || CHECK_PANTIES("시스루")
						LOCALS:1 = 시스루 팬티 너머로 엉덩이 계곡을 감상하면서
					ENDIF
				ELSE
					IF (ABL:노출벽 >= 3 && RAND:2) || CHECK_PANTIES("食い込み")
						IF RAND:3 == 0 && PENIS(TARGET) == 0
							LOCALS:1 = 가랑이에 먹혀든 팬티를 밀어서 보지를 노출시켜
						ELSEIF (AGE(TARGET) >= 3 && RAND:2) || COND("오토코노코")
							LOCALS:1 = 작은 팬티가 먹혀든 부푼것을 바라보면서
						ELSE
							LOCALS:1 = 작은 팬티가 파고든 언덕을 바라보면서
						ENDIF
					ELSEIF ABL:노출벽 >= 3 || CHECK_PANTIES("시스루")
						LOCALS:1 = 어렴풋이 비쳐보이는
						IF COND("음모") == 0 || RAND:3 == 0
							LOCALS:1 = %LOCALS:1% 하복부
						ELSEIF COND("음모") <= 3
							LOCALS:1 = %LOCALS:1% 털없는 언덕
						ELSEIF COND("음모") <= 6
							LOCALS:1 = %LOCALS:1% 음모
						ELSE
							LOCALS:1 = %LOCALS:1% 무성한 음모
						ENDIF
						LOCALS:1 = %LOCALS:1%를 감상하면서 
					ENDIF
				ENDIF
				SIF LOCALS:1 == "" || COND("身嗜み：노팬티") || RAND:4 == 0
					LOCALS:1 = 생각도 못한 전개에 마음속으로 환호성을 지르면서 
				PRINTFORMW %LOCALS:1%,  다리 사이의 부드러운 감촉이나 촉촉한 허벅지 안쪽을 손바닥 전체로 즐겼습니다…
			ENDIF
		ELSE
			PRINTFORMW …만, %CALLNAME:TARGET%는 익숙한 모습으로 흘려보내고 있습니다…
		ENDIF
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 무심코, 뺨을 붉히며 색기 넘치는 목소리를 내고 말았습니다…. 
		PRINTFORMW 예상외의 \@ LOCAL ? 감촉에 고개를 갸웃하면서, 손님은 # 반응에 당황한 손님은, \@바로 장난을 그만두고 사죄했습니다…
	ENDIF
CASE "オムライス"
	PRINTFORMW %CALLNAME:TARGET%는 오무라이스에 케찹으로 초상화를 그리는 서비스를 주문받았습니다. 
	PRINTL 
	IF TALENT:교만 + TALENT:강기 && RANDIF(2, TALENT:양기, TALENT:호색, TALENT:현명함)
		PRINTFORMW …%CALLNAME:TARGET%는 자신의 웃는얼굴을 그리는데 저항감이 있는듯, 어찌해도 \@TALENT:건방짐 ? 건방진 # 새침한\@ 느낌의 그림이 되어버렸습니다. 
		PRINTFORMW 하지만, 그럴듯한 리얼함이 손님에겐 먹힌 것 같습니다…
	ELSEIF TALENT:양기 && RANDIF(2, TALENT:호색, TALENT:현명함)
		IF TALENT:작은몸집 && TALENT:유치 && RAND:2
			PRINTFORMW …%CALLNAME:TARGET%는 손님의 무릎위에 앉아, 익숙한 모습으로 그려냈습니다…
			PRINTFORMW 손님은 %타겟을()% 쓰다듬고 싶은 충동을 필사적으로 견디고 있습니다…
		ELSE
			PRINTFORMW …%CALLNAME:TARGET%는 데포르메한 자신의 웃는얼굴을 그리며, 싱긋 손님에게 미소지었습니다…
			PRINTFORMW 손님은 즐거워 보이는 웃는 얼굴에 완전히 넉다운 당해 버렸습니다…
		ENDIF
	ELSEIF TALENT:호색 && RANDIF(2, TALENT:현명함)
		PRINTFORM ……%CALLNAME:TARGET%는 손님의 
		IF ABL:욕망 >= 5 && RAND:2
			PRINTFORM 허벅지 위에 내려앉아서,
		ELSE
			PRINTFORM 옆에 내려앉아서,
		ENDIF
		IF ABL:봉사정신 >= 3 && RAND:2
			PRINTFORMW 다리 사이의 크게 된 물건을 테이블 밑에서 어루만지면서 비비적비비적대고 있습니다.
			PRINTFORMW 손님은 이런 곳으로 사정할 수는 없다며, 이 행운인지 불행한지 모를 상황을 불가피하게 참고있지만 한계까지 얼마남지 않은 것같습니다…
		ELSE
			PRINTFORMW 일부러 몸을 접촉시켜가며 비비적비비적대고 있습니다. 
			PRINTFORMW 손님은 너무나도 밀착한 %CALLNAME:TARGET%에, 두근두근 하고 있습니다…
		ENDIF
	;현명함人にはみんな厳しいｗ
	ELSEIF TALENT:현명함 && RAND:2
		PRINTFORMW ……조금 실수한 듯, %CALLNAME:TARGET%가 그린것은 추상화로밖에 보이지 않았습니다. 
		PRINTFORMW 손님은「맛있어」라고 달래주었지만, 동료들 사이에선『거장, 거장! 』하고 놀림받은 듯 합니다…
	ELSEIF RAND:2
		PRINTFORMW …만, %CALLNAME:TARGET%가 그린 것은「좋을것도 나쁠것도 없는」정도의 모양새 였습니다. 
		PRINTFORMW 손님의 미묘한 반응에, %CALLNAME:TARGET%는 리벤지를 결심한 듯 합니다…
	ELSE
		PRINTFORMW ……아무래도, 자기로서도 만족할 만한 솜씨의 것을 그려낸 것 같습니다. 
		IF ABL:봉사정신 + TALENT:헌신*3 >= 4 && RAND:2
			PRINTFORMW 자신작을 만들고 기분이 좋은 %CALLNAME:TARGET%는, 손님에게『아-앙』도 해주고 있습니다…
		ELSE
			PRINTFORMW 손님도 감탄하고 있습니다…
		ENDIF
	ENDIF
CASE "스커트めくり"
	IF RAND:2 && CHECK_PANTIES("끈")
		PRINTFORMW %CALLNAME:TARGET%는 장난꾸러기 꼬마에게 끈팬티의 끈 한쪽을 풀려져 버렸습니다! 
		LOCALS = 紐解き
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 장난꾸러기 꼬마에게 아이스께끼를 당해버렸습니다! 
	ENDIF

	IF TALENT:교만 + TALENT:강기 && TALENT:부끄럼쟁이 == 0 && RANDIF(1, TALENT:부끄럼없음, TALENT:냉정 + TALENT:감정결핍)
		PRINTFORMW %CALLNAME:TARGET%는 \@ TALENT:조야 && RAND:3 == 0 ? 엉덩이 쪽의 팬티에 먹혀든 것을 손을 뻗어 고치면서  # \@발랑까진 아이를 가볍게 설교하기 시작했지만, 아이가 이해한 모습을 보이자, 웃는 얼굴로 머리를 쓰다듬으면서 보내준 것같습니다…
	ELSEIF TALENT:냉정 + TALENT:감정결핍 && RANDIF(1, TALENT:부끄럼없음)
		IF LOCALS == "紐解き" && RAND:2 && ABL:노출벽 >= 1
			PRINTFORM 배꼽이 보일 정도로 걷어올려진 스커트 아래에 드러난 
			IF AGE(TARGET) <= 2 && RAND:4
				PRINTFORM 얌전한 팬티에 
			ELSEIF RAND:4 == 0
				PRINTFORM \@ RAND:2 ? 빨간 # 검은 \@ 레이스 속옷에 
			ELSEIF RAND:3 == 0
				PRINTFORM 시스루 팬티에 
			ELSEIF ABL:노출벽 < 3
				IF RAND:3 == 0
					PRINTFORM 실크 속옷에 
				ELSEIF RAND:2
					PRINTFORM 순백의 팬티에 
				ELSE
					PRINTFORM 청초한 속옷에 
				ENDIF
			ELSEIF RAND:2
				PRINTFORM 아슬아슬한 팬티에 
			ELSE
				PRINTFORM \@ RAND:2 ? Ｈ # 과격 \@한 속옷에 
			ENDIF
		ENDIF
		PRINTFORMW 일순 점내가 웅성거렸습니다만, %CALLNAME:TARGET%는 화려하게 흘려넘긴 듯 합니다…
	ELSEIF TALENT:부끄럼없음
		IF TALENT:솔직 + TALENT:양기 && TALENT:작은몸집 == 0
			PRINTFORM %CALLNAME:TARGET%는 쭈구리고 앉아 눈높이를 맞추고, 
			IF ABL:노출벽 >= 3 + RAND:4 && COND("제복：미니스커트")
				PRINTFORM 자연스럽게 다리를 열어 
				IF RAND:3 == 0
					PRINTFORM 보고싶었던 것을 
				ELSEIF RAND:2
					PRINTFORM 팬티를 
				ELSEIF CHECK_PANTIES("Ｈ") || ABL:노출벽 >= 5 + RAND:2
					PRINTFORM Ｈ한 팬티가 먹혀든 가랑이를 
				ELSE
					PRINTFORM 팬티를 부드럽게 부풀리고 있는 가랑이를 
				ENDIF
				PRINTFORM 보여주면서, 얼굴을 붉히는 
			ELSE
				PRINTFORM 장난치기 좋아하는 
			ENDIF
			IF TALENT:솔직 == 0 || RAND:2
				PRINTFORMW 아이를 장난기 넘치게 설득한 것같습니다…
			ELSE
				PRINTFORMW 아이를 얼굴을 쓰다듬으며 부드럽게 설득했습니다…
			ENDIF
		ELSE
			PRINTFORM …만, %CALLNAME:TARGET%는 
			IF TALENT:냉정 + TALENT:감정결핍 && ABL:봉사정신 >= 3 && ABL:노출벽 && COND("제복：미니스커트") && RAND:2
				PRINTFORM 스커트를 붙잡아 아이에게만 몰래 팬티를 보여주면서 주의시키고 
			ELSE
				PRINTFORM 아이에게 가볍게 주의를 주고
			ENDIF
			PRINTFORMW , 아무일도 없었다는 듯이 접객을 시작했습니다…
		ENDIF
	ELSEIF RAND:2
		IF COND("오토코노코") && RAND:2
			PRINTFORM %CALLNAME:TARGET%는 부자연스럽게 가랑이가 부푼 것을 지적당하지 않을까 조마조마해하면서, 
		ELSE
			PRINTFORM %CALLNAME:TARGET%는 부끄러운데도 화를 내지도 못하고, 
		ENDIF
		PRINTFORMW 그 뒤엔 어떻게 말도 못할 기분으로 지낸 듯 합니다…
	ELSEIF RAND:2 && EQUIP:팬티 && LOCALS != "紐解き"
		PRINTFORMW 주변의 손님들에게 완전히 먹혀버린 %PANTIES(TARGET)%를 죄다 보여버린 %CALLNAME:TARGET%는, 새빨간 얼굴로 빠른 목소리로 아이를 타이르고 있습니다…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 살짝 뺨을 붉히고, 곤란한 얼굴로 아이를 타이르고 있습니다…
	ENDIF
CASE "こけてパンもろ"
	PRINTFORM %CALLNAME:TARGET%는 
	IF COND("제복：스커트") && TALENT:부끄럼쟁이 && RAND:2
		PRINTFORM 스커트 길이에 너무 신경쓴 나머지, 
	ELSE
		PRINTFORM 너무 바쁜 나머지, 
	ENDIF
	PRINTFORMW 가게 내에서 넘어져 다리를 쩍 벌린 채로 엉덩방아를 찢고 말았습니다. 
	IF TALENT:부끄럼쟁이 == 0 && (TALENT:부끄럼없음 || ABL:노출벽 >= 3)
		IF ABL:노출벽 >= 3 && RAND:2
			PRINTFORM 주목받고있는 것을 깨달은 %CALLNAME:TARGET%는 
			IF COND("제복：바니")
				IF RAND:2
					PRINTFORMW 양 다리를 안쪽 허벅지가 보일 정도로 벌려, 가랑이를 근처의 손님에게 마음껏 보였습니다
				ELSE
					PRINTFORMW 더욱 다리를 벌려, 에로한 Ｍ자 자세로 만들어 하이레그 의상이 파고 들어가고 있는 가랑이를 근처 손님에게 과시하기 시작했습니다
				ENDIF
				PRINTFORMW 주위의 손님들은 다방 안이라고는 생각되지않는 광경에, 일제히 앞으로 몸을 숙이고 꼼짝도 못하게되어버렸습니다… 
			ELSEIF TALENT:음란 && PENIS(TARGET) == 0 && RAND:2
				PRINTFORMW %TEXTR("가랑이를 연 채로", "허리를 쑥 내밀고", "M자로 다리를 벌리고")% 주변 손님에게 팬티를 과시하기시작했습니다 
				PRINTFORM 몇 초의 짧은 시간이었지만, 
				IF TALENT:젖기쉬움 && RAND:3 == 0
					PRINTFORM 허리를 내밀어 얼룩이 생긴 팬티를 과시했다
				ELSEIF RAND:2
					PRINTFORM 팬티를 먹혀 들게 한뒤 약간 짙게된 입술 주변을 과시했다
				ELSE
					PRINTFORM 속옷 너머로 보지를 넓히는 치태를 선보이고있다
				ENDIF
				PRINTFORMW %CALLNAME:TARGET%의 모습에 주변의 손님들은 꼼짝도 할 수 없게되고말았습니다…
			ELSE
				PRINTFORMW , 가랑이를 벌린 자세로 근처 손님에게 팬티를 과시하기 시작했습니다.
				PRINTFORMW \@ PENIS(TARGET) ?  다리 사이의 부푼 물건 # 그곳 \@을 팬티 너머로 어루만지고 문지르고 있는 %CALLNAME:TARGET%의 모습에, 주위의 시선이 완전히 못박혔습니다…
			ENDIF
		ELSE
			IF COND("제복：바니")
				IF AGE(TARGET) >= 3 && RAND:2
					PRINTFORM 딱 달라붙는 의상덕분에 다리 사이 주변의 매력적인 곡선이 
				ELSE
					PRINTFORM 바니 슈트 너머긴하지만 다리사이가 
				ENDIF
			ELSE
				PRINTFORM 팬티가 
			ENDIF
			IF TALENT:양기 && RAND:2
				PRINTFORMW 보이는 자세 그대로 어색함을 숨기기위해 미소를 흩뿌리고 있는 %CALLNAME:TARGET%에게 감사하면서, 주변 손님은 마음껏 눈요기를 하고있는 것같습니다… 
			ELSE
				PRINTFORMW 보이는 것을 숨기려고도 하지 않는 %CALLNAME:TARGET%에게 감사하면서, 주위의 손님은 마음껏 눈요기를 하고있는 것같습니다…
			ENDIF
		ENDIF
	ELSE
		IF COND("제복：스커트")
			PRINTFORM 순간적으로 손으로 숨겼지만, 주변의 손님에게 확실히 
		ELSE
			PRINTFORM 순간에 매무새를 고쳤지만, 주변의 손님에게 칠칠치 못하게 다리를 벌린 모습을 
		ENDIF
		PRINTFORMW 보이고 만것같습니다. 
		IF COND("오토코노코") && RAND:2
			PRINTFORM 다행히도 발기하고있지 않아서 남자라는 것을 들키지는 않았지만, 
		ELSEIF TALENT:기품 == 0 && ABL:노출벽 <= 1 && PENIS(TARGET) == 0 && RAND:2
			PRINTFORM 보여줄 예정 없던 평소의 팬티를 보여 주었습니다.
		ENDIF
		PRINTFORMW 한심하다고할지 부끄럽다고해야할지, 그 뒤부터는 일을 제대로 할 수 없었던 것같습니다… 
	ENDIF
CASE "자위중독"
	PRINTFORMW %CALLNAME:TARGET%는, 그럭저럭 성실하게 일한 것 같습니다. 
	PRINTFORM …하지만, 점내가 한가하게 되었다거나 할 때, 무의식적으로 
	IF RAND:2
		PRINTFORM 테이블 모서리에 \@ COND("오토코노코") ?  페니스를 # 그곳을 \@ 문질러붙이고 있는 것
	ELSE
		PRINTFORM \@ COND("오토코노코") ? 페니스를 # 그곳을 \@를 만지작거리는 것
	ENDIF
	WAITRESS = FIND_COND("출연가능な점원")
	SETBIT CFLAG:WAITRESS:95, 1
	PRINTFORMW 을 %CALLNAME:WAITRESS%에게 걸려 혼나고 말았습니다. 
	IF (ABL:욕망 >= 5 || ABL:자위중독 >= 5 || TALENT:음란) && ABL:WAITRESS:레즈끼 + COND("오토코노코") + COND("오토코노코", WAITRESS)
		PRINTFORM 그 발정난 모습
		IF ABL:WAITRESS:레즈끼 >= 3 && RAND:2
			PRINTFORM 에 두근거려버린 
		ELSE
			PRINTFORM 이 눈에 거슬린 
		ENDIF
		PRINTFORM %CALLNAME:WAITRESS%는, %타겟을()% 탈의실
		IF ABL:WAITRESS:레즈끼 >= 3 && RAND:2
			PRINTFORM 에 데려가 만족시키기로 
		ELSE
			PRINTFORM 에서 해소해주기로 
		ENDIF
		PRINTFORMW 한 것 같습니다. 
		PRINTL 
		PRINTFORMW －탈의실－
		IF ABL:레즈끼 >= 3 || (TALENT:부끄럼쟁이 == 0 && ABL:레즈끼 >= 5)
			PRINTFORM 그 제안은 %CALLNAME:TARGET%도 흥미가 있었는지, 
			IF RAND:2 || COND("제복：바니") || COND("身嗜み：노팬티")
				IF COND("제복：바니") || COND("身嗜み：노팬티")
					PRINTFORM 하반신을 노출시킨 후 
				ELSE
					PRINTFORM \@ COND("오토코노코") ?  사랑스러운 # 촉촉히 하게 습기찬 \@ 팬티를 발목에 걸고서 
				ENDIF
				PRINTFORM 의자에 앉아 가랑이를 열고,
				IF PENIS(TARGET) && RAND:2 + COND("오토코노코")
					PRINTFORMW 크게 부푼 페니스를 훑으면서 졸라대기 시작했습니다…
				ELSE
					PRINTFORMW 그곳을 벌리면서 졸라대기 시작했습니다…
				ENDIF
			ELSE
				PRINTFORMW 의자에 앉아 다리를 벌리고, \@ COND("오토코노코") ? 여성용의 # \@ 팬티를 비껴내고 졸라대기 시작했습니다… 
			ENDIF
		ELSE
			PRINTFORM 의자에 앉은 %CALLNAME:TARGET%는 
			IF TALENT:교만 + TALENT:강기 && TALENT:부끄럼쟁이 == 0
				PRINTFORM 쑥쓰러운 표정으로 
			ELSEIF TALENT:호색 && ABL:레즈끼
				PRINTFORM 기대에 찬 눈빛으로 
			ELSEIF TALENT:냉정 || TALENT:감정결핍
				PRINTFORM 호흡을 가다듬으면서 
			ELSEIF TALENT:부끄럼쟁이
				PRINTFORM 뺨을 빨갛게 붉히면서 
			ELSEIF TALENT:솔직 || TALENT:양기
				PRINTFORM 두근두근하면서 
			ELSE
				PRINTFORM 쭈뼌쭈뼛거리면서 
			ENDIF
			PRINTFORMW 약간만 다리를 벌려, \@ COND("오토코노코") ? 자지를 키우면서 # \@%CALLNAME:WAITRESS%의 모습을 지켜 보고있습니다…
		ENDIF
		PRINTFORMW ………
		PRINTFORMW ……

		PLAYER = WAITRESS
		CALL ADD_STATE, TARGET, "미약"
		PALAM:쾌Ｃ = MAXBASE:성감허용치여자
		PALAM:욕정 = 30000

		LOCAL:1 = LIMIT(RAND:4 + (ABL:WAITRESS:욕망 + ABL:WAITRESS:레즈끼)/2, 2, 7)
		CALL SIMULATE_TRAIN_FIRST
		IF TALENT:남자
			CALL SIMULATE_TRAIN_COM, 4, LOCAL:1
		ELSEIF TALENT:후타나리
			CALL SIMULATE_TRAIN_COM, 4, LOCAL:1/2
			CALL SIMULATE_TRAIN_COM, 1, LOCAL:1/2
		ELSE
			CALL SIMULATE_TRAIN_COM, 1, LOCAL:1
		ENDIF
		SIF COND("오토코노코", WAITRESS) && ABL:정액중독 >= 1
			CALL SIMULATE_TRAIN_COM, 91, 1+RAND:2

		CALL SIMULATE_TRAIN_CALC
		CALL SIMULATE_TRAIN_SHOW
		PLAYER = MASTER
		TEQUIP:미약 = 0

		PRINTFORM …%CALLNAME:WAITRESS%가 %CALLNAME:TARGET%의 
		IF (CONDS("음모") == "ツルツル" || CONDS("음모") == "剃り跡") && RAND:3 == 0
			PRINTFORM 사타구니가 제모되어있는 것 등을 조롱하면서 
		ELSEIF ABL:새드끼 >= 3 && RAND:2
			PRINTFORM 가랑이의 모습을 실황하면서 
		ELSEIF TALENT:남자
			PRINTFORM 가랑이의 
		ELSEIF RAND:2
			PRINTFORM 가랑이를 열어젖히고 
		ELSE
			PRINTFORM 다리사이에 얼굴을 묻고 
		ENDIF
		PRINTFORM 구석구석까지 혀로 핥아, 
		IF TALENT:남자 && TALENT:클리피어스 && RAND:3 == 0
			PRINTFORM 피어스로 장식된 
		ELSEIF TALENT:남자 == 0 && TALENT:클리피어스 + TALENT:라비아피어스 && RAND:3 == 0
			IF PENIS(TARGET) == 0 && TALENT:클리피어스 && TALENT:라비아피어스 && RAND:2
				PRINTFORM 피어스 투성이인 
			ELSE
				PRINTFORM 피어스로 장식된 
			ENDIF
		ELSEIF TALENT:오줌싸개버릇 && RAND:3 == 0
			PRINTFORM %TEXTS("匂い")%의 귤나무째인
		ELSEIF TALENT:남자
			PRINTFORM 발기해버린 
		ELSEIF RAND:2
			PRINTFORM 발정한 
		ELSE
			PRINTFORM 후끈거리는 
		ENDIF
		IF AGE(TARGET) >= 4 && AGE(WAITRESS) < 4
			PRINTFORM 어른의 
		ELSEIF AGE(TARGET) == 1 && AGE(WAITRESS) > 1
			PRINTFORM 어린 
		ENDIF
		IF TALENT:남자
			PRINTFORM 페니스를 마음껏 
		ELSEIF TALENT:후타나리
			PRINTFORM 보지와 페니스를 마음껏 
		ELSE
			PRINTFORM 보지를 마음껏 
		ENDIF
		IF ABL:레즈끼 >= 5 && RAND:2
			PRINTFORMW 맛보았습니다. 
		ELSEIF ABL:레즈끼 >= 3 && RAND:2
			PRINTFORMW 즐겼습니다. 
		ELSE
			PRINTFORMW 자극했습니다. 
		ENDIF
		IF COND("오토코노코", WAITRESS) && ABL:정액중독 >= 1
			PRINTFORM 그리고, 발기 해 버린 페니스를 대가로 입으로 처리해 달라고한 %CALLNAME:WAITRESS%는, 
		ELSE
			PRINTFORM 그 후, 녹초가 된 %CALLNAME:TARGET%에게 
		ENDIF
		IF ABL:레즈끼 >= 5 && TALENT:양기 + TALENT:소악마 && COND("오토코노코", WAITRESS) == 0 && RAND:3 == 0
			PRINTFORM 『잘 먹었습니다♪』
		ELSEIF RAND:3 == 0 && COND("오토코노코", WAITRESS) && ABL:정액중독 >= 1
			PRINTFORM 만약을 위해 입을 헹구고 플로어에 돌아오도록 
		ELSEIF RAND:3 == 0
			PRINTFORM 의자를 닦아놓도록 
		ELSEIF RAND:2 == 0
			IF COND("제복：바니") || COND("身嗜み：노팬티")
				PRINTFORM 몸가짐을 정돈하도록 
			ELSE
				PRINTFORM 팬티를 갈아입도록 
			ENDIF
		ELSE
			PRINTFORM 기운을 차리고 플로어에 돌아오도록 
		ENDIF
		PRINTFORMW 하라는 말을 남기고, 정상업무로 돌아왔습니다…
	ELSE
		PRINTFORMW 손님에게 발견되기 전에 제대로 교육 다시 해야할 것인가도 모릅니다…
	ENDIF
CASE "身体が화照る"
	PRINTFORMW 손님의 주문을 받고있는 %CALLNAME:TARGET%는, 어딘가 열에 들뜬 듯한 얼굴로 멍-하니 있습니다. 
	PRINTFORMW 의아해하는 손님이 말을 걸자, %CALLNAME:TARGET%는 당황한 모습으로 주문을 받기시작했습니다. 
	PRINTFORML …주방에 주문을 전달하고, 도망치듯이 화장실에 틀어박힌 %CALLNAME:TARGET%는, 
	IF COND("제복：바니")
		LOCALS = 속옷
	ELSE
		LOCALS = 팬티
	ENDIF
	IF COND("다방：Ｖ섹스") && RAND:2
		EXP:Ｖ경험 += ABL:욕망
		EXP:Ｖ성교경험 += ABL:욕망
		EXP:허리사용경험 += RAND:(ABL:욕망 + 1) + 1
		EXP:정액경험 += ABL:욕망 + 2
		CALL SETFLAG, "질내정액경험", ABL:욕망/2 + 1, TARGET, -4
	ENDIF
	IF COND("다방：Ａ섹스") && RAND:2
		EXP:Ａ경험 += ABL:욕망
		EXP:Ａ성교경험 += ABL:욕망
		EXP:허리사용경험 += RAND:(ABL:욕망 + 1) + 1
		EXP:정액경험 += ABL:욕망 + 2
		CALL SETFLAG, "장내정액경험", ABL:욕망/2 + 1, TARGET, -4
	ENDIF
	IF COND("다방：인서트") && RAND:2
		EXP:Ｖ삽입경험 += ABL:욕망
		EXP:허리사용경험 += ABL:욕망
		EXP:사정경험 += ABL:욕망/2 + 1
	ENDIF
	IF EXP_GAIN(17) + EXP_GAIN(18) + EXP_GAIN(15)
		PRINTFORM 방금 전에 몰래 손댄 손님과의 정사를 회상하면서, 
		CFLAG:36 += 1
	ELSE
		PRINTFORM %CALLNAME:MASTER%과 함께한 정사를 회상하면서, 
	ENDIF
	;내사정された
	IF EXP_GAIN(17) && RAND:2
		IF RAND:2 && (COND("身嗜み：노팬티") || ABL:정액중독 >= 3)
			PRINTFORMW 질에서 긁어낸 정액을 핥고, 뜨거운 한숨을 쉬었습니다… 
		ELSE
			IF RAND:3 == 0 && ABL:정액중독 >= 5
				PRINTFORMW %LOCALS%의 가랑이 부분을 더럽힌 정액을 핥아먹었습니다…
			ELSE
				PRINTFORMW 질에서 흘러나온 정액으로 끈적끈적해진 %LOCALS%를 갈아입었습니다…
			ENDIF
		ENDIF
	;Ａ내사정された
	ELSEIF EXP_GAIN(18) && RAND:2
		IF RAND:2 && ABL:정액중독 >= 3 && ABL:Ａ감각 >= 3 && (TALENT:악취둔감 + TALENT:불결무시 + TALENT:도착적 || ABL:Ａ감각 >= 5 || ABL:마조끼 >= 3)
			PRINTFORMW 애널에서 긁어낸 정액을 입에 머금고, 도착적인 쾌락을 맛보았습니다…
		ELSE
			IF ABL:Ａ확장 >= 3 && COND("身嗜み：노팬티") == 0 && RAND:2
				PRINTFORMW 벌어진 애널에서 흘러나온 정액으로 더럽혀진 %LOCALS%를 갈아입었습니다…
			ELSE
				PRINTFORMW 뜨거운 애널을 만지작거리면서 열기를 받아들였습니다…
			ENDIF
		ENDIF
	ELSEIF TALENT:남자
		PRINTFORMW 페니스에서 뿜어져나오는 쿠퍼액을 손가락으로 닦아 입에 넣었습니다…
	ELSE
		PRINTFORMW 허벅지에 흘러내려오는 액체를 황홀한 얼굴로 닦았습니다…
	ENDIF
CASE "テーブル拭き１"
	PRINTFORM %CALLNAME:TARGET%는 
	IF RAND:2
		PRINTFORM 디저트를 가져가는 일만 남은 
	ELSE
		PRINTFORM 정리가 끝나지 않은 
	ENDIF
	PRINTFORM 테이블에 가서, 
	IF TALENT:냉정 || TALENT:감정결핍
		PRINTFORM 정중하게 
	ELSEIF TALENT:건방짐
		PRINTFORM 스스럼없이 
	ELSE
		PRINTFORM 웃는 얼굴로 
	ENDIF
	PRINTFORMW 인사한 후, 익숙한 모습으로 식기를 쟁반위에 올려놓고, 행주로 빠르게 테이블을 청소하기 시작했습니다. 
	PRINTFORML 그 모습을 어째선지 바라보고 있떤 손님의 시선이, 갑자기 어느 한점에 못박혔습니다. 
	PRINTFORM 테이블을 닦는 %CALLNAME:TARGET%의 
	IF TALENT:거유 && RAND:4
		IF TALENT:거유 >= 2
			PRINTFORM 굉장한 크기의 가슴이
		ELSE
			PRINTFORM 거유가
		ENDIF
		PRINTFORM  , 눈 앞에서 
		IF RAND:2
			PRINTFORM 출렁출렁 중량감있게 
		ELSE
			PRINTFORM 푸들푸들 
		ENDIF
		PRINTFORMW 흔들리고있습니다…
		LOCAL = 1
	ELSEIF TALENT:빈유 && (RAND:4 || COND("제복：바니") )
		IF COND("제복：바니")
			PRINTFORM 에나멜 재질 의상의 앞가슴 틈새로, 
		ELSE
			PRINTFORM 옷깃 틈으로, 
		ENDIF
		IF RAND:3 == 0 && TALENT:니플피어스
			PRINTFORM 피어스가 빛나는 연분홍색의 돌기
		ELSEIF RAND:3 == 0
			PRINTFORM 엷은 가슴이 
		ELSEIF RAND:2 && COND("거유륜")
			PRINTFORM 소극적인 젖가슴과는 정반대로 면적의 넓은 유륜
		ELSEIF COND("함몰유두")
			PRINTFORM 함몰 상태의유두주변
		ELSEIF COND("유두비대")
			PRINTFORM 부푼 곳에 반해 큰 돌기
		ELSE
			PRINTFORM 작은 연분홍색의 돌기
		ENDIF
		PRINTFORMW 보이고있습니다…
		LOCAL = 2
	ELSEIF COND("제복：바니")
		PRINTFORMW 의상 앞가슴의 틈새로, 땀이차서 후끈거리는 가슴 골짜기가 훤히 보이고있습니다…
	ELSE
		IF ABL:노출벽 + TALENT:호색 >= 5 && RAND:5 == 0
			IF TALENT:니플피어스 && RAND:3
				PRINTFORM 유두에 달린 %TEXTS("니플피어스")%가, 
			ELSEIF RAND:2 && COND("거유륜")
				PRINTFORM 눈에 선명한 큰 유륜이,
			ELSEIF COND("함몰유두")
				PRINTFORM 함몰 상태의유두하지만,
			ELSEIF COND("유두비대")
				PRINTFORM 평균보다 분명하게 큰유두하지만,
			ELSE
				PRINTFORM 핑크빛 유두가, 
			ENDIF
		ELSE
			IF TALENT:호색 && RAND:2
				IF RAND:3 == 0
					PRINTFORM 과격한 
				ELSEIF TALENT:유치 == 0
					PRINTFORM 어른스러운 
				ENDIF
			ELSEIF RAND:3 == 0
				PRINTFORM 컬러풀한 
			ELSEIF TALENT:작은몸집 && TALENT:유치 && RAND:3 == 0
				PRINTFORM 어린애같은 
			ENDIF
			PRINTFORM 브래지어가, 
		ENDIF
		PRINTFORMW 땀에 희미하게 보이고있습니다…
		LOCAL = 3
	ENDIF
	;욕망+노출벽-부끄럼쟁이*3が３이상なら
	IF LOCAL <= 2 && ABL:욕망 + ABL:노출벽 - TALENT:부끄럼쟁이*3 >= 3 && RAND:4
		PRINTFORM 문득 자신의 가슴팍에 열중한 시선을 눈치 챈 %CALLNAME:TARGET%는, 
		IF LOCAL == 1 && COND("다방：おさわり")
			PRINTFORM 테이블 위에 놓인 손님의 손에, 
			IF RAND:2
				PRINTFORM 출렁 
			ELSE
				PRINTFORM 물컹 
			ENDIF
			PRINTFORMW 거유를 올려놓고 걸레질을 다시 시작했습니다. 
		ELSEIF COND("제복：바니")
			PRINTFORM 상체를 구부린 자세 그대로 손가락으로  바니 슈트의 가슴을 내리고 
			IF TALENT:니플피어스 && RAND:3
				PRINTFORM 유두에 달린 피어스
			ELSEIF RAND:2 && COND("거유륜")
				PRINTFORM 훌륭한 크기의 유륜
			ELSEIF COND("함몰유두")
				PRINTFORM 자칫하면 컴플렉스나 될 것 같은 함몰 상태의유두
			ELSEIF COND("유두비대")
				IF TALENT:음유
					PRINTFORM 은밀한 자랑이 되고 있는 비대했다유두
				ELSE
					PRINTFORM 부끄러운 위에 큰유두
				ENDIF
			ELSE
				PRINTFORM 핑크빛 유두
			ENDIF
			PRINTFORMW 를 보여주었습니다. 
		ELSE
			PRINTFORMW  비어있는 손으로 슬쩍 옷깃을 내렸습니다. 
		ENDIF
		PRINTFORM 놀란 얼굴을 한 손님에게, %CALLNAME:TARGET%는 
		IF TALENT:교만 && TALENT:강기
			IF TALENT:심술쟁이 && RAND:2
				PRINTFORMW 히죽히죽 웃으면서 윙크했습니다…
			ELSE
				PRINTFORMW 씨익 도발적인 미소를 지었습니다…
			ENDIF
		ELSEIF TALENT:냉정 || TALENT:감정결핍
			IF RAND:2
				PRINTFORMW 찡긋 윙크했습니다…
			ELSE
				PRINTFORMW 희미하게 미소지었습니다…
			ENDIF
		ELSE
			IF RAND:3 == 0
				PRINTFORM 뺨을 붉게 물들이면서 
			ELSEIF RAND:2
				PRINTFORM 장난스러운 미소로 
			ELSE
				PRINTFORM 『비밀』이라며 검지 손가락을 입술에 대고 
			ENDIF
			PRINTFORMW 윙크했습니다…
		ENDIF
	ELSE
		PRINTFORM 깨끗하게 청소를 끝마친 %CALLNAME:TARGET%는, 
		IF RAND:2
			PRINTFORM 얼굴을 붉히고 수줍어하고 있는 
		ELSE
			PRINTFORM 몸을 앞쪽으로 구부리고 있는 
		ENDIF
		PRINTFORM 손님을 보고, 
		;見せてんのよor事態に気づくｗ
		IF TALENT:현명함
			IF TALENT:부끄럼쟁이
				IF LOCAL == 2
					PRINTFORMW 얼굴을 붉히고 옷깃을 눌렀습니다…
				ELSE
					PRINTFORMW 당황해서 가슴을 손으로 가렸습니다…
				ENDIF
			ELSE
				PRINTFORMW 즐거운듯이 주문을 받기시작했습니다…
			ENDIF
		ELSE
			PRINTFORMW 고개를  갸웃거렸습니다…
		ENDIF
	ENDIF
CASE "クリーム拭き取り"
	IF RAND:10
		LOCALS = 소년
	ELSEIF RAND:10 == 0
		LOCALS = 여자아이
	ELSE
		LOCALS = 청년
	ENDIF
	PRINTFORM 주방에 돌아가려던 %CALLNAME:TARGET%는, 뺨을 크림으로 더럽히면서 
	IF RAND:3 == 0
		PRINTFORM 파르페를 입에 가져가는 
	ELSEIF RAND:2
		PRINTFORM 슈크림을 베어무는 
	ELSE
		PRINTFORM 소프트크림을 덥석 문 
	ENDIF
	PRINTFORMW %LOCALS%의 앞을 지나갔습니다. 
	PRINTFORM %CALLNAME:TARGET%는 %LOCALS%의 얼굴을 보고 
	IF TALENT:강기 && RAND:2 == 0
		PRINTFORM 히죽 웃고, 
	ELSEIF RAND:3 == 0 || TALENT:심술쟁이
		PRINTFORM 가볍게 웃음을 터뜨리고, 
	ELSEIF RAND:2 == 0
		PRINTFORM 쿡쿡 웃고, 
	ELSE
		PRINTFORM 얼굴을 활짝 펴고, 
	ENDIF
	IF TALENT:호색 && RAND:(TALENT:호색 + TALENT:헌신 + 1) == 0 && COND("다방：おさわり")
		PRINTFORMW 살짝 얼굴을 가까이 가져가, 할짝 크림을 혀로 핥았습니다. 
		LOCAL = 1
	ELSEIF TALENT:헌신 && RAND:(TALENT:헌신 + 1) == 0
		PRINTFORMW 갖고있던 손수건으로 부드럽게 닦아주었습니다…
		LOCAL = 2
	ELSEIF MAX(ABL:욕망, ABL:봉사정신) >= 3 && RAND:2 && COND("다방：おさわり")
		PRINTFORMW 크림을 손가락으로 닦아서, 할짝 핥아먹었습니다. 
		LOCAL = 3
	ELSE
		PRINTFORMW 크림을 손가락으로 가리켜주었습니다…
	ENDIF
	;舐めとった時
	IF LOCAL == 1 || LOCAL == 3
		IF LOCAL == 1
			PRINTFORM 뺨에 남아있는 
			IF RAND:2
				PRINTFORM 부드러운 입술의 감촉에 
			ELSE
				PRINTFORM 따뜻한 혀의 온도에 
			ENDIF
			PRINTFORM 얼굴이 새빨개진 
		ELSE
			PRINTFORM 갑작스러운 일에 깜짝 놀란 
		ENDIF
		PRINTFORM %LOCALS%에게 
		IF RAND:2
			PRINTFORM 장난스럽게 윙크하고
		ELSE
			PRINTFORM 미소짓고
		ENDIF
		PRINTFORMW , %CALLNAME:TARGET%는 가게 안쪽으로 돌아갔습니다…
	ENDIF
;あーん（하피さんは, 손がそのままの場合はついばむ？）
CASE "あ～ん"
	PRINTFORM %CALLNAME:TARGET%는 계산 일을 하면서, 계산대 옆에 놓인 
	IF RAND:3 == 0
		PRINTFORM 각양각색의 
	ELSEIF RAND:2
		PRINTFORM 달콤한 냄새를 풍기는 
	ELSE
		PRINTFORM 바구니에 담긴 
	ENDIF
	IF RAND:4 == 0
		LOCALS = 쿠키
	ELSEIF RAND:3 == 0
		LOCALS = 츄로스
	ELSEIF RAND:2
		LOCALS = 소트브레드
	ELSE
		LOCALS = 비스킷
	ENDIF
	PRINTFORMW %LOCALS%를 손님에게 서비스하고 있습니다. 
	PRINTFORM 단순작업의 반복에 질린것인지, %CALLNAME:TARGET%는 거스름돈을 지갑에 넣고 있는 손님의 입가에, 
	;솔직
	IF TALENT:솔직 && RANDIF(2, TALENT:감정결핍, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마, TALENT:작은몸집)
		PRINTFORM 빙긋 웃으면서 
	;양기
	ELSEIF TALENT:양기 && RANDIF(2, TALENT:감정결핍, TALENT:부끄럼쟁이, TALENT:소악마, TALENT:작은몸집)
		PRINTFORM 활기차게 
	;소악마
	ELSEIF TALENT:소악마 && RANDIF(2, TALENT:감정결핍, TALENT:부끄럼쟁이, TALENT:작은몸집)
		PRINTFORM 히죽히죽 웃으면서 
	;감정결핍
	ELSEIF TALENT:감정결핍 && RANDIF(2, TALENT:부끄럼쟁이, TALENT:작은몸집)
		PRINTFORM 표정을 바꾸지 않고 
	;小柄
	ELSEIF TALENT:작은몸집 && RANDIF(2, TALENT:부끄럼쟁이)
		PRINTFORM 웃는 얼굴로 발돋움해 
	;부끄럼쟁이
	ELSEIF TALENT:부끄럼쟁이 && RAND:2
		IF RAND:2
			PRINTFORM 살짝 뺨을 붉히고 
		ELSE
			PRINTFORM 살짝 부끄러운 듯이 
		ENDIF
	ELSE
		PRINTFORM 미소를 보이면서 
	ENDIF
	PRINTFORMW %LOCALS%를 내밀었습니다. 

	MEMO_LINECOUNT = LINECOUNT
	CALL SELF_KOJO_L, EVENT_NAMES
	IF LINECOUNT == MEMO_LINECOUNT
		;구상案
		SELECTCASE NO:TARGET
		CASE 1
			PRINTFORM 「후훗…서비스 야♪자, 아~앙
			CALL HEARTMARK_E
		CASE 2
			IF RAND:2
				PRINTFORMW 「자아, 아~앙. 하웁♪」
			ELSE
				PRINTFORMW 「저기있지, 반 나누자! 우쌰. 하웁」
			ENDIF
		CASE 5
			PRINTFORMW 「이거, 어쩐지 연인 같은 느낌이야♪」
		CASE 6
			IF RAND:2
				PRINTFORMW 「네에, 야옹♪」
			ELSE
				PRINTFORMW 「나？난 너무 달콤한건 먹기 힘들어서 그렇다냥～」
			ENDIF
		CASE 7
			;워울프は…喋らないで良いか
			SIF TALENT:사족보행 == 0
				PRINTFORMW 「자, 아앙~♪」
		CASE 8, 9
			IF RAND:3 == 0
				PRINTFORMW 「저, 저기…입을, 열어주세요…」
			ELSEIF RAND:2 == 0
				PRINTFORMW 「…구워진 상태는, 마음에 드시는가요？」
			ELSE
				PRINTFORMW 「저…저기! 이거… 부디 먹어주세요」
			ENDIF
		CASE 11
			IF RAND:2
				PRINTFORMW 「자~, 어린 새처럼 입을 열어보라구♪」
			ELSE
				PRINTFORMW 「여기서 좀 더 하는건 별도의 요금이 들어갑니다~아 …랄까나♪」
			ENDIF
		CASE 12
			IF RAND:2
				PRINTFORMW 「그, 그러니까, 빨리먹으란 말이야, 정말이지…」
			ELSE
				PRINTFORMW 「뭘, 히죽히죽거리고 있는거야 …. …그래. 그러니까, 히죽히죽거리지 말라고」
			ENDIF
		CASE 16
			IF RAND:2
				PRINTFORMW 「세상의 "약속" 이라는것은, 첩에겐 조금 어려운것 같군」
			ELSE
				PRINTFORMW 「뭐지, 이것도 "서비스"의 일환이다. 자, 아～앙, 하도록」
			ENDIF
		CASE 19
			IF RAND:2
				PRINTFORM 「이것도, 맛있게 먹기 위한 마법이라구…아～앙
				CALL HEARTMARK_E
			ELSE
				PRINTFORMW 「자. 검게 태운 도마뱀 들어간다. …거짓말이야. 농담이라구」
			ENDIF
		CASE 20
			IF RAND:2
				PRINTFORMW 「이것도 하나님의 은혜랍니다？ 자, 입을 크게 벌려주세요♪」
			ELSE
				PRINTFORMW 「자아, 제대로 씹어 먹어주세요？ 그 이후 양치질도 깨끗히 하시고요？ 하나님께 기도하는것 잊지 마시고요？ 따뜻하게 한 다음 자야합니다？」
			ENDIF
		CASE 21
			IF RAND:2
				PRINTFORMW 「성인도 타락시키는 달콤한 유혹…. 너는 거스를수 있을까？」
			ELSE
				PRINTFORMW 「맛있는 것은 당연한거 아니야？」
			ENDIF
		CASE 22
			IF RAND:2
				PRINTFORMW 「특별하게다. 내 마음이 바뀌기 전에, 받는게 좋을거다」
			ELSE
				PRINTFORMW 「남쪽의 과자도 꽤나 좋은것 이지만. 나의 나라에도, 전병을 구워 먹는것이 인기가 있단말이지…」
			ENDIF
		CASE 24
			PRINTFORMW 「후훗…. 내가, 구워 놓은 것… 먹어봐…」
		CASE 25
			IF RAND:2
				PRINTFORM 「이것도 나중엔, 당신의 피가 될 것들이군요…. 꼼꼼히 씹어 먹도록해요
				CALL HEARTMARK_E
			ELSE
				PRINTFORMW 「저로서는, 간이나 시금치를 권하고 싶군요. …시금치 쿠키. 이걸 드세요」
			ENDIF
		CASE 27
			IF RAND:2
				PRINTFORMW 「후훗, 당신이 맛있게 이것을 먹는 모습, 나는 이미 봤었어요♪」
			ELSE
				PRINTFORMW 「예언 하도록 하죠, 당신은 이것을 먹을 거에요♪」
			ENDIF
		CASE 28
			IF RAND:2
				PRINTFORMW 「빨리 입을 열어. …자」
			ELSE
				PRINTFORMW 「맛있어？ 그래. 다행이네」
			ENDIF
		CASE 29
			IF RAND:2
				PRINTFORMW 「부들부들～ 은 아니지만, 맛있다구♪」
			ELSE
				PRINTFORMW 「부들부들 한 것도, 먹고 싶지？」
			ENDIF
		CASE 34
			PRINTFORMW 「자아, 먹여 주도록 하지♪」
		CASE 35
			PRINTFORMW 「후후훗, 어때, 맛있어～？」
		CASE 46
			PRINTFORMW 「후후, 어떠한가요？」
		CASE 54
			PRINTFORMW 「에헤헤, 조금이지만 자신있다구요♪」
		CASE 58
			IF RAND:3 == 0
				PRINTFORMW 「그래…. 그대로 삼키도록해. 나의 에이스오브스페이드…」
			ELSEIF RAND:2
				PRINTFORMW 「와인은 어떤가」
			ELSE
				PRINTFORMW 「그녀석은 최상의 버터다」
			ENDIF
		CASEELSE
			PRINTFORMW 「자, 아～앙♪」
		ENDSELECT
	ENDIF
	PRINTFORMW 예상치도 못한 럭키한 전개에, 손님은 헤롱헤롱해져버리고 말았습니다…

	;唇に挟んで・아이専用→皿を置くと, %CALLNAME:TARGET%는 おもむろに1つを唇に挟んで(咥えて), 「んっ」と差し出してきました. 
	;소년(소녀)はどきどきしながらも(ためらいながらも)입を近づけ, %CALLNAME:TARGET%가 咥えたお菓子に입を近づけました. 
	;(TARGET유치専用？)唇が触れそうになる瞬間, %CALLNAME:TARGET%는 お菓子を半分持っていってしまいました. %CALLNAME:TARGET%는, 悪戯が成功した아이のように微笑んでいます. 
	;소년(略)が唇が触れないように受け取ろうとすると, %CALLNAME:TARGET%는 입を突き出し, 부드러운 唇を押し付けてきました(ついばむ様なくちづけをしました). 
	;唇を触れ合わせたまま, %CALLNAME:TARGET%는 舌を使って, お菓子を소년(略)の입に含ませました. 
	;얼굴を真っ빨강にしたまま固まっている(柔らかくなったお菓子を呆然と噛んでいる)소년(略)に, %CALLNAME:TARGET%는 소년(略)の頭を撫でて(ウィンクして・ニカッと笑って), 店の奥へ戻っていきました. 
;どきどき노팬티接손님(노출벽？음란？両方？)
CASE "노팬티接손님"
	PRINTFORM 손님에게 주문을 받고있는 %CALLNAME:TARGET%는, 
	IF RAND:3 == 0 && COND("제복：스커트")
		PRINTFORMW 끊임없이 스커트 옷자락을 신경쓰고 있습니다.
	ELSEIF RAND:2
		PRINTFORMW 어째선지 꿈지럭꿈지럭  허벅지를 문질러붙이며 초조한 모습입니다. 
	ELSE
		PRINTFORMW 뺨을 살짝 상기시키고, 거동이 의심스럽습니다. 
	ENDIF
	PRINTFORML 이상하게 생각한 손님이 말을 걸자, %CALLNAME:TARGET%는 당황해서 주문을 복창하기 시작했습니다. 
	PRINTFORM 그리고, 고개를 갸웃대는 손님을 거들떠보지도않고, 
	IF TALENT:부끄럼쟁이 && RAND:3
		PRINTFORM 도망치듯이 
	ELSE
		PRINTFORM 재빠르게 
	ENDIF
	PRINTFORMW 안쪽으로 돌아갔습니다. 
	PRINTL 
	IF RAND:2 == 0
		PRINTFORM 화장실
	ELSE
		PRINTFORM 아무도 없는 탈의실
	ENDIF
	PRINTFORM 로 도망친 %CALLNAME:TARGET%는 
	IF RAND:3 == 0
		PRINTFORM 흥분으로 눈동자를 흐리면서 
	ELSEIF RAND:2
		PRINTFORM 수치와 흥분이 뒤섞인 표정으로 
	ELSE
		PRINTFORM 수치로 새빨개진 얼굴에 요염한 미소를 지으면서 
	ENDIF
	IF COND("제복：파스너")
		PRINTFORM 열어놓고 있던 지퍼의 틈새로 손가락을 넣고, 
	ELSE
		PRINTFORM 스커트를 걷어올리고, 
	ENDIF
	IF RAND:3 == 0
		PRINTFORM 가릴 것 없는 
	ELSEIF RAND:2 == 0
		PRINTFORM 열을 띤 
	ELSE
		PRINTFORM 노출된 
	ENDIF
	IF RAND:2
		PRINTFORM 그곳
	ELSE
		PRINTFORM 비소
	ENDIF
	IF TALENT:처녀 == 0 && ABL:욕망 >= 5 && RAND:4 == 0
		PRINTFORMW 에서 코드를 잡아 로터를 꺼내고, 혀로 깨끗하게 핥았습니다. 
		PRINTFORM 리고, 다시금 그곳에 넣고, 
	ELSE
		IF RAND:2
			PRINTFORMW 에서 새어나오는 액체를 손가락으로 떠서 핥아먹었습니다. 
		ELSE
			PRINTFORMW 에서 흘러나오는 액체가 허벅지를 타고내려오는 감촉에, 몸을 움찔, 하고 떨었습니다. 
		ENDIF
		PRINTFORM 그리고, 
		IF RAND:3
			PRINTFORM  허벅지를 적시는 액체를 손수건으로 닦고, 
		ELSE
			PRINTFORM 젖은 그곳을 그대로, 
		ENDIF
	ENDIF

	IF RAND:2
		IF RAND:2
			PRINTFORM 허벅지를 마주 비비면서 
		ELSE
			PRINTFORM 비소에 닿는 공기의 감촉에 뺨을 붉히면서 
		ENDIF
	ELSE
		IF RAND:3 == 0
			PRINTFORM 콩닥콩닥 
		ELSEIF RAND:2 == 0
			PRINTFORM 두근두근 
		ELSE
			PRINTFORM 답답할 정도로 
		ENDIF
		IF RAND:3 == 0
			PRINTFORM 크게 울리는 고동을 느끼면서 
		ELSEIF RAND:2
			PRINTFORM 가슴을 을 크게 울리면서 
		ELSE
			PRINTFORM 마구 쿵쾅대는 가슴에 손을 대면서 
		ENDIF
	ENDIF
	PRINTFORMW 가게 안으로 돌아갔습니다…
CASE "飲み物溢してつまみ食い"
	PRINTFORMW %CALLNAME:TARGET%는 구석의 후미진 자리에 앉아 독서를 하고 있는 손님의 바지에,  찬물을 쏟아버렸습니다. 
	PRINTFORMW 당황해 발치에 몸을 굽혀 젖은 부분을 닦기 시작했습니다만, 아무래도 청년의 가랑이에 텐트가 쳐져있는 것을 눈치채버린 것같습니다…
	PRINTFORM 그것을 
	;호색
	IF TALENT:호색 && RANDIF(2, TALENT:냉정, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 보고 입맛을 다신 
	;양기
	ELSEIF TALENT:양기 && RANDIF(2, TALENT:냉정, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 보고 생긋 웃은 
	;소악마
	ELSEIF TALENT:소악마 && RANDIF(2, TALENT:냉정, TALENT:부끄럼쟁이)
		PRINTFORM 보고 히죽 웃은 
	;냉정
	ELSEIF TALENT:냉정 && RANDIF(2, TALENT:부끄럼쟁이)
		PRINTFORM 보고 상황을 이해한 
	;부끄럼쟁이
	ELSEIF TALENT:부끄럼쟁이 && RAND:2
		PRINTFORM 보고 얼굴을 붉힌 
	ELSEIF RAND:2
		PRINTFORM 보고 얼굴이 상기된 
	ELSE
		PRINTFORM 본 
	ENDIF
	PRINTFORM %CALLNAME:TARGET%는 \@RAND:2 ? 점내가 한가한 # 자리가 사각인 \@ 것을 재빨리 확인하고, 손님
	;교만
	IF TALENT:교만 && RANDIF(2, TALENT:냉정, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마, TALENT:호색)
		PRINTFORM 을 자신있게 
	;호색
	ELSEIF TALENT:호색 && RANDIF(2, TALENT:냉정, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 을 요염하게 
	;양기
	ELSEIF TALENT:양기 && RANDIF(2, TALENT:냉정, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 을 가벼운 모습으로 
	;소악마
	ELSEIF TALENT:소악마 && RANDIF(2, TALENT:냉정, TALENT:부끄럼쟁이)
		PRINTFORM 을 장난스럽게 
	;냉정
	ELSEIF TALENT:냉정 && RANDIF(2, TALENT:부끄럼쟁이)
		PRINTFORM 을 무표정하게 
	ELSEIF RAND:3 == 0
		PRINTFORM 을 올려다보고 
	ELSEIF RAND:2
		PRINTFORM  을 치켜뜬 눈으로 올려보면서 
	ELSE
		PRINTFORM 의 부푼 사타구니를 만지면서 
	ENDIF
	PRINTFORMW 유혹하기 시작했습니다! 
	PRINTFORM 갑작스런 전개에 당황하면서도 청년이 고개를 끄덕이자, %CALLNAME:TARGET%는 재빠르게 손님의 페니스를 꺼내어 
	IF TALENT:교만 + TALENT:심술쟁이 + TALENT:소악마 && RANDIF(2, TALENT:악취둔감 == 0)
		PRINTFORM %TEXTR("발기한 것을", "포경인 것을", "치구가 끼어있는 것을", "오줌 냄새가 나는 것을")% 
		PRINTFORM %TEXTR("추궁하면서", "놀리면서")% 
	ELSEIF TALENT:악취둔감 == 0 && RAND:2
		PRINTFORM %조사처리(TEXTS("匂い"),"를")% 냄새 맡으면서
	ELSEIF PENIS(TARGET) && RAND:2
		IF TALENT:남자 && RAND:2
			PRINTFORM 음란한 행위에 흥분하면서 
		ELSE
			PRINTFORM 자신의 것과의 차이를 즐기면서  
		ENDIF
	ELSEIF TALENT:부끄럼없음 == 0
		PRINTFORM 소리를 눌러죽이면서 
	ELSE
		PRINTFORM 즐거운듯이 
	ENDIF

	CALL SETFLAG, "일반인상대の성행위初期설정"

	IF TALENT:남자 == 0 && (RAND:3 == 0 || (TALENT:거유 && RAND:2) )
		IF TALENT:거유
			IF COND("제복：웨이트리스") && TALENT:호색 && RAND:3 == 0
				PRINTFORM , 셔츠의 버튼을 1개만 제외해 만든 구멍에 그것을 맞이해
			ELSE
				PRINTFORM 자랑스러운 가슴으로
			ENDIF
		ELSEIF TALENT:빈유
			PRINTFORM 작은 가슴으로 
		ELSE
			PRINTFORM 부드러운 가슴으로 
		ENDIF
		PRINTFORMW 파이즈리하기 시작했습니다…
		SELECTCOM = 92
	ELSE
		IF ABL:목확장 >= 2 && RAND:2
			PRINTFORMW 목구멍 안쪽까지 물고 맛보기시작했습니다…
		ELSE
			PRINTFORMW 핥기 시작했습니다…
		ENDIF
		SELECTCOM = 91
	ENDIF

	CALL SIMULATE_TRAIN_COM, SELECTCOM, RAND:(ABL:욕망 + ABL:봉사정신 + 1)/3 + 1
	;文章に齟齬が出ないように調整
	EXP:정액경험 += 1
	CALL SETFLAG, "정음경험", 1, TARGET, -4

	MEMO_LINECOUNT = LINECOUNT
	;SELECTCOMで펠라展開か파이즈리展開かを判別가능
	CALL SELF_KOJO_L, EVENT_NAMES
	IF LINECOUNT == MEMO_LINECOUNT
		PRINTL 
		PRINTFORML ………
		PRINTFORML ……
		PRINTFORMW …
		PRINTL 
	ENDIF

	PRINTFORM 타고난 테크닉으로 쥐어짜낸 정액을 \@TALENT:작은몸집 ? 꿀꺽꿀꺽 # 꿀꺽 \@ 삼킨 %CALLNAME:TARGET%는, 타액따위로 질퍽질퍽해진 페니스를 
	LOCAL = 0
	IF ABL:노출벽 >= 3 && TALENT:호색 + TALENT:음란 + TALENT:도착적 && COND("제복：스커트") && RAND:3 == 0
		PRINTFORM 갓 벗은 팬티로 닦아내
		SIF TALENT:호색 && ABL:욕망 >= 4 && RAND:3 == 0
			PRINTFORM 고, 손님의 눈앞에서 다시 입
		PRINTFORMW 었습니다. 
		LOCAL = 1
	ELSE
		PRINTFORMW 손수건으로 깨끗하게 닦았습니다. 
	ENDIF
	PRINTFORM 그렇게 대충 일을 매듭지은 %CALLNAME:TARGET%는, 
	IF LOCAL == 0 && TALENT:호색 && ABL:봉사정신 >= 3 && ABL:노출벽 >= 3 && PENIS(TARGET) == 0 && RAND:3 == 0
		IF COND("제복：바니")
			PRINTFORM 봉사하고 있는 동안에 흥분해 뜨겁게 뜸들어 버린 스스로의 다리 사이를 손님에게 1어루만지게 하면,
		ELSEIF ABL:욕망 >= 5 && ABL:정액중독 >= 5 && RAND:2
			PRINTFORM 스커트를 걷어 올리고라고 발정한 다리 사이를 즐거운 듯이 손님에게 과시하면,
		ELSE
			PRINTFORM 손님의 눈앞에서 팬티를 스르리와 벗어\@ ABL:봉사정신 >= 3 ?  "사과" # "기념품" \@으로서 건네준 후 ,
		ENDIF
	ELSE
		PRINTFORM 손님에게 닦는데 시간이 걸린 일을 조금 큰 소리로 사과해,
	ENDIF
	IF ABL:정액중독 >= 5
		PRINTFORM 만족인 발걸음으로
	ELSE
		PRINTFORM 가벼운 발걸음으로
	ENDIF
	PRINTFORMW 일하러 돌아왔습니다…

	CALL SETFLAG, "일반인상대の성행위종료"
	;つまみ食い人数+1
	CFLAG:36 += 1

CASE "トイレでつまみ食い"
	;LOCALS==お손님, LOCALS:1==휴식or청소, LOCALS:2==ＶorＡ

	;여자손님を犯す
	IF COND("다방：인서트") && RANDIF(1, COND("다방：Ｖ섹스"), COND("다방：Ａ섹스") )
		SELECTCASE RAND:10
		CASE 8 TO 9
			LOCALS = 소녀
		CASEELSE
			LOCALS = 여자손님
		ENDSELECT
		SIF SIZE("페니스", TARGET) >= 2
			LOCALS = 여자손님
	ELSE
		LOCALS = 청년
	ENDIF

	LOCALS:1 = \@ RAND:2 ? 휴식 # 청소 \@

	IF LOCALS:1 == "휴식"
		PRINTFORM 휴식시간에 화장실에 들른 
	ELSE
		PRINTFORM 화장실을 청소하러 온 
	ENDIF
	PRINTFORMW %CALLNAME:TARGET%가 안의 칸을 열자, 한 명의 %LOCALS%가 변기에 앉아 \@ ( PENIS(TARGET) || RAND:3 == 0) ? %CALLNAME:TARGET%의 이름을 말하면서 # \@ 자위행위를 하는 장면과 우연히 마주쳤습니다! 
	PRINTFORM %CALLNAME:TARGET%는 순간 멍한 얼굴이 되었지만, 
	IF LOCALS:1 == "휴식" && RAND:2
		PRINTFORM 아직 시간이 있다는 것을 생각해내고, 
	ELSEIF RAND:2 && ABL:정액중독 >= 3 && LOCALS == "청년"
		IF RAND:2
			PRINTFORM 매우 좋아하는%TEXTS("匂い")%에 코를 울리면,
		ELSE
			PRINTFORM 손에 붙든 페니스에 군침을 삼키고, 
		ENDIF
	ELSE
		PRINTFORM 상황을 이해하고, 
	ENDIF
	PRINTFORM 뒤로손을 돌려 문을 잠그
	IF ABL:새드끼 >= MAX(ABL:봉사정신, 3)
		PRINTFORMW 었습니다. 
		PRINTFORMW 아무래도, 이 %LOCALS%로 즐기기로 한 것 같습니다…
	ELSE
		PRINTFORMW 고 %LOCALS%를 유혹하기 시작했습니다…
	ENDIF
	PRINTL 
	PRINTFORM 손님은 부끄러운 장면을 들켜 잠깐 굳어있었지만, 
	IF MIN(ABL:욕망, ABL:노출벽) >= 3 && RAND:2
		IF TALENT:거유 && RAND:3 == 0
			IF COND("제복：바니")
				PRINTFORM 커다란 젖가슴을 과시하면서 
			ELSE
				PRINTFORM 앞가슴의 단추를 풀고 커다란 가슴을 과시하면서 
			ENDIF
		ELSEIF LOCALS != "청년" && PENIS(TARGET)
			PRINTFORM 노출된  
			SELECTCASE SIZE("페니스", TARGET)
			CASE IS >= 3
				PRINTFORM \@ RAND:2 ? 흉복한 # 훌륭한 크기의 \@ 
			CASE 2
				PRINTFORM \@ RAND:2 ? 자랑스러운 # 황홀한 사이즈의  \@ 
			CASE 0
				PRINTFORM \@ RAND:2 ? 귀여운 # 싱싱한 \@ 
			ENDSELECT
			PRINTFORM 페니스를 \@ RAND:2 ? 과시하면서 # 훑으면서 \@ 
		ELSEIF COND("제복：바니")
			IF COND("제복：파스너")
				PRINTFORM 가랑이 부분의 지퍼를 내리면서 
			ELSE
				PRINTFORM 바니 슈트의 가랑이 부분을 고정하는 단추를 풀면서 
			ENDIF
		ELSEIF RAND:2 || COND("身嗜み：노팬티")
			PRINTFORM 제복을 풀면서 
		ELSE
			PRINTFORM 팬티를 벗으면서 
		ENDIF
	;호색
	ELSEIF TALENT:호색 && RANDIF(2, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마)
		IF LOCALS == "청년"
			PRINTFORM 부푼 다리사이
		ELSE
			PRINTFORM 자위때문에 젖어버린 가랑이
		ENDIF
		IF RAND:2
			PRINTFORM 를 쓰다듬으면서 
		ELSE
			PRINTFORM 에 손을 문지르면서 
		ENDIF
	;양기
	ELSEIF TALENT:양기 && RANDIF(2, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 흥겨운 미소를 지으면서 
	;소악마
	ELSEIF TALENT:소악마 && RANDIF(2, TALENT:부끄럼쟁이)
		PRINTFORM 히죽히죽 웃으면서 
	;부끄럼쟁이
	ELSEIF TALENT:부끄럼쟁이 && RAND:2
		PRINTFORM 얼굴을 붉히면서 
	;강기＆건방짐な人はここまで
	ELSEIF TALENT:교만 && TALENT:건방짐
		PRINTFORM 겁없는 미소를 지으면서 
	ELSEIF RAND:2
		PRINTFORM 얼굴을 상기시키면서 
	ELSE
		PRINTFORM 미소지으면서 
	ENDIF
	IF TALENT:교만 && RANDIF(2, TALENT:양기, TALENT:호색)
		PRINTFORM 자신있게 
	;호색
	ELSEIF TALENT:호색 && RANDIF(2, TALENT:양기)
		PRINTFORM 요염하게 
	;양기
	ELSEIF TALENT:양기 && RAND:2
		PRINTFORM 가벼운 모습으로 
	ENDIF
	IF TALENT:호색 && RAND:2
		IF LOCALS == "청년"
			PRINTFORM 올라탄 %CALLNAME:TARGET%
		ELSE
			PRINTFORM 다리사이에 몸을 끼어넣은 %CALLNAME:TARGET%
		ENDIF
	ELSE
		PRINTFORM 안겨온 %CALLNAME:TARGET%
	ENDIF
	IF LOCALS == "청년"
		IF RAND:3 == 0
			PRINTFORM 의 부드러움
		ELSEIF RAND:2
			PRINTFORM 의 발정한 냄새
		ENDIF
		PRINTFORMW 에, 순식간에 엄청난 각도로 발기했습니다……!! 
	ELSE
		PRINTFORMW 에, 몸을  허락할 기분이 되버렸습니다…!! 
	ENDIF

	CALL SETFLAG, "일반인상대の성행위初期설정"

	IF LOCALS == "청년"
		;애널好き？
		IF COND("다방：Ａ섹스") && (TALENT:처녀 || TALENT:남자 || RAND:2)
			SELECTCOM = 75
			;文章に齟齬が出ないように調整
			EXP:정액경험 += 1
			CALL SETFLAG, "장내정액경험", 1, TARGET, -4
		ELSE
			SELECTCOM = 65
			EXP:정액경험 += 1
			CALL SETFLAG, "질내정액경험", 1, TARGET, -4
			IF TALENT:PLAYER:동정
				EXP:동정탈취경험 += 1
				EXP:경험치 += 50
			ENDIF
		ENDIF

		MEMO_LINECOUNT = LINECOUNT
		;SELECTCOMでＶＡ기승위を判別가능
		CALL SELF_KOJO_L, EVENT_NAMES
		IF LINECOUNT == MEMO_LINECOUNT
			PRINTL 
			PRINTFORML ………
			PRINTFORML ……
			PRINTFORMW …
			PRINTL 
		ENDIF

		PRINTFORM %CALLNAME:TARGET%는 
		IF RAND:10 == 0
			IF RAND:2
				PRINTFORM 나이에 비해 큰 크것에 
			ELSE
				PRINTFORM 청년의 씩씩한 육봉에 
			ENDIF
		ELSEIF TALENT:PLAYER:동정
			PRINTFORM 청년이 동정이란 사실에 
		ELSEIF RAND:2
			PRINTFORM 자신에게 열중한 청년의 태도에 
		ELSE
			PRINTFORM 젊은 남자 특유의 딱딱한 물체에 
		ENDIF
		PRINTFORM 기분이 좋았는지, 
		IF ABL:새드끼 >= MAX(ABL:봉사정신, 3) && RAND:3 == 0
			IF ABL:허리놀림 >= 3 && RAND:2
				PRINTFORM 맹렬한 허리놀림으로 
			ELSE
				PRINTFORM 초심자용의 언어고문을 하면서 
			ENDIF
		ELSEIF TALENT:작은몸집 && TALENT:유치 && !TALENT:거유 && ABL:Ｖ확장 >= 2 && RAND:3 == 0
			PRINTFORM 어린 신체로 확실히 뿌리까지 삼키면서 
		ELSEIF TALENT:거유 && RANDIF(2, ABL:기교 >= 3, ABL:Ｍ감각 >= 3, TALENT:양기 + TALENT:부끄럼쟁이, TALENT:호색)
			IF TALENT:작은몸집 == 0 && RAND:2
				PRINTFORM 풍만한 몸으로 꼭 껴안으면서
			ELSE
				PRINTFORM 큰 가슴하거나 하면서
				CALL SIMULATE_TRAIN_COM, 104, RAND:(ABL:욕망 + ABL:봉사정신 + 1)/3 + 1
			ENDIF
		ELSEIF TALENT:호색 && RANDIF(2, ABL:기교 >= 3, ABL:Ｍ감각 >= 3, TALENT:양기 + TALENT:부끄럼쟁이)
			PRINTFORM 살짝 느끼고있는 얼굴로 응원하면서 
		ELSEIF TALENT:양기 + TALENT:부끄럼쟁이 && RANDIF(2, ABL:기교 >= 3, ABL:Ｍ감각 >= 3)
			PRINTFORM 사랑스럽게 졸라대면서 
		ELSEIF ABL:Ｍ감각 >= 3 && RANDIF(2, ABL:기교 >= 3)
			IF COND("身嗜み：키스독점") || RAND:3 == 0
				PRINTFORM 얼굴이나 몸에키스를 하면서
			ELSE
				PRINTFORM 몇번이나키스를 하면서
				CALL SIMULATE_TRAIN_COM, 6, RAND:(ABL:욕망 + 1)/2 + 2
			ENDIF
		ELSEIF ABL:기교 >= 3 && RAND:2
			PRINTFORM 이것저것 가르쳐주면서 
		ELSE
			PRINTFORM 껴안으면서 
		ENDIF

		IF ABL:정액중독 >= 5 || TALENT:상위몽마 || ABL:새드끼 >= MAX(ABL:봉사정신, 3)
			LOCAL += RAND:5 + 5
			PRINTFORMW \@ SELECTCOM == 75 ? 엉덩이구멍으로 # \@ 한계 이상의 정액을 짜냈다. 
		ELSE
			LOCAL += RAND:5 + 2
			PRINTFORMW 원하는만큼 마음껏 {LOCAL}번 정도 즐겼다. 
		ENDIF

		CALL SIMULATE_TRAIN_COM, SELECTCOM, LOCAL

		IF ABL:새드끼 >= MAX(ABL:봉사정신, 3) && RAND:3
			PRINTFORM 어느정도 만족한 %CALLNAME:TARGET%는, 정액을 흘리는 \@ SELECTCOM == 75 ? 애널 # 그곳 \@을 녹초가 된 
			IF ABL:새드끼 >= 5 && RAND:3
				PRINTFORM 청년에게 안쪽까지 깨끗하게 빨게 한 
				CALL SIMULATE_TRAIN_COM, 1, 3
			ELSEIF ABL:새드끼 >= 3 && RAND:3
				PRINTFORM 청년에게 깨끗하게 핥게 한 
				CALL SIMULATE_TRAIN_COM, 1, 1
			ELSE
				PRINTFORM 청년에게 깨끗하게 닦게 한 
			ENDIF
			PRINTFORMW 후에, 팔랑팔랑 손을 흔들면서 \@ LOCALS:1 == "휴식" ? 그 장소를 떠났다 # 아르바이트로 돌아왔다 \@……
		ELSE
			PRINTFORM 그 노력에 부응해서, %CALLNAME:TARGET%는 
			IF EQUIP:팬티 && RAND:5 == 0 && (COND("제복：스커트") || COND("제복：파스너") == 0)
				IF COND("제복：바니")
					PRINTFORM 땀에 젖은 하이레그 팬티를 선물로 준 
				ELSE
					PRINTFORM 자신의 팬티를 선물로 준 
				ENDIF
				CALL SET_PANTIES, TARGET
			ELSEIF RAND:4 == 0 && COND("제복：스커트") && COND("身嗜み：노팬티") == 0
				PRINTFORM 온기가 남아있는 팬티로 깨끗하게 닦아준 
			ELSEIF RAND:3 == 0 && ABL:봉사정신 >= 3
				PRINTFORM 눈을 치켜 떠 올려다보며 청소펠라를 한 
				EXP:청소펠라경험 += 1
				CALL SIMULATE_TRAIN_COM, 91, 1
			ELSEIF RAND:3 == 0
				PRINTFORM 이마에 키스를 한 
			ELSEIF RAND:2
				PRINTFORM 웃는 얼굴로 칭찬한 
			ELSE
				PRINTFORM 가슴을 에 끌어안고 머리를 쓰다듬어준 
			ENDIF
			PRINTFORM 후에 
			IF RAND:2 && ABL:정액중독 >= 5
				IF EQUIP:팬티 && RAND:2
					PRINTFORM 정액이 흐르는 \@ SELECTCOM == 75 ? 애널 # 그곳 \@을 그대로  둔 채 
					IF COND("제복：바니")
						PRINTFORM 제복을 고쳐입고,
					ELSE
						PRINTFORM 속옷을 입고,
					ENDIF
				ELSE
					PRINTFORM \@ SELECTCOM == 75 ? 애널 # 그곳 \@의 정액을 손가락으로 핥고 빨면서 
				ENDIF
			ELSE
				PRINTFORM 손을 흔들고 
			ENDIF
			PRINTFORMW \@ LOCALS:1 == "휴식" ? 그 장소를 떠났다 # 점내로 돌아왔다 \@……
		ENDIF
	ELSE
		CALL SETFLAG, "일반인상대の성행위설정：상대が女"

		;애널好き？
		IF RAND:10 == 0 && TALENT:도착적
			SELECTCOM = 126
		ELSE
			SELECTCOM = 121
		ENDIF
		EXP:사정경험 += 1

		MEMO_LINECOUNT = LINECOUNT
		;SELECTCOMでＶＡ섹스を判別가능
		CALL SELF_KOJO_L, EVENT_NAMES
		IF LINECOUNT == MEMO_LINECOUNT
			PRINTL 
			PRINTFORML ………
			PRINTFORML ……
			PRINTFORMW …
			PRINTL 
		ENDIF

		PRINTFORM %CALLNAME:TARGET%는 

		IF SELECTCOM == 126
			PRINTFORM 애널섹스를 요구해오는 %LOCALS%의 음란함에 
		ELSEIF (LOCALS == "소녀" && RAND:3 == 0) || RAND:20 == 0
			PRINTFORM %LOCALS%가 처녀라는 사실에 
		ELSEIF RAND:2
			PRINTFORM 자신에게 열중한 %LOCALS%의 모습에 
		ELSE
			PRINTFORM %LOCALS%의 질의 상태에 
		ENDIF
		PRINTFORM 기분이 좋았는지, 
		IF ABL:새드끼 >= MAX(ABL:봉사정신, 3) && RAND:3 == 0
			IF ABL:허리놀림 >= 3 && RAND:2
				PRINTFORM 맹렬한 허리놀림으로 
			ELSE
				PRINTFORM 초심자용의 언어고문을 하면서 
			ENDIF
		ELSEIF SIZE("페니스", TARGET) >= 2 && RAND:3 == 0
			PRINTFORM 겉보기에 어울리지 않는 크기의 페니스로 안쪽까지 정성스럽게 풀어주면서 
		ELSEIF TALENT:호색 && RANDIF(2, ABL:기교 >= 3, ABL:Ｍ감각 >= 3, TALENT:양기 + TALENT:부끄럼쟁이)
			PRINTFORM 살짝 가버린 얼굴로 계속 칭찬하면서 
		ELSEIF TALENT:양기 + TALENT:부끄럼쟁이 && RANDIF(2, ABL:기교 >= 3, ABL:Ｍ감각 >= 3)
			PRINTFORM 사랑스럽게 졸라대면서 
		ELSEIF ABL:Ｍ감각 >= 3 && RANDIF(2, ABL:기교 >= 3)
			IF COND("身嗜み：키스독점") || RAND:3 == 0
				PRINTFORM 얼굴이나 몸에키스를 하면서
			ELSE
				PRINTFORM 몇번이나키스를 하면서
				CALL SIMULATE_TRAIN_COM, 6, RAND:(ABL:욕망 + 1)/2 + 2
			ENDIF
		ELSEIF ABL:기교 >= 3 && RAND:2
			PRINTFORM 이것저것 가르쳐주면서 
		ELSE
			PRINTFORM 껴안으면서 
		ENDIF
		IF ABL:새드끼 >= MAX(ABL:봉사정신, 3)
			LOCAL = RAND:5 + 5
			PRINTFORMW 너무 강한 쾌락에 실신할 정도로 범했다. 
		ELSE
			LOCAL = RAND:5 + 2
			PRINTFORMW 원하는만큼 마음껏 {LOCAL}번 정도 즐겼다. 
		ENDIF

		CALL SIMULATE_TRAIN_COM, SELECTCOM, LOCAL

		IF ABL:새드끼 >= MAX(ABL:봉사정신, 3) && RAND:3
			PRINTFORM 잔뜩 사정해 만족한 %CALLNAME:TARGET%는, \@ SELECTCOM == 126 ? 엉덩이구멍에서 뽑아낸 # 서로의 점액으로 질척질척한 \@ 페니스를 완전히 쾌락에 녹아버린 %LOCALS%
			IF ABL:새드끼 >= 5 && RAND:3
				PRINTFORM 에게 정성껏 청소펠라해서 깨끗하게 만들도록 시킨 
				CALL SIMULATE_TRAIN_COM, 4, 3
			ELSEIF ABL:새드끼 >= 3 && RAND:3
				PRINTFORM 에게 깨끗하게 핥게 한 
				CALL SIMULATE_TRAIN_COM, 4, 3
			ELSE
				PRINTFORM 에게 \@ RAND:2 ? 팬티로 # \@ 깨끗하게 닦게 한 
			ENDIF
			PRINTFORMW 후에, 팔랑팔랑 손을 흔들면서 \@ LOCALS:1 == "휴식" ? 그 장소를 떠났다 # 아르바이트로 돌아왔다 \@……
		ELSE
			PRINTFORM 그 노력에 부응해서, %CALLNAME:TARGET%는 
			IF RAND:3 == 0
				PRINTFORM 이마에 키스한 
			ELSEIF RAND:2
				PRINTFORM 웃는 얼굴로 칭찬한 
			ELSE
				PRINTFORM 가슴을 에 끌어안고 머리를 쓰다듬어준 
			ENDIF
			PRINTFORM 후에 %LOCALS%
			IF RAND:2 && ABL:정액중독 >= 5
				IF RAND:2
					PRINTFORM 의 \@ SELECTCOM == 126 ? 애널 # 그곳 \@을 핥고 빨아서 깨끗하게 하고, 
					CALL SIMULATE_TRAIN_COM, 98, 1
				ELSE
					PRINTFORM 의 정액이 흐르는 \@ SELECTCOM == 126 ? 애널 # 그곳 \@을 세심하게 문질러닦고, 
				ENDIF
			ELSE
				PRINTFORM 에게 손을 흔들고 
			ENDIF
			PRINTFORMW \@ LOCALS:1 == "휴식" ? 그 장소를 떠났다 # 아르바이트로 돌아왔다 \@……
		ENDIF
	ENDIF

	CALL SETFLAG, "일반인상대の성행위종료"
	;つまみ食い人数+1
	CFLAG:36 += 1

CASE "テーブル拭き２"
	PRINTFORM 점내의 혼잡이 일단락되어서, %CALLNAME:TARGET%는 
	IF RAND:2
		PRINTFORMW 식기가 정돈된 테이블을 청소하며 돌기시작했습니다. 
	ELSE
		PRINTFORMW 걸레질을 시작했습니다. 
	ENDIF
	IF TALENT:작은몸집 && (TALENT:유치 || RAND:2)
		IF RAND:2
			IF TALENT:유치 && RAND:2
				PRINTFORM %CALLNAME:TARGET%가 테이블에 엎드려, 발을 동동거리면서 행주로 
			ELSE
				PRINTFORM %CALLNAME:TARGET%같이 작은 아이가 기다란 마포걸레로 
			ENDIF
			PRINTFORMW 쓱쓱 닦는 모습은, 마치 소꿉놀이같아 주위의 손님을 편안하게합니다…
			IF TALENT:장사
				PRINTFORMW …만. 
				PRINTFORMW 다인용의 무거워보이는 테이블을 한손으로 가볍게 들어올리고 한손으로 걸레질을 하기 시작하는 모습은, 항상 있는 일이지만 처음보는 손님을 깜짝 놀라게 하는 것 같습니다…
			ENDIF
		ELSEIF RAND:2
			PRINTFORMW …만, 팔길이가 부족한건지, 테이블 위에 엎드려 상의 안쪽을 닦고 있습니다. 
			PRINTFORMW 주위의 손님들은, 발을 동동거리면서 걸레질을 하고 있는 %타겟을()% 멀리서 귀여워하면서, 차를 즐기고 있는 것 같습니다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는 안 쪽에 손이 닿지 않아서, 자리에 앉아 테이블을 정성들여 닦고 있습니다. 
			PRINTFORMW …만, 혼잡을 처리한 후의 단순작업의 반복때문인지, 마지막 테이블을 닦던 도중에 푹 엎드려 자버렸습니다…
		ENDIF
	ELSEIF TALENT:장사 && RAND:2
		PRINTFORMW 그정도로 근육질로는 보이지않는 %CALLNAME:TARGET% 이지만, 무거워보이는 테이블 등을 휙휙 한손으로 움직이면서 콧노래를 하면서 걸레질을 하고있습니다…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 몸을 앞으로 기울이고 템포 좋게 테이블을 안쪽까지 깨끗하게 닦고있었습니다만…
		IF COND("제복：미니스커트")
			LOCALS = 스커트가 말려올라가, 
			IF COND("身嗜み：노팬티")
				LOCALS = 팬티を穿いて無いせいで丸出しの보지
			ELSEIF COND("오토코노코") || (PENIS(TARGET) && RAND:2)
				LOCALS = %LOCALS% 달아오른 가랑이 부분
			ELSEIF RAND:3 == 0 && CHECK_PANTIES("하이레그")
				LOCALS = %LOCALS% 음란한 팬티가 먹혀들어 \@ COND("음모") >= 6 ? 음모가 비어져있는 # 당장이라도 그곳이 보일듯한 \@ 가랑이의 모습
			ELSEIF RAND:2
				LOCALS = %LOCALS% \@ CHECK_PANTIES("Ｈ") ? Ｈ한 # \@ 팬티가 먹혀든 \@ RAND:2 ? 엉덩이 # 가랑이 \@
			ELSE
				LOCALS = %LOCALS% 땀을 흘려 살짝 젖은 팬티의 가랑이 부분
			ENDIF
		ELSEIF COND("제복：바니")
			IF RAND:2
				LOCALS = 망사타이즈로 감싸인 엉덩이
			ELSE
				LOCALS = Ｔ백이 먹혀들어간 엉덩이
			ENDIF
		ELSE
			LOCALS = 스커트너머로 엉덩이 
		ENDIF
		;ひそひそ話に気付く？
		IF RAND:2
			PRINTFORMW 뒷자리 손님이 소곤거리는 대화의 내용으로, %LOCALS%를 보이고있었다는 사실을 깨달은 것 같습니다! 
			PRINTFORM 그 사실을 깨달은 %CALLNAME:TARGET%는
			IF TALENT:호색 && ABL:노출벽 >= 3
				IF ABL:노출벽 >= 5 && COND("제복：스커트") && CHECK_PANTIES("Ｈ") && RAND:2
					PRINTFORMW , …뒤로 손을 뻗어 스커트를 누르는 척하면서, Ｔ백을 손가락으로 밀어 한순간만 엉덩이 구멍을 보여준 것 같습니다! 
				ELSE
					PRINTFORM , …몸을 테이블에 수평이 될 때까지 숙여 다리를 크게 벌리고, 
					IF COND("제복：바니")
						PRINTFORMW 뜨겁고 축축한 다리사이를 손님에게 과시하면서 즐기기시작했습니다! 
					ELSEIF COND("身嗜み：노팬티") && COND("제복：스커트")
						PRINTFORMW 좌우에 대음순이 끌려가 안쪽의 보일 것 같은보지를오손님에 과시하고 즐기기 시작했습니다! 
					ELSE
						PRINTFORMW 팬티 너머의 다리사이를 손님에게 과시하면서 즐기기시작했습니다! 
					ENDIF
				ENDIF
				PRINTFORMW 그리고, 걸레질을 마친 %CALLNAME:TARGET%는 뒷자리의 손님의 커다랗게 부푼 사타구니를 슬쩍보고, 즐거운 듯한 미소를 지으며 다음 테이블로 향했습니다…
			ELSEIF TALENT:냉정 || TALENT:감정결핍 || (TALENT:부끄럼없음 && TALENT:조야)
				PRINTFORMW , …딱히 안색을 바꾸는 일 없이, 손님에게 보여지면서 잠자코 계속했습니다…
			ELSE
				PRINTFORMW 당황해서 뒤로 손을 돌리고 \@ COND("제복：바니") ?  엉덩이를 가렸 # 스커트를 눌렀 \@지만, 그 후부터는 그다지 청소에 힘이 들어가지 않았던 것 같습니다…
			ENDIF
		ELSE
			PRINTFORMW 열중한 나머지, 뒷자리 손님에게 %LOCALS%를 차분히 감상하게 해버린 것 같습니다. 
			PRINTFORMW 그것을 눈치채지 못한 %CALLNAME:TARGET%는, 어딘가 행복한 표정을 짓는 손님에게 이상하다는 듯한 표정을 지으며 다음 테이블로 향했습니다…
		ENDIF
	ENDIF
CASE "チップ"
	IF ABL:봉사정신 >= 5 && RANDIF(2, TALENT:매력, TALENT:청초, TALENT:호색 + TALENT:음란)
		PRINTFORM 세세한 곳까지 세심한 서비스가 
	ELSEIF TALENT:호색 + TALENT:음란 && RANDIF(2, TALENT:매력, TALENT:청초)
		PRINTFORM 조금 야한 행동이 
	ELSEIF TALENT:청초 && RANDIF(2, TALENT:매력)
		PRINTFORM 정숙한 행동이 
	ELSEIF TALENT:매력 && RAND:2 == 0
		PRINTFORM 남다른 아름다움이 
	ELSEIF RAND:2
		PRINTFORM 긴장된 모습이 
	ELSE
		PRINTFORM 말투가 
	ENDIF
	PRINTFORMW 마음에 들었는 지, %CALLNAME:TARGET%는 담당한 손님에게서 상당히 넉넉한 팁을 받은 것 같습니다. 
	IF TALENT:호색 && RANDIF(1, TALENT:유치, TALENT:하피)
		IF COND("제복：바니") || (TALENT:거유 && RAND:2) || COND("身嗜み：노팬티")
			LOCALS = 가슴
		ELSE
			LOCALS = 팬티
		ENDIF
		PRINTFORM 그러자, 팁을 준 손님에게 %CALLNAME:TARGET%는 
		SELECTCASE LOCALS
		CASE "가슴"
			IF COND("제복：바니")
				PRINTFORM 앞으로 몸을 굽히고, 
			ELSE
				PRINTFORM 가슴을 팍을 풀고, 
			ENDIF
		CASEELSE
			PRINTFORM 스커트를 배꼽이 보이도록 걷어올리고, 
		ENDSELECT
		IF TALENT:솔직 && RANDIF(2, TALENT:부끄럼쟁이, TALENT:소악마)
			PRINTFORM 빙긋 웃으면서 
		ELSEIF TALENT:소악마 && RANDIF(2, TALENT:부끄럼쟁이)
			PRINTFORM 히죽히죽 웃으면서 
		ELSEIF TALENT:부끄럼쟁이 && RAND:2
			IF RAND:2
				PRINTFORM 살짝 뺨을 붉히고 
			ELSE
				PRINTFORM 살짝 부끄러운 듯이 
			ENDIF
		ELSE
			PRINTFORM 미소지으면서 
		ENDIF
		SELECTCASE LOCALS
		CASE "가슴"
			PRINTFORMW \@ RAND:2 ? 부드러운 # 매력적인 \@ 가슴계곡을 내밀었습니다! 
			IF RAND:3 == 0
				PRINTFORMW %CALLNAME:TARGET%는 돈을 찔러넣기만하고 떠나려던 손님의 손을 위에서 누르고, 잠깐만 가슴을 만지게해준 것 같습니다…
			ELSEIF RAND:2
				PRINTFORMW %CALLNAME:TARGET%는 가슴을 반죽하듯 주물러 손님의 눈을 즐겁게 하면서, 틈새에 팁을 넣어달라고 재촉했습니다…
			ELSE
				PRINTFORMW 가슴에 닿지 않도록 조심스럽게 꽂아넣은 손님에게, %CALLNAME:TARGET%는 안쪽까지 깊이 넣어달라고 속삭였습니다…
			ENDIF
		CASEELSE
			PRINTFORMW 팬티 안에 넣어달라고 재촉했습니다! 
			IF RAND:2
				PRINTFORMW %CALLNAME:TARGET%는 팁을 꽂아넣고 그냥 떠나려던 손님의 손을 위에서 누르고, 잠깐동안 가랑이를 만지게 해준 것 같습니다…
			ELSE
				PRINTFORMW 민감한 장소에 닿지 않도록 신중하게 꽂아넣는 손님에게, %CALLNAME:TARGET%는 안쪽까지 깊이 넣어달라고 속삭였습니다…
			ENDIF
		ENDSELECT
		PRINTL 
		PRINTFORMW ……그 뒤는 신기하게도 고액의 팁을 %CALLNAME:TARGET%에게 내미는 손님이 끊이지 않았습니다. 
	ELSEIF TALENT:하피 && RANDIF(1, TALENT:유치)
		PRINTFORMW 설마하던 고액 동전의 등장에 %CALLNAME:TARGET%는 눈동자를 빛내고,  초조한 마음을 억누르면서 팁을 품에 넣었습니다. 
		PRINTFORMW 본능적인 욕구가 충족 되었는지, 금품을 받은 기쁨에 잠겨 있는 것 같습니다…
	ELSEIF TALENT:유치
		PRINTFORML %CALLNAME:TARGET%는 눈을 빛낼내며, 온몸으로 기쁨을 표현했습니다. 
		PRINTFORMW 머리속은 \@ RAND:2 ?  과자로 # 무엇을 살까하는 생각으로 \@ 가득한 것같습니다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%는 
		IF TALENT:냉정 + TALENT:감정결핍
			PRINTFORMW 표정으로 드러내지는 않았지만, 기분탓인인지 기뻐보입니다. 
		ELSE
			PRINTFORMW 뜻밖의 추가 소득에  얼굴을 펴기시작했습니다. 
		ENDIF
		PRINTFORMW 그 광경이 방아쇠가 되었는지, 그날 가게의 서비스는 최상이었던 것같습니다… 
	ENDIF
CASE "財布忘れた상대をつまみ食い"
	PRINTFORM %CALLNAME:TARGET%가 일단락 된 점내
	IF RAND:2
		PRINTFORM 에서 접시를 치우는 일을 
	ELSE
		PRINTFORM 를 청소
	ENDIF
	PRINTFORMW 하고있자, 불안한 모습으로 주머니를 뒤적뒤적거리고 있는 손님을 발견했습니다. 
	PRINTFORMW …아무래도 식사를 마친 뒤 지갑을 깜빡한 것을 깨달아버린 것 같습니다. 
	PRINTFORM 그것을 들은 %CALLNAME:TARGET%는 
	;솔직
	IF TALENT:솔직 && RANDIF(2, TALENT:감정결핍, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 빙긋 미소를 짓고, 
	;양기
	ELSEIF TALENT:양기 && RANDIF(2, TALENT:냉정 + TALENT:감정결핍, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORM 즐거운듯이 미소짓고, 
	;소악마
	ELSEIF TALENT:소악마 && RANDIF(2, TALENT:냉정 + TALENT:감정결핍, TALENT:부끄럼쟁이)
		PRINTFORM 씨익 웃고, 
	;감정결핍or냉정
	ELSEIF TALENT:냉정 + TALENT:감정결핍 && RANDIF(2, TALENT:부끄럼쟁이)
		PRINTFORM 살짝 고민하고, 
	;부끄럼쟁이
	ELSEIF TALENT:부끄럼쟁이 && RAND:2
		PRINTFORM 살짝 뺨을 붉게 물들이고, 
	ELSE
		PRINTFORM 미소 짓고, 
	ENDIF
	PRINTFORMW 청년의 귓가에 「몸으로 받겠다」고 말하고, "일"에 대한 설명과 질문을 하면서 함께 스텝룸으로 갔습니다…
	PRINTL 
	PRINTFORMW ～탈의실～

	VARSET LOCAL
	CALL SETFLAG, "일반인상대の성행위初期설정"

	IF RAND:2
		PRINTFORM 일단 
	ELSE
		PRINTFORM 첫시작으로 
	ENDIF
	CALL COM_ABLE99
	IF RESULT && ABL:봉사정신 >= 3 && RAND:3 == 0
		PRINTFORM 식스나인 자세로 
		IF ABL:새드끼 >= 3 && RAND:3 == 0
			PRINTFORM 얼굴을 짓누르면서 
		ELSEIF RAND:2
			PRINTFORM 다리 사이를 탐하면서 
		ELSE
			PRINTFORM 보지를 마음대로 하도록 하면서 
		ENDIF
		CALL SIMULATE_TRAIN_COM, 99, RAND:3 + 2
	ELSE
		CALL COM_ABLE92
		LOCAL:92 = RESULT
		CALL COM_ABLE95
		LOCAL:95 = RESULT
		;풋잡
		IF LOCAL:95 && ABL:발놀림 >= 3 && RAND:4 == 0
			LOCAL = ABL:발놀림
			LOCALS = 풋잡
			CALL SIMULATE_TRAIN_COM, 95, RAND:3 + 2
		;파이즈리
		ELSEIF LOCAL:92 && (ABL:마유 >= 3 || TALENT:거유) && RAND:3 == 0
			LOCAL = ABL:마유
			LOCALS = 파이즈리
			CALL SIMULATE_TRAIN_COM, 92, RAND:3 + 2
		ELSEIF RAND:2
			LOCAL = ABL:혀놀림
			IF ABL:봉사정신 >= 3 && CHECK("喉姦") && RAND:3 == 0
				LOCALS = 딥펠라
			ELSE
				LOCALS = 펠라
			ENDIF
			CALL SIMULATE_TRAIN_COM, 91, RAND:3 + 2
		ELSE
			LOCAL = ABL:손가락놀림
			LOCALS = 손훑기
			CALL SIMULATE_TRAIN_COM, 90, RAND:3 + 2
		ENDIF
		IF RAND:2 && LOCAL >= MAX(3, ABL:혀놀림, ABL:손가락놀림, ABL:발놀림, ABL:마유)
			PRINTFORM 비장의 
		ELSEIF LOCAL >= 3
			PRINTFORM 능수능란한 
		ENDIF
		PRINTFORM %LOCALS%로 
	ENDIF
	PRINTFORM 몇번이고 뽑 \@ ABL:봉사정신 >= 3 ? 아준 # 아낸\@ %CALLNAME:TARGET%는, 
	IF COND("제복：바니")
		PRINTFORM 가랑이 부분만 노출시킨 상태로 
	ELSEIF ABL:노출벽 >= 3 && RAND:2
		PRINTFORM 스커트를 걷어올려 하복부를 과시하면서 
	ELSEIF RAND:2 || COND("身嗜み：노팬티")
		PRINTFORM 제복차림인 채로 
	ELSEIF RAND:3 == 0
		PRINTFORM 스커트만 벗은 상태로 
	ELSEIF RAND:2
		PRINTFORM 스커트와 팬티만 벗은 상태로 
	ELSE
		PRINTFORM 팬티를 무릎에 걸친 상태로 
	ENDIF
	PRINTFORM 청년의 허리 위에 올라타 
	IF TALENT:거유 && RAND:2
		;폭유
		IF TALENT:거유 >= 2 && RAND:3 == 0
			PRINTFORM 중량감이 느껴지는 가슴
		ELSEIF RAND:3 == 0
			PRINTFORM 풍만한 가슴
		ELSEIF RAND:2
			PRINTFORM 찹쌀떡같은 가슴
		ELSE
			PRINTFORM 탄력넘치는 가슴
		ENDIF
		IF RAND:3 == 0
			PRINTFORM 을 흔들면서, 
		ELSEIF RAND:2
			PRINTFORM 을 퉁기면서, 
		ELSE
			PRINTFORM 로 얼굴을 압박하면서, 
		ENDIF
	ENDIF
	PRINTFORM 몇번이고 몇번이고, 
	LOCAL = 4 + RAND:5
	IF ABL:새드끼 >= MAX(ABL:봉사정신, 3)
		LOCAL:1 = 2 + RAND:5
		LOCAL += LOCAL:1
		PRINTFORM 정확히 말하면 『더 이상 안나와요』라는 말을 들은 후에 {LOCAL:1}회 정도 
	ELSE
		PRINTFORM 원하는 만큼 
	ENDIF
	;동정？
	SIF TALENT:PLAYER:동정
		LOCALS:1 = 동정
	IF TALENT:처녀 || (RAND:2 && ABL:Ａ감각 > MAX(2, ABL:Ｖ감각) && TALENT:심술쟁이 + TALENT:정조관념 + TALENT:애널광 + TALENT:도착적)
		PRINTFORM 엉덩이구멍으로 
		CALL SIMULATE_TRAIN_COM, 75, LOCAL
	ELSE
		IF ABL:Ａ감각 >= MAX(3, ABL:Ｖ감각)
			PRINTFORM 양쪽의 구멍으로 
			LOCAL:1 = LOCAL/2
			LOCAL -= LOCAL:1
			CALL SIMULATE_TRAIN_COM, 75, LOCAL:1
		ENDIF
		CALL SIMULATE_TRAIN_COM, 65, LOCAL
		EXP:정액경험 += LOCAL:1
		;동정狩り完了! 
		IF LOCALS:1 == "동정"
			EXP:동정탈취경험 += 1
			EXP:경험치 += 50
		ENDIF
	ENDIF
	IF ABL:새드끼 >= MAX(ABL:봉사정신, 3)
		PRINTFORMW 정액을 짜 취했습니다…
	ELSE
		PRINTFORMW 사정을 받아들였습니다…
	ENDIF
	CALL SETFLAG, "일반인상대の성행위종료"

	PRINTFORM %CALLNAME:TARGET%는 
	;상대は동정
	IF LOCALS:1 == "동정"
		IF RAND:3
			PRINTFORM 청년이 동정이었다는 사실에 
		ELSEIF EXP_GAIN(14)
			PRINTFORM 예상치 못한 동정격추 횟수를 늘리고 
		ELSE
			PRINTFORM 동정상대에게 Ａ섹스를 경험시키는 특별한 플레이를 즐기고 
		ENDIF
	ELSEIF RAND:3
		PRINTFORM 젊은 정액을 충분히 섭취하고 
	ELSE
		PRINTFORM 기분좋은 땀을 흘리고 
	ENDIF
	PRINTFORML 위기분모습으로 음식비를 입체해 주면,

	MEMO_LINECOUNT = LINECOUNT
	CALL SELF_KOJO_L, EVENT_NAMES
	SIF LINECOUNT == MEMO_LINECOUNT
		PRINTFORML 「다음은 지갑 잊지 않도록」

	PRINTFORMW (와)과 못을 별로로부터 의기양양과 일하러 돌아와 갔습니다…
	;つまみ食い人数+1
	CFLAG:36 += 1

CASE "暇なときの시간潰し"
	PRINTFORMW 오늘은 날씨가 조금 안좋은 탓인지, 점내는 손님의 모습이 드문드문합니다
	PRINTFORM 짬을 주체 못한 %CALLNAME:TARGET%는
	IF ABL:정액중독 >= 3 && (TALENT:호색 || ABL:새드끼 >= 3) && FIND_COND("출연가능な점원", "!여자") && RANDIF(2, ABL:봉사정신 >= 3, ABL:자위중독 + TALENT:음란 >= 4)
		WAITRESS = FIND_COND("출연가능な점원", "!여자")
		SETBIT CFLAG:WAITRESS:95, 1
		IF RAND:2 && (TALENT:교만 || TALENT:악취민감)
			PRINTFORMW , 직원실의 %CALLNAME:WAITRESS%에게 밀크티를 끓여달라고 한 것 같습니다.
			PRINTFORMW ……
			PRINTFORM …%CALLNAME:TARGET%는 준비 해준 %CALLNAME:WAITRESS%에게 고맙다는 말을 하며, 
			IF BASE:WAITRESS:정력 > BASE:MASTER:정력
				PRINTFORMW 감칠맛과 목넘김의 훌륭함에 감탄의 한숨을 쉬면서 티타임을 즐겼습니다…
			ELSEIF AGE(WAITRESS) <= 2
				PRINTFORMW 신선한 맛을 만끽하면서 티타임을 즐겼습니다…
			ELSE
				PRINTFORMW 새삼 %CALLNAME:MASTER%의 맛이 제일이라고 재확인하면서 티타임을 보냈습니다…
			ENDIF
			CALL SETFLAG, "정음경험", 1, TARGET, -4
			PRINTFORML 정음경험＋1
			EXP:정액경험 += 2
			PRINTFORMW 정액경험＋2
		ELSE
			PRINTFORM 계산대의 사각 지대에 은신하며, 결제를 하고 있던 %CALLNAME:WAITRESS%의\@ SIZE("페니스", WAITRESS) >= 2 ? 大きな # \@페니스
			IF CHECK("喉姦", TARGET, WAITRESS) >= 2 && RAND:2
				PRINTFORMW 를 뿌리까지 물고 맛보기 시작했습니다
			ELSE
				PRINTFORMW 를 핥고 빨기 시작했습니다
			ENDIF
			IF ABL:자위중독 >= 3 && RAND:3 == 0
				PRINTFORMW %CALLNAME:TARGET%는 목에 휘감기는 정액의 맛을 음미하고, 자위를 함께 하면서 매장이 혼잡 해지기 전까지 %CALLNAME:WAITRESS%를 쾌락 고문 시킨것 같습니다…
			ELSEIF RAND:2
				PRINTFORM %CALLNAME:TARGET%는, 자신의 입에 대량으로 사정하면서 
				SIF ABL:ＬＶ > ABL:WAITRESS:ＬＶ
					PRINTFORM 필사적으로 
				PRINTFORMW 침착하게 하게 %CALLNAME:WAITRESS%의 일하는 모습을 곁눈질로 바라보며, 즐겁게 쉬는 시간을 만끽 한 것 같습니다.…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%는 접객 하는것은 안중에도 없는 듯 %CALLNAME:WAITRESS%의 정액을 몇번이나 싸게만들어, 목을 적시면서 즐겁게 보낸 것 같습니다…
			ENDIF
			LOCAL:1 = 2 + RAND:(ABL:정액중독 + 1)
			EXP:펠라경험 += LOCAL:1
			PRINTFORML 펠라경험＋{LOCAL:1}
			CALL SETFLAG, "정음경험", LOCAL:1/2 + 1, TARGET, -4
			PRINTFORML 정음경험＋{LOCAL:1/2 + 1}
			EXP:정액경험 += LOCAL:1 + 2
			PRINTFORML 정액경험＋{LOCAL:1 + 2}
			EXP:WAITRESS:사정경험 += LOCAL:1/2 + 1
			PRINTFORMW 사정경험＋{LOCAL:1/2 + 1}（%CALLNAME:WAITRESS%）
		ENDIF
	ELSEIF COND("身嗜み：자위금지") == 0 && ABL:자위중독 + TALENT:음란 >= 4 && RANDIF(2, ABL:봉사정신 >= 3)
		PRINTFORMW  탈의실에서 자위를 하기로 한 모양입니다
		IF RAND:3
			PRINTFORMW …하지만, 절정 직전에 호출되어버린 %CALLNAME:TARGET%는, 옷차림을 최소한으로 정돈하고 그냥 플로어에 돌아오는 처지가 되어버리고 말았습니다
			IF TALENT:음란 && (COND("발정기") || TALENT:발정체질) && RAND:3 == 0
				PRINTFORMW 달아오른 신체로 숨기지 못한다 엣치한%조사처리(TEXTS("匂い"),"를")% 시키고 있다%CALLNAME:TARGET%에 접근해진 손님들은, 예외없이 일어설 수 없게 되어 버리고 있습니다…
			ELSEIF RAND:3 || COND("제복：바니") || COND("身嗜み：노팬티")
				PRINTFORM \@ TALENT:현명함 ? 익숙하게 # \@ 생각을 바꾸고 바쁜 가게안을 이리저리 돌아다니기 시작한 %CALLNAME:TARGET%이었지만, 
				IF TALENT:남자 == 0 && (COND("발정기") || TALENT:발정체질) && RAND:2
					PRINTFORM 흥분이 식지않는 자궁과 질에서 흘러나오는 애액
					IF COND("제복：바니")
						PRINTFORMW 이 허벅지의 망사 타이즈에까지 새어나와버렸습니다…
						PRINTFORMW 이 모습 제대로 숨기지 않으면… 하고 %CALLNAME:TARGET%는 넓적다리를 문질러붙이고 가랑이의 틈새를 메우면서, 평소보다도 가슴을 두근거리면서 일을 했습니다 
					ELSE
						IF COND("身嗜み：노팬티")
							PRINTFORMW 이 한줄기, 팬티를 입고 있지 않은 탓에 허벅지를 타고 내려갔습니다…
						ELSE
							PRINTFORMW 에서 팬티는 시시각각 커다란 얼굴이 넓어지고 있습니다…
						ENDIF
						PRINTFORMW %CALLNAME:TARGET%는 개구장이 아이의 아이스케키를 경계하면서, 평소보다도 가슴을 두근거리면서 일을 했습니다 
					ENDIF
				ELSEIF TALENT:남자 == 0 && TALENT:젖기쉬움 && COND("身嗜み：노팬티") == 0 && RAND:2
					PRINTFORM 애액이 나오기 쉬운 체질 탓에 
					IF COND("제복：바니")
						PRINTFORMW 땀을 흘렸다는 것으로는 속이지 못할정도로 음미로운%조사처리(TEXTS("匂い"),"를")% 환기성이 나쁜 제옷의 사타구니로부터 근처로 뿌리고 있습니다…
					ELSE
						PRINTFORMW 질척질척해진 팬티가 조금 불쾌한 것 같습니다…
					ENDIF
				ELSE
					PRINTFORMW 방금 전의 플레이의 여운 때문에 배어나오는 것을 본 손님은 두근두근해하고 있습니다…
				ENDIF
			ELSE
				PRINTFORM 급하게 나온 탓에 팬티를 입는 것을 잊어버리고 나타난 %CALLNAME:TARGET%는, 
				IF TALENT:부끄럼쟁이
					PRINTFORMW 허벅지를 맞비비듯이 딱 붙이고 부끄러운듯이 절박한 상황을 헤쳐나간 것 같습니다…
				ELSEIF ABL:노출벽 >= 3
					PRINTFORMW 「이건 이것대로 즐거운것 같다」라며 신경쓰지 않고 서빙을 계속했습니다…
				ELSE
					PRINTFORMW 자신의 경솔함을 반성하면서도 가랑이가 손님에게 보이지 않도록 주의하면서 서빙을 계속했습니다…
				ENDIF
			ENDIF
			EXP:자위경험 += ABL:자위중독
			PRINTFORMW 자위경험＋{ABL:자위중독}
		ELSE
			LOCAL = LIMIT(RAND:(ABL:자위중독 + 1), 1, 5)
			PRINTFORMW ……
			PRINTFORMW …\@ LOCAL == 1 ? 気持ちよく # {LOCAL}회 정도 \@절정を迎えてすっかり만족した%CALLNAME:TARGET%저, 전신으로부터 희미하게 땀흘린 엣치한%조사처리(TEXTS("匂い"),"를")% 감돌게 하면서 플로어에 복귀했습니다…
			IF TALENT:남자 == 0
				EXP:절정경험 += LOCAL
				PRINTFORML 절정경험＋{LOCAL}
			ENDIF
			IF PENIS(TARGET)
				EXP:사정경험 += LOCAL
				PRINTFORML 사정경험＋{LOCAL}
			ENDIF
			EXP:자위경험 += LOCAL + ABL:자위중독/2
			PRINTFORMW 자위경험＋{LOCAL + ABL:자위중독/2}
		ENDIF
	ELSEIF ABL:봉사정신 >= 3 && RAND:2
		PRINTFORMW , 좋은 기회니까 라며 기합을 넣고 청소를 하기로 한 모양입니다
		IF FIND_COND("출연가능な점원", "흡혈귀") && RAND:4 == 0
			WAITRESS = FIND_COND("출연가능な점원", "흡혈귀")
			SETBIT CFLAG:WAITRESS:95, 1
			PRINTFORMW 가게에서 은식기를 꺼내 변색되는것을 제거 하고있는 %타겟을()%, %CALLNAME:WAITRESS%는 기둥의 그림자 뒤에서 정말 복잡한 얼굴로 엿보고 있습니다…
		ELSE
			WAITRESS = FIND_COND("출연가능な점원")
			SETBIT CFLAG:WAITRESS:95, 1
			IF MIN(ABL:봉사정신, ABL:WAITRESS:봉사정신) >= 4
				PRINTFORMW 마찬가지로 점내의 더러움이 신경 쓰였다는 %CALLNAME:WAITRESS%와 함께 구석 구석까지 깨끗하게 만든 %CALLNAME:TARGET%는, 상쾌한 표정으로 땀을 닦고 있습니다…
			ELSEIF ABL:WAITRESS:봉사정신 >= 3
				PRINTFORMW 청소의 보좌로서%CALLNAME:WAITRESS%를 권했다%CALLNAME:TARGET%하, 언제나 그다지 청소 할 수 없는 부분을 깨끗이 되어있고 어쩐지 즐거운 듯 합니다…
			ELSE
				PRINTFORMW %CALLNAME:TARGET%의 철저한 모습을 알고있는 %CALLNAME:WAITRESS%는, 도와달라고 하기 부탁받기 전에 허둥지둥 몸을 숨겼습니다…
			ENDIF
		ENDIF
	ELSE
		WAITRESS = FIND_COND("출연가능な점원")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW , %CALLNAME:WAITRESS%를 초대하여 직원실에서 시간을 보내기로 한 것 같습니다…
		PRINTFORMW ……
		;勝ち
		IF RAND:(10 + TALENT:현명함*5 - TALENT:미련함*5) >= RAND:(10 + TALENT:WAITRESS:현명함*5 - TALENT:WAITRESS:미련함*5)
			PRINTFORM …도박 트럼프에서 큰 승리를 한 %CALLNAME:TARGET%는 승자의 권리로서 %CALLNAME:WAITRESS%
			IF RAND:3 == 0 && TALENT:호색 + TALENT:해방 + TALENT:소악마 + TALENT:쾌감에솔직 && CHECK("性別による拒絶反応", TARGET, WAITRESS) == 0
				IF TALENT:여자 && COND("다방：Ｖ섹스") && PENIS(WAITRESS) && COND("다방：인서트", WAITRESS) && RAND:2
					PRINTFORMW 에게 봉사하라고 명령하고, \@ COND("身嗜み：노팬티") == 0 ? 눈앞에서 팬티를 내린 뒤 # \@의자에 앉아 다리를 펼쳤습니다
					PRINTFORMW 그리고, 승부에 패배하고 순종하기로한 %CALLNAME:WAITRESS%는 봉사에 몸을 맡기며, 즐거운 휴식을 즐길 수 없었습니다…
					LOCAL:1 = 1 + RAND:(ABL:Ｖ감각 + ABL:WAITRESS:기교 + 1)/2
					LOCAL:2 = 1 + RAND:(LIMIT(ABL:기교 - ABL:WAITRESS:기교, 1, 5) )
					EXP:절정경험 += LOCAL:1/2 + 1
					PRINTFORML 절정경험＋{LOCAL:1/2 + 1}
					IF TALENT:오줌싸개버릇 && LOCAL:1 >= 4
						EXP:실금경험 += 1
						PRINTFORML 실금경험＋1
					ENDIF
					EXP:Ｖ경험 += LOCAL:1
					PRINTFORML V경험＋{LOCAL:1}
					EXP:Ｖ성교경험 += LOCAL:1
					PRINTFORML V성교 경험＋{LOCAL:1}
					EXP:정액경험 += LOCAL:2*2
					PRINTFORML 정액경험 += {LOCAL:2*2}

					EXP:WAITRESS:사정경험 += LOCAL:2
					PRINTFORML 사정경험＋{LOCAL:2}（%CALLNAME:WAITRESS%）
					EXP:WAITRESS:Ｖ삽입경험 += LOCAL:1
					PRINTFORML Ｖ삽입경험＋{LOCAL:1}（%CALLNAME:WAITRESS%）
					EXP:WAITRESS:허리사용경험 += LOCAL:1
					PRINTFORML 허리사용경험＋{LOCAL:1}（%CALLNAME:WAITRESS%）
					EXP:WAITRESS:커닐링구스경험 += 1
					PRINTFORML 커닐링구스경험＋1（%CALLNAME:WAITRESS%）
					EXP:WAITRESS:정액경험 += 1
					PRINTFORMW 정액경험＋1（%CALLNAME:WAITRESS%）
					PRINTFORMW 안에 가득해진 자신의 정액을 입으로 청소하게 된, %CALLNAME:WAITRESS%는 이번엔 자신의 물건을 깨끗하게 만들도록 해주겠다며 복수에 불타고 있습니다…
				ELSE
					PRINTFORMW 에게 봉사하라고 명령하고, \@ COND("身嗜み：노팬티") == 0 ? 눈앞에서 팬티를 내린 뒤 # \@의자에 앉아 다리를 펼쳤습니다
					PRINTFORMW 그리고, 승부의 패배한것을 순종하기로한 %CALLNAME:WAITRESS%의 정성스런 봉사에 몸을 맡기면서, 시간을 보냈습니다…
					LOCAL:1 = 1 + RAND:(ABL:Ｃ감각 + ABL:WAITRESS:기교 + 1)/2
					IF PENIS(TARGET)
						EXP:사정경험 += LOCAL:1/2 + 1
						PRINTFORML 사정 경험＋{LOCAL:1/2 + 1}
						EXP:WAITRESS:펠라경험 += LOCAL:1
						PRINTFORML 펠라경험＋{LOCAL:1}（%CALLNAME:WAITRESS%）
						CALL SETFLAG, "정음경험", LOCAL:1/2 + 1, WAITRESS, -4
						PRINTFORML 정음경험＋{LOCAL:1/2 + 1}（%CALLNAME:WAITRESS%）
						EXP:WAITRESS:정액경험 += LOCAL:1 + 2
						PRINTFORMW 정액경험＋{LOCAL:1 + 2}（%CALLNAME:WAITRESS%）
					ELSE
						EXP:절정경험 += LOCAL:1/2 + 1
						PRINTFORML 절정경험＋{LOCAL:1/2 + 1}
						IF TALENT:오줌싸개버릇 && LOCAL:1 >= 4
							EXP:실금경험 += 1
							PRINTFORML 실금경험＋1
						ENDIF
						EXP:WAITRESS:커닐링구스경험 += LOCAL:1
						PRINTFORMW 커닐링구스경험＋{LOCAL:1}（%CALLNAME:WAITRESS%）
					ENDIF
				ENDIF
			ELSEIF RAND:2 && TALENT:호색 + TALENT:심술쟁이 + TALENT:소악마 && TALENT:WAITRESS:호색 + TALENT:WAITRESS:심술쟁이 + TALENT:WAITRESS:소악마 + TALENT:WAITRESS:교만
				IF RAND:3 == 0 && COND("제복：스커트")
					PRINTFORMW 의 어른 스러운 팬티를 몰수해하고, 스커트 주머니에서 꺼낸 상당히 H한 디자인의 팬티를 입고 아르바이트를 하라고 명령했습니다
					PRINTFORMW %CALLNAME:WAITRESS%는 노팬티보다는 좋다고 말하며 순순히 따랐습니다만, 이번엔 %타겟을()% 생각하며 복수에 불타고있는 것 같습니다.…
				ELSE
					IF COND("제복：바니")
						PRINTFORMW 의 팬티를 몰수해하고, 바니 슈트 밑에 속옷을 입지않고 아르바이트하도록 명령했습니다 
					ELSE
						PRINTFORMW 의 팬티를 몰수하고, 노팬트로 아르바이트하라고 명령했습니다 
					ENDIF
					PRINTFORMW %CALLNAME:WAITRESS%는 부들부들하며…분노의 목소리를 내면서도 순순히 따르고는 있지만, 이번엔 %CALLNAME:TARGET%에게 노팬티로 일하게 해주겠다며 복수를 다짐하고 있는것 같습니다…
				ENDIF
			ELSE
				PRINTFORM 에게 차를 준비하라고 시키고, 패자의 억지를 한귀로 흘려들으면서 
				IF RAND:5 == 0
					PRINTFORM 쵸콜렛이 잔뜩 들어간 
				ELSEIF RAND:4 == 0
					PRINTFORM 제철 과일이 가득 들어간 
				ELSEIF RAND:3 == 0
					PRINTFORM 생크림이 잔뜩 들어간 
				ELSEIF RAND:2
					PRINTFORM 맛있어보이는 신작 
				ELSE
					PRINTFORM 공들여서 데코레이션한 
				ENDIF
				PRINTFORMW 케이크를 먹으며 즐거운 시간을 보냈습니다…
			ENDIF
		ELSE
			PRINTFORM …거기에, 도박 트럼프에 어이없이 대패를 당한 %CALLNAME:TARGET%는 %CALLNAME:WAITRESS%에게
			IF RAND:2 && TALENT:WAITRESS:호색 + TALENT:WAITRESS:심술쟁이 + TALENT:WAITRESS:소악마 && COND("身嗜み：노팬티") == 0
				PRINTFORM 하루 급료는 물론이고 팬티까지 빼앗겨버려, 
				IF COND("제복：스커트")
					PRINTFORMW 아이스케키나 못된 장난을 경계하면서 일을 해야하는 처지가 되고말았습니다…
				ELSE
					PRINTFORMW 야한 장난을 경계하면서 일을 해야하는 처지가 되고말았습니다…
				ENDIF
			ELSE
				PRINTFORMW 케이크 셋트를 쏴야하는 처지가 되어, 우울한 얼굴로 탁자에 푹 엎드려 승자의 우아한 티타임을 바라보고 있습니다…
			ENDIF
		ENDIF
	ENDIF

CASE "휴식"
	PRINTFORMW ――시간은 딱 정오. 
	PRINTFORMW 몽마들은 점내에 넘치는 손님을 상대하면서, 교대로 휴식을 합니다.

	IF COND("다방：펠라") && RAND:4 == 0
		PRINTFORMW %타겟을()% 자신의 차례가 오면, 손님중 한명에게 말을걸어, 교묘하게 휴식실에 데리고 들어갑니다! 
		PRINTFORMW ……잠시 후 나온%CALLNAME:TARGET%의 얼굴은, 유난히 윤기가 납니다

		CALL SETFLAG, "일반인상대の성행위初期설정"

		CALL SIMULATE_TRAIN_COM, 91, RAND:(ABL:욕망 + ABL:봉사정신 + 1)/3 + 1
		;文章に齟齬が出ないように調整
		EXP:정액경험 += 1
		CALL SETFLAG, "정음경험", 1, TARGET, -4

		CALL SETFLAG, "일반인상대の성행위종료"
		;つまみ食い人数+1
		CFLAG:36 += 1
	ELSEIF RAND:3 == 0
		WAITRESS = FIND_COND("출연가능な점원")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW 차례가 온%CALLNAME:TARGET%와 %CALLNAME:WAITRESS%는, 함께 휴식실로 이동했습니다.
		PRINTFORMW ……휴식실의 문이 닫히면, 안에서 두 사람의 작고 달콤한 목소리가 들리기 시작합니다.
		PRINTFORMW …
		PRINTFORMW …
		PRINTFORMW 잠시 후 방에서 나온 두 사람은, 굉장히 만족한 표정을 하고있었습니다.
	ELSEIF RAND:2
		WAITRESS = FIND_COND("출연가능な점원")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW 차례가 온%CALLNAME:TARGET%와 %CALLNAME:WAITRESS%는, 조달한 빵과 약간의 과자를 들고 휴식실에 처박혔습니다
		PRINTFORMW 두 사람은 점심 식사를하면서, %TEXTR("ガールズトーク", "さっきのイヤな손님の事", "新メニューの評判")%로 분위기가 살았습니다
	ELSE
		PRINTFORMW %타겟을()% 자신의 차례가 오면, 조달한 샌드위치를 들고 휴식시에 처박혔습니다
		PRINTFORMW 바쁜 점내을 뒷전으로, %타겟을()% 잠시 날개를쉬고 재충전했습니다.
	ENDIF

CASE "査察"
	PRINTFORMW 몽마들이 잘 일하고 있는지 궁금해졌다 %CALLNAME:MASTER%는 , 변장하고 사찰 해보기로했습니다
	PRINTFORMW ……………
	PRINTFORMW ………
	PRINTFORMW …
	IF ABL:MASTER:기교 - ABL:기교/2 - RAND:10 >= 0
		PRINTFORMW 솜씨좋게 변장해%CALLNAME:MASTER%는 입점시 누구에게도 들키지 않고 몰래 매장의 모습을 들었습니다
		IF RAND:2 && (FIND_COND("출연가능な점원", "강기") || TALENT:상사상애)
			PRINTFORMW 그러자,%CALLNAME:TARGET%의 폐인 손님 이 따라다녀 성희롱 비슷한 행위를 받고 있습니다！
			IF FIND_COND("출연가능な점원", "강기") && RANDIF(1, TALENT:상사상애)
				WAITRESS = FIND_COND("출연가능な점원", "강기")
				SETBIT CFLAG:WAITRESS:95, 1
				PRINTFORMW 이것은 돕는 것이 좋은가……？라고 생각한 정면,%CALLNAME:WAITRESS%하지만 손님의 앞에 서%TEXTR("一睨み", "微笑み一つ", "実力行使")%그리고 되돌려 보냈습니다！
				PRINTFORMW 그녀가 있으면 가게 중(안)에서 트러블을 일으키려는 손님은 없게 될 것 같다…
			ELSE
				PRINTFORMW 보기 힘들었다%CALLNAME:MASTER%하지만 자연스럽게 구조선을 내면, 다시 반했다%CALLNAME:TARGET%의 혼잡한 틈을 노림에 껴안아 왔습니다…
			ENDIF
		ELSE
			PRINTFORMW 점내는 보기좋게 번성하고 있어,몽마들의 일하는 태도에도 문제는 없는 것 같습니다
			PRINTFORMW ……회계를 끝마쳐 조용하게 가게를 나오려고 했을 때,%CALLNAME:TARGET%만이 남몰래 윙크를 해 왔습니다
		ENDIF
	ELSEIF CFLAG:30 >= 50 && RAND:2
		PRINTFORMW 몽마들은 변장을 한눈에 간파하면, 무언가 이유를 붙여%CALLNAME:MASTER%테이블에 가까워 지려고하고 있습니다
		PRINTFORMW 하지만%CALLNAME:TARGET%의 목소리에, 모두 일하러 돌아갔습니다
		PRINTFORMW %CALLNAME:TARGET%는 상당한 발언권이 있는 것 같습니다
	ELSE
		PRINTFORMW 변장 한%CALLNAME:MASTER%는 , 몰래 가게를 관찰하고있었습니다 만……
		PRINTFORMW 가까이 다가온%CALLNAME:TARGET%가 갑자기%CALLNAME:MASTER%의가랑이에 접근해 왔습니다.
		WAITRESS = FIND_COND("출연가능な점원")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW %CALLNAME:TARGET%가 조용이 웃고 멀어지면서 이번에는%CALLNAME:WAITRESS%가 마찬가지로 다가오고 있습니다
		PRINTFORMW ……%CALLNAME:MASTER%는 전부 들켜버려서, 몽마들의 장난감이 되어버렸습니다
		PRINTFORMW 너무 당당히 점원들이 끈적 끈적하고 있었으므로, 점내의 손님에게 \@ TALENT:MASTER:남자 ? 질투 # 好奇 \@보여져 버렸습니다.
	ENDIF

;이하は소질が필요な이벤트群
CASE "눈에띄고싶어함"
	SELECTCASE RAND:100
	CASE IS > 75
		PRINTFORMW %CALLNAME:TARGET%는 너무 바빠서 힘들어보입니다. 
		PRINTFORMW 그럼에도 중간 중간에 적절히 포즈를 취하며 주목을 받고있습니다. 
		IF RAND:2 == 0
			WAITRESS = FIND_COND("출연가능な점원")
			SETBIT CFLAG:WAITRESS:95, 1
			PRINTFORMW 함께 일하던 %CALLNAME:WAITRESS%가 쓴 웃음을 지었다는 느낌이 들었다. 
		ELSE
			PRINTFORMW 너무 너무 흥이 올랐는지, %CALLNAME:TARGET%는 나동그라졌습니다. 
			IF COND("제복：바니")
				PRINTFORMW 손님이 주목하고 있었으니, 어느 의미로는 대성공입니다. 
			ELSE
				PRINTFORMW 스커트 안을 손님이 주목하고 있었으니, 어느 의미로는 대성공입니다. 
			ENDIF
		ENDIF
	 CASE 20 TO 75
		PRINTFORMW %CALLNAME:TARGET%는, 노래를 부르면서 접객를 하고있습니다. 
		PRINTFORMW 그 노랫소리에, 점내의 분위기가 좋아졌습니다.
		IF RAND:2 == 0
			PRINTFORMW 주목을 받아서, %CALLNAME:TARGET%도 기분이 좋은 것같습니다. 
		ELSE
			PRINTFORML 손님은 편안한 모습이었지만, 연출이라고 생각한 듯 
			PRINTFORMW 평범하게 듣기만 할 뿐이라 살짝 유감스러운 얼굴을 하고있었습니다. 
		ENDIF
	CASEELSE
		PRINTFORMW 점내가 상당히 한산해서 그런지, 딱히 실수도 없고 일은 순조롭습니다. 
		PRINTFORMW 주목해주는 손님도 적어서, %CALLNAME:TARGET%는 불만스러워보입니다. 
		PRINTFORMW 동료가 묻자, %CALLNAME:TARGET%는 쓴웃음을 지어주었습니다. 
	ENDSELECT
CASE "레즈끼"
	SELECTCASE RAND:100
	CASE IS > 75
		PRINTFORMW %CALLNAME:TARGET%는 미인 손님이 신경쓰이는 모양입니다
		PRINTFORMW 하지만 바쁜 탓에, 좀처럼 말을 걸 수 없습니다. 
		IF RAND:5 < ABL:레즈끼
			PRINTFORMW %CALLNAME:TARGET%에게, 그 손님이 말을 걸어주었습니다. 
			PRINTFORMW 기쁜듯이 그 곳으로 향했습니다. 
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는, 틈을 봐서 말을 걸려고 하고있습니다. 
			PRINTFORMW 하지만 잘 되지않았던 모양입니다. 
		ENDIF
	 CASE 20 TO 75
		IF RAND:5 < ABL:레즈끼
			PRINTFORMW %CALLNAME:TARGET%는, 미인 손님을 바라보거나 하면서 일을 열심히 하고있는 것 같습니다
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는, 미인 손님에게 적극적으로 말을 걸고있습니다
			PRINTFORMW 아무래도, 상대도 %타겟을()% 마음에 들어하는 것 같습니다
		ENDIF
	CASEELSE
		IF RAND:5 < ABL:레즈끼
			PRINTFORMW %CALLNAME:TARGET%는, 미인 손님에게 말을 걸지 말지 고민하고있는 것 같습니다. 
			PRINTFORMW 약속이 있었는지, 남자 친구가 와서 실망인 것 같습니다. 
		ELSE
			PRINTFORMW 한가해서 %CALLNAME:TARGET%는, 미인 손님을 꼬시고있습니다…
		ENDIF
	ENDSELECT
CASE "不遜な接손님"
	PRINTFORM %CALLNAME:TARGET%는 손님에게 
	IF TALENT:교만 || RAND:2
		PRINTFORM 교만한 
	ELSE
		PRINTFORM 건방진
	ENDIF
	PRINTFORMW 태도로 대했습니다. 
	IF RAND:(1 + TALENT:매혹 + TALENT:수수께끼의매력 + TALENT:매력 + TALENT:인기)
		PRINTFORMW …그런데, 도리어 손님의 마음에 들어버린 것같습니다.
		PRINTFORMW 그것에 기분이 좋아졌는지, %CALLNAME:TARGET%는 내내 좋은 결과를 보여주었습니다. 
	ELSE
		WAITRESS = FIND_COND("출연가능な점원")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW 당장 달려온 %CALLNAME:WAITRESS%에게 한 소리 들었지만, 그 이후에는 열중하지 않았던것 같습니다…
	ENDIF
CASE "모유"
	PRINTFORMW %CALLNAME:TARGET%가 일하러 나오고서부터는, 카페오레와 밀크티가 지금까지의 배 이상으로 팔리게되었습니다. 
	IF TALENT:부끄럼쟁이 && RAND:2 == 0
		PRINTFORMW 결코 만드는 모습을 보여주진 않지만, 주방에서 나오는 %CALLNAME:TARGET%는, 확실히 뺨을 상기시키고 만족한 표정을 하고 있습니다…
	ELSE
		PRINTFORMW 손님들 사이에서는 『보통 우유로는 맛볼 수 없는 감칠맛』이라고 호평을 받고 있습니다만, 아마도……
	ENDIF
CASE "아이"
	IF RAND:2 == 0
		PRINTFORMW %CALLNAME:TARGET%는 양손으로 쟁반을 머리위에 받쳐들고, 뚜벅뚜벅 손님의 테이블에 가서, 혀짧은 말투로 주문을 복창하고 있습니다. 
		PRINTFORMW 손님은, 손자를 보는 듯한 얼굴로 미소짓고있습니다…
	ELSE
		PRINTFORM %CALLNAME:TARGET%는 활기차게 주문을 받았던 것까지는 좋았습니다만, 
		IF RAND:3 == 0
			PRINTFORM 츄로스를 
		ELSEIF RAND:2
			PRINTFORM 허니비스켓을 
		ELSE
			PRINTFORM 베이크드 애플을 
		ENDIF
		PRINTFORM 주문한 손님의 테이블에, 
		IF RAND:3 == 0
			PRINTFORM 나무열매 샐러드가 
		ELSEIF RAND:2
			PRINTFORM 훈제연어가 
		ELSE
			PRINTFORM 시프리샌드가 
		ENDIF
		PRINTFORMW 차려져있었습니다. 
		PRINTFORMW 어떻게하면, 이렇게까지 훌륭하게 실수할 수 있는 걸까…? 
	ENDIF
CASE "폭유"
	PRINTFORMW 손님에게 호출되어 주문을 받고있는 %CALLNAME:TARGET%는, 어딘가 기분이 언짢은 듯 합니다. 
	PRINTFORMW 그래도 마음을 다잡고 손님에게 미소를 지은 순간, 파앙! 하는 소리를 내며, 
	IF COND("제복：바니")
		PRINTFORM 앞가슴 단추 
	ELSEIF COND("제복：웨이트리스")
		PRINTFORM 블라우스의 가슴 단추
	ELSEIF COND("제복：메이드복")
		PRINTFORM 메이드복의 가슴 단추
	ELSEIF COND("제복：무녀풍")
		PRINTFORM 무녀풍 제복의 앞가슴 단추
	ENDIF
	PRINTFORMW 가 튕겨날아갔습니다. 
	PRINTFORMW 아무래도, 제복의 가슴께가 불편한 모양입니다…. 
	IF TALENT:부끄럼쟁이
		PRINTFORMW %CALLNAME:TARGET%는 잠깐의 틈이 지난 후, 수치심에 비명을 지르면서 노출된 \@ COND("제복：바니") ?  가슴을 # 브래지어를 \@ 양손으로 가리고, 가게 안쪽으로 뛰쳐들어갔습니다…
	ELSEIF TALENT:부끄럼없음
		PRINTFORMW %CALLNAME:TARGET%는, 점 내의 시선이 자신의 가슴팍에 모이는 것을 신경쓰는 모습도 없이, 손님에게 다른 점원을 보내겠다고 말한 뒤, 남성손님의 시선을 모으면서 가게 안으로 들어갔습니다. 
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는, 수줍은 모습으로 자리를 얼버무리고, 재빨리 가게 안으로 들어갔습니다…
	ENDIF
;食器の落ちた音に反応すると, そこには…! 
CASE "팬티見せつけ"
	IF RAND:10
		LOCALS = 소년
	ELSE
		LOCALS = 청년
	ENDIF
	PRINTFORMW 떨어뜨린 식기들을 주우려고 몸을 굽힌  %CALLNAME:TARGET%는, 한 %LOCALS%가 소리에 반응해 시선을 자신에게 향한 것을 깨달은 모양입니다…
	IF COND("제복：바니") || COND("身嗜み：노팬티")
		LOCALS:1 = 가랑이
	ELSE
		LOCALS:1 = 팬티
	ENDIF
	IF RAND:2
		PRINTFORM 핑하고 와버린 %CALLNAME:TARGET%는, 
		IF RAND:2
			PRINTFORMW 일부러 한쪽 무릎을 세우고 %LOCALS%에게 %LOCALS:1%가 보이기 쉬운 자세로 정리를 계속했습니다. 
		ELSE
			PRINTFORMW 일부러 다리를 크게 벌리고 %LOCALS%에게 %LOCALS:1%를 보여주면서 정리를 계속했습니다. 
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%는 
		IF TALENT:강기 && RAND:3
			PRINTFORM 씨익 웃고, 
		ELSE
			PRINTFORM 요염하게 웃고, 
		ENDIF
		PRINTFORMW 천천히 다리를 열고, %LOCALS%에게 %LOCALS:1%를 보여주면서 정리를 계속했습니다. 
	ENDIF
	IF ABL:노출벽 >= 5
		IF TALENT:젖기쉬움 && RAND:2
			PRINTFORM 보여지는 것에 흥분했는지, 
			IF RAND:2 || COND("제복：바니") || COND("身嗜み：노팬티")
				PRINTFORMW 허벅지 부분이 약간 홍조 되어있고, \@ COND("身嗜み：노팬티") ? 노출하고 있던 틈새도 # \@ 기분 탓인지 촉촉하게 되었습니다…
			ELSE
				PRINTFORMW 중요한 부분에 접해있는 부분은 시간이 지날수록 희미하게 얼룩이 지고 있습니다…
			ENDIF
		ELSEIF COND("제복：스커트")
			PRINTFORM 
			IF COND("身嗜み：노팬티")
				PRINTFORM %CALLNAME:TARGET%는 %CALLNAME:MASTER%의 아래에서 팬티를 입고 있지 않은, \@ TALENT:클리피어스 + TALENT:라비아피어스 ? 귀걸이 장식 된 # \@성기를 훤히 들여다 보이도록 하고 있습니다…
			ELSE
				PRINTFORM 완전히 노출의 즐거움에 눈을 떠 버린듯한 %CALLNAME:TARGET%의 팬티는
				IF RAND:3 == 0
					PRINTFORM 매우 작고, 
					SELECTCASE CONDS("음모")
					CASE "ナチュラル", "生え放題"
						PRINTFORMW 부드러울 듯한 음모가 확실히 나와 보여지고 있습니다…
					CASE "직접 손에 넣어"
						PRINTFORMW 면도를 하여 다듬어진 음모가 확실히 보여지고 있습니다…
					CASEELSE
						SELECTCASE CONDS("음모")
						CASE "剃り跡"
							PRINTFORM 약간 남아있는 둔부는
						CASE "生えかけ"
							PRINTFORM 약간 자라있는 둔부는
						CASE "ツルツル"
							PRINTFORM 짙게 되어있는 둔부는
						CASEELSE
							PRINTFORM 맨들맨들한 둔부는
						ENDSELECT
						PRINTFORMW 물론, \@ TALENT:클리피어스 && RAND:2 ? 민감한 장소에 부착된 피어스 # 흥분으로 커진 클리 \@까지 보여 버릴 지경입니다…
					ENDSELECT
				ELSEIF RAND:2
					PRINTFORMW 그냥 끈 이라고 말해도 좋을 것 같은 과격한 디자인이, 완전히 보여 버릴것 같았습니다…
				ELSE
					PRINTFORMW 둔부를 덮어주는 부분이 망사인 T 팬티를 입고 있어서, \@ TALENT:클리피어스 + TALENT:라비아피어스 ? 귀걸이로 장식 된 # \@보지가 희미하게 보여지고 있습니다…
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	IF RAND:2
		PRINTFORMW \@ COND("제복：스커트") ?  미니스커트 안에서 보이는 # 눈앞의 \@광경에 푹 빠진 %LOCALS%를 곁눈질로 보면서, %CALLNAME:TARGET%는 즐겁게 작업을 마쳤습니다…
	ELSE
		PRINTFORMW ………
		PRINTFORMW ……

		CALL SELF_KOJO_L, EVENT_NAMES

		PRINTFORM …작업을 마친 %CALLNAME:TARGET%가 %LOCALS%에게 
		;소악마
		IF TALENT:소악마 && RANDIF(1, TALENT:심술쟁이)
			IF ABL:노출벽 >= 3 && TALENT:여자 && COND("제복：스커트") && RAND:2
				PRINTFORM 장난스러운 미소를 보이며
				IF COND("身嗜み：노팬티")
					PRINTFORM 보지를 넓혀보이며
				ELSE
					PRINTFORM 팬티를 비켜 놓아 보지를 살짝 보여 주며
				ENDIF
			ELSE
				PRINTFORM 장난스럽게 미소지어 주
			ENDIF
		ELSEIF TALENT:심술쟁이
			PRINTFORM 심술궂게 웃어주
		ELSEIF TALENT:부끄럼쟁이 && RAND:2
			PRINTFORM 부끄러운 듯이 미소짓
		ELSE
			PRINTFORM 히죽 웃어주
		ENDIF
		PRINTFORMW 자, %LOCALS%는 불이 붙은듯 새빨갛게 물들었습니다. 
		PRINTFORMW 이제, 이 %LOCALS%는 %CALLNAME:TARGET%의 포로겠지요…
	ENDIF
;내성적（おどおど웨이트리스は萌える）
CASE "내성적"
	SELECTCASE RAND:100
	CASE IS > 60
		PRINTFORMW %CALLNAME:TARGET%는 사람이 많은 탓에 곤혹스러워하고 있습니다…
		PRINTFORMW 당황스러워 하던 중에 %CALLNAME:TARGET%는 손님에게 불려갔습니다. 
		IF RAND:2 == 0
			PRINTFORMW %CALLNAME:TARGET%는 긴장으로 눈물을 짓고 있습니다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는 자신에게 잘된 일이라고 말하면서 갔습니다…
		ENDIF
	CASE 20 TO 60
		PRINTFORMW %CALLNAME:TARGET%는 울상을 지으면서도 열심히 일하고 있습니다…
		PRINTFORMW 단지 타고난 성격때문인지, 아직도 접객에는 익숙해지지 않는 것같습니다. 
		IF RAND:2 == 0
			WAITRESS = FIND_COND("출연가능な점원")
			SETBIT CFLAG:WAITRESS:95, 1
			PRINTFORMW 그 모습을 보다 못한 %CALLNAME:WAITRESS%는 %타겟을()% 격려하였습니다…
		ELSE
			PRINTFORMW 어떻게 주방담당이 될 수 없을까 고민하고 있습니다…
		ENDIF
	CASEELSE
		PRINTFORMW 오늘은 손님이 적은 것같습니다. 
		PRINTFORMW %CALLNAME:TARGET%는 기분 탓인지 기뻐보입니다…
	ENDSELECT
;양기, 미련함（おバカ）
CASE "양기"
	SELECTCASE RAND:100
	CASE IS > 70
		PRINTFORMW  %CALLNAME:TARGET%는 주문을 틀려버린 모양입니다. 
		IF RAND:2 == 0
			PRINTFORMW 「바쁘니까 어쩔 수 없다」며  손님은 웃으며 용서해주었습니다…
		ELSE
			PRINTFORMW ……틀린 주문은 %CALLNAME:TARGET%가 맛있게 먹었습니다…
		ENDIF
	CASE 30 TO 70
		PRINTFORMW 한가해서 %CALLNAME:TARGET%는 개인기연습을 하고있습니다. 
		PRINTFORMW 그것을 보고있던 손님에게는 대호평이었습니다…
	CASEELSE
		PRINTFORMW 언제나 실수가 눈에 띄던 %CALLNAME:TARGET%도, 바쁘지 않았던 덕분에 실수가 적었던 모양입니다. 
		PRINTFORMW 오히려, 조금 바보같은 행동이 좋은 분위기를 만들고있다는 느낌이 들어 기묘합니다…
	ENDSELECT
;음기(店内が妙に暗くなる感じで)
CASE "음기"
	SELECTCASE RAND:100
	CASE IS > 70
		PRINTFORMW 오늘은 상당히 혼잡한 것 같습니다만, %CALLNAME:TARGET%의 주변에만 손님이 없습니다. 
		IF RAND:2 == 0 && COND("제복：스커트") && COND("身嗜み：노팬티") == 0
			PRINTFORMW …%CALLNAME:TARGET%는 팬티를 슬쩍 노출시켜 봤습니다
			PRINTFORMW 아무도 떡밥을 물지 않았습니다…
		ELSE
			PRINTFORMW 손님이 점원을 불렀습니다만, 다른 점원이 왔습니다. 
			PRINTFORMW 음침함이 진해진 기분이 듭니다…
		ENDIF
	CASE 20 TO 70
		PRINTFORMW %CALLNAME:TARGET%가 가져오는 커피에는 코어한 수요가 있는 것 같습니다. 
		PRINTFORMW %CALLNAME:TARGET%는 어딘가 복잡한 얼굴을 하고 있습니다…
	CASEELSE
		PRINTFORMW 오늘은 손님도 별로 들어오지 않았습니다. 
		PRINTFORMW 가게안이 전체적으로 가라앉은 분위기입니다…
	ENDSELECT
;쿨系（と見られてしまう이벤트, ルリコンホイホイ）
CASE "쿨"
	PRINTFORMW %CALLNAME:TARGET%는 억양이 없는 목소리로 메뉴를 읽고있습니다. 
	IF RAND:(1 + TALENT:매혹 + TALENT:수수께끼의매력 + TALENT:매력 + TALENT:인기) >= 2
		PRINTFORMW %CALLNAME:TARGET%의 단골손님인 듯, 황홀한 표정을 보이고 있습니다…
	ELSEIF RAND:(1 + TALENT:매혹 + TALENT:수수께끼의매력 + TALENT:매력 + TALENT:인기)
		PRINTFORMW …그 손님은 참을수 없었던 것 같습니다…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 붙임성이 없다라며 불평했습니다…
	ENDIF
;요정さん
CASE "요정"
	PRINTFORM %CALLNAME:TARGET%는 케이크가 든 접시를 들고, 
	IF RAND:100 >= 70
		PRINTFORM 아주 혼잡한 
	ELSE
		PRINTFORM 혼잡해지기 시작한 
	ENDIF
	PRINTFORMW 가게의 천장 근처를 휙휙 콧노래를 섞어가면서 날고있습니다. 
	WAITRESS = FIND_COND("출연가능な점원")
	SETBIT CFLAG:WAITRESS:95, 1
	PRINTFORM 어수선한 가게를 돌아 다니는 %CALLNAME:WAITRESS%를 거들떠 보지도 않고 %CALLNAME:TARGET%는 고객의 
	IF RAND:2
		PRINTFORM 앞
	ELSE
		PRINTFORM 어깨
	ENDIF
	PRINTFORM 에 우아하게 착륙해, 케이크를 두고 
	IF RAND:2
		PRINTFORMW 인사했습니다. 
	ELSE
		PRINTFORMW 미소지었습니다. 
	ENDIF
	PRINTFORM 손님이 
	IF RAND:2
		PRINTFORM 고맙다고 말하고 
	ELSE
		PRINTFORM 미소로 답례하고 
	ENDIF
	PRINTFORM 케이크를 먹으려하자, %CALLNAME:TARGET%가 
	IF RAND:3 == 0
		PRINTFORM 손가락을 물고 
	ELSEIF RAND:2
		PRINTFORM 부끄러운 듯이 
	ELSE
		PRINTFORM 눈물을 글썽이고 
	ENDIF
	IF RAND:2
		PRINTFORMW 손님의 얼굴을 바라보고 있습니다…
	ELSE
		PRINTFORMW 입으로 옮겨지는 케이크에 뜨거운 시선을 보내고 있습니다…
	ENDIF
	PRINTFORMW 손님은 뺨을 느슨하게하고, 포크로 케이크를 %CALLNAME:TARGET%의 눈 앞에 내밀었습니다. 
	IF RAND:2
		PRINTFORM %CALLNAME:TARGET%는 
		IF RAND:2
			PRINTFORM 눈을 빛내고
		ELSE
			PRINTFORM 케이크와 손님의 얼굴을 번갈아보고
		ENDIF
		PRINTFORMW , 케이크를 덥석 물었습니다. 행복 그 자체인 웃는 얼굴에, 손님의 얼굴에도 미소가 피어났습니다…
	ELSE
		WAITRESS = FIND_COND("출연가능な점원")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW … 멋지게 달려들려고 한 %CALLNAME:TARGET%의 목덜미를, %CALLNAME:WAITRESS%가 잡았습니다. 
		PRINTFORM %CALLNAME:WAITRESS%는 쓴 웃음을 짓는 고객에게 사과를 하며, 
		IF RAND:2
			PRINTFORM 울면서 물고늘어지는 
		ELSE
			PRINTFORM 허우적대면서 날뛰는 
		ENDIF
		PRINTFORMW %타겟을()% 끌고 가게 안쪽으로 돌아갔습니다…
	ENDIF
;라미아さん
CASE "라미아"
	IF RAND:2
		PRINTFORM 주문을 받는 
	ELSE
		PRINTFORM 식기를 정리하는 
	ENDIF
	IF RAND:2
		PRINTFORMW %CALLNAME:TARGET%의 뱀부분이 신기한지, 손님의 시선은 무심코 그 쪽을 보고맙니다.
		IF RAND:3 == 0
			PRINTFORMW %CALLNAME:TARGET%가 하반신을 재주좋게 움직여서 이동하는 광경은, 꽤 박력있습니다… 
		ELSEIF RAND:2
			PRINTFORMW %CALLNAME:TARGET%는 그것을 신경쓰는 기색도 없이, 오히려 즐기고있는 것처럼 보입니다…
			;「ふふ♪蛇の身体が珍しいかしら」
			;「じろじろ見ちゃって…. 触ってみたい？」
		ELSE
			IF RAND:2
				PRINTFORM 오늘은 기분이 좋은지
			ELSE
				PRINTFORM 뭔가 좋은 일이 있었는지
			ENDIF
			PRINTFORMW , 꼬리의 끝이 살랑살랑 흔들리고있습니다…
		ENDIF
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 커다란 꼬리를, 아이가 찰싹찰싹 만지고있습니다. 
		IF (TALENT:임신 + TALENT:출산경험 || ABL:봉사정신 >= 5) && RAND:2
			PRINTFORMW 꺄꺄거리며 즐거워하는 아이를, %CALLNAME:TARGET%는 자애로 가득 찬 표정으로 어루만지고있습니다… 
		ELSE
			PRINTFORMW 당황해서 그만두게하려는 부모를 말리고, %CALLNAME:TARGET%는 미소지으며, 꼬리 끝을 만지게해주었습니다… 
		ENDIF
	ENDIF
;머메이드さん
CASE "머메이드"
	PRINTFORM 제복의 스커트 자락에서 사람 다리가 아닌 윤기있는 싱싱한 물고기 비늘이 뻗어나와있는 %CALLNAME:TARGET%가, 
	;今にも寝入りそうな(ペンとメモを握ったまま目を閉じてる)
	IF RAND:2
		PRINTFORM 어딘가 멍한 모습으로 
	ELSE
		PRINTFORM 살짝 졸려보이는 표정으로 
	ENDIF
	PRINTFORMW 끼익끼익… 휠체어로 가게안을 이동하고 있습니다. 
	PRINTFORM 손님이 주문을 하려고 말을걸자, 
	IF RAND:2
		PRINTFORMW %CALLNAME:TARGET%는 퍼뜩 눈을 뜨고 조용히 품에서 물통을 꺼내서, 뚜껑에 붓고 물을 꿀꺽꿀꺽 마시기시작했습니다. 
		PRINTFORM 맛있게 물을 마신 %CALLNAME:TARGET%는, 
		IF RAND:2
			PRINTFORM 돌변해서 발랄한 모습으로 
		ELSE
			PRINTFORM 방금전보다 두배는 촉촉한 얼굴로 
		ENDIF
		PRINTFORMW 활기차게 주문을 받고, 드륵드륵 방금전보다 재빠르게 휠체어를 움직여 주방으로 돌아갔습니다… 
	ELSE
		PRINTFORM %CALLNAME:TARGET%는 팟하고 눈을 뜨고, 갑자기 육지에 밀려올라온 물고기마냥 입을 뻐끔뻐끔거리기 시작하더니, 
		PRINTFORMW 손님의 손에 있는 찬물을 붙잡고, 꿀꺽꿀꺽 목을 울리며 단숨에 마셨습니다. 
		PRINTFORM 한숨 돌린 %CALLNAME:TARGET%는, 
		IF RAND:2
			PRINTFORM 감사를 담은 미소를 지으며
		ELSE
			PRINTFORM 만면에 미소를 지으며
		ENDIF
		PRINTFORMW , 주문을 받기시작했습니다. 
		IF RAND:3 == 0
			PRINTFORMW 손님은, 갑작스러운 전개에 멍해있습니다… 
		ELSEIF RAND:2
			PRINTFORMW 손님은 입을 쩍 벌리고있습니다… 
		ELSE
			PRINTFORMW 손님은, 놀라서 멍하니 앉아있습니다… 
		ENDIF
	ENDIF
;カオス料理
CASE "カオス料理"
	;一部の組み合わせ…「何かのようなもの」, 「七色の악마風」などは料理の形をなしていないので, どっちかを선택지から除外したりして, 
	;不自然な組み合わせが作られるのは避けた方がいいかもしれません. 
	;もっと食材などを발せばマトモな料理も増えるかもしれません. ガンガン발して下さい. 

	PRINTFORMW %CALLNAME:TARGET%는 알고지내는 손님에게 자신의 요리를 먹어달라고 부탁하고있다. 
	PRINTFORMW 아무래도 상당한 자신작인 모양이다. 
	VARSET LOCALS
	SPLIT "리조또, 쇼트케이크, 커피콩, 홍차잎, 훈제연어, 양고기육포, 몽마의 촉수, 무지개색으로 녹아있는 유동체 알수없는 그ー것ー은ー정ー말ー로 에피쿠로스적인 투구벌레, 츄로스, 허니비스켓, 베이크드애플, 전병, 나타데코코, 젤리, 프리터, 주먹밥, 햄버거, 고명야키소바, 버섯, 카르보나라, 마운틴스테이크, 시더우드, 페퍼민트, 로즈마리, 시프리샌드, 루이스브레드, 바나나, 챠밍부케, 문드롭스, 오므라이스, 후추, 수박, 알로에, 도롱뇽, 한방차, 동충하초, 일곱가지색, 극채색, 무언가", ", ", LOCALS
	LOCAL = RAND:39
	PRINTFORM 흔쾌히 승낙한 손님 앞에 나온 것은…%LOCALS:LOCAL% 
	VARSET LOCALS
	SPLIT "볶음, 아이스, 샤베트, 꼬치구이, 쥬스, 꿀절임, 닭튀김, 카츠파초, 마리네, 튀김, 프라이, 초콜렛바른것, 된장조림, 조림, 시럽절임, 얼절임, 버터구이, 잼, 소테, 스튜, 카레, 스프, 악마스타일, 키아스마스타일, 오뎅, 탄것, 비슷것", ", ", LOCALS
	LOCAL = RAND:27
	PRINTFORMW %LOCALS:LOCAL%다.

	;反応
	IF RAND:9 == 0
		PRINTFORMW 손님이 정말 먹어도 괜찮은가 고민하고있자, %CALLNAME:TARGET%는 「양념을 잊었다」라고 말하고 타바스코를 한가득 부었다! 
		PRINTFORMW 손님의 움직임은 완전히 멈춰있다……
	ELSEIF RAND:8 == 0
		PRINTFORMW 외형은 이름이상으로 정상적이었지만, 아무래도 요리에서 자극적인 냄새가 풍겨나오고있다. 
		PRINTFORMW 손님은 올라오는 자극을 어떻게는 참으면서도, 눈물을 흘리면서 요리를 먹었다! 
		PRINTFORMW 하지만 %CALLNAME:TARGET%는 무자비하게도 한그릇을 더 가져왔다……
	ELSEIF RAND:7 == 0
		PRINTFORMW 손님이 그것을 입에 넣은 순간, 움직임이 딱 멈춘 채 굳어버렸다. 아무래도 입에 넣은 그것을 삼킬 수 없는 모양이다. 
		PRINTFORMW 하지만 그런 것따윈 상관없이, %CALLNAME:TARGET%는 두입째를 권하고있다………
	ELSEIF RAND:6 == 0
		PRINTFORMW 손님은 한입 먹은 순간 순식간에 안색을 바꾸고, 화장실을 향해 달리기 시작했다. 
		PRINTFORMW %CALLNAME:TARGET%는 잠시동안, 뭐가 문제였는지 계속 고민했다. 
	ELSEIF RAND:5 == 0
		PRINTFORMW 손님은 요리를 절반정도 입에 넣고, 갑자기 정신을 잃고 쓰러져버렸다. 
		PRINTFORMW %CALLNAME:TARGET%는 손님이 정신을 차릴 때까지, 한동안 간호해야만 했다………
	ELSEIF RAND:4
		PRINTFORM 요리를 입에 넣은 
		IF RAND:2
			PRINTFORMW 남자손님은 몸을 슬쩍 테이블 쪽으로…정확히는 하반신을 테이블 아래에 밀어넣었다. 
			PRINTFORMW %CALLNAME:TARGET%가 맛의 감상을 물으려고 접근하자, 남자 손님은 더욱더 테이블에 몸을 기대었다. 
		ELSE
			PRINTFORMW 여성 손님은 갑자기 얼굴을 붉히고, 쭈뼛쭈뼛 두 다리를 움직이기 시작했다. 
			PRINTFORMW %CALLNAME:TARGET%에게 요리의 감상을 말하는 동안에도, 여성 손님은 움직임을 멈추려하지 않았다. 
		ENDIF
		PRINTFORMW 손님의 모습이 이상한 것을 깨달은  %CALLNAME:TARGET%는 「의무실에 가죠」라고 손님의 손을 당겼지만, 손님은 붕붕 고개를 젓고 『참을 수 없게 되어버려! 』라고 필사적으로 거절했다. 
	ELSEIF RAND:3
		PRINTFORMW 천천히 그것을 입에 넣은 손님은………갑자기 허리를 핑하고 펴고, 통로 바닥에 쓰러졌다! 
		PRINTFORMW ―만, 그 표정은 황홀함으로 물들어, 왠지 기분좋은 얼굴로 꿈틀댔다. 
		PRINTFORMW %CALLNAME:TARGET%는 상상한 대로 요리가 만들어진건지, 만족한 얼굴로 미소짓고있다…. 
	ELSEIF RAND:2
		PRINTFORMW 손님은 조심조심 요리를 입에 넣고……곧 눈빛을 바꾸더니, 정신없이 그것을 먹기시작했다! 
		PRINTFORMW 그 모습을 본 %CALLNAME:TARGET%는, 자신의 요리실력에 자신감을 갖게 된 것같다………
	ELSE
		PRINTFORMW 손님 앞에 나온 요리는 매우 이질적인 외형이었지만, 손님은 요리를 한입 먹더니 행복한 표정을 짓고는, 매우 맛있었다고 말했다. 
		PRINTFORMW 아무래도 겉보기와는 다르게 상당히 맛있었던 모양이다. 
		PRINTFORMW %CALLNAME:TARGET%는, 언젠가 %CALLNAME:PLAYER%에게 이 요리를 만들어주려 다짐했다………
	ENDIF
	;※一度作ったらDAILY LIFEの方で実際마스터に食べさせてもいいかも. 
;인기者が盗撮される
CASE "盗撮"
	IF COND("제복：바니")
		PRINTFORMW %CALLNAME:TARGET%는 가슴과 다리사이 , 엉덩이 등 성적인 부분을 도촬당한 것같습니다! 
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 스커트 안을 도촬당한 것같습니다! 
	ENDIF
	IF ABL:노출벽 >= 3
		IF TALENT:겁쟁이 + TALENT:내성적 + TALENT:부끄럼쟁이 && RANDIF(2, TALENT:교만, TALENT:양기+TALENT:눈에띄고싶어함)
			IF RAND:2
				PRINTFORM %CALLNAME:TARGET%는 가슴을 두근두근거리면서, 
			ELSE
				PRINTFORM 그것을 깨달은 %CALLNAME:TARGET%는 부끄러움에 몸을 붉히면서, 
			ENDIF
			IF COND("제복：바니")
				PRINTFORMW 야한 의상을 입은 자신의 모습이 녹화되는 것을 즐겼습니다…
			ELSEIF CHECK_PANTIES("Ｈ") && TALENT:남자 == 0
				PRINTFORMW 야한 팬티로 감싸인 다리사이를 녹화당하는  배덕감을 즐겼습니다…
			ELSEIF COND("身嗜み：노팬티")
				PRINTFORMW 근로중의 뜸든 성기가 기록되는 일을 즐겼습니다…
			ELSE
				PRINTFORMW 팬티 속을 찍히는 것을 즐겼습니다…
			ENDIF
		ELSEIF TALENT:양기 + TALENT:눈에띄고싶어함 && RANDIF(2, TALENT:교만)
			PRINTFORM  …하지만, 그것을 눈치챈 %CALLNAME:TARGET%는 손님의 귓가에 그만두라고 속삭이면서도, 자신
			SIF RAND:2 && (ABL:노출벽 >= 5 || TALENT:젖기쉬움) && TALENT:남자 == 0
				PRINTFORM 의 \@ COND("제복：바니") ?  부끄러운 장소 # 노출의 쾌락에 얼룩진 팬티 \@
			PRINTFORMW 를 찍은 사진만큼은 용서해준 것 같습니다…
		ELSEIF TALENT:교만 && RAND:2
			PRINTFORMW …하지만, 그것을 깨달은 %CALLNAME:TARGET%는, \@ PENIS(TARGET) ? 페니스를 찍히지 않도록 주의하면서 # \@ 적극적으로 셔터찬스를 만든 것 같습니다…
			PRINTFORMW 아무래도, 다른 점원보다 찍힌 횟수에서 밀리는게 참을 수 없었던 것 같다…
		ELSE
			PRINTFORMW …하지만, 그것을 눈치챈 %CALLNAME:TARGET%는, 오히려 보폭을 넓히고 부끄러운 부분을 찍히는 즐거움을 맛봤습니다…
		ENDIF
	ELSEIF TALENT:장난감이되어버려라
		PRINTFORMW …하지만, 그것을 눈치챈 %CALLNAME:TARGET%는 촬영장비를 장난감으로 만들어 무사할 수 있었습니다. 
		SIF NO:TARGET == 50
			PRINTFORMW 「여기가 가게가 아니었다면…… 장난감이 되는건 오빠일지도 몰라♪」
	ELSEIF TALENT:교만
		WAITRESS = FIND_COND("출연가능な점원")
		PRINTFORMW 그것을 %CALLNAME:WAITRESS%에게 배운 %CALLNAME:TARGET%는, 보이는 것과 보는 것은 상당히 다른 것 같고, 분개하고 있습니다…
		IF TALENT:강기 && RAND:2
			PRINTFORMW 참을 수 없었는지, 범인을 향해 발소리를 크게 울리며 다가가서……아. 카메라를 발로 차부숴버렸다……
		ELSE
			PRINTFORMW 범인은 이미 없어진 모양입니다만, 다음에 왔을때는 어떻게 되려나…
		ENDIF
	ELSEIF TALENT:겁쟁이 || TALENT:내성적 || TALENT:부끄럼쟁이
		IF CHECK_PANTIES("Ｈ") && COND("제복：스커트")
			PRINTFORMW %CALLNAME:TARGET%는\@ COND("오토코노코") ? , 남자인데도 분위기를 타서 # \@ 야한 팬티를 입고있었다는 것을 기억하고는 얼굴에서 불이 날것만 같습니다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는 신경쓰지 않는 것처럼 행동하지만, 조금 슬픈 것 같습니다…
		ENDIF
	ELSE
		IF TALENT:도착적 && RAND:3
			PRINTFORMW %CALLNAME:TARGET%는 자신의 사진이 딸감으로 사용되는 것을 상상하고, 뺨을 붉히고 있습니다…
		ELSEIF ABL:자위중독 >= 3 && COND("제복：스커트") && COND("身嗜み：노팬티") == 0 && TALENT:남자 == 0 && TALENT:젖기쉬움 && TALENT:부끄럼없음 == 0 && RAND:2
			WAITRESS = FIND_COND("출연가능な점원")
			PRINTFORMW 그것을 %CALLNAME:WAITRESS%에게 배운 %CALLNAME:TARGET%는, 팬티가 애액으로 흠뻑 젖은 것을 떠올리고 머리를 싸쥐고 고민하고 있습니다.
			PRINTFORMW …%CALLNAME:TARGET%는 아르바이트 중에 자위하는 것을 자제하기로 다짐하면서, 탈의실로 팬티를 갈아입으러 갔습니다……
		ELSEIF COND("음모") >= 6 && TALENT:호색 && TALENT:유치 == 0 && TALENT:남자 == 0 && RAND:3
			PRINTFORMW %CALLNAME:TARGET%는 그곳 털을 정리하지 않은 것을 후회하고 있는 것 같습니다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는 흥흥 거리면서도, 자신의 \@TALENT:매력 ? 매력이 # 인기가\@ 증명된거 같아 나쁘지만은 않아보입니다…
		ENDIF
	ENDIF
;꼬리
CASE "꼬리"

;가슴の谷間
CASE "가슴の谷間"
	IF TALENT:양기 && RAND:2
		WAITRESS = FIND_COND("출연가능な점원", "!거유")
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW %CALLNAME:TARGET%는 어딘가 기운이 없어보입니다. 
		PRINTFORMW 걱정한%CALLNAME:WAITRESS%가 묻자, 「말을 건넨 사람이 모두 시선을 밑으로 돌린다」라는 것입니다.. 
		PRINTFORM …그것을 들은%CALLNAME:WAITRESS%는 지친 얼굴로, %CALLNAME:TARGET%의 
		IF COND("제복：바니")
			PRINTFORMW  흘러넘칠 만큼 큰 가슴이 만들어낸 크고 아름다운 계곡을 응시했습니다… 
		ELSE
			PRINTFORMW 앞가슴의 단추를 너무 풀어서 훤히 드러나보이는 골짜기를 바라보았습니다… 
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%는
		IF TALENT:양기 && RAND:3 == 0
			PRINTFORM 가볍게 서비스하는 기분으로, 
		ELSEIF RAND:2
 			IF COND("제복：바니")
				PRINTFORM 움직여도 덥지않도록, 
			ELSE
				PRINTFORM 움직였더니 더워져서, 
			ENDIF
		ELSE
			PRINTFORM 뜨거운 온기가 차지않게, 
		ENDIF
		IF COND("제복：바니")
			PRINTFORMW 가슴이 절반정도밖에 가려지지 않은 아슬아슬한 바니슈트를 입고서 일하고있습니다. 
		ELSE
			PRINTFORMW 앞가슴 단추를 몇개씩이나 풀고 일하고있습니다.
		ENDIF
		IF RAND:3 == 0 && ABL:봉사정신 >= 3
			PRINTFORM …깊숙히 고개를 숙이고 인사를 하거나할 때, 훤히 보이는 
		ELSEIF RAND:2
			PRINTFORM …주문을 끝내고 인사할 때, 아무렇지도않게 보이는 
		ELSE
			PRINTFORM …본인은 특별하게 의식하고있지 않은 것같지만, 슬쩍슬쩍 보이는 
		ENDIF
		IF RAND:2
			IF RAND:3 == 0
				PRINTFORM 장엄한 경치
			ELSEIF RAND:2
				PRINTFORM 가슴을  골짜기
			ELSE
				PRINTFORM 찰져보이는 구체
			ENDIF
		ELSEIF COND("제복：바니")
			IF TALENT:니플피어스 && RAND:3 == 0
				PRINTFORM 유두에 달린 %TEXTS("니플피어스")%
			ELSEIF ABL:Ｂ감각 >= 3 && RAND:3 == 0
				PRINTFORM 꼿꼿이 발기한 유두
			ELSEIF RAND:2 && COND("거유륜")
				PRINTFORM 음미로움 감도는 큰 유륜
			ELSEIF COND("함몰유두")
				PRINTFORM 함몰 상태의 드물다유두
			ELSEIF COND("유두비대")
				IF TALENT:음유 || COND("유두비대") >= 2
					PRINTFORM 도발적인정도로 비대했다유두
				ELSE
					PRINTFORM 집고 싶어질 정도의 훌륭한유두
				ENDIF
			ELSEIF RAND:2
				PRINTFORM 핑크빛 유두
			ELSE
				PRINTFORM 귀여운 유두
			ENDIF
		ELSE
			IF TALENT:호색 && RAND:2
				PRINTFORM 아슬아슬한 
			ELSEIF AGE(TARGET) >= 3 && RAND:2
				PRINTFORM 대담한 
			ELSEIF AGE(TARGET) >= 3 && RAND:2
				PRINTFORM 어른스러운 
			ELSEIF AGE(TARGET) >= 4
				PRINTFORM 예쁘게 장식된 
			ELSEIF RAND:2
				PRINTFORM 컬러풀한 
			ELSE
				PRINTFORM 사랑스러운 
			ENDIF
			PRINTFORM 브래지어
		ENDIF
		IF RAND:3 == 0
			PRINTFORMW 를 가까이서 보려는 손님들의 지명싸움으로, 점내는 천천히 열기를 더해가고있습니다…
		ELSEIF RAND:2
			PRINTFORMW 에, 손님은 뺨이 느슨하게 풀리는 것을 필사적으로 참고있습니다…
		ELSE
			PRINTFORMW 에, 자리에서 일어날 수 없어진 남성 고객들은 추가 주문을 할 수 밖에 없게되고말았습니다……
		ENDIF
	ENDIF
CASE "치료の名目でつまみ食い"
	CALL SETFLAG, "일반인상대の성행위初期설정"
	;상대は基本的に男
	SIF RAND:2 && (ABL:레즈끼 > RAND:10 || (TALENT:남자 && ABL:ＢＬ끼 < 3) )
		CALL SETFLAG, "일반인상대の성행위설정：상대が女"

	PRINTFORMW %CALLNAME:TARGET%는 안색이 나빠보이는 \@ TALENT:PLAYER:남자 ? 남 # 여\@자손님을 발견하고, 스탭룸 옆의 의무실에서 치료하도록 권했습니다. 
	PRINTFORMW 손님은 상당히 괴로워보이는 모습으로, %CALLNAME:TARGET%에게 기대면서 의무실에 갔습니다…
	PRINTL 
	PRINTFORMW －의무실－
	PRINTFORMW 의자에 앉은 손님을 간단하게 문진한 %CALLNAME:TARGET%는, 손님에게 찬장의 약품을 혼합해서 만든 즉효성의 약을 먹인 것 같습니다. 
	PRINTFORMW 그 효과는 굉장해서, 반신반의하며 약을 먹은 손님의 안색이 순식간에 좋아져, 금새 건강해지게 되었습니다. 
	PRINTFORMW …그런데. 
	IF TALENT:PLAYER:남자
		PRINTFORMW 손님의 다리 사이에 24인용 텐트가 쳐져서 걸을 수 없을 정도가 되었습니다!
	ELSE
		PRINTFORMW 손님은 얼굴을 붉히고, 뜨거운 한숨을 흘리면서 다리 사이를 스커트 위에서 누르고 있습니다. 
		PRINTFORML 그 상태를 %CALLNAME:TARGET%가 보고 손님에게 스커트를 걷어올리라고 말하자, 걷어올려진 손님의 다리 사이에는… 
		PRINTFORMW 애액이 흘러넘쳐서 팬티는 물론, 무릎까지 적실정도로 굉장한 상태입니다…! 
	ENDIF
	PRINTFORMW 아무래도, 약의 효력이 너무 강한 것 같습니다. 
	PRINTFORM 이 상태에 자신의 책임을 느낀 %CALLNAME:TARGET%는, 
	IF TALENT:PLAYER:남자
		IF COND("다방：Ｖ섹스") && COND("제복：스커트") && COND("身嗜み：노팬티") == 0
			IF RAND:3 == 0
				PRINTFORM 팬티를 옆으로 비껴열고 손님에게 그곳을 보여주면서, 
			ELSEIF RAND:2
				PRINTFORM 팬티를 내리면서, 
			ELSE
				PRINTFORM 스커트를 걷어 올리고 팬티가 먹혀든 가랑이를 손님에게 보여주면서, 
			ENDIF
		ELSEIF RAND:2
			PRINTFORM 손님의 다리사이를 어루만지고 문지르면서, 
		ELSE
			PRINTFORM 손님의 다리사이 앞에 무릎 꿇고 입으로 지퍼를 내리면서, 
		ENDIF
	ELSE
		PRINTFORM \@ RAND:2 ?  흠뻑 젖은 팬티를 벗겨내서 드러난 비밀장소를 # 팬티 너머로 그곳을 \@ \@ RAND:2 ?  빨 # 손가락끝으로 만지작거리 \@면서, 
	ENDIF
	PRINTFORMW 성욕 처리를 해주겠다고 자청했습니다. 
	PRINTFORM 격렬한 성욕에 시달리고있던 손님은 너무나 매력적인 그 제안을 거절하지 못하고, 

	IF TALENT:PLAYER:남자
		IF COND("다방：Ｖ섹스")
			PRINTFORMW %타겟을()% 침대에 밀어넘어뜨리고 딱딱해진 거근으로 이성을 잃은 짐승과 같이 범하기시작했습니다! 
			SELECTCOM = 61
			;文章に齟齬が出ないように調整
			EXP:정액경험 += 10
			CALL SETFLAG, "질내정액경험", 5, TARGET, -4
		ELSE
			PRINTFORMW 페니스에 뺨을 대고 문질러오는  %CALLNAME:TARGET%에게 입으로 해달라고 애원하기시작했습니다…! 
			SELECTCOM = 91
			;文章に齟齬が出ないように調整
			EXP:정액경험 += 10
			CALL SETFLAG, "정음경험", 5, TARGET, -4
		ENDIF
	ELSE
		PRINTFORMW %타겟을()% 향해 그곳을 잡아벌리고 졸라대기시작했습니다…! 
		SELECTCOM = 98
	ENDIF

	CALL SIMULATE_TRAIN_COM, SELECTCOM, RAND:7 + 4

	PRINTL 
	PRINTFORMW 의무실안에, 난폭한 숨결과 진한 성취(성취[性臭])가 가득 차고있다… 
	PRINTL 
	PRINTFORMW ………
	PRINTFORMW ……
	IF TALENT:PLAYER:남자
		IF COND("다방：Ｖ섹스")
			PRINTFORMW …%CALLNAME:TARGET%는 자궁을 짓눌러버릴 것같은 사정없는 피스톤운동과 묻지도 따지지도 않고 반복되는 대량의 질내 사정을 마음껏 즐긴 것같습니다. 
		ELSE
			PRINTFORMW …%CALLNAME:TARGET%는 손님의 가랑이에 얼굴을 묻고 목에 휘감기는 듯한 진한 정액을 듬뿍 맛보고 만족한 것같습니다. 
		ENDIF
	ELSE
		PRINTFORMW …%CALLNAME:TARGET%는 손님의 다리 사이에 얼굴을 묻고 질에 꽂아넣은 손가락으로 Ｇ스폿을 계속 자극하면서 계속 혀로 그곳을 애무해, 물총 뿐만 아니라 실금까지 시키면서 즐긴 것같습니다. 
	ENDIF
	PRINTL 
	PRINTFORM 그 후, 제정신으로 돌아온 손님의 
	IF TALENT:PLAYER:남자
		PRINTFORM 옷차림을 정돈해주고, 
	ELSE
		PRINTFORM 가랑이를 닦아준 뒤에 자신의 예비의 팬티를 입히고, 
	ENDIF
	PRINTFORMW 방금 벌어진 일을 비밀로 할것을 말하면서, 기분좋게 손님을 돌려보냈습니다… 
	PRINTFORMW 방금 전에 사용한 약의 효능과 개선점의 데이터를 진지한 표정으로 메모하기시작했습니다…

	CALL SETFLAG, "일반인상대の성행위종료"
	;つまみ食い人数+1
	CFLAG:36 += 1

CASE "ハーブティー"
	PRINTFORMW %CALLNAME:TARGET%가 일하기 시작한 뒤부터, 허브티 등의 약효가 있는 음료가 배 이상 팔리게 되었습니다. 
	WAITRESS = FIND_COND("출연가능な점원", "약독내성")
	IF WAITRESS && RAND:4 == 0
		SETBIT CFLAG:WAITRESS:95, 1
		PRINTFORMW 효과가 강한듯한 것에 대해서는 %CALLNAME:WAITRESS%시음 받거나 하면서, 오늘도 %CALLNAME:TARGET%는 새로운 제품 개발에 힘 쓰고 있는 것 같습니다…
	ELSEIF RAND:3 == 0
		PRINTFORMW 특별히 수당같은 것은 지급하지 않고 있습니다만, %CALLNAME:TARGET%는 「이것저것 실험하니까」라며 즐겁게 만들고 있는 것 같습니다…
	ELSEIF RAND:2 == 0
		PRINTFORMW 가끔씩 피로회복에 효과가 있는 드링크를 마신 남성손님이 자리에서 일어날수 없게 되었습니다만, 평판은 아주 좋은 것같습니다…
	ELSE
		PRINTFORMW 만들고 있는 장면을 본 동료들은 마실 생각이 거의 없어졌습니다만, 효과의 수준은 과연 전문가, 의심의 여지는 없습니다…
	ENDIF
CASE "주인がトイレで"
	PRINTFORMW %CALLNAME:MASTER%이 다방 준비를 하러가자, 아무래도 %CALLNAME:TARGET%가 화장실에서 돌아오지 않은 것 같다. 
	PRINTFORMW 어쩔수 없어서 보러가보니, 명백하게 요염함이 섞여있는 목소리가 화장실의 개인실 안에서 들려온다…
	PRINTFORMW 설마, 하고 생각하고 문을 황급히 열어보자, 도대체 무슨 일인건지 %CALLNAME:TARGET%가 여성손님의 다리사이에 얼굴을 묻고있었다! 
	PRINTFORMW 음핵을 혀로 희롱하면서 Ｇ스팟을 괴롭히는 %CALLNAME:TARGET%의 테크닉에, 여성 손님은 신음소리를 억누르지 못하고 몸을 떨면서 끊임없이 절정하고, 
	PRINTFORMW 게다가, %CALLNAME:TARGET%는 그것도 모자라서, 황홀한 표정을 하면서 실금까지해대는 손님의 엉덩이 구멍에까지 손을 뻗어 즐기려는 것 같다…
	PRINTL 
	PRINTFORMW …근데. 이 뒤처리는 어떻게 해야하려나……
	PRINTL 
	WAITRESS = FIND_COND("출연가능な점원", "조합지식")
	SETBIT CFLAG:WAITRESS:95, 1
	PRINTFORMW 참상에 현기증을 느낀 %CALLNAME:MASTER%이었지만, 역시 일반인에게 손대는 것은 곤란 하기 때문에 %CALLNAME:WAITRESS%의 약으로 여성 고객의 기억을 꿈처럼 애매 모호하게 만들어 돌려보냈습니다. 
	PRINTFORMW %타겟을()% 크게 혼내놨지만, 정말로 반성하고 있는 건지는 의문입니다…
	PRINTL 
	PRINTFORML 덧붙여, 그 여성 손님은 분명히 기억이 없어졌을텐데도, 다방 단골이 되어 가게에 와서는, 
	PRINTFORMW 뜨거운 시선을 %CALLNAME:TARGET%에게 보내고있는 것 같습니다……

	PRINTFORML %EXPNAME:23%＋{(ABL:레즈끼 + 1)/2}
	EXP:수음경험 += (ABL:레즈끼 + 1)/2
	PRINTFORML %EXPNAME:28%＋{ABL:레즈끼}
	EXP:커닐링구스경험 += ABL:레즈끼
	PRINTFORML %EXPNAME:40%＋{ABL:레즈끼 * 5}
	EXP:레즈경험 += ABL:레즈끼 * 5
	WAIT

	;MASTERに出演플래그
	SETBIT CFLAG:MASTER:95, 1
CASE "お茶を淹れる"
	PRINTFORM %CALLNAME:TARGET%는 손수 끓인 홍차를 
	IF TALENT:기품
		IF RAND:2
			PRINTFORM 우아한 태도로 
		ELSE
			PRINTFORM 기품 흘러넘치는 움직임으로 
		ENDIF
	ELSEIF TALENT:헌신
		IF RAND:2
			PRINTFORM 보람차게 
		ELSE
			PRINTFORM 적당한 때를 가늠해서 
		ENDIF
	ELSE
		PRINTFORM 기운차게 
	ENDIF
	PRINTFORMW 손님들에게 서비스하고 있습니다. 
	PRINTFORM 손님들에게 맛있다고 칭찬을 받은 %CALLNAME:TARGET%는 
	IF TALENT:조합지식 && RAND:5 == 0
		PRINTFORMW 즐거운 듯이 대답 하면서, 딱 한방울 떨어뜨린 미약의 효과가 언제나올까―, 하고 손님의 상태를 살피고있습니다…
	ELSEIF TALENT:현명함 + TALENT:조합지식 && RAND:2
		PRINTFORMW 여태까지 쌓아올린 실력을 좋은 기회에 드러낼 수 있어서 즐거운 것같습니다… 
	ELSEIF RAND:2
		PRINTFORMW 자기만의 브랜드 비율을 가르쳐주면서 즐거운 것같습니다…
	ELSE
		PRINTFORMW 찻잎의 종류와 끓이는 요령 등을 즐겁게 이야기하고 있습니다…
	ENDIF

CASE "コスプレ"
	PRINTFORMW 오늘은 코스프레 데이입니다 
	PRINTFORMW 봄에 맞춰서 점원 모두가 기모노를 입고, 점내도 벚꽃에 어울리는 화려한 색조로 장식되어있습니다 
	IF TALENT:빈유
		PRINTFORMW 기모노는 가슴이 작은 쪾이 예쁘게 보여서, 머리카락을 틀어올린 %CALLNAME:TARGET%는 기쁜 얼굴로 접객을 했습니다… 
	ELSEIF BASE:종족사고 == 16 || BASE:종족사고 == 22
		PRINTFORMW 역시 전통복을 입는데 익숙해서 그런지, %CALLNAME:TARGET%의 행동은 빼어나게 아름답습니다… 
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 익숙하지않은 기모노에, 조금 움직이기 힘들어하고있습니다… 
	ENDIF

;一定확률이벤트
CASEELSE
	;経営, 계절이벤트, コスプレデーなど

	;PRINTFORMW 꽃놀이같이, 계절감이 있는 것을 넣습니다

	;PRINTFORMW 계절에 의해 코스프레를 결정이 우선으로 ,
	;PRINTFORMW 그 후에 간호사라든지를 넣기로 하겠습니다

	;PRINTFORMW 경영에 대해 점장이 고민인 느낌입니다
	;PRINTFORMW 바쁨에 의해 기뻐 무늬키 충분하고와 분기도……
ENDSELECT


;バイトでの인기度の上下計算
LOCAL = RAND:6 - RAND:6
SIF ABL:봉사정신 >= 3
	LOCAL += RAND:(ABL:봉사정신 - 2) + 1
IF ABL:노출벽 >= 3
	IF ABL:봉사정신 >= ABL:욕망
		LOCAL += RAND:(ABL:노출벽 - 2) + 1
	ELSE
		LOCAL -= RAND:(ABL:노출벽 - 2) + 1
	ENDIF
ENDIF
SIF ABL:자위중독
	LOCAL -= RAND:(ABL:자위중독)
SIF TALENT:매혹
	LOCAL += 1
SIF TALENT:수수께끼의매력
	LOCAL += 1
SIF TALENT:매력
	LOCAL += 1
SIF TALENT:교만
	LOCAL += 2 - RAND:5
SIF TALENT:건방짐
	LOCAL -= 1

CFLAG:30 += LOCAL

;出演者同士が仲良くなる
CALL CHANGE_RELATION, 2, TARGET, WAITRESS

WAITRESS = 0

SELECTCASE LOCAL
CASE IS >= 3
	PRINTL 
	IF LOCAL >= 4
		PRINTFORMW 이번에는 대성공이었습니다
	ELSEIF LOCAL == 3
		PRINTFORMW %CALLNAME:TARGET%는 고정팬이 생긴 모양입니다
	ENDIF
	;솔직
	IF TALENT:솔직 && RANDIF(2, TALENT:내성적, TALENT:양기)
		PRINTFORM %CALLNAME:TARGET%는 콧노래를 부르며 
		IF RAND:2
			PRINTFORMW 청소를 하고있습니다…
		ELSE
			PRINTFORMW 식기를 정리하고 있습니다…
		ENDIF
	;내성적
	ELSEIF TALENT:내성적 && RANDIF(2, TALENT:양기)
		IF LOCAL >= 4
			WAITRESS = FIND_COND("출연가능な점원")
			SETBIT CFLAG:WAITRESS:95, 1
			PRINTFORMW %CALLNAME:WAITRESS%의 칭찬에, %CALLNAME:TARGET%는 수줍어 하고있습니다…
		ELSE
			PRINTFORMW 오늘의 근무도 끝나, %CALLNAME:TARGET%는 안심하고 가슴을 쓸어내리고 있습니다…
		ENDIF
	;양기
	ELSEIF TALENT:양기 && RAND:2 == 0
		IF TALENT:미련함 && RAND:2
			PRINTFORM %CALLNAME:TARGET%는 
			IF TALENT:빈유
				PRINTFORM 조그만 
			ELSEIF TALENT:거유
				PRINTFORM 커다란 
			ENDIF
			PRINTFORM 가슴을 을 펴고 
			IF RAND:2
				PRINTFORMW 자랑스러워하고있습니다…
			ELSE
				PRINTFORMW 뽐내고있습니다…
			ENDIF
		ELSE
			WAITRESS = FIND_COND("출연가능な점원")
			SETBIT CFLAG:WAITRESS:95, 1
			PRINTFORMW %CALLNAME:TARGET%는 %CALLNAME:WAITRESS%와 수고의 하이파이브를 나누고 있습니다…
		ENDIF
	ELSEIF LOCAL >= 4
		IF RAND:2
			PRINTFORMW %CALLNAME:TARGET%의 표정도 환합니다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는 동료로부터의 평판도 좋은 것같습니다…
		ENDIF
	ELSE
		PRINTFORMW 어딘지 모르게 기쁜듯한 표정을 하고있습니다…
	ENDIF

	;인기を得る
	IF GETBIT(TALENT:인기, 2) == 0 && COND("인기：다방")
		PRINTL 
		SETCOLOR DEF_COLOR("황색")
		PRINTFORMW %CALLNAME:TARGET%는 다방 %NAMES("다방")%의 간판아가씨\@ TALENT:남자 ? (？) # \@가 되었습니다
		SETBIT TALENT:인기, 2
		RESETCOLOR
	ENDIF
CASE 0
	PRINTL 
	PRINTFORMW 이번에는 아무 사고없이 끝났습니다
	PRINTFORMW 단지 자극이 적어, %CALLNAME:TARGET%는 조금 불만족스러운 것 같습니다…
CASE IS < 0
	PRINTL 
	PRINTFORMW %CALLNAME:TARGET%는 \@ LOCAL <= -2 ? 마지막으로 큰실수를 저질렀습니다 # 실수가 많았던 것 같습니다 \@
	IF LOCAL <= -3 && COND("다리が無い") == 0 && PENIS(TARGET) == 0 && TALENT:겁쟁이 + TALENT:냉정 + TALENT:감정결핍 + TALENT:음기 == 0 && TALENT:교만 + TALENT:건방짐 + TALENT:강기 + TALENT:양기
		PRINTFORMW 너무 끔찍한 실수를 저질렀기 때문에……%CALLNAME:TARGET%는 의자에 M자 다리 형태로 구속되어 깃털 빗자루로 간지럼 당했습니다! 
		IF TALENT:오줌싸개버릇 || ABL:요도확장 >= 0
			PRINTFORMW 웃음이 너무 지나쳐 버린 %CALLNAME:TARGET%는, 가벼운 요실금때문에 바지에 점점 얼룩이 늘리면서 필사적으로 숨을 고르고 있습니다…
		ELSE
			PRINTFORMW 웃음이 너무 지나쳐 버린 %CALLNAME:TARGET%는, 복근의 통증에 시달리면서 필사적으로 숨을 고르고 있습니다…
		ENDIF
	ELSE
		PRINTFORMW 기억이 남은것 인지, 우울한 표정을 하고 있습니다…
	ENDIF
ENDSELECT

;出演者同士が仲良くなる
CALL CHANGE_RELATION, 2, TARGET, WAITRESS

WAITRESS = 0

;LOCAL = RAND:5 - RAND:5
;난교이벤트플래그(まずは5人まで)
;IF COND("다방：난교") && LOCAL > 0 && NUM("다방：난교予定人数") < 5
;	LOCAL = MIN(5 - NUM("다방：난교予定人数"), LOCAL)
;	IF LOCAL == 1
;		PRINTFORMW (%CALLNAME:TARGET%는 자신에게 마음이 있을 것 같은 손님의 한사람에게 비밀의 권유를 한 것 같습니다…)
;	ELSE
;		PRINTFORMW (%CALLNAME:TARGET%는{LOCAL}인의 자신에게 마음이 있을 것 같은 손님에게 비밀의 권유를 한 것 같습니다…)
;	ENDIF
;	CFLAG:32 += LOCAL
;ENDIF
;$EVENT_AFTER_CAFE

;다방で난교플레이（これは条件満たしているなら発生しやすい）
;作成中

;閉店後이벤트（たまにしか発生しないようにしよう）
SIF RAND:5
	RETURN 0

CALL CALC, "랜덤선택"
;更衣室で거유をうらやまれる
SIF TALENT:거유 && FIND_COND("출연가능な점원", "!거유")
	CALL CALC, "랜덤선택", 1
;疲れて眠っている同僚に, ちょっと優しくする
SIF ABL:봉사정신 >= 3 || TALENT:헌신
	CALL CALC, "랜덤선택", 2
;モップでオナニー
SIF ABL:욕망 >= 5 && ABL:자위중독 >= 3
	CALL CALC, "랜덤선택", 3
;お店の雰囲気がエロエロでエロ이벤트可の場合. かつ, それなりに忙しめで인기あり. 인기度は未설정
SIF COND("다방：섹스") && TALENT:처녀 + TALENT:남자 == 0 && RAND:4 == 0
	CALL CALC, "랜덤선택", 4
;忙しい일の後でMASTERがマッサージ
SIF COND("다방출연끝", MASTER) == 0 && COND("다리が無い") == 0
	CALL CALC, "랜덤선택", 5
;業務일誌
SIF COND("다방출연끝", MASTER) == 0 && TALENT:반하기쉬움 + TALENT:호색 + TALENT:광기
	CALL CALC, "랜덤선택", 6
;ケーキじゃんけん
SIF TALENT:냉정 + TALENT:무관심 + TALENT:감정결핍 == 0
	CALL CALC, "랜덤선택", 7
;寝落ちした몽마をMASTERが背負って帰る
SIF COND("다방출연끝", MASTER) == 0
	CALL CALC, "랜덤선택", 8

PRINTL 
SELECTCASE RESULT
CASE 1
	PRINTFORMW 폐점 후의 탈의실에서, %CALLNAME:TARGET% 일행이 옷을 갈아입고 있습니다. 
	PRINTFORM %CALLNAME:TARGET%가 제복을 벗자, 
	IF TALENT:거유 >= 2 && RAND:3
		PRINTFORM 거함거포주의인 
	ELSEIF RAND:4 == 0
		PRINTFORM 앞가슴이 풍만한 
	ELSEIF RAND:3 == 0
		PRINTFORM 볼륨감 있는 
	ELSEIF RAND:2
		PRINTFORM 모성이 넘치는 
	ELSE
		PRINTFORM 꿈이 가득 차있는 
	ENDIF
	WAITRESS = FIND_COND("출연가능な점원", "!거유")
	SETBIT CFLAG:WAITRESS:95, 1
	PRINTFORM 바스트에 %CALLNAME:WAITRESS%들은
	IF TALENT:질투 && RAND:3
		PRINTFORM 질투섞인 
	ELSEIF RAND:5 == 0
		PRINTFORM 핥아먹는 듯한 
	ELSEIF RAND:4 == 0
		PRINTFORM 뜨거운 
	ELSEIF RAND:3 == 0
		PRINTFORM 동경어린 
	ELSEIF RAND:2
		PRINTFORM 선망의 
	ELSE
		PRINTFORM 쇼윈도우의 웨딩드레스를 보는 듯한 
	ENDIF
	PRINTFORMW 시선이 집중되었습니다. 
	IF TALENT:부끄럼쟁이 == 0
		IF (ABL:레즈끼 >= 3 || TALENT:바이) && RAND:3 == 0
			PRINTFORMW 좋은 먹이를 포착한 표정의 %CALLNAME:TARGET%는, 『비비면 커진다』를 내세우면서, %CALLNAME:WAITRESS%의 가슴을 주물럭 거리기 시작했습니다…
		;현명함
		ELSEIF TALENT:현명함 && RANDIF(1, TALENT:양기, TALENT:냉정, TALENT:심술쟁이, TALENT:강기, TALENT:교만, TALENT:호색)
			PRINTFORMW 표정으로 모든 것을 헤아린 %CALLNAME:TARGET%는, 재빨리 화제를 바꿔 자리를 떠났습니다…
		;양기
		ELSEIF TALENT:양기 && RANDIF(1, TALENT:냉정, TALENT:심술쟁이, TALENT:강기, TALENT:교만, TALENT:호색)
			PRINTFORMW %CALLNAME:TARGET%는 쾌활하게 웃으며, 말랑말랑 흔들리는 가슴을 %CALLNAME:WAITRESS%에게 맞추면서 떠들고 있습니다…
		;냉정
		ELSEIF TALENT:냉정 && RANDIF(1, TALENT:심술쟁이, TALENT:강기, TALENT:교만, TALENT:호색)
			PRINTFORMW %CALLNAME:TARGET%는 별로 상관하지않고, 빠르게 옷을 갈아입었습니다…
		;심술쟁이
		ELSEIF TALENT:심술쟁이 && RANDIF(1, TALENT:강기, TALENT:교만, TALENT:호색)
			PRINTFORMW %CALLNAME:TARGET%는 능글 능글 거리며, 보라는듯 부드러운 가슴을 %CALLNAME:WAITRESS%에게 과시하고 있습니다…
		;강기
		ELSEIF TALENT:강기 && RANDIF(1, TALENT:교만, TALENT:호색)
			PRINTFORMW %CALLNAME:TARGET%는 풍만한 가슴을 시선에서 숨기지 않고, 유유히 갈아입고있습니다…
		;교만
		ELSEIF TALENT:교만 && RANDIF(1, TALENT:호색)
			PRINTFORMW %CALLNAME:TARGET%는 가슴을 강조하는 포즈를 취하고, 그다지 싫지는 않아 보입니다…
		;호색+その他
		ELSE
			PRINTFORMW %CALLNAME:TARGET%는 호기심이 가득해 보이는 %CALLNAME:WAITRESS%에게, 가슴을 만지게 해주고 있습니다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%는, 
		;강기
		IF TALENT:강기 && RANDIF(1, TALENT:솔직, TALENT:내성적)
			IF RAND:3 == 0
				PRINTFORM 평소보다 약간 당황한 모습으로 
			ELSEIF RAND:2
				PRINTFORM 평소의 여유가 느껴지지 않는 표정으로 
			ELSE
				PRINTFORM 눈에 눈물이 맺힌 새빨간 얼굴로 
			ENDIF
			IF RAND:2
				PRINTFORMW 가슴팍을 제복으로 가렸습니다…
			ELSE
				PRINTFORMW 로커 문의 그림자에 숨어버렸습니다…
			ENDIF
		;내성적
		ELSEIF TALENT:내성적 && RANDIF(1, TALENT:솔직)
			IF RAND:2
				PRINTFORMW 얼굴을 새빨갛게 물들이고 양손으로 가슴을 가려버리고 말았습니다…
			ELSE
				PRINTFORMW 눈물맺힌 눈으로 이쪽에서 등을 돌려버렸습니다…
			ENDIF
		;솔직(その他もアリとします)
		ELSE
			IF RAND:3 == 0
				PRINTFORM 조금 기분나쁜 듯이 
			ELSEIF RAND:2
				PRINTFORM 뺨을 살짝 붉히고 
			ELSE
				PRINTFORM 조금 곤란한 모습으로 
			ENDIF
			IF RAND:3 == 0
				PRINTFORMW 미소짓고 있습니다…
			ELSEIF RAND:2
				PRINTFORMW 살그머니 가슴을 가렸습니다…
			ELSE
				PRINTFORMW 수줍어하고 있습니다…
			ENDIF
		ENDIF
	ENDIF
CASE 2
	PRINTFORM 폐점 후, 옷을 다 갈아입은 %CALLNAME:TARGET%가 탈의실에서 나오자, 
	IF RAND:2
		PRINTFORM 점내의 정리 
	ELSE
		PRINTFORM 청소
	ENDIF
	WAITRESS = FIND_COND("출연가능な점원")
	SETBIT CFLAG:WAITRESS:95, 1
	PRINTFORM 를 하고 있던 %CALLNAME:WAITRESS%가 제복 차림 그대로 책상에 푹 엎드려
	IF RAND:2
		PRINTFORM 얕게 
	ELSE
		PRINTFORM 조용히 
	ENDIF
	PRINTFORMW 숨소리를 내고 있었습니다. 
	PRINTFORM %CALLNAME:TARGET%는 
	IF RAND:2
		PRINTFORM 작게 미소짓고 
	ELSE
		PRINTFORM 어깨를 으쓱하고 
	ENDIF
	IF RAND:2
		PRINTFORMW , 격려의 홍차와 케이크를 내오기 위해 조명이 꺼진 주방에 들어갔습니다…
	ELSE
		PRINTFORMW , 어깨에 살짝 웃옷을 걸쳐주었습니다…
	ENDIF
CASE 3
	PRINTFORMW 영업시간도 끝나고, 조명도 반쯤 꺼진 점내에서, %CALLNAME:TARGET%가 대걸레질을 하고있습니다. 
	PRINTFORML 문득, 뭔가 생각난 듯이 안절부절 못하고 주의를 신경쓰기 시작한 %CALLNAME:TARGET%는, 
	IF PENIS(TARGET) && RANDIF(1, TALENT:후타나리)
		WAITRESS = FIND_COND("출연가능な점원")
		PRINTFORMW 탈의실로부터 빌려 왔다%CALLNAME:WAITRESS%노팬티노%조사처리(TEXTS("匂い"),"를")% 냄새 맡으면서,노출시켰다페니스를 잡아당기기 시작했습니다…
	ELSE
		PRINTFORMW 테이블에 기대어 놓은 대걸레 자루에, 비소를 눌러대고 천천히 신체를 흔들기 시작했습니다…
	ENDIF
	PRINTFORMW 가게의 안에서 들려오는 종업원의 잡담에, %CALLNAME:TARGET%의 억누른 목소리가 전혀 들리지않습니다…
CASE 4
	PRINTFORML 오늘의 가게는 매우 바빳던 것 같습니다. 
	PRINTFORMW %CALLNAME:TARGET%는 인적없는 가게 안에서 혼자 늦은 귀가를 하려하고 있습니다. 
	PRINTFORMW 탈의실에서, %CALLNAME:TARGET%가 혼자 옷을 갈아입고 있을 때, 갑자기 그녀의 팬 한사람이 새빨갛게 얼굴을 붉히고 들어왔습니다. 
	PRINTFORMW 아무래도 %CALLNAME:TARGET%의 색기와 가게의 분위기에 참을 수 없게되어버렸던 모양입니다. 

	;相愛か연모か음란かそれ以外で分岐　31
	;嫁はさすがに손を出すとマズイよね
	IF TALENT:상사상애
		PRINTFORMW %CALLNAME:TARGET%는 침착하게 사랑하는 사람이 있다는 것을 말하고 정중하게 돌려보낸 모양입니다. 
	;연모は음란かつ정액중독なら我慢できずにお입で　36
	ELSEIF TALENT:연모
		PRINTFORM %CALLNAME:TARGET%는 텐트를 치고있는 팬의 사타구니 근처를 보고 웃으
		IF TALENT:음란 && ABL:정액중독 >= 3
			PRINTFORMW 면서, 
			PRINTFORMW %CALLNAME:MASTER%의 페니스를 떠올리면서 핥아서 봉사해준 것 같습니다. 
		ELSE
			PRINTFORMW 면서도, 
			PRINTFORMW 뇌리를 스쳐지나간 %CALLNAME:MASTER%의 얼굴을 지우지 못하고, 정중하게 돌려보낸 것 같습니다. 
		ENDIF
	;음란なら大サービス! 　45
	ELSEIF TALENT:음란
		PRINTFORMW %CALLNAME:TARGET%는 요염한 미소를 짓고 속옷 차림으로 팬에게 다가가, 
		PRINTFORMW 바지 위에서도 알수있을 정도로 격분한 사타구니를 문지르면서 귀에 뜨거운 입김을 내뿜자
		PRINTFORMW 흥분의 한계에 도달한 팬에게 덮쳐졌습니다. 
		PRINTL
		IF TALENT:처녀 == 0 && TALENT:Ａ처녀 == 0
			PRINTFORMW 몸 안의 모든 부분에 정액을 쏟아부어져, %CALLNAME:TARGET%는 크게 만족한 것 같습니다. 
		ELSE 
			PRINTFORMW 팬의 정액을 쥐어짜내, %CALLNAME:TARGET%는 크게 만족한 것 같습니다. 
		ENDIF
		;つまみ食い人数+1
		CFLAG:36 += 1
	;その他　ちょっとカワイソス？
	ELSE
		PRINTFORMW %CALLNAME:TARGET%는 갑작스러운 침입자에 놀라면서도, 그 목적을 짐작해보면 몽마로서의 본능때문인지 
		PRINTFORMW %CALLNAME:MASTER%의 조교의 성과때문인지, 몸이 뜨거워지는 것을 느꼈습니다. 
		PRINTFORMW %CALLNAME:TARGET%가 자기 몸의 이변에 당황한 사이에 팬에게 덮쳐져버려, 
		PRINTFORM 변변한 저항도 하지못한채 
		IF TALENT:처녀 == 0
			PRINTFORMW 정액이 쏟아부어져버리고 만 모양입니다…
		ELSE
			PRINTFORMW 입으로 봉사하게 되어버린 것 같습니다…
		ENDIF
	ENDIF
CASE 5
	PRINTFORMW %CALLNAME:MASTER%이 폐점 후의 탈의실을 방문하자, %CALLNAME:TARGET%가 녹초가 되어 소파에 앉아있습니다…
	PRINTFORMW 그 모습을 보다못한 %CALLNAME:MASTER%는, 지친 다리를 맛사지해 주기로 했습니다. 
	PRINTFORM %CALLNAME:TARGET%는 
	;솔직
	IF TALENT:솔직 && RANDIF(1, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마, TALENT:호색, TALENT:교만, TALENT:강기)
		PRINTFORMW 오늘 있었던 사건을 이야기하거나하며 여유로운 모습으로, 기분좋게 다리를 주무르는 것을 즐겼습니다…
	;강기
	ELSEIF TALENT:강기 && RANDIF(1, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마, TALENT:호색, TALENT:교만)
		PRINTFORMW 한번은 거절했지만, 결국 %CALLNAME:MASTER%이 발을 주무르는 것을 기분좋게 몸부림치면서 즐겼습니다…
	;교만
	ELSEIF TALENT:교만 && RANDIF(1, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마, TALENT:호색)
		PRINTFORMW 피곤을 감추지않고 다리를 내밀고서, 이런저런 주문을 하면서 다리안마를 즐겼습니다…
	;호색
	ELSEIF TALENT:호색 && RANDIF(1, TALENT:양기, TALENT:부끄럼쟁이, TALENT:소악마)
		IF RAND:2 && TALENT:부끄럼쟁이 == 0 && COND("身嗜み：노팬티") == 0
			IF RAND:2 && COND("제복：스커트")
				PRINTFORMW 「땀이 찼다」며 팬티를 벗어 옆에 놓고, 가슴께를 풀고 툭툭치면서 기분 좋게 다리맛사지를 즐겼습니다…
			ELSE
				PRINTFORMW 「이쪽이 릴렉스할 수 있어」라며 속옷차림이 되어, 기분 좋은 듯 다리맛사지를 만끽했습니다…
			ENDIF
		ELSEIF COND("제복：바니")
			PRINTFORMW 땀때문에 몸에 달라붙은 의상을 벗고 속옷차림이 되어, %CALLNAME:MASTER%에게 부채를 부치게하면서 기분좋게 시간을 보냈습니다… 
		ELSE
			PRINTFORMW 스커트를 팔락팔락 움직여 모인 열기를 %CALLNAME:MASTER%에게 보내면서, 미소짓고 있습니다…
		ENDIF
	;양기
	ELSEIF TALENT:양기 && RANDIF(1, TALENT:부끄럼쟁이, TALENT:소악마)
		PRINTFORMW %CALLNAME:MASTER%의 손가락 자극에 간지러워하는 소리를 내면서, 잠깐의 휴식을 즐겼습니다…
	;소악마
	ELSEIF TALENT:소악마 && RANDIF(1, TALENT:부끄럼쟁이)
		IF COND("제복：바니")
			PRINTFORMW , 자연스럽게 슬쩍슬쩍 다리를 움직여서 %CALLNAME:MASTER%의 시선을 달아오른 다리사이로 유도하거나하면서 즐겼습니다… 
		ELSEIF RAND:2
			PRINTFORMW , 다리를 움직여서 자연스럽게 스커트 속이 보여주거나하면서 즐겼습니다…
		ELSE
			PRINTFORMW , 가슴팍을 풀고 편안하게 %CALLNAME:MASTER%의 손놀림을 즐겼습니다…
		ENDIF
	ELSEIF TALENT:부끄럼쟁이
		IF COND("제복：바니")
			PRINTFORMW 허리 근처에 타올을 덮고, %CALLNAME:MASTER%의 다리맛사지에 몸을 맡겼습니다…
		ELSE
			PRINTFORMW 뺨을 붉히고 스커트 자락을 손으로 누르면서, %CALLNAME:MASTER%의 맛사지에 몸을 맡겼습니다…
		ENDIF
	ELSE
		IF COND("제복：바니") || RAND:2
			PRINTFORMW 제복 앞가슴을 풀고 편안하게 쉬면서,  %CALLNAME:MASTER%의 다리맛사지에 몸을 맡겼습니다…
		ELSE
			PRINTFORMW 스커트 자락을 손으로 누르면서, %CALLNAME:MASTER%의 다리 맛사지에 몸을 맡겼습니다…
		ENDIF
	ENDIF
	;MASTERに出演플래그
	SETBIT CFLAG:MASTER:95, 1
CASE 6
	PRINTFORMW 개점후에 %CALLNAME:MASTER%은 점내의 모습을 둘러보다가, 펜으로 뭔가를 노트에 끼적이는 %타겟을()% 발견했다. 
	PRINTFORMW 신경쓰여서 들여다보자, 그것은………
	IF TALENT:반하기쉬움 && RANDIF(2, TALENT:호색, TALENT:광기)
		PRINTFORMW 망상넘치는 연애소설이었다…
		PRINTFORMW 들여다보는 것을 깨달은 %CALLNAME:TARGET%가 무서운 기세로 숨긴 덕에 정신적 피해는 적었지만……아우 달달해! 
	ELSEIF TALENT:광기 && RANDIF(2, TALENT:호색)
		IF TALENT:망신
			PRINTFORMW %CALLNAME:TARGET%의 「살해리스트」였다!! ! 
			PRINTFORMW 접객업이라는게 역시 상당히……아니, 그렇다치더라도 리스트의 이름이……너무 지나쳐!! 
		ELSE
			PRINTFORMW 어쩐지 추상화같습니다
			PRINTFORMW 대놓고 보고있어도 %CALLNAME:TARGET%가 전혀 신경쓰지 않기에 잠시 보고있었지만…
			PRINTFORMW 이 그림은, 깊이 관여하지 않는 편이 좋다는 느낌이 든다…
		ENDIF
	ELSEIF TALENT:호색 && RAND:2
		IF COND("다방：섹스") && RAND:2
			PRINTFORMW %CALLNAME:TARGET%가 지금까지 손댄 손님 목록이었다
			PRINTFORMW 손님의 눈에 띄기 전에 그만두게 해야하는 건 아닐지……
		ELSE
			PRINTFORMW 바이트 중에 떠오른 플레이내용인 것같습니다
			PRINTFORMW 잊어먹기 전에 미리 써두는 것이 좋은게 맞긴 하지만… 으-음
		ENDIF
	ELSE
		PRINTFORMW 단순한 업무일지였다
		PRINTFORMW 뭔가 너무 평범해서 맥이빠져버렸다…
	ENDIF

CASE 7
	PRINTFORMW 폐점 후의 점내에서는, 몽마들이 진지한 표정으로%TEXTR("じゃんけん", "交渉", "くじ引き")%를 하고 있었습니다. 
	PRINTFORMW 테이블 위에는……팔리지 않고 남은 케이크가 줄지어 있습니다. 
	PRINTFORMW 그래!  그녀들은 남은 케이크의 우선권을 걸고 있습니다! ! 
	IF RAND:CHARA_NUM("플로어점원") == 0
		PRINTFORMW ……목적의 케이크를손에 넣은 것 같다%CALLNAME:TARGET%가 , 큰 환성을 올렸습니다! ! ! 
		PRINTL 

		SETCOLOR DEF_COLOR("황색")
		PRINTFORMW %CALLNAME:TARGET%의 욕구불만하지만 큰폭으로 해소되었습니다. 
		RESETCOLOR
		CALL CALC, "욕구불만解消", TARGET, 10
	ELSE
		WAITRESS = FIND_COND("출연가능な점원")
		PRINTFORMW 배분이 끝났다%타겟과()%%CALLNAME:WAITRESS%하, 서로의 케이크를 능숙한 일교환하려고 일하고 있을 때보다 진지한 표정으로 서로 이야기하고 있습니다……
	ENDIF

CASE 8
	PRINTFORMW 이제(벌써) 가게의 빛을 꺼, 관에 돌아간다시간(이)가 되었습니다만……%CALLNAME:TARGET%는 의자에 앉아 골아떨어지고 있습니다…
	PRINTFORMW 이대로 방치할 수도 없기 때문에,%CALLNAME:MASTER%하지만 짊어져 돌아가는 일이 되었습니다！
	PRINTFORMW ………
	PRINTFORMW ……
	IF RAND:2 && FIND_COND("출연가능な점원", "연모")
		WAITRESS = FIND_COND("출연가능な점원", "연모")
		PRINTFORMW …옆에서 걷고 있다%CALLNAME:WAITRESS%들은 부러워해, 「다음은%CNAMET(WAITRESS, "一人称")%하지만！」 『아니오 내가……』라고 서로 견제하고 있습니다…
	ELSEIF TALENT:겁쟁이 || TALENT:내성적 || TALENT:부끄럼쟁이
		PRINTFORMW …도중에 눈을 떴다%CALLNAME:TARGET%는 얼굴을 새빨갛게 하면,%CALLNAME:MASTER%의 등에 얼굴을 가라앉혔습니다…
	ELSE
		PRINTFORMW …%CALLNAME:TARGET%는 돌아가는 길의 도중에 눈을 떴지만, 관에 도착할 때까지 특등석을 만끽했습니다.
	ENDIF

ENDSELECT

;出演者同士が仲良くなる
CALL CHANGE_RELATION, 2, TARGET, WAITRESS

WAITRESS = 0

;閉店後이벤트
;(不真面目か真面目で텍스트分岐？)
;疲れて청소の途中で(or片付けの途中で)제복のまま居眠りしている몽마に同僚(or몽마or店長)が웃옷をかけてあげる(or少しだけ寝かせておいてあげるか, と微笑むor紅茶とケーキを持って起こしてあげる)



;(호색/음란/바이/부끄럼쟁이等で텍스트分岐？)
;業務終了後, 同僚と一緒に着替え. 
