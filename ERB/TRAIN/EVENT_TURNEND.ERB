@TEXT_TURNEND
#DIM EJECT_FLAG
#DIM OSOUJI_FLAG

PRINTL 
;--------------------------------------------------------
;촉수で처녀상실
;--------------------------------------------------------
;撃沈判定ON
SIF STATE("撃沈", PLAYER)
	RETURN 0

;처녀상실で촉수が사정
IF TFLAG:15 && NOWEX:처녀상실
	PRINTFORMW %CALLNAME:TARGET%의 비순에서, 더러운 액체가 섞인 파과혈이 추악한 촉수를 타고흘러갔다…
	RETURN 0
;처녀상실で사정なし
ELSEIF TFLAG:15 == 0 && NOWEX:처녀상실
	PRINTFORMW %CALLNAME:TARGET%의 비순에서, 처녀의 상징인 붉은 피가 뚝뚝떨어졌다…
	RETURN 0
ENDIF

;３Ｐ中
SIF COND("３Ｐ")
	RETURN 0

;--------------------------------------------------------
;섹스後に抜く
;--------------------------------------------------------
;抜くかどうかの判断(10ぐらいなら, ほぼ抜く). とりあえず, ペニバンの時には抜かないことに. 
EJECT_FLAG = 0
IF CONFIG("사정後に抜かない") == 0 && SHOOT("내사정") && COND("절정", PLAYER) && INSERT("인서트")
	EJECT_FLAG = NOWEX:동시절정 - TALENT:흡정체질*2

	SIF TALENT:작은몸집
		EJECT_FLAG += 2
	;イってない
	SIF NOWEX:절정수합계 == 0
		EJECT_FLAG -= 10
	SIF RAND:(MAXBASE:정액욕 + 1) >= EJECT_FLAG
		EJECT_FLAG = 0

	;中に出した정액の量
	IF SHOOT("질内")
		EJECT_FLAG += EXP_GAIN(17)
	ELSEIF SHOOT("애널")
		EJECT_FLAG += EXP_GAIN(18)
		;정액관장の時には배설욕게이지ＭＡＸなら抜く
		SIF COND("정액관장") && BASE:배설욕 >= MAXBASE:배설욕
			EJECT_FLAG = 100
	ELSEIF SHOOT("요도")
		EJECT_FLAG += EXP_GAIN(80)
	ELSE
		EJECT_FLAG = 0
	ENDIF
ENDIF

;정액관장の時には배설욕게이지ＭＡＸなら抜く
IF EJECT_FLAG >= 100 || (RAND:3 == 0 && EJECT_FLAG > 0)
	PRINTL 

	VARSET LOCAL
	VARSET LOCALS

	IF SHOOT("질内")
		LOCAL = NOWEX:Ｖ절정
		LOCAL:1 = EXP:Ｖ경험
		LOCAL:2 = ABL:Ｖ확장
		LOCAL:3 = EXP_GAIN(17)

		LOCALS = 질
		LOCALS:1 = 질구
		LOCALS:2 = 보지
	ELSEIF SHOOT("애널")
		LOCAL = NOWEX:Ａ절정
		LOCAL:1 = EXP:Ａ경험
		LOCAL:2 = ABL:Ａ확장
		LOCAL:3 = EXP_GAIN(18)

		LOCALS = 애널
		LOCALS:1 = 애널
		LOCALS:2 = 엉덩이구멍
	ELSEIF SHOOT("요도")
		LOCAL = NOWEX:요도절정
		LOCAL:1 = EXP:요도경험
		LOCAL:2 = ABL:요도확장
		LOCAL:3 = EXP_GAIN(80)

		LOCALS = 
		LOCALS:1 = 배설입
		LOCALS:2 = オシッコの穴
	ENDIF
	IF INSERT("기승위") && (TEQUIP:넘어뜨리기 || ABL:봉사정신 >= 3)
		PRINTFORM %조사처리(CALLNAME:TARGET,"가")% 
		IF LOCAL && RAND:3 == 0
			PRINTFORM 쾌감에 떨리는 허리를 
		ELSEIF LOCAL:1 >= 100 && RAND:2 == 0
			PRINTFORM 뿌리까지 삼켜문 허리를 
		ELSE
			PRINTFORM %CALLNAME:PLAYER%의 가슴에 손을 얹고 허리를 
		ENDIF
		IF BASE:PLAYER:절정 >= 3000 && RAND:2 == 0
			PRINTFORM 기세좋게 
		ELSE
			PRINTFORM 천천히 
		ENDIF
		PRINTFORM 들어올리자, 
	ELSE
		IF COND("절정", PLAYER) >= 2 && RAND:2 == 0
			PRINTFORM 잔뜩 들이부어진 
		ELSEIF NOWEX:절정수합계 && RAND:2 == 0
			PRINTFORM 절정에 떨리는 
		ELSEIF LOCAL > 2 && LOCAL:2 >= 3
			SIF NOWEX:Ｖ절정 >= 2 || NOWEX:Ａ절정 >= 2
				PRINTFORM 쾌감으로 
			PRINTFORM 풀어진 
		ELSE
			PRINTFORM 달라붙어오는 
		ENDIF
		PRINTFORM %LOCALS%에서 페니스를 
		IF BASE:PLAYER:절정 >= 3000 && RAND:2 == 0
			PRINTFORM 기세좋게 
		ELSE
			PRINTFORM 천천히 
		ENDIF
		PRINTFORM 당겨뽑자, 
		IF RAND:3 == 0 && NOWEX:절정수합계 >= 2 && COND("다리 사용")
			IF INSERT("후배위")
				PRINTFORM 네발로 엎드리고 엉덩이를 높이 든 자세인 
			ELSEIF INSERT("굴곡위") && COND("손가락 사용") && RAND:2
				PRINTFORM 흥분한 나머지 자신의 팔로 다리를 껴안아 가랑이를 완전히 오픈하고있는 
			ELSEIF INSERT("정상위") || INSERT("굴곡위")
				PRINTFORM 쾌감에 몸을 맡기고 느슨하게 \@ RAND:2 ? 가랑이 # 다리 \@를 열고있던 
			ENDIF
		ENDIF
		PRINTFORM %CALLNAME:TARGET%의 
	ENDIF
	IF LOCAL >= MAX(2, RAND:5) && EX:절정수합계 >= 10
		PRINTFORM 녹아버린 
	ELSEIF TALENT:작은몸집 && RAND:2 == 0
		PRINTFORM 어린 
	ELSEIF TALENT:작은몸집 && LOCAL:2 <= 1
		PRINTFORM 작은 
	ENDIF

	PRINTFORM %LOCALS:1%에서 

	IF SHOOT("애널") && BASE:배설물 && COND("액체관장") + COND("정액관장")
		PRINTFORM 오물
		IF COND("온천관장")
			PRINTFORM 과 소변
		ELSEIF COND("액체관장")
			PRINTFORM 과 미약
		ENDIF
		PRINTFORM 섞인 탁한 
	ELSEIF NOWEX:처녀상실 || (EX:처녀상실 && RAND:3 == 0)
		PRINTFORM 파과혈이 섞인 
	ELSEIF LOCAL:3 >= 9 && RAND:3 == 0
		PRINTFORM 엄청난 양의 
	ELSEIF LOCAL:3 >= 4 && RAND:3 == 0
		PRINTFORM 주륵 
	ELSEIF LOCAL && RAND:3 == 0
		PRINTFORM 거품섞인 
	ELSEIF RAND:3 == 0
		PRINTFORM 주루륵 
	ELSEIF RAND:2 == 0
		PRINTFORM 주르륵 
	ELSE
		PRINTFORM 하얀 
	ENDIF
	PRINTFORM 정액이 

	IF LOCAL:3 >= 9 && RAND:3 == 0
		PRINTW 역류한다…
	ELSEIF LOCAL:3 >= 4 && RAND:2 == 0
		PRINTW 넘쳐나온다…
	ELSEIF LOCAL:3 >= 4
		PRINTW 밀려나온다…
	ELSEIF RAND:3 == 0
		PRINTW 방울져떨어진다…
	ELSEIF RAND:2 == 0
		PRINTW 흘러나온다…
	ELSE
		PRINTW 쏟아진다…
	ENDIF

	LOCALS:9 = 0
	;イきまくって無いなら, 視線に気づく. そして, サービス. 
	IF SHOOT("애널") && COND("정액관장") && BASE:배설욕 >= MAXBASE:배설욕
		;イっているなら, 快感に浸ったり
		IF NOWEX:절정수합계 && MARK:반발각인 == 0 && ABL:마조끼 >= 3
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
			SIF MAX(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정, NOWEX:Ｍ절정, NOWEX:요도절정) >= 2
				PRINTFORM 격렬하다
			PRINTFORM 절정의 여운에 몸을 맡기고, 느슨한 항문에서 정액에 이어 
			SELECTCASE BASE:배설물
			CASE IS <= 2
				PRINTFORM 굉장한 냄새가 나는 
			CASE IS <= 4
				PRINTFORM 대량의 
			CASE IS <= 6
				PRINTFORM 며칠분은 되겠다 싶은 
			CASEELSE
				PRINTFORM 작은 산을 이룰 정도의 
			ENDSELECT
			PRINTFORM %TEXTR("배설물", "오물")%을 배출하면서 
			IF RAND:2
				PRINTW 기분 좋은 한숨을 토하고있다…
			ELSE
				PRINTW 기분좋아하고있다…
			ENDIF

			;배설した時のSOURCEを加算
			CALL SETFLAG, "배설"
			CALL SOURCE_COM136, 100
			CALL CALC_SOURCE
			CALL MARK_GOT_CHECK

			LOCALS:9 = 질내사정후황홀
		ELSE
			PRINTW ……그리고
			IF NOWEX:절정수합계
				PRINTFORM 절정의 쾌락을 견디지 못한건지, 
			ELSE
				IF CUP:쾌Ａ >= 1000
					PRINTFORM 애널의 자극을 견디지 못했는지, 
				ELSE
					PRINTFORM 변의를 참지 못하게되어버렸는지, 
				ENDIF
			ENDIF
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
			SELECTCASE BASE:배설물
			CASE IS <= 2
				PRINTFORMW 정액으로 물든 장내의 오물을 항문에서 흩뿌리고말았다…
			CASE IS <= 6
				PRINTFORMW 장내에 모여진 수일분은 되어보이는 오물을 정액과 함께 굉장한 기세로 쏟아버렸다…
			CASEELSE
				PRINTFORMW 정액투성이가 된 \@ RAND:2 ? 배설물 # 장내의 오물 \@을 성대하게 뿌리고말았다
				PRINTFORMW 한동안 변비끼가 있었는지, 처음에는 부드러운 것이 굉장한 기세로 튀어나온 후에, \@ RAND:2 ? 아주 단단해보이는 # 하나로 연결된  \@ 굵은 초코바같은 오물이 천천히 항문을 넓히면서 밀려나오고있다…
			ENDSELECT

			;배설した時のSOURCEを加算
			CALL SETFLAG, "배설"
			CALL SOURCE_COM136, 100
			CALL CALC_SOURCE
			CALL MARK_GOT_CHECK

			CALL SELF_KOJO, "플러그無し관장이벤트"
		ENDIF
	ELSEIF (NOWEX:절정수합계 < 12 || PALAM:연속절정COMBO수 < 5 || TALENT:호색 || TALENT:음란) && BASE:기력 > 0 && COND("손가락 사용") && COND("액체관장") == 0 && RAND:2
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 시선을 깨닫고, 

		IF (TALENT:부끄럼쟁이 && PALAM:욕정*(TALENT:연모 + 1) < 30000) || (TALENT:부끄럼없음 == 0 && PALAM:욕정*(TALENT:연모 + 1) < 10000)
			IF CFLAG:2 * ABL:봉사정신 >= 4500 && RAND:3 == 0
				PRINTFORM 부끄러운듯이 웃는 얼굴을 하면서, 
				IF RAND:3 == 0 && LOCAL:3 >= 3
					PRINTFORM 잔뜩 사정당한 
				ELSEIF RAND:2
					PRINTFORM 정액정액투성이인 
				ELSE
					PRINTFORM 사정직후라서 정액을 늘어뜨리고있는 
				ENDIF
				SIF AGE(TARGET) == 1 && SHOOT("요도") == 0
					PRINTFORM 아이같은 
				PRINTFORMW %조사처리(LOCALS:2,"를")% 보기쉽도록 %NAMES("손가락")% 끝으로 벌려보였다…
				LOCALS:9 = 질내사정후벌리기
			ELSEIF CFLAG:2 * ABL:봉사정신 >= 2000 && COND("다리 사용") && RAND:2 == 0
				PRINTFORM 수치심때문에 %조사처리(NAMES("손"),"로")% 얼굴을 가리면서, 
				IF RAND:2
					PRINTFORMW 다리를 벌리고 눅진눅진한 부끄러운 부분을 보여주었다…
				ELSE
					PRINTFORMW 긴장과 이완을 반복하며 정액을 흘리고있는 %조사처리(LOCALS:1,"를")% 보여주었다…
				ENDIF
				LOCALS:9 = 질내사정후벌리기
			ELSE
				PRINTFORMW 서둘러 부끄러운 부분을 %조사처리(NAMES("손"),"로")% 가리고말았다…
				LOCALS:9 = 질내사정후부끄러움
			ENDIF
		ELSEIF TALENT:음란 || (TALENT:부끄럼없음 && TALENT:호색 && PALAM:욕정 >= 3000) || (TALENT:연모 && PALAM:욕정 >= 10000)
			IF LOCAL:2 >= 7 && RAND:5 == 0
				PRINTFORM 훤이 벌어진채인 
			ELSEIF LOCAL:2 >= 5 && RAND:4 == 0
				PRINTFORM 뻥하니 입을 연 
			ELSEIF LOCAL:2 >= 3 && RAND:3 == 0
				PRINTFORM 완전히 열리진않은 
			ELSEIF LOCAL:3 >= 4 && RAND:3 == 0
				PRINTFORM 질척질척한 
			ELSEIF LOCAL:1 >= 100 && RAND:3 == 0
				PRINTFORM 말려나온 
			ELSEIF ( (SHOOT("질内") && EX:Ｖ절정 >= 3) || (SHOOT("애널") && EX:Ａ절정 >= 3) ) && RAND:3 == 0
				PRINTFORM 빨갛게 부은 
			ELSEIF PALAM:욕정 >= 3000 && RAND:3 == 0
				PRINTFORM 발정한 
			ELSEIF LOCAL:2 >= 2 && RAND:3 == 0
				PRINTFORM 느슨한 
			ELSEIF TALENT:작은몸집 && RAND:2 == 0
				PRINTFORM 어린 
			ELSEIF LOCAL:1 <= 50 && RAND:3 == 0
				PRINTFORM 귀여운 
			ELSEIF SHOOT("질内") && PALAM:Ｖ윤 >= 3000
				PRINTFORM 질퍽질퍽한 
			ENDIF

			PRINTFORM %LOCALS:2%

			;정액を味わう
			IF ABL:정액중독 > RAND:10
				IF LOCAL:3 >= 9
					PRINTFORM 에허 흘러넘치는 대량의 정액을 %조사처리(NAMES("손"),"로")% 받아서, 
				ELSEIF LOCAL:3 >= 4
					PRINTFORM 에서 늘어진 정액을 %조사처리(NAMES("손가락"),"로")% 떠서, 
				ELSE
					PRINTFORM 에 %NAMES("손가락")% 끝을 집어넣어 정액을 긁어내, 
				ENDIF
				IF ABL:정액중독 == 5 && RAND:2 == 0
					PRINTFORM 황홀한 미소를 지으면서 
				ELSEIF ABL:정액중독 >= 3 && RAND:2 == 0
					PRINTFORM 흥분한 표정으로 
				ELSE
					PRINTFORM 맛있게 
				ENDIF
				IF LOCAL:3 >= 9 && RAND:2 == 0
					PRINTFORM 목을 축이는 
				ELSEIF LOCAL:3 >= 4
					PRINTFORM 맛보는 
				ELSE
					PRINTFORM 핥는 
				ENDIF
				PRINTW 모습을 과시해왔다…
				;입に정액汚れ
				CALL ADD_STAIN, TARGET, "Ｍ", "정액"
				LOCALS:9 = 질내사정후정액퍼내기
			;くぱぁ
			ELSE
				IF RAND:2 == 0
					PRINTFORM %조사처리(LOCALS:2,"를")% 잡아벌려 
				ELSE
					PRINTFORM %조사처리(LOCALS:2,"를")% %조사처리(NAMES("손가락"),"로")% 열어, 
				ENDIF
				IF ( (SHOOT("질内") && TALENT:음호) || (SHOOT("애널") && TALENT:애널광)  || (SHOOT("요도") && TALENT:요도광) ) && RAND:3 == 0
					PRINTFORM 탐욕스럽게 꿈틀대는 
				ELSEIF ( (SHOOT("질内") && ABL:Ｖ감각 >= 3) || (SHOOT("애널") && ABL:Ａ감각 >= 3)  || (SHOOT("요도") && TALENT:요도성감) ) && RAND:3 == 0
					PRINTFORM 부들부들거리는 
				ELSE
					PRINTFORM 정액투성이 
				ENDIF
				IF SHOOT("요도")
					PRINTFORM 모습을 
				ELSEIF LOCAL:2 >= 3 && RAND:3 == 0
					PRINTFORM 고기동굴
					IF ( (SHOOT("질内") && TALENT:음호) || (SHOOT("애널") && TALENT:애널광) ) && RAND:3 == 0
						IF SHOOT("질内")
							PRINTFORM  안쪽의 자궁구까지 
						ELSE
							PRINTFORM  안쪽까지
						ENDIF
					ELSE
						PRINTFORM 을 
					ENDIF
				ELSEIF PALAM:욕정 >= 3000 && RAND:2 == 0
					IF SHOOT("질内")
						PRINTFORM 질내를 
					ELSE
						PRINTFORM 직장의 점막을 
					ENDIF
				ELSE
					PRINTFORM 모습을 
				ENDIF
				PRINTW 과시해왔다…
				LOCALS:9 = 질내사정후벌리기
			ENDIF
		ELSE
			IF MARK:반발각인
				PRINTW 허둥지둥 자세를 바로고치기 시작했다…
				LOCALS:9 = 질내사정후부끄러움
			ELSE
				PRINTFORM 다리를 더욱 벌려, 
				IF AGE(TARGET) < 3 || ABL:Ａ확장 < 2 || SHOOT("애널") || RAND:3 == 0
					PRINTFORM 부끄러운 부분
				ELSEIF SHOOT("요도")
					IF PENIS(TARGET) == 0 && CALCF("Ｃ발정") >= 3 && RAND:4 == 0
						IF TALENT:클리피어스 && RAND:2
							PRINTFORM 피어스가 세련된 \@ TALENT:음핵 ? 자랑스러운 # 충혈된\@ 음핵의 발기상태
						ELSE
							PRINTFORM 격렬한 흥분으로 볼록 부풀어 오르고 있다 클리토리스
						ENDIF
					ELSEIF TALENT:호색 + TALENT:음란 && RAND:3 == 0
						PRINTFORM 느슨해져 오줌을 흘리면서 실룩거리는 오줌의 구멍
					ELSEIF TALENT:요도성감 && RAND:3 == 0
						PRINTFORM 주어진 쾌감으로 야무지지 못하게 벌어지고 있다입
					ELSEIF RAND:2
						PRINTFORM 범해진지 얼마 안 되는으로 바보가 되어 연 채로의 점막
					ELSE
						PRINTFORM 묻고 있던 것이 없어져입하지만 다 닫을 수 있지 않은 오줌의 구멍
					ENDIF
				ELSE
					IF PENIS(TARGET) == 0 && CALCF("Ｃ발정") >= 3 && RAND:4 == 0
						IF TALENT:클리피어스 && RAND:2
							PRINTFORM 피어스가 세련된 \@ TALENT:음핵 ? 자랑스러운 # 충혈된\@ 음핵의 발기상태
						ELSE
							PRINTFORM 격렬한 흥분으로 볼록 부풀어 오르고 있다 클리토리스
						ENDIF
					ELSEIF TALENT:호색 + TALENT:음란 && RAND:3 == 0
						PRINTFORM 거품나는 점액을 흘리며 꿈틀대는 보지
					ELSEIF ABL:Ｖ감각 >= 3 && RAND:3 == 0
						PRINTFORM 쾌감에 느슨하게 피어나기 시작하는 보지
					ELSEIF RAND:2
						PRINTFORM 범해져서 충혈된 음순이 열린채인 보지
					ELSE
						PRINTFORM 묻혀있던 물건이 없어져 입이 닫히지않는 질구
					ENDIF
				ENDIF
				PRINTW 를 과시해왔다…
				LOCALS:9 = 질내사정후벌리기
			ENDIF
		ENDIF
	;イっているなら, 快感に浸ったり
	ELSEIF NOWEX:절정수합계 && MARK:반발각인 == 0
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF MAX(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:Ａ절정, NOWEX:Ｂ절정, NOWEX:Ｍ절정, NOWEX:요도절정) >= 2
			PRINTFORM 격렬한 
		PRINTFORM 절정의 여운에 몸을 맡기고, 
		IF NOWEX:절정실금 && PENIS(TARGET) == 0
			PRINTFORM 벌어진 가랑이에서 오줌을 \@ RAND:2 ? 졸졸 # 줄줄 \@ 흘리면서 
		ELSEIF COND("다리 사용") == 0 || RAND:2 == 0 || TEQUIP:구속
		ELSE
			PRINTFORM 단정치 못하게 다리를 벌리며
		ENDIF
		IF RAND:2
			PRINTW 기분좋은 듯한 한숨을 토하고있습니다…
		ELSE
			PRINTW 기분좋은 듯한 얼굴을 하고있습니다…
		ENDIF
		LOCALS:9 = 질내사정후황홀
	ENDIF

	;몽마の台詞（内容は, 上で決定）
	SIF LOCALS:9 != ""
		CALL SELF_KOJO, LOCALS:9

	;청소펠라(삽입後)
	CALL OSOUJI_F

	CALL RESET_INSERT, TARGET, PLAYER
	RETURN 0
ENDIF

;청소펠라…毎回だとウザいかな？
OSOUJI_FLAG = 0
SIF SHOOT("질内") && CHECK_STAIN(STAIN:MASTER:Ｐ, "Ｖ")
	OSOUJI_FLAG = 1
SIF SHOOT("애널") && CHECK_STAIN(STAIN:MASTER:Ｐ, "Ａ")
	OSOUJI_FLAG = 2
SIF SHOOT("요도") && CHECK_STAIN(STAIN:MASTER:Ｐ, "尿")
	OSOUJI_FLAG = 3

IF OSOUJI_FLAG && COND("절정", PLAYER) && INSERT("인서트") == 0 && PENIS(PLAYER)
	CALL OSOUJI_F
ELSEIF TFLAG:8 && PENIS(PLAYER) && STAIN:PLAYER:Ｐ != SET_STAIN(0, "Ｐ")
	;두개펠라はひとまず中止
	;IF SELECTCOM == 
	;	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%と%CALLNAME:ASSI%의 페니스を交互に咥え, 汚れを舐め取っていった…
	;ELSE
		SIF ASSI && SELECTCOM:1 == SELECTCOM
			PRINTFORM %조사처리(CALLNAME:ASSI,"과")% 
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF (ABL:순종 >= 4 || ABL:정액중독 >= 2) && COND("절정", PLAYER)
			 PRINTFORM 남은 정액을 빨아내면서, 
		PRINTW 페니스의 더러운 것을 깨끗하게 핥아내었다…
	;ENDIF

	TFLAG:47 = 1
	CALL SELF_KOJO, "청소펠라"

	IF ASSI && SELECTCOM:1 == SELECTCOM
		SWAP TARGET, ASSI
		TFLAG:47 = 1
		CALL SELF_KOJO, "청소펠라"
		SWAP TARGET, ASSI

		IF (ABL:순종 >= 4 || ABL:정액중독 >= 2) && (ABL:ASSI:순종 >= 4 || ABL:ASSI:정액중독 >= 2)
			PRINTW 두사람은 그것만으로는 발모자란 듯이 서로의 입에 고인 정액을 핥고있다…
			CALL EQ_STAIN, TARGET, ASSI, 0, 0
		ENDIF
	ENDIF

	;조수のためにPLAYERのＰ汚れを記録
	LOCAL = STAIN:PLAYER:Ｐ

	;経験や汚れなどの処理は全部ここで行う
	CALL SETFLAG, "청소펠라", TARGET, MASTER

	PRINTFORML 펠라경험＋１
	PRINTFORML 청소펠라경험＋１
	PRINTL 

	IF ASSI && SELECTCOM:1 == SELECTCOM
		STAIN:PLAYER:Ｐ = LOCAL

		CALL SETFLAG, "청소펠라", ASSI, MASTER

		PRINTFORML 펠라경험＋１ (%TEXTS("조수の명칭")%)
		PRINTFORML 청소펠라경험＋１ (%TEXTS("조수の명칭")%)
		PRINTL 
	ENDIF

	RETURN 0
ENDIF

;--------------------------------------------------------
;조수のお仕事としての청소펠라等
;--------------------------------------------------------
CALL EVENT_ASSI_AUTO, "お청소"

;--------------------------------------------------------
;커맨드텍스트 その２
;--------------------------------------------------------
SIF FLAG:6 && TFLAG:90 == 0
	CALL TEXT_B
;--------------------------------------------------------
;自発脱ぎ(체력が限界ならしてこない)
;--------------------------------------------------------
IF BASE:체력 >= 500
	CALL EVENT_DATUI
	CALL EVENT_ZURENAOSHI
ENDIF
IF TEQUIP:Ｗ넘어뜨리기
	SWAP TARGET, TARGET:1
	SWAP SELECTCOM, SELECTCOM:1
	SWAP PREVCOM, PREVCOM:1
	IF BASE:체력 >= 500
		CALL EVENT_DATUI
		CALL EVENT_ZURENAOSHI
	ENDIF
	SWAP TARGET, TARGET:1
	SWAP SELECTCOM, SELECTCOM:1
	SWAP PREVCOM, PREVCOM:1
ENDIF


@TEXT_B
;--------------------------------------------------------
;애무
;--------------------------------------------------------
IF SELECTCOM == 0
	SELECTCASE CUP:쾌Ｃ + CUP:쾌Ｂ + CUP:쾌Ｍ
	CASE IS < 100
		IF TALENT:반항적
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애무를 당하면서도 반항적인 태도를 취하고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 간지러운 듯이 몸을 비비꼬지만 쾌락을 느끼기엔 모자란 모양이다. 
		ENDIF
	CASE IS < 300
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 이쪽의 애무에 반응해 몸을 떨고있다. 
	CASE IS < 1000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 가벼운 전격이라도 몸에 흐르는 듯이 작게 떨었다. 
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 명백하게 쾌감을 느끼는 모양새로 요염한 신음소리를 내고있다. 
	CASE IS < 6000
	 	IF COND("손가락 사용")
			IF RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 어떻게든 쾌락을 억누르려는듯이, 이쪽에 매달려왔다. 
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애무에 격렬하게 쾌락을 느끼고, 스스로 조르듯이 팔을 목에 감아왔다. 
			ENDIF
		ELSE
			IF RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 어떻게든 쾌락을 억누르려는 듯이, 딱딱하게 몸에 힘을 주었다. 
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애무에 격렬하게 쾌락을 느끼고, 스스로 조르듯이 몸을 맡겨왔다. 
			ENDIF
		ENDIF
	CASE IS < 10000
		IF RAND:2 == 0
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 이쪽의 손가락이 몸 위를 기어갈 때마다 부들부들 몸을 떨고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 이쪽의 애무만으로 강렬하게 느껴대며, 더욱더 자극을 갈구하면서 몸을 맡겨왔다. 
		ENDIF
	CASEELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 이쪽의 손가락이 움직이는 것만으로도 신체를 꿈틀꿈틀 튕기면서 
		IF TALENT:부끄럼쟁이
			PRINTL 얼굴을 새빨갛게 붉히고, 어떻게든 신음을 억누르려는 쓸데없는 노력을 하고있다. 
		ELSEIF TALENT:쾌감에솔직 || TALENT:해방
			PRINTL 완전히 쾌감에 녹는 듯한 표정으로, 더욱더 애원하듯이 몸을 문질러왔다. 
		ELSEIF (TALENT:쾌감을부정 || TALENT:억압) && COND("손가락 사용")
			PRINTL 고개를 붕붕 저으면서도, 지나친 쾌감에 매달려왔다. 
		ELSE
			PRINTL 끊임없이 신음소리를 내고있다. 
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;컨닐링구스
;--------------------------------------------------------
IF SELECTCOM == 1
	;새드끼3이상か새드持ちの몽마に押し倒されて生안면기승をされると, 汚れを舐め取る破目に
	IF TEQUIP:넘어뜨리기 && CHECK_CLO("Ｖ노출") && ABL:새드끼 >= 3 && CHECK_STAIN(STAIN:Ｖ, "정액", "Ａ", "尿") && RAND:10 == 0
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 보지를 
		SIF TALENT:하피 == 0
			PRINTFORM %조사처리(NAMES("손가락"),"로")%  벌리고, 
		PRINTFORM 혀로 깨끗하게
		IF ABL:새드끼 >= 5 && RAND:5 > 0
			PRINTW 하도록 재촉했다. 
		ELSE
			PRINTW 하고싶어하는 것같다…
		ENDIF
		PRINTFORM %조사처리(CALLNAME:PLAYER,"는")% 
		IF TALENT:PLAYER:불결무시 == 0 && (CHECK_STAIN(STAIN:Ｖ, "정액", "Ａ") || RAND:2 == 0)
			PRINTW 열심히 깨끗하게 해주었다…
		ELSE
			SIF COND("♀에약함", PLAYER)
				PRINTFORM 탐하듯이 
			PRINTW 더러운 것을 핥아서 깨끗하게 해주었습니다…
		ENDIF
		STAIN:Ｖ = SET_STAIN(0, "Ｖ")
	ELSE
		SELECTCASE CUP:쾌Ｃ
		CASE IS < 100
			PRINTFORML %CALLNAME:TARGET%의 의 그곳은 아직 촉촉하게 닫힌채다. 쾌락을 느끼기에는 아직 부족한지도 모른다. 
		CASE IS < 1000
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 컨닐링구스의 자극에 가만히 견디고있지만, 확실하게 느끼고있는 것같다. 
		CASE IS < 3000
			PRINTFORM \@ PENIS(TARGET) ? 페니스의 뿌리를 # 민감한 콩을 \@ 핥는 것으로 느끼는 건지, %조사처리(CALLNAME:TARGET,"는")% 
			SIF TALENT:부끄럼쟁이
				PRINTFORM 수치심으로 뺨을 새빨갛게 물들이고 손으로 자기입을 억누르고 소리가 새어나오는 것을 참고, 
			IF TALENT:쾌감에솔직 || TALENT:해방
				PRINTL 뜨거운 한숨을 내쉬면서 쾌락에 몸을 맡기고있다. 
			ELSEIF TALENT:쾌감을부정 || TALENT:억압
				PRINTL 쾌락을 부정하듯이 고개를 내저으며 참고있다. 
			ELSE
				PRINTL 비소에서 애액을 흘리면서 작은 한숨을 흘렸다. 
			ENDIF
		CASE IS < 6000
			PRINTFORM %CALLNAME:TARGET%의 \@ PENIS(TARGET) ? 보지부근 # 민감한 콩 \@을 혀로 문질러올려주자, %조사처리(CALLNAME:TARGET,"는")% 
			SIF TALENT:부끄럼쟁이
				PRINTFORM 수치심으로 뺨을 빨갛게 물들이고, 지나친 쾌락을 참을 수 없는지 
			IF TALENT:쾌감에솔직 || TALENT:해방
				PRINTL 부들부들 몸을 떨면서 덮쳐오는 쾌락에 몸부림쳤다. 
			ELSEIF TALENT:쾌감을부정 || TALENT:억압
				PRINTL 쾌락을 부정하듯이 고개를 젓고, 앙다문 입술 사이로 달콤한 한숨을 흘리고있다. 
			ELSE
				PRINTL 사랑스러운 소리를 흘리면서 부들부들 몸을 떨었다. 
			ENDIF
		CASE IS < 10000
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 명백하게 쾌락의 소리를 지르면서, 
			IF COND("손가락 사용") && RAND:2
				PRINTFORML 무의식 중에 이쪽의 머리를손그리고 눌러 한층 더 그 부분을 강압해 왔다.
			ELSE
				PRINTFORML 무의식 중에\@ TEQUIP:넘어뜨리기 == 0 ? 腰を浮かして # \@사타구니를 강압해 왔다.
			ENDIF
		CASEELSE
			IF TALENT:Ｃ민감
				PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 민감한\@ PENIS(TARGET) ? 남근 # 클리토리스 \@에 가해지는 자극에 몸부림치고, 
				SIF TALENT:부끄럼쟁이 && COND("손가락 사용")
					PRINTFORM 도를 넘어선 수치심에 새빨개진 얼굴을 숨기듯이 두손으로 가리면서도 억눌러지지 않는 듯이 
				IF TALENT:쾌감에솔직 || TALENT:해방
					PRINTL 격렬하게 신음을 지르면서 미친듯이 몸부림쳤다. 
				ELSEIF TALENT:쾌감을부정 || TALENT:억압
					PRINTL 깨물은 입술사이로 고통의 소리와도 비슷한 쾌락의 소리를 내면서 몸부림쳤다. 
				ELSE
					PRINTL 얌전치못한 소리를 계속 질러댔다. 
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%하\@ PENIS(TARGET) ? 敏感な場所 # 클리토리스 \@에의 자극에,
				SIF TALENT:부끄럼쟁이 && COND("손가락 사용")
					PRINTFORM 도를 넘어선 수치심에 새빨개진 얼굴을 숨기듯이 두손으로 가리면서도 억눌러지지 않는 듯이 
				IF TALENT:쾌감에솔직 || TALENT:해방
					PRINTL 격렬하게 신음을 지르면서 미친듯이 몸부림쳤다. 
				ELSEIF TALENT:쾌감을부정 || TALENT:억압
					PRINTL 꽉 깨문 이빨 사이로 고통의 소리와도 비슷한 쾌락의 소리를 내면서 몸부림쳤다. 
				ELSE
					PRINTL 격렬하게 신음소리를 내면서 계속 몸부림쳤다. 
				ENDIF
			ENDIF
		ENDSELECT
	ENDIF
ENDIF
;--------------------------------------------------------
;애널애무
;--------------------------------------------------------
IF SELECTCOM == 2
	SELECTCASE CUP:쾌Ａ
	CASE IS < 1000
		IF CUP:고통 >= 300
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널을 만져지는 것에 익숙하지 않은지 아픔에 몸을 비비 꼬았따. 
			IF ABL:Ａ감각 < 3
				PRINTL 천천히 개발하면 되겠지. 
			ELSE
				PRINTL 몸의 준비가 부족한 모양이다. 
			ENDIF
		ELSE
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 작게 몸을 떨었다. 
			IF ABL:Ａ감각 < 3
				PRINTL 좀더 개발해 줄 필요가 있어보인다. 
			ELSE
				PRINTL 쾌락을 얻기위해서는 좀더 몸을 풀어줄 필요가 있을 것이다. 
			ENDIF
		ENDIF
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 작게 교성을 내면서 몸을 퉁겼다. 
		SIF TALENT:Ａ민감
			PRINTFORM 애초에 애널의 자극에 약했던 것인지, 
		PRINTL 멱백하게 쾌락을 느끼기 시작하고있다…
	CASE IS < 6000
		IF RAND:2 && ABL:Ａ확장 >= 2 && COND("애널栓") == 0
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널로 %CALLNAME:PLAYER%의 손가락을 간단히 받아들이고, 쾌락에 몸부림치고있다…
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널의 자극에 명백하게 쾌락을 느끼는 모양이다. 
		ENDIF
	CASE IS < 10000
		IF TALENT:Ａ민감
			PRINTFORM 민감한 애널을 괴롭혀주자 
		ELSE
			PRINTFORM 애널에 몰려오는 자극에 
		ENDIF
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 저도 모르게 신음소리를 내면서 
		IF TALENT:쾌감에솔직 || TALENT:해방
			PRINTL 쾌락을 호소했다. 
		ELSEIF TALENT:쾌감을부정 || TALENT:억압
			PRINTL 필사적으로 쾌락을 부정했다. 
		ELSE
			PRINTL 미친듯이 몸부림쳤다. 
		ENDIF
	CASEELSE
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 이미 개발되어버린 애널을 만져주자, 참을 수 없는 듯이 비명섞인 신음을 내면서 미친 듯이 몸부림쳤다. 
	ENDSELECT
ENDIF
;--------------------------------------------------------
;자위
;--------------------------------------------------------
IF SELECTCOM == 3
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	SIF TALENT:부끄럼쟁이 && ABL:노출벽 <= 3
		PRINTFORM 수치심에 얼굴을 새빨갛게 물들이고, 
	SIF TALENT:부끄럼쟁이 && ABL:노출벽 <= 1
		PRINTFORM 눈물을 짓고 
	SIF TALENT:작은몸집
		PRINTFORM 작은 몸에 
	SIF TALENT:작은몸집 == 0 && (TEQUIP:바이브 || TEQUIP:애널바이브)
		PRINTFORM 자신의 안에 
	IF TEQUIP:바이브 && TEQUIP:애널바이브 && GETBIT(CHECK_ONANIE(SELECTCOM), 1) && GETBIT(CHECK_ONANIE(SELECTCOM), 2)
		PRINTFORM ２책의바이브를 물고 있는 추잡한 모양을 보이면서, 
	ELSEIF TEQUIP:바이브 && GETBIT(CHECK_ONANIE(SELECTCOM), 1)
		PRINTFORM 들어가있는 바이브를 들고, 
	ELSEIF TEQUIP:애널바이브 && GETBIT(CHECK_ONANIE(SELECTCOM), 2)
		PRINTFORM 찔러들어가있는 애널바이브를 들고, 
	ENDIF
	SIF TALENT:거유 && (ABL:노출벽 == 3 || ABL:노출벽 == 4)
		PRINTFORM 도발하듯이 풍만한 가슴의 끝부분을 핥으면서 
	SIF TALENT:거유 == 0 && ABL:노출벽 == 4
		PRINTFORM 노출의 쾌감에 취한 표정을 지으면서 
	SIF ABL:노출벽 == 5
		PRINTFORM 전부 보여지는 쾌감에 눈뜬건지, 음탕한 표정을 짓고 스스로 애액이 끝도없이 넘쳐나는 비소를 잡아벌려보이면서 
	SIF TEQUIP:비디오카메라
		PRINTFORM 비디오카메라를 향해 
	PRINTL 자위를 계속하고있다. 
	SELECTCASE CUP:쾌Ｃ + CUP:쾌Ｖ + CUP:쾌Ａ + CUP:쾌Ｂ
	CASE IS < 300
		PRINTFORML 그 %NAMES("손가락")% 움직임은 아직 어색하고, 쾌감보다도 수치심이 강해보인다. 
	CASE IS < 1000
		PRINTL 때때로 작게 몸을 떠는 모습은 확실히 쾌감을 느끼기 시작하는 것같지만 아직은 몰입할 수는 없는 모양이다. 
	CASE IS < 3000
		PRINTL 그 표정에는 명백하게 쾌감의 조짐이 보이고, 입에서는 뜨거운 한숨이 흘러나오고있다. 
	CASEELSE
		IF ABL:자위중독 < 3
			PRINTL 그 표정에는 명백하게 쾌감의 조짐이 보이고, 입에서는 뜨거운 한숨이 흘러나오고있다. 
		ELSE
			SELECTCASE CUP:쾌Ｃ + CUP:쾌Ｖ + CUP:쾌Ａ + CUP:쾌Ｂ
			CASE IS < 6000
				PRINTL 그리고 참을 수 없는 쾌락의 소리를 지르면서 자위행위에 몰두하고있다. 
			CASE IS < 10000
				PRINTL 그리고 자위행위를 계속하면서 쾌락에 신체를 퉁기며, 등으로 브릿지를 만들면서 스스로 성기를 계속 만지고있다. 
			CASEELSE
				PRINTL 지나친 쾌락에 모든 것을 잊은 녹는 듯한 표정으로 찌걱찌걱 소리를 내면서 성기를 걔속 만지고있다. 놔두면 언제까지고 계속할 것만같다. 
			ENDSELECT
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;펠라한다
;--------------------------------------------------------
IF SELECTCOM == 4
	IF TEQUIP:넘어뜨리기
		IF ABL:새드끼 >= 5 && STAIN:Ｐ !=  SET_STAIN(0, "Ｐ") && RAND:3
			PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 더러워진 페니스를 %CALLNAME:PLAYER%의 입안에 쓱쓱 문질러, 더러운 것을 제거하였다…
			STAIN:Ｐ =  SET_STAIN(0, "Ｐ")
		;PLAYERが당신なら, 불결무시が필요
		ELSEIF NO:PLAYER == 0 && ABL:PLAYER:기교 + ABL:PLAYER:마조끼 >= 5 && TALENT:PLAYER:불결무시 && STAIN:Ｐ !=  SET_STAIN(0, "Ｖ") && RAND:3
			PRINTFORM %조사처리(CALLNAME:PLAYER,"는")%, 
			IF CHECK_STAIN(STAIN:Ｐ, "Ａ")
				PRINTFORM 장액으로 더러워진 
			ELSEIF CHECK_STAIN(STAIN:Ｐ, "정액")
				PRINTFORM 정액투성이가 된 
			ELSEIF CHECK_STAIN(STAIN:Ｐ, "Ｖ")
				PRINTFORM 애액으로 더러워진 
			ENDIF
			PRINTFORMW %CALLNAME:TARGET%의 페니스를 정성스레 혀로 청소해서, 꿀꺽 삼켰다…
			CALL RESET_STAIN_EX, "Ｐ", TARGET
			STAIN:PLAYER:Ｍ = 0
		ELSEIF ABL:PLAYER:기교 + ABL:PLAYER:봉사정신 + ABL:PLAYER:마조끼 >= 8 && STAIN:Ｐ != 2 && RAND:3
			PRINTFORM %조사처리(CALLNAME:PLAYER,"는")% , 
			IF CHECK_STAIN(STAIN:Ｐ, "Ａ")
				PRINTFORM 장액으로 더러워진 
			ELSEIF CHECK_STAIN(STAIN:Ｐ, "정액")
				PRINTFORM 정액투성이가 된 
			ELSEIF CHECK_STAIN(STAIN:Ｐ, "Ｖ")
				PRINTFORM 애액으로 더러워진 
			ENDIF
			PRINTFORMW %CALLNAME:TARGET%의 페니스를 정성스레 혀로 청소해서, 꿀꺽 삼켰다…
			STAIN:Ｐ = SET_STAIN(0, "Ｖ")
			STAIN:PLAYER:Ｍ = 0
		ENDIF
	ENDIF
ENDIF
;--------------------------------------------------------
;가슴애무
;--------------------------------------------------------
IF SELECTCOM == 5
	SELECTCASE CUP:쾌Ｂ
	CASE IS < 100
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 아직 가슴으로 쾌락을 느낄 수준은 아닌 것같다. 
	CASE IS < 1000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 가슴으로의 자극에, 간지러운 듯이 몸을 비비꼬았다. 
	CASE IS < 3000
		PRINTFORML %CALLNAME:TARGET%의 가슴을 계속 주물러주자 유두가 단단해지고, 뜨거운 한숨을 흘리기시작했다. 
	CASE IS < 6000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 가슴 애무에 황홀한 표정이 되어 멍해졌다. 
	CASE IS < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF TALENT:빈유
			PRINTFORM 그 작게 흔들리는 가슴으로 
		PRINTFORM 완전히 느껴버려, 가슴을 자극하는 것만으로 극한까지 절정하고있다.  
	CASEELSE
		IF RAND:2 == 0 && CALCF("Ｂ발정")
			PRINTFORM %CALLNAME:TARGET%의 딱딱하게 선 유두를 꼬집어주자, %조사처리(CALLNAME:TARGET,"는")% 
			IF TALENT:거유
				PRINTL 출렁출렁 거유를 흔들며, 지독한 쾌락에 몸부림쳤다. 
			ELSE
				PRINTL 등골을 마음껏 젖히고, 강렬한 쾌감에 몸을 떨었다. 
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 가슴을 계속 주물러주자 그것만으로 엄청나게 느껴 몸을 퉁기며, 
			IF TALENT:거유
				PRINTL 눈앞에서 커다란 가슴을 흔들어 이쪽의 눈을 즐겁게 해주고있다. 
			ELSE
				PRINTL 풀어진 표정으로 쾌락에 몸을 맡기고있다. 
			ENDIF
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;키스
;--------------------------------------------------------
IF SELECTCOM == 6
	SELECTCASE CUP:쾌Ｍ
	CASE IS < 1000
		IF MARK:반발각인 == 3 && CUP:반감 >= 500
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 키스를 받고, 기분나쁜 듯이 얼굴을 찡그리고있다. 
		ELSEIF MARK:반발각인 == 3 && CUP:반감 >= 100
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 싫다는 듯이 키스를 거부하고있다…. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:PLAYER,"가")% 입안을 혀로 휘저어 주면, %조사처리(CALLNAME:TARGET,"는")% \@ ABL:혀놀림 == 0 ? ぎこちなく # \@혀를 얽어라고 왔다. 
		ENDIF
	CASE IS < 5000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 입안을 애무해주자, 자신도 모르게 혀를 얽어왔다…. 
	CASE IS < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF TALENT:음탕한입술
			PRINTFORM 탐욕스럽게 
		PRINTL 입안으로의 자극을 받아들이고, 그것만으로 가버릴 것같이 되어버렸다….  
	CASEELSE
		IF NOWEX:Ｍ절정
			IF RAND:2 == 0
				PRINTFORM %CALLNAME:TARGET%의 입 안을 혀로 유린해주자, %조사처리(CALLNAME:TARGET,"는")% 
				IF TALENT:음탕한입술
					IF TALENT:젖기쉬움
						PRINTL 지나친 쾌감에 연달아 물을 뿜어내면서도, 탐욕스럽게 혀를 얽어왔다. 
					ELSE
						PRINTL 지나친 쾌감에 연달아 절정하면서도, 탐욕스럽게 혀를 얽어왔다. 
					ENDIF
				ELSEIF TALENT:젖기쉬움
					PRINTL 연달아 물을 뿜어내면서, 지나친 절정에 몸을 흠칫흠칫 퉁겼다. 
				ELSE
					PRINTL 지나친 쾌락에 다리를 떨면서, 연달아 절정에 달했다. 
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의 입 안을 유린해주자 그것만으로 마구 느껴버려 몸을 퉁기며, 
				IF TALENT:음탕한입술
					IF TALENT:젖기쉬움
						PRINTL 실금하듯이 푸슛푸슛 물을 뿜으면서, 정신없이 혀를 얽어왔다. 
					ELSE
						PRINTL 지나친 쾌락에 우스울 정도로 다리를 떨면서도, 정신없이 혀를 얽어왔다. 
					ENDIF
				ELSEIF TALENT:젖기쉬움
					PRINTL 마치 실금하듯이 푸슛푸슛 물을 뿜으면서, 지나친 쾌락에 다리를 떨고있다. 
				ELSE
					PRINTL 지나친 쾌락에 다리를 떨면서, 연달아 절정했다. 
				ENDIF
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 입 안을 혀로 유린해주자, %조사처리(CALLNAME:TARGET,"는")% 지나치게 강렬한 쾌감에, 혀를 섞는 일마저 잊고 부들부들 몸을 퉁겼다. 
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;손가락넣기＆Gスポット刺激
;--------------------------------------------------------
IF SELECTCOM == 8
	IF NOWEX:Ｖ절정 && TALENT:부끄럼쟁이 && TEQUIP:재갈 == 0 && EXP_GAIN(71) == 0
		IF TEQUIP:바이브
			PRINTFORMW 쾌감으로 녹아내리는 %CALLNAME:TARGET%의 입가에 애액으로 뿌리까지 번들거리는 바이브를 내밀자, %조사처리(CALLNAME:TARGET,"는")% 부끄러운 듯이 할짝 핥았다…
			;몽마の입に몽마の보지の汚れが追加
			CALL EQ_STAIN, TARGET, TARGET, 0, 1
			STAIN:손 = 0
		ELSE
			PRINTFORML 쾌감으로 녹아내리는 %CALLNAME:TARGET%의 입가에 보지에서 뽑아낸 손가락을 내밀자, %조사처리(CALLNAME:TARGET,"는")% 부끄러운 듯이 할짝 핥았다…
			;몽마の입⇔조교자の손가락の汚れが移動
			CALL EQ_STAIN, TARGET, PLAYER, 0, 1
		ENDIF
	ENDIF
ENDIF
;--------------------------------------------------------
;로터
;--------------------------------------------------------
IF SELECTCOM == 30
	SELECTCASE CUP:쾌Ｃ
	CASE IS < 300
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 아직 쾌락을 얻는데까지는 이르지 못한 모양이다. 
	CASE IS < 1000
		PRINTFORML %CALLNAME:TARGET%의 \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@에 로터를 밀어붙이자 작은 비명을 지르면고 움찔 몸을 튕겼다. 
	CASE IS < 3000
		IF TALENT:쾌감에솔직
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 로터의 자극에 기분좋은 듯이 쾌락을 받아들이고 있다. 
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@에 로터를 밀어붙이자 쾌락의 소리를 내면서 자극에서 도망치려는 듯이 몸을 비틀었다. 
		ENDIF
	CASE IS < 6000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@를 완전히 자극당해, 격렬한 쾌락에 온몸을 퉁겼다. 
	CASE IS < 10000
		PRINTFORML %CALLNAME:TARGET%의 \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@를 로터로 자극해주자 격렬한 쾌락에 뒹굴며, 허리를 음란하게 그라인드시켰다. 
	CASEELSE
		IF RAND:2 == 0
			IF TALENT:젖기쉬움
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 로터가 밀어붙여지자, 물을 내뿜으면서 몸부림쳤다. 
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 로터가 밀어붙여지자, 연달아 절정에 도달해, 부들부들 몸을 떨었다. 
			ENDIF
		ELSE
			IF TALENT:젖기쉬움
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 로터가 밀어붙여지자, 너무나도 강렬한 쾌락에 격렬하게 등골을 젖힌채 연속으로 물을 뿜었다. 
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 로터가 밀어붙여지자 지독한 쾌락에 침을 흘리면서 몸부림쳤다. 
			ENDIF
		ENDIF
	ENDSELECT
	IF !CHECK_CLO("Ｖ노출") && CUP:쾌Ｃ/1000 > RAND:10 && PALAM:Ｖ윤 >= 3000
		PRINTFORM %CLO("가랑이")% 위로 보지를 
		IF CHECK_CLO("染み")
			PRINTFORMW 쓰다듬고 문질러주자, 스르륵 얼룩이 퍼져나간다…
		ELSE
			PRINTFORMW 손가락으로 밀어넣어주자, 미끈미끈한 감촉이 전해져온다…
		ENDIF
	ENDIF
ENDIF
;--------------------------------------------------------
;바이브
;--------------------------------------------------------
IF SELECTCOM == 31
	IF TEQUIP:바이브
		SELECTCASE CUP:쾌Ｖ
		CASE IS < CUP:고통
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 바이브의 두께에 괴로운 듯이 몸부림쳤다. 
		CASE IS < 1000
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 익숙하지 않은 바이브의 감각에 당황하면서도 때때로 밀려올라오는 쾌락의 충격에 몸을 떨고있다. 
		CASE IS < 3000
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 이미 그곳으로 쾌락을 느낄 수 있는 수준까지 올라왔는지, 진동하는 바이브를 넣어주자 명백하게 쾌락이 섞인 목소리를 냈다. 
		CASE IS < 6000
			PRINTFORM %CALLNAME:TARGET%의 
			SIF TALENT:작은몸집
				PRINTFORM 작은 
			PRINTL 바기나에 바이브를 찔러넣어주자 곧바로 쾌락에 녹아내리는 얼굴이 되었다. 
		CASE IS < 10000
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
			SIF TALENT:부끄럼쟁이
				PRINTFORM 얼굴을 수치심에 새빨갛게 물들이면서 
			SIF TALENT:작은몸집
				PRINTFORM 작은 몸으로 
			IF SIZE("바이브", TARGET) >= 3
				PRINTFORM 극태의 
			ELSEIF SIZE("바이브", TARGET) == 2
				PRINTFORM 굵은 
			ENDIF
			PRINTL 바이브를 비틀어물고, 격렬한 진동이 가져오는 자극과 쾌락에 알몸을 신선한 은어처럼 퉁기고있다. 
		CASEELSE
			PRINTFORM %CALLNAME:TARGET%에게 진동하는 바이브를 격렬하게 넣고뽑아주자, 애액을 흘리며 격렬한 쾌감에 
			SIF TALENT:부끄럼쟁이
				PRINTFORM 아직 남아있는 수치심으로 얼굴을 손으로 가리면서, 
			SIF TALENT:쾌감에솔직
				PRINTFORM 더욱더 쾌락을 강구하듯이 허리를 흔들면서, 
			PRINTFORML \@ TALENT:작은몸집 ? 작은 # 그 \@ 몸뚱아리를 몸부림치며 뒹굴렀다. 
		ENDSELECT
	ENDIF
ENDIF
;--------------------------------------------------------
;전기안마기
;--------------------------------------------------------
IF SELECTCOM == 32
	SELECTCASE CUP:쾌Ｃ
	CASE IS < 300
		PRINTFORML %CALLNAME:TARGET%의 \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@에 안마기를 꽉 눌렀지만, 진동에 견디는 것만으로 커다란 쾌감을 얻지는 못하는 것같다. 
	CASE IS < 1000
		PRINTFORML %CALLNAME:TARGET%의 그곳에 안마기를 꽉 눌러주자 간지러워하면서도 그 표정에는 쾌락의 조짐이 보인다. 
	CASE IS < 3000
		IF RAND:2 == 0
			PRINTFORML 안마기의 진동에, %조사처리(CALLNAME:TARGET,"는")% 희미하게 쾌락의 목소리를 높이고있다. 
		ELSE
			PRINTFORML \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@에 안마기를 꽉 갖다대주자 %조사처리(CALLNAME:TARGET,"는")% 작고 사랑스러운 소리를 내면서 몸을 떨었다. 
		ENDIF
	CASE IS < 6000
		IF RAND:2 == 0
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 안마기의 쾌락에, 부들부들 작게 몸을 퉁기고있다. 
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 그곳에 안마기를 꽉 눌러주자 견딜 수 없는 쾌락에 무심코 두다리를 꽉 닫고 안마기를 조여버려, %조사처리(CALLNAME:TARGET,"는")% 몸부림쳤다. 
		ENDIF
	CASE IS < 10000
		IF RAND:2 == 0
			PRINTFORML 안마기가 가져다주는 쾌락에, %조사처리(CALLNAME:TARGET,"는")% 어떻게든 도망치려 허리를 구불대면서 요염한 신음소리를 냈다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@에 느껴지는 안마기의 진동이 가져다주는 지나친 쾌락에 필사적으로 도망치려하면서 계속 요염한 신음소리를 내고있다. 
		ENDIF
	CASEELSE
		IF RAND:2 == 0
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 개발되어버린 \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@에 안마기가 눌리자, 
			IF TALENT:젖기쉬움
				PRINTL 몇번이고 물을 뿜으면서 미친듯이 몸부림쳤다. 
			ELSE
				PRINTL 지나치게 강렬한 쾌감에 미친듯이 몸부림쳤다. 
			ENDIF
		ELSE
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 안마기가 닿은 순간에 격렬하게 몸을 젖히고, 
			IF TALENT:젖기쉬움
				PRINTL 그곳에서 몇번이고 물을 뿜었다. 
			ELSE
				PRINTL 강렬한 쾌락에 몸을 꿈틀대며 퉁겨댔다. 
			ENDIF
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;애널바이브
;--------------------------------------------------------
IF SELECTCOM == 33 && COND("애널비즈") == 0
	IF TEQUIP:애널바이브
		SELECTCASE CUP:쾌Ａ
		CASE IS < CUP:고통
			IF CUP:쾌Ａ >= 1000
				PRINTFORML %CALLNAME:TARGET%의 애널은 대부분을 받아들인 것같지만 아직 조금 괴로워보인다. 
			ELSE
				PRINTFORML 애널에 바이브를 넣어주자 %조사처리(CALLNAME:TARGET,"는")% 비명을 질렀다. 아직 쾌감보단 고통쪽이 더 한 것같다. 
			ENDIF
		CASE IS < 1000
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널에 바이브가 들어가서 조금 괴로워보이지만 쾌락이 고통을 이기고 있는 것같다. 
		CASE IS < 3000
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널에 바이브를 찔러넣어주자 명백하게 교성을 지르면서 쾌락을 느끼고있다. 
		CASE IS < 10000
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널바이브가 가져다주는 강렬한 쾌락에 표정을 풀고있다. 
		CASEELSE
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 애널의 자극에 격렬하게 느껴서, 
			PRINTFORML 그 \@ TALENT:작은몸집 ? 작은 신체로 # 땀투성이 몸뚱아리로 \@ 몸부림쳤다. 
		ENDSELECT
	ENDIF
ENDIF
;--------------------------------------------------------
;클리캡
;--------------------------------------------------------
IF SELECTCOM == 34
	SELECTCASE CUP:쾌Ｃ
	CASE IS < 300
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 클리토리스로의 자극에 눈을 감고 참고있다. 
	CASE IS < 1000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 음핵에 매달린 음탕한 도구의 자극에 견디지 못하고, 작게 신음소리를 냈다. 
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 클리토리스로의 자극에 견디지 못하고 몸을 작게 떨며 쾌락에 압도되고있다. 
	CASE IS < 6000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 클리캡의 자극이 가져다주는 쾌락에 허리를 쑥 내밀고 음탕한 댄스를 추고있다. 
	CASE IS < 10000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 클리토리스에 보내지는 진동에, 몸을 부들부들 퉁겼다. 
	CASEELSE
		IF NOWEX:Ｃ절정
			PRINTFORML 클리캡이 만들어내는 격렬한 진동에, %조사처리(CALLNAME:TARGET,"는")% 연속으로 절정에 달하고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 클리토리스에 보내지는 쾌락에, 의식을 흐릿하게 한 채로 풀어진 표정을 짓고있다. 
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;니플캡
;--------------------------------------------------------
IF SELECTCOM == 35
	SELECTCASE CUP:쾌Ｂ
	CASE IS < 300
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 유두로의 자극에 간지러운 듯이 몸을 비비꼬았다. 
	CASE IS < 1000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 유두에 매달린 음탕한 도구의 자극에 견디지 못하고, 작게 신음소리를 냈다. 
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 유두로 보내지는 쾌감을 견디지 못하고 몸을 조금씩 떨고있다. 
	CASE IS < 6000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 니플캡의 자극이 가져다주는 쾌락에 등골을 뒤로 젖히고 음탕한 댄스를 추고있다. 
	CASE IS < 10000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 니플캡에서 유두로 보내지는 쾌락에 의식을 흐릿하게 한 채로 풀어진 표정을 짓고있다. 
	CASEELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 
		IF TALENT:작은몸집
			PRINTL 니플캡이 만들어내는 격렬한 진동에 그 작은 몸을 퉁겼다. 
		ELSEIF TALENT:거유
			PRINTL 커다른 가슴을 퉁기면서 니플캡이 만들어내는 격렬한 쾌감에 잠겨있다. 
		ELSE
			PRINTL 니플캡이 낳는 지나친 쾌감에 미친듯이 몸부림쳤다. 
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;정상위
;--------------------------------------------------------
IF (SELECTCOM == 60 || SELECTCOM == 62) && INSERT("Ｖ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ｖ
	CASE IS < 1000
		IF CUP:쾌Ｖ < CUP:고통 && MARK:반발각인 == 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 괴롭게 몸부림치고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 작게 몸을 떨고있지만, 아직 쾌락을 느끼는 정도에는 이르지 못한 것같다. 
		ENDIF
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 격렬한 %CALLNAME:PLAYER%의 허리움직임에, 사랑스러운 신음을 흘리고있다. 
	CASE IS < 6000
		PRINTFORML 질내를 휘저어지는 감각에, %조사처리(CALLNAME:TARGET,"는")% 쾌락에 녹는듯한 표정을 하고있다. 
	CASE IS < 10000
		PRINTFORML 격렬하게 질내를 찔러주자, %조사처리(CALLNAME:TARGET,"는")% 꿈틀꿈틀 신체를 퉁기면서 쾌락을 받아들였다. 
	CASEELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF COND("마음에 드는 체위中") && CHOSECOM(SELECTCOM) == 0
			IF SELECTCOM == 60
				PRINTFORM 아주 좋아하는 정상위로 범해지면서, 
			ELSE
				PRINTFORM 정말 좋아하는 부끄러운 자세로 범해지면서, 
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:PLAYER%의 격렬한 허리움직임에, 
		ENDIF
		IF TALENT:작은몸집
			PRINTL 작은 몸을  부들부들 퉁기면서 쾌락에 격렬하게 계속 신음소리를 내고있다. 
		ELSE
			PRINTL 요염한 신음소리를 내면서 계속 몸부림쳤다.
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;후배위
;--------------------------------------------------------
IF SELECTCOM == 61 && INSERT("Ｖ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ｖ
	CASE IS < 3000
		IF CUP:쾌Ｖ < CUP:고통 && MARK:반발각인 == 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 괴롭게 얼굴을 찡그리고 붕붕 고개를 좌우로 흔들고있다. 
		ELSEIF CUP:쾌Ｖ < 1000 && MARK:반발각인 == 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 체념한듯이 %CALLNAME:PLAYER%의 허리움직임을 받아들이고있다…. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 격렬한 허리움직임에, 몸뚱아리를 떨면서 신음소리를 냈다. 
		ENDIF
	CASE IS < 6000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 뒤에서 질내를 찔리는 쾌감에 등골을 젖히며 격렬하게 몸부림쳤다. 
	CASE IS < 10000
		PRINTFORML 쾌감이 너무 강한건지, %조사처리(CALLNAME:TARGET,"는")% 황홀한 표정으로 고개를 좌우로 붕붕 젓고있다. 
	CASEELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF COND("마음에 드는 체위中") && CHOSECOM(SELECTCOM) == 0
			PRINTFORM 아주 좋아하는 후배위로 범해지면서, 
		ELSE
			PRINTFORM %CALLNAME:PLAYER%의 격렬한 허리움직임에 등골을 젖히고, 
		ENDIF
		IF TALENT:거유
			PRINTL 찔릴때마다 거유를 출렁출렁 흔들면서 몸을 꼬았다. 
		ELSE
			PRINTL 머리카락를 흐뜨러뜨리면서 전신에 울려오는 쾌락에 몸부림쳤다. 
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;대면좌위
;--------------------------------------------------------
IF SELECTCOM == 63 && INSERT("Ｖ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ｃ + CUP:쾌Ｖ
	CASE IS < 1000
		IF CUP:쾌Ｃ + CUP:쾌Ｖ < CUP:고통 && MARK:반발각인 == 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 싫어싫어라고 말하는 듯이 고개를 좌우로 흔들고 있다…. 
		ELSE
			PRINTFORML 천천히 아래에서 찔러올려주자, %조사처리(CALLNAME:TARGET,"는")% 충격에 눈썹을 찡그렸다. 
		ENDIF
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 질내를 콩콩 노크당해, 희미한 신음을 흘렸다. 
	CASE IS < 6000
		PRINTFORM 아래에서 %조사처리(CALLNAME:TARGET,"를")% 찔러올려주자, %조사처리(CALLNAME:TARGET,"는")% 
		IF COND("손가락 사용") && RAND:2
			PRINTFORML %CALLNAME:PLAYER%에게 매달려왔다. 
		ELSE
			PRINTFORM %CALLNAME:PLAYER%에게 몸을 밀착하
			SIF TALENT:거유
				PRINTFORM 고 커다란 가슴을 눌러붙
			PRINTL 였다. 
		ENDIF
	CASE IS < 10000
		PRINTFORML 격렬하게 아래에서 찔러올려져, %조사처리(CALLNAME:TARGET,"는")% 도를 넘어서는 쾌감에 
		IF COND("손가락 사용") && RAND:2
			PRINTFORML %CALLNAME:PLAYER%에게 꽈악 매달려 몸부림쳤다. 
		ELSE
			PRINTFORM %CALLNAME:PLAYER%에게 몸을 밀착하
			SIF TALENT:거유
				PRINTFORM  고 커다란 가슴을 눌러붙이
			PRINTL 며 몸부림쳤다. 
		ENDIF
	CASEELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF RAND:2
			IF COND("손가락 사용") && RAND:2
				PRINTFORM %CALLNAME:PLAYER%의 등에 팔다리를 감고 꽈악 껴안으면서, 
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에게 몸을 밀착시키
				SIF TALENT:거유
					PRINTFORM 고 커다란 가슴을 눌러붙이
				PRINTFORM 면서, 
			ENDIF
		ELSEIF COND("마음에 드는 체위中") && CHOSECOM(SELECTCOM) == 0
			PRINTFORM 아주 좋아하는 대면좌위 자세로 
			IF COND("손가락 사용") && RAND:2
				PRINTFORM %CALLNAME:PLAYER%에게 달라붙어서, 
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에게 몸을 밀착시키
				SIF TALENT:거유
					PRINTFORM 고 커다란 가슴을 눌러붙이
				PRINTFORM 면서, 
			ENDIF
		ELSE
			PRINTFORM %CALLNAME:PLAYER%의 격렬한 찌르기에 등골을 뒤로 젖히고, 
		ENDIF
		IF RAND:2
			PRINTFORML 하반신에서 올라오는 달콤한 쾌락에 뜨거운 한숨을 흘리고있다…
		ELSE
			PRINTFORML 기분 좋은듯한 얼굴을 하면서 미친듯이 몸부림쳤다
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;배면좌위
;--------------------------------------------------------
IF SELECTCOM == 64 && INSERT("Ｖ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ｃ + CUP:쾌Ｖ + CUP:쾌Ｂ
	CASE IS < CUP:고통
		IF MARK:반발각인 == 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 등뒤에서 밀어올려져서 불쾌감으로 얼굴을 찡그리고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 아래에서 천천히 밀어올려져, 싫어싫어라고 말하듯이 고개를 붕붕 좌우로 흔들었다. 
		ENDIF
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 찔러올려질 때마다 희미한 신음소리를 흘리고있다. 
	CASE IS < 6000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 아래에서 찔러올려져서, 쾌락으로 계속 신음소리를 내었다. 
	CASE IS < 10000
		PRINTFORML 아래에서 강하게 찔러올려져, %조사처리(CALLNAME:TARGET,"는")% 격렬하게 신음을 질렀다. 
	CASEELSE
		SIF COND("마음에 드는 체위中") && CHOSECOM(SELECTCOM) == 0
			PRINTFORM 아주 좋아하는 체위로 
		PRINTFORML 아래에서 찔러올리면서 유두와 \@ PENIS(TARGET) ? 페니스 # 클리토리스 \@를 괴롭혀주자, %조사처리(CALLNAME:TARGET,"는")% 부들부들 신체를 떨면서 연달에 절정의 쾌감에 농락당했다. 
	ENDSELECT
ENDIF
;--------------------------------------------------------
;기승위
;--------------------------------------------------------
IF SELECTCOM == 65 && INSERT("Ｖ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ｖ
	CASE IS < CUP:고통
		IF MARK:반발각인 == 3 || CUP:쾌Ｖ < 500
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 괴로워보이지만 명령에 따라 허리를 흔든다0. 하지만, 아직 쾌락을 얻기엔 부족한 모양이다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 괴로워 하면서도 그 표정에는 쾌락의 조짐이 보인다. 
		ENDIF
	CASE IS < 3000
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 허리를 흔들면서 참을 수 없는 쾌락에 사랑스러운 소리를 내고있다. 
	CASE IS < 6000
		IF TALENT:거유
			PRINTFORML 눈 앞에서 %조사처리(CALLNAME:TARGET,"는")% 커다란 가슴을 허리 움직임에 맞춰 위아래로 흔들면서 쾌락에 잠겨있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 상당히 익숙해졌는지, 찔러올려지면서 쾌락에 스스로 허리를 흔들고있다. 
		ENDIF
	CASE IS < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF RAND:2
			IF TALENT:작은몸집
				PRINTFORM 그 작은 몸으로 
			ELSEIF TALENT:거유
				PRINTFORM 그 커다란 가슴을 위 아래로 퉁기며 
			ENDIF
			IF TALENT:억압 || TALENT:쾌감을부정
				PRINTFORML 쾌락을 부정하는 말을 토하면서, 견디지 못하고 신음 소리를 계속 흘리고있다. 
			ELSE
				PRINTFORML 정신없이 허리를 흔들고, 등골을 뒤로 젖히면서 쾌락의 신음소리를 질렀다. 
			ENDIF
		ELSE
			IF TALENT:억압 || TALENT:쾌감을부정
				PRINTFORML 필사적으로 쾌락을 부정하고 있지만, 그 표정은 분명히 쾌락에 녹아있다…
			ELSE
				IF TALENT:거유 && RAND:2
					PRINTFORM 출렁출렁 커다란 유방을 퉁기면서, 
				ELSEIF PALAM:Ｖ윤 >= 30000 && RAND:2
					PRINTFORM 가랑이를 부끄러운 음액으로 질척질척 적시고, 
				ELSE
					PRINTFORM 정신없이 허리를 흔들면서, 
				ENDIF
				IF COND("마음에 드는 체위中") && CHOSECOM(SELECTCOM) == 0
					PRINTFORM 아주 좋아하는 기승위 자세로 
				ELSE
					PRINTFORM 칠칠치 못하게 풀린 얼굴로 
				ENDIF
				PRINTFORML 쾌락을 탐하고있다. 
			ENDIF
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;애널섹스
;--------------------------------------------------------
IF SELECTCOM >= 70 && SELECTCOM <= 79 && SELECTCOM != 75 && INSERT("Ａ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ａ
	CASE IS < 3000
		;애널許可
		IF CFLAG:100
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널을 풀고, %CALLNAME:PLAYER%의 피스톤 운동을 순순히 받아들이고있다…. 
		ELSEIF CUP:쾌Ａ < CUP:고통 && CUP:쾌Ａ < 500
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널을 꿰뚫는 페니스감촉에 허둥지둥 날뛰고 있다…. 
		ELSEIF CUP:쾌Ａ < CUP:고통
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 아직 아픔 쪽이 강한지, 얼굴을 찡그리고있다. 
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 애널을 천천히 찔러주자, %조사처리(CALLNAME:TARGET,"는")% 부들부들 몸을 떨었다. 
		ENDIF
	CASE IS < 6000
		IF TALENT:남자 == 0 && RAND:2
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널을 덮치는 쾌락에, 피소에서 감출 수 없는 쾌락의 증거를 흘리고있다…
		ELSE
			PRINTFORM 조금 강하게 안쪽을 찔러넣어주자, %조사처리(CALLNAME:TARGET,"는")% 몸을 뒤로 젖히고 
			IF TEQUIP:재갈
				PRINTL 흐릿한 신음소리를 흘렸다. 
			ELSE
				PRINTL 입을 뻐끔뻐끔 거렸다. 
			ENDIF
		ENDIF
	CASE IS < 10000
		PRINTFORML 격렬한 피스톤 운동에도 익숙해졌는지, %조사처리(CALLNAME:TARGET,"는")% 당장이라도 절정할 것같은 상태이면서도 더욱더 쾌감을 탐해왔다. 
	CASEELSE
		SIF COND("마음에 드는 체위中") && CHOSECOM(SELECTCOM) == 0
			PRINTFORM 아주 좋아하는 체위로 
		PRINTFORM 격렬하게 애널을 괘롭혀주자, %조사처리(CALLNAME:TARGET,"는")% 
		IF ABL:Ａ감각 >= 5
			PRINTFORM 쾌락에 몸부림치면서 
		ELSEIF TALENT:Ａ민감
			PRINTFORM 민감한 그곳을 농락당하는 쾌감에 머리를 쥐어뜯으면서 
		ELSE
			PRINTFORM 강렬한 쾌감에 출렁출렁 엉덩이살을 흔들면서 
		ENDIF
		SIF TALENT:남자 == 0
			PRINTFORM 요염한 
		PRINTFORML 신음을 내며, 미친 듯이 몸부림쳤다. 
	ENDSELECT
ENDIF
;--------------------------------------------------------
;애널기승위
;--------------------------------------------------------
IF SELECTCOM == 75 && INSERT("Ｖ인서트") && COND("절정", PLAYER) == 0
	SELECTCASE CUP:쾌Ａ
	CASE IS < 3000
		IF CUP:쾌Ａ < CUP:고통 && (MARK:반발각인 == 3 || CUP:쾌Ａ < 1000)
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 괴로워하면서도 명령에 따라 허리를 흔든다. 하지만, 아직 쾌락을 느끼기엔 먼 것같다. 
		ELSEIF CUP:쾌Ａ < CUP:고통
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 괴로워하면서도 그 표정에서는 쾌락의 기미가 보인다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 허리를 흔들면서 참을 수 없는 쾌락에 사랑스러운 소리를 흘리고있다. 
		ENDIF
	CASE IS < 6000
		IF TALENT:거유
			PRINTFORML 눈 앞에서 %조사처리(CALLNAME:TARGET,"는")% 커다란 유방을 허리 움직임에 맞춰 위아래로 퉁기면서 쾌락에 잠기고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 꽤 익숙해졌는지, 쾌락에 찔러올려지면서 스스로 허리를 흔들고있다. 
		ENDIF
	CASE IS < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF RAND:2
			IF TALENT:작은몸집
				PRINTFORM 그 작은 몸으로 
			ELSEIF TALENT:거유
				PRINTFORM 그 커다란 가슴을 위아래로 튕기며
			ENDIF
			IF TALENT:억압 || TALENT:쾌감을부정
				SIF TEQUIP:재갈 == 0
					PRINTFORM 쾌락을 부정하는 말을 토하면서
				PRINTL 참지못하고 계속 신음소리를 내고있다. 
			ELSE
				PRINTFORML 열심히 허리를 흔들고 등골을 뒤로 젖히면서 쾌락의 신음소리를 질렀다. 
			ENDIF
		ELSE
			IF TALENT:거유
				PRINTFORM 출렁출렁 커다란 유방을 퉁기면서 
			ELSE
				PRINTFORM 허리를 흔들면서 
			ENDIF
			IF TALENT:억압 || TALENT:쾌감을부정
				PRINTFORML 필사적으로 쾌감을 부정하고있지만, 그 얼굴에는 명백하게 쾌락의 표정으로 뒤죽박죽되어있다. 
			ELSE
				PRINTFORML 완전히 풀려버린, 정숙치 못한 얼굴로 쾌락을 탐하고있다. 
			ENDIF
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;스팽킹, 바늘, 채찍
;--------------------------------------------------------
IF SELECTCOM == 130 || SELECTCOM == 131 || SELECTCOM == 132
	SELECTCASE CUP:고통
	CASE IS < 100
	CASE IS < 300
		IF ABL:마조끼 >= 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 쾌락을 느끼기 시작했는지 뜨문뜨문 감미로운 소리를 내고있따. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 비명을 참아가면서 아픔을 견뎌냈다. 
		ENDIF
	CASE IS < 2000
		IF ABL:마조끼 >= 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 아픔 속에서 쾌락을 느끼는지 때때로 물기를 띈 눈으로 이쪽을 바라보고있따. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 격렬한 고통에 악다문 이빨 틈으로 괴로워하는 신음을 흘렸다. 
		ENDIF
	CASEELSE
		IF ABL:마조끼 >= 3
			PRINTFORML %CALLNAME:TARGET%의 얼굴에 고통의 색은 보이지않고 얼굴을 상기시키고 유혹하듯이 엉덩이를 흔들고있다. 
		ELSE
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 고통에 견디지 못하고 비명을 지르면서 온몸을 퉁겼다. 
		ENDIF
	ENDSELECT
ENDIF
;--------------------------------------------------------
;컨닐링구스強制or69
;--------------------------------------------------------
IF SELECTCOM == 98 || (SELECTCOM == 99 && PENIS(PLAYER) == 0)
	;보지를お청소…봉사정신, 기교, 레즈끼の合計値が8이상で실행치を30이상上回っているなら汚れをなめ取る
	IF ABL:기교 + ABL:봉사정신 + ABL:레즈끼 >= 8 && TCVAR:28 >= 30 && STAIN:PLAYER:Ｖ != SET_STAIN(0, "Ｖ")
		CALL OSOUJI_V
	ENDIF
ENDIF
;--------------------------------------------------------
;요도확장기
;--------------------------------------------------------
IF SELECTCOM == 151 && TEQUIP:요도확장기
	;オシッコが溜まっているなら排尿
	IF BASE:요의 >= 10000
		IF GETBIT(TEQUIP:근육이완제, 1)
			PRINTFORML %CALLNAME:TARGET%의 요도로부터 브지를 뽑아 내면, 느슨해진 구멍으로부터 죠로죠로와 오줌이 흘려 보내졌다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 요도로부터 브지를 뽑아 내, 잡균을 씻어 없애기 위해서(때문에) 배뇨 시켰습니다
		ENDIF

		;방뇨のソース獲得
		CALL SOURCE_COM144, 80

		;이뇨제の効果は消失
		TEQUIP:이뇨제 = 0
		;自分から방뇨したので, 最後まで出し切ります
		BASE:요의 = 0
		;방뇨
		EXP:방뇨경험 += 1
		PRINTL 방뇨경험＋１
		NOWEX:방뇨 += 1
		EX:방뇨 += 1
	ELSE
		PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% 흡족해 할 때까지%CALLNAME:TARGET%의 요도를 괴롭힌 후, 브지를 신중하게 뽑아 냈다…
	ENDIF
	TEQUIP:요도확장기 = 0
ENDIF
;--------------------------------------------------------
;촉수바이브
;--------------------------------------------------------
IF SELECTCOM == 201 && TEQUIP:바이브
	IF GETBIT(FLAG:80, 0) || (GETBIT(FLAG:80, 1) && RAND:2 == 0)
		TEQUIP:바이브 = 0
		PRINTFORML 질내를 능욕한 후에, 촉수는 %CALLNAME:TARGET%의 질에서 뽑혀나왔다…
	ENDIF
ENDIF
;--------------------------------------------------------
;애널촉수
;--------------------------------------------------------
IF SELECTCOM == 202 && TEQUIP:애널바이브
	IF GETBIT(FLAG:80, 2) || (GETBIT(FLAG:80, 3) && RAND:2 == 0)
		TEQUIP:애널바이브 = 0
		PRINTFORML 항문을 능욕한 후에, 촉수는 %CALLNAME:TARGET%의 애널에서 뽑혀나왔다…
	ENDIF
ENDIF
;--------------------------------------------------------
;촉수클리자극
;--------------------------------------------------------
IF SELECTCOM == 203 && TEQUIP:클리캡
	IF GETBIT(FLAG:80, 4) || (GETBIT(FLAG:80, 5) && RAND:2 == 0)
		TEQUIP:클리캡 = 0
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 클리토리스 고문을 중단했따…
	ENDIF
ENDIF
;--------------------------------------------------------
;촉수유두자극
;--------------------------------------------------------
IF SELECTCOM == 204 && TEQUIP:니플캡
	IF GETBIT(FLAG:80, 6) || (GETBIT(FLAG:80, 7) && RAND:2 == 0)
		TEQUIP:니플캡 = 0
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 유두에서 떨어졌다…
	ENDIF
ENDIF
;--------------------------------------------------------
;촉수착유
;--------------------------------------------------------
IF SELECTCOM == 205 && TEQUIP:착유기
	IF GETBIT(FLAG:80, 8) || (GETBIT(FLAG:80, 9) && RAND:2 == 0)
		TEQUIP:착유기 = 0
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 유두를 풀어주었다…
	ENDIF
ENDIF
;--------------------------------------------------------
;촉수구욕
;--------------------------------------------------------
IF SELECTCOM == 208 && TEQUIP:촉수구욕
	IF GETBIT(FLAG:80, 12) || (GETBIT(FLAG:80, 13) && RAND:2 == 0)
		TEQUIP:촉수구욕 = 0
		PRINTFORML 실컷 입안을 휘저은 후, 촉수는 %CALLNAME:TARGET%의 입에서 뽑혀나왔다…
	ENDIF
ENDIF
;--------------------------------------------------------
;촉수페니스자극
;--------------------------------------------------------
IF SELECTCOM == 209 && TEQUIP:오나홀
	IF GETBIT(FLAG:80, 10) || (GETBIT(FLAG:80, 11) && RAND:2 == 0)
		TEQUIP:오나홀 = 0
		PRINTFORML 촉수는 %CALLNAME:TARGET%의 페니스를 풀어주었다…
	ENDIF
ENDIF
;--------------------------------------------------------
;피스트系커맨드
;--------------------------------------------------------
IF SELECTCOM == 141
	IF CHECK("Ｖ피스트") && COND("손가락 사용") && RAND:3
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 자신의 질구를 손가락으로 벌리며 여운을 맛보고있다…
	ELSEIF ABL:Ｖ확장 >= ABL:Ｖ놀림
		IF ABL:Ｖ확장 >= 6
			PRINTFORML %CALLNAME:TARGET%의 성기에서 고기주름이 비어져 나오고, 뻥하니 열린 구멍 안에서는 자궁구가 보이고있다…
		ELSEIF ABL:Ｖ확장 >= 4
			PRINTFORML %CALLNAME:TARGET%의 질은 뻥하니 입을 벌리고있다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 질은 벌어진 채로 좀처럼 돌아오지않는다…
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 142
	IF CHECK("Ａ피스트") && COND("손가락 사용") && RAND:3
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 자신의 항문을 손가락으로 벌리고, 황홀해하면서 장내의 경치를 드러내고있다…
	ELSEIF ABL:Ａ확장 >= ABL:Ａ놀림
		IF ABL:Ａ확장 >= 6
			PRINTFORML %CALLNAME:TARGET%의 항문은 주름이 완전히 늘어나, 장 속이 훤히 들여다보이고있다…
		ELSEIF ABL:Ａ확장 >= 4
			PRINTFORML %CALLNAME:TARGET%의 항문은 뻥하니 입을 열고있다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 항문은 벌어진 채로 좀처럼 돌아오지않는다…
		ENDIF
	ENDIF
ELSEIF SELECTCOM == 143
	IF CHECK("양구멍피스트") && COND("손가락 사용") && RAND:3
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 여운을 맛보고있따. 
		PRINTFORML 양손의 손가락이, 완전히 벌어져 속을 드러내고있는, 질구와 항문 주변을 어루만지고있다…
	ELSEIF ABL:Ａ확장 >= ABL:Ａ놀림 || ABL:Ｖ확장 >= ABL:Ｖ놀림
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 양다리를 벌리고 축 처져있다. 
		PRINTFORML 벌어져버린 질구와 항문에서는 점액이 넘쳐나오고있다…
	ENDIF
ENDIF

;--------------------------------------------------------
;용간
;--------------------------------------------------------
IF SELECTCOM == 304 || SELECTCOM == 305
	;TEQUIP:넘어뜨리기 > 0 なら역용간
	SIF TEQUIP:넘어뜨리기 + TEQUIP:PLAYER:넘어뜨리기
		SWAP PLAYER, TARGET

	IF SELECTCOM == 305
		LOCAL = ABL:Ａ확장
	ELSE
		LOCAL = ABL:Ｖ확장
	ENDIF

	IF SELECTCOM == 305
		IF CHECK("Ａ용간") == 0
			;확장경험が少しはある
			IF LOCAL >= 3
				IF COND("손가락 사용") && RAND:3 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 뻥하니 입을 열고있는 항문을 말없이 만지고있다…
				ELSEIF RAND:2 == 0
					PRINTFORML 벌어진채로 좀처럼 되돌아오지않는 항문에, %CALLNAME:TARGET%의 얼굴이 일그러졌다
				ELSE
					PRINTFORML 격렬한 행위에 %조사처리(CALLNAME:TARGET,"는")% 거친 호흡을 반복하고있다
				ENDIF
			ELSE
				IF COND("손가락 사용") && RAND:4 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 자신의 애널을 만지며, 무언가를 중얼거리고있다…
				ELSEIF RAND:3 == 0
					PRINTFORML %CALLNAME:TARGET%의 온몸은 탈진해, 눈동자에 생기가 없다…
				ELSEIF RAND:2 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 지나친 고통에 정신을 잃은 모양이다…
				ELSE
					PRINTFORML %CALLNAME:TARGET%의 가랑이는 보기에도 무참한 상태다…
				ENDIF
			ENDIF
		ELSEIF LOCAL > SIZE("드래곤") && RAND:3
			IF COND("손가락 사용") && RAND:3 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 자신의 항문을 손가락으로 벌리고, 황홀해하고있다
			ELSEIF COND("손가락 사용") && RAND:2 == 0
				PRINTFORML 페니스가 뽑힌 항문은 벌어진채로 좀처럼 되돌아오지않는다. 
				PRINTFORML %CALLNAME:TARGET%의 손은 그 틈새를 메우려는 듯이 뻗어있다…
			ELSE
				PRINTFORML 침입자가 떠난 항문은 뻥하니 입을 열고있다. 
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 부족한 듯이 뽑혀나간 페니스를 바라보고있다…
			ENDIF
		ELSE
			IF COND("손가락 사용") && RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 뻥하니 입을 열고있는 항문을 사랑스러운 듯이 만지고있다…
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 탈진해서 여운에 잠겨있다…
			ENDIF
		ENDIF
	ELSE
		IF CHECK("Ｖ용간") == 0
			;확장경험が少しはある
			IF LOCAL >= 3
				IF COND("손가락 사용") && RAND:3 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 뻥하니 입을 열고있는 질을 말없이 어루만졌다…
				ELSEIF RAND:2 == 0
					PRINTFORML 벌어진채로 좀처럼 되돌아오지않는 질에, %CALLNAME:TARGET%의 얼굴이 일그러졌다…
				ELSE
					PRINTFORML 거친 행위에 %조사처리(CALLNAME:TARGET,"는")% 거친 호흡을 반복하고있다…
				ENDIF
			ELSE
				IF COND("손가락 사용") && RAND:4 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 자신의 질구를 만지며, 무언가 중얼거리고있다…
				ELSEIF RAND:3 == 0
					PRINTFORML %CALLNAME:TARGET%의 온몸은 탈진해서, 눈동자에 생기가 없다…
				ELSEIF RAND:2 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 지나친 고통에 정신을 잃은 모양이다…
				ELSE
					PRINTFORML %CALLNAME:TARGET%의 가랑이는 보기에도 무참한 상태다…
				ENDIF
			ENDIF
		ELSEIF LOCAL > SIZE("드래곤") && RAND:3
			IF COND("손가락 사용") && RAND:3 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 자신의 질구를 손가락으로 벌리며, 황홀해하고있다…
			ELSEIF COND("손가락 사용") && RAND:2 == 0
				PRINTFORML 페니스가 뽑힌 질은 좀처럼 되돌아오지않는다. 
				PRINTFORML %CALLNAME:TARGET%의 손은 그 틈을 메우려는 듯이 뻗어있다
			ELSE
				PRINTFORML 침입자가 떠난 질은 뻥하니 입을 열고있다. 
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 부족한 듯이 뽑혀진 페니스를 바라보고있다…
			ENDIF
		ELSE
			IF COND("손가락 사용") && RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 뻥하니 입을 연채인 질을 사랑스러운 듯이 만지고있다…
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 탈진해서 여운에 잠겨있다…
			ENDIF
		ENDIF
	ENDIF

	SIF TEQUIP:넘어뜨리기 + TEQUIP:PLAYER:넘어뜨리기
		SWAP PLAYER, TARGET

	TFLAG:16 = 2
	CALL KOJO_MESSAGE_COM
	TFLAG:16 = 0
ENDIF
