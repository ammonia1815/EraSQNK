;특수なギミックなどがないものはここに記述. 몸부림や용간など특수すぎるものはCOMFに記述. 
;선택지を内包するなど書くのが困難なものについては, TRAIN_MESSAGE_COM(커맨드番号)を用いて, 呼び出すようにすること. 
@TRAIN_MESSAGE_B
#DIM LCOUNT
#DIM MEMO_LINECOUNT

;-------------------------------------------------
;인서트など, 체위を表す플래그の管理
;-------------------------------------------------
CALL SETFLAG, "体勢戻し"


;-------------------------------------------------
;CUPの記録
;-------------------------------------------------
FOR LCOUNT, 0, 20
	LOCAL:LCOUNT = CUP:LCOUNT
NEXT
LOCAL:20 = DOWNBASE:체력
LOCAL:21 = DOWNBASE:기력
;-------------------------------------------------
;ソースの計算(予測値)
;-------------------------------------------------
CALL CALC, "조교소스の試算"

;-------------------------------------------------
;처녀체크
;-------------------------------------------------
IF CONFIG("脱처녀필터")
	;선택지の表示を체크
	MEMO_LINECOUNT = LINECOUNT

	SELECTCASE SELECTCOM
	CASE 60 TO 69
		IF TALENT:처녀 == 1 && TEQUIP:넘어뜨리기 == 0
			PRINTFORM %CALLNAME:TARGET%의 처녀
			IF PENIS(PLAYER) && (CHECK_STAIN(STAIN:PLAYER:Ｐ, "Ａ") || (PREVCOM >= 70 && PREVCOM <= 79) || INSERT("Ａ인서트") )
				PRINTFORM 애널에서 더러운 페니스
			ELSE
				PRINTFORM %TRAINNAME:SELECTCOM%
			ENDIF
			PRINTFORML 를 가져가시겠습니까？
		ENDIF
	;-------------------------------------------------
	;Ａ처녀の喪失체크
	;-------------------------------------------------
	CASE 70 TO 79
		IF TALENT:Ａ처녀 && TEQUIP:넘어뜨리기 == 0
			PRINTFORML %CALLNAME:TARGET%의 \@ TALENT:Ａ처녀 ? 엉덩이의 처녀를 가져 가시겠습니까？ # 애널을 범하시겠습니까？ \@
		ENDIF
	;-------------------------------------------------
	;쌍두바이브플레이での처녀상실체크
	;-------------------------------------------------
	CASE 80
		IF TEQUIP:넘어뜨리기 == 0 && TALENT:PLAYER:처녀 && CHECK_COM("쌍두바이브플레이")
			PRINTFORML 쌍두 바이브 플레이를 진행하면 %CALLNAME:PLAYER%의 처녀를 가져가게 됩니다
			PRINTFORML 그래도 진행합니까？
		ENDIF
	CASE 83
		IF EXP:요도성교경험 == 0
			IF PENIS(TARGET)
				PRINTFORML %CALLNAME:TARGET%노페니스노처녀를 %CALLNAME:PLAYER%의 그래서 빼앗아도 좋습니까？
			ELSE
				PRINTFORML %CALLNAME:TARGET%노노처녀를 빼앗아 정말로 좋습니까？
			ENDIF
		ENDIF
	ENDSELECT

	IF LINECOUNT > MEMO_LINECOUNT
		CALL PRINT_SELECT, "はい", "やっぱやめる"
		SIF RESULT == 1
			TFLAG:14 = 1
	ENDIF
ENDIF

;-------------------------------------------------
;커맨드の부정判定
;-------------------------------------------------
SIF REFUSE_CHECK()
	RETURN 0


;-------------------------------------------------
;サイズ체크と윤활체크
;サイズが大きい場合にはそっち優先
;-------------------------------------------------
VARSET LOCALS
IF TEQUIP:넘어뜨리기 == 0
	SELECTCASE SELECTCOM
	CASE 60 TO 69
		;너무나 큰 
		IF EXP_GAIN(12) == 0 && PENIS(PLAYER) && SIZE("페니스", PLAYER) >= ABL:Ｖ확장 + 2
			LOCALS:0 = %CALLNAME:PLAYER%의 물건은 너무 커서 %CALLNAME:TARGET%의 몸이 받아 들이질 못해 다칠수 있습니다
			LOCALS:2 = 정말로 삽입합니까？
		ENDIF
	CASE 70 TO 79
		IF EXP_GAIN(13) == 0 && PENIS(PLAYER) && SIZE("페니스", PLAYER) >= ABL:Ａ확장 + 2
			LOCALS:0 = %CALLNAME:PLAYER%의 물건은 너무 커서 %CALLNAME:TARGET%의 엉덩이 구멍이 받아 들이질 못해 다칠수 있습니다
			LOCALS:2 = 정말로 삽입합니까？
		ENDIF
	ENDSELECT

	;조수の助言
	IF ASSI && ABL:ASSI:조수 + TALENT:ASSI:현명함 >= 3
		IF CALCF("반발각인획득", TARGET)
			LOCALS:1 = 「이 플레이는 %CALLNAME:TARGET%\@ TALENT:ASSI:마도인형 ? 님 # \@에게는 \@ CALCF("반발각인획득", TARGET) >= 2 ? 너무 이를지도 # 이를지도 \@」
		ELSEIF CALCF("고통각인획득", TARGET)
			LOCALS:1 = 「이 플레이는 \@ CALCF("고통각인획득", TARGET) >= 2 ? 상당한 고통을 줄지도 # 조금 아플지도 \@」
			SELECTCASE SELECTCOM
			CASE 2, 33
				SIF PALAM:Ａ윤 < 10000
					LOCALS:1 = 「%CALLNAME:TARGET%\@ TALENT:ASSI:마도인형 ? 님 # \@의 미숙한 애널로 즐긴다면 로션을 사용하는 쪽이…」
			;Ｖ윤が필요と助言
			CASE 60 TO 79
				SELECTCASE GET_PALAMLV(PALAM:Ｖ윤 + PALAM:PLAYER:Ｐ윤) - MAX(SIZE("페니스", PLAYER) - ABL:Ｖ확장, 0)
				CASE 1

				CASE IS <= 0

				ENDSELECT
			;Ａ윤が필요と助言
			CASE 70 TO 79
				SELECTCASE GET_PALAMLV(PALAM:Ａ윤 + PALAM:PLAYER:Ｐ윤) - MAX(SIZE("페니스", PLAYER) - ABL:Ａ확장, 0)
				CASE 1
					LOCALS:1 = 「%CALLNAME:TARGET%\@ TALENT:ASSI:마도인형 ? 様 # \@의 미성숙한 애널로 즐긴다면, 로션이나 입으로 우선 하는것이…」
				CASE IS <= 0
					LOCALS:1 = 「%CALLNAME:TARGET%\@ TALENT:ASSI:마도인형 ? 님 # \@의 미성숙한 애널로 즐긴다면 로션을 사용하는 쪽이…」
				ENDSELECT
			ENDSELECT
		ENDIF
		IF LOCALS:1 != ""
			IF LOCALS:0 != ""
				LOCALS:1 = %CALLNAME:ASSI%도 %LOCALS:1%라고 합니다
			ELSE
				LOCALS:1 = %조사처리(CALLNAME:ASSI,"가")% %LOCALS:1%라고 조언했습니다
			ENDIF
			LOCALS:2 = …그래도 실행하시겠습니까？
		ENDIF
	ENDIF

	MEMO_LINECOUNT = LINECOUNT
	FOR LCOUNT, 0, 3
		SIF LOCALS:LCOUNT != ""
			PRINTFORML %LOCALS:LCOUNT%
	NEXT

	IF LINECOUNT > MEMO_LINECOUNT
		CALL PRINT_SELECT, "はい", "やっぱやめる"
		SIF RESULT == 1
			TFLAG:14 = 1
	ENDIF
ENDIF
;-------------------------------------------------
;커맨드の부정判定
;-------------------------------------------------
SIF REFUSE_CHECK()
	RETURN 0

;-------------------------------------------------
;커맨드実行直前の구상
;-------------------------------------------------
CALL KOJO_MESSAGE_BEFORE_COM

;-------------------------------------------------
;ボイス再生
;-------------------------------------------------
SIF CONFIG("EmueraTunes")
	TRYCALL VPLAY


;-------------------------------------------------
;커맨드の부정判定
;-------------------------------------------------
SIF REFUSE_CHECK()
	RETURN 0

;-------------------------------------------------
;조교時メッセージ
;-------------------------------------------------
SIF CONFIG("地の文ＯＦＦ") || CONFIG("구상による地の文ＯＦＦ")
	RETURN 0

PRINTL 
TRYCALLFORM TRAIN_MESSAGE_COM{SELECTCOM}

;-------------------------------------------------
;CUPの復元
;-------------------------------------------------
FOR LCOUNT, 0, 20
	CUP:LCOUNT = LOCAL:LCOUNT
NEXT
DOWNBASE:체력 = LOCAL:20
DOWNBASE:기력 = LOCAL:21

;-------------------------------------------------
;ソースの計算(確定値)
;-------------------------------------------------
CALL CALC, "조교소스の計算"

;이하の追加地の文はどれかひとつしか表示させない
MEMO_LINECOUNT = LINECOUNT

;조교ごとの初回限定の地の文
SELECTCASE SELECTCOM
CASE 60 TO 69, 177
	CALL TEXT, "추가地の文：Ｖ삽입後"
CASE 70 TO 79
	CALL TEXT, "추가地の文：Ａ삽입後"
CASE 83
	CALL TEXT, "추가地の文：요도삽입後"
ENDSELECT

SIF LINECOUNT > MEMO_LINECOUNT || COND("３Ｐ")
	RETURN 0

;追加オナニー
IF SELECTCOM != PREVCOM && SELECTCOM >= 90 && SELECTCOM <= 119
	CALL PLAY_ONANIE
	CALL PLAY_ONANIE_2
;Ａ감각LV3+절정あり+連続애널섹스
ELSEIF SELECTCOM >= 70 && SELECTCOM <= 79 && SELECTCOM == PREVCOM && TEQUIP:넘어뜨리기 == 0 && ABL:Ａ감각 >= 3 && EX:절정수합계 && INSERT("Ａ인서트") && COND("前Ｔ조교자") == PLAYER && TALENT:남자 == 0
	IF RAND:3 == 0
		PRINTFORM 격렬한 피스톤 운동에 일그러진 %CALLNAME:TARGET%의 그곳에서는, 
	ELSEIF RAND:2
		PRINTFORM 애널에서 울려퍼지는 쾌락으로, %CALLNAME:TARGET%의 그곳에서는 
	ELSE
		PRINTFORM 용서없는 찌르기에, %CALLNAME:TARGET%의 그곳에서는 
	ENDIF
	;今回Ｖ내사정された
	IF EXP_GAIN(17)
		PRINT 안에 사정된 정액과 섞인 
	ELSEIF TALENT:처녀 && RAND:3 == 0
		PRINT 처녀라고는 생각되지 않는 양의 
	ELSEIF RAND:4 == 0
		PRINT 만지지도 않았는데 
	ELSEIF RAND:3 == 0
		PRINT 질질 
	ELSEIF RAND:2 == 0
		PRINT 부끄러워하는 만큼 
	ELSE
		PRINT 눅진눅진하게 
	ENDIF
	PRINTFORMW 애액이 흘러나오고있다…
;기승위の追加텍스트
ELSEIF (SELECTCOM == 65 || SELECTCOM == 75 || SELECTCOM == 83) && (INSERT("인서트") == 0 || COND("前Ｔ조교자") != PLAYER)
	CALL TEXT, "추가地の文：기승위"
;どちらかがそろそろイきそうな状態
;커맨드ごとに固有の追加台詞などはここに書く
ELSEIF SELECTCOM >= 60 && SELECTCOM <= 89 && COND("イきそう") + COND("イきそう", PLAYER)
	SIF ABL:순종 < 3 && TALENT:연모 == 0 && ABL:욕망 < 3
		RETURN 0

	SELECTCASE SELECTCOM
	CASE 60 TO 69
		SIF CHECK("Ｖ인서트") == 0
			RETURN 0
	CASE 70 TO 79
		SIF CHECK("Ａ인서트") == 0
			RETURN 0
	CASE 83
		SIF TEQUIP:넘어뜨리기
			RETURN 0
		SIF CHECK("요도인서트") == 0
			RETURN 0
	CASEELSE
		RETURN 0
	ENDSELECT

	SELECTCASE SELECTCOM
	CASE 60, 83
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF RAND:2 && COND("손가락 사용") && CHECK_CLO("겨드랑이노출") && (COND("성벽：겨드랑이", PLAYER) || RAND:3 == 0)
			IF RAND:2
				PRINTFORM 머리카락을 쓸어올리는 자세로 %조사처리(TEXTS("겨드랑이：視覚"),"를")% 
			ELSE
				PRINTFORM \@ TALENT:팔많음 ? 손 # 양손 \@을 머리 뒤로 깍지끼고 %조사처리(TEXTS("겨드랑이：視覚"),"를")% 
			ENDIF
			IF RAND:2
				PRINTFORM 어필하면서, 
			ELSE
				PRINTFORM 과시하면서, 
			ENDIF
			IF COND("視力") && RAND:2
				PRINTFORMW 흥분한 얼굴로 %조사처리(CALLNAME:PLAYER,"를")% 바라보고있다…
			ELSE
				PRINTFORMW 가쁜 숨을 몰아쉬고 %조사처리(CALLNAME:PLAYER,"가")% 찔러오는 것을 받아들이고있다…
			ENDIF
			CALL SETFLAG, "겨드랑이見せ플래그", TARGET
		ELSEIF COND("다리 사용")
			IF RAND:2
				PRINTFORM 가랑이를 크게 벌리고 허리를 내밀며, 
				IF RAND:2
					PRINTFORMW 기쁘게 %조사처리(CALLNAME:PLAYER,"를")% 받아들이고있다…
				ELSE
					PRINTFORMW 탐욕스럽게 쾌감을 탐하고 있다…
				ENDIF
			ELSE
				IF COND("イきそう") && RAND:2
					PRINTFORMW 숨이 거칠어 지면서, 무의식적으로 양 다리를 %CALLNAME:PLAYER%의 허리에 감았다…
				ELSE
					PRINTFORMW %CALLNAME:PLAYER%의 허리 뒤로 다리를 감아, 가랑이를 밀어왔다…
				ENDIF
			ENDIF
		ELSE
			IF RAND:2
				PRINTFORM %조사처리(CALLNAME:PLAYER,"를")% 향해 허리를 내밀고, 
				IF RAND:2
					PRINTFORMW 기쁘게 피스톤 운동을 받아들이고있다…
				ELSE
					PRINTFORMW 탐욕스럽게 쾌감을 탐하고 있다…
				ENDIF
			ELSE
				PRINTFORMW %CALLNAME:PLAYER%에게 온몸을 밀어붙여왔다…
			ENDIF
		ENDIF
	CASE 70
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF RAND:2 && COND("손가락 사용") && CHECK_CLO("겨드랑이노출") && (COND("성벽：겨드랑이", PLAYER) || RAND:3 == 0)
			IF RAND:2
				PRINTFORM 머리카락을 쓸어올리는 자세로 %조사처리(TEXTS("겨드랑이：視覚"),"를")% 어필하면서, 
			ELSE
				PRINTFORM \@ TALENT:팔많음 ? 손 # 양손 \@을 머리 뒤로 깍지끼고 %조사처리(TEXTS("겨드랑이：視覚"),"를")% 과시하면서, 
			ENDIF
			IF COND("視力") && RAND:2
				PRINTFORMW 흥분한 얼굴로 %조사처리(CALLNAME:PLAYER,"를")% 바라보고있다…
			ELSE
				PRINTFORMW 가쁜 숨을 몰아쉬고 %조사처리(CALLNAME:PLAYER,"가")% 찔러오는 것을 받아들이고있다…
			ENDIF
			CALL SETFLAG, "겨드랑이見せ플래그", TARGET
		ELSEIF COND("다리 사용")
			PRINTFORMW Ｍ자로 다리를 벌리는 자세로 허리를 들고, 애널을 범하기 쉬운 자세를 취하고 즐거워하고있다…
		ELSE
			PRINTFORMW 허리를 들고, 애널을 범하기 쉬운 자세를 취하고 즐거워하고있다…
		ENDIF
	CASE 61, 71
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 쾌락에 몸부림치면서, 엉덩이를 높이 들고 %CALLNAME:PLAYER%의 격렬한 삽입을 즐기고있다…
	CASE 62, 72
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF RAND:2 && COND("손가락 사용") && CHECK_CLO("겨드랑이노출") && (COND("성벽：겨드랑이", PLAYER) || RAND:3 == 0)
			IF RAND:2
				PRINTFORM 머리카락을 쓸어올리는 자세로 %조사처리(TEXTS("겨드랑이：視覚"),"를")% 
			ELSE
				PRINTFORM \@ TALENT:팔많음 ? 손 # 양손 \@을 머리 뒤로 깍지끼고 %조사처리(TEXTS("겨드랑이：視覚"),"를")% 
			ENDIF
			IF RAND:2
				PRINTFORM 어필하면서, 
			ELSE
				PRINTFORM 과시하면서, 
			ENDIF
			IF COND("視力") && RAND:2
				PRINTFORMW 흥분한 얼굴로 %조사처리(CALLNAME:PLAYER,"를")% 바라보고있다…
			ELSE
				PRINTFORMW 가쁜 숨을 몰아쉬고 %조사처리(CALLNAME:PLAYER,"가")% 찔러오는 것을 받아들이고있다…
			ENDIF
			CALL SETFLAG, "겨드랑이見せ플래그", TARGET
		ELSEIF RAND:2 && ABL:노출벽 >= 3 && MARK:굴복각인 >= 2 && COND("손가락 사용")
			PRINTFORMW 자신의 다리로 다리를 껴안고, 적극적으로 부끄러운  체위를 즐기고있다…
		ELSE
			IF SELECTCOM == 72
				PRINTFORM 애널을 푹푹 쑤셔지면서, 
				IF PENIS(TARGET)
					PRINTFORMW 흥분한 얼굴로 %조사처리(CALLNAME:PLAYER,"를")% 바라보고있다…
				ELSE
					PRINTFORMW 흥분한 얼굴로 군침을 흘리고있는 자신의 보지를 바라보고있다…
				ENDIF
			ELSE
				PRINTFORMW 푹푹 쑤셔지는 보지를, 흥분하면서 바라보고있다…
			ENDIF
		ENDIF
	CASE 63, 73
		IF COND("손가락 사용")
			IF RAND:2 && CHECK_CLO("겨드랑이노출") && (COND("성벽：겨드랑이", PLAYER) || RAND:3 == 0)
				PRINTFORMW 목에손를 돌려 껴안아 왔다%CALLNAME:TARGET%(이)가 땀흘렸다%TEXTS("겨드랑이")%노%TEXTS("匂い")%하지만%CALLNAME:PLAYER%의 비강을 자극한다…
			ELSE
				PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 목에 팔을 감고 안겨왔다…
			ENDIF
		ENDIF
	CASE 64, 74
		SIF COND("손가락 사용")
			PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 등을 뒤로 젖히고 %CALLNAME:PLAYER%에게 응석을 부려왔다…
	ENDSELECT
;インサート中の애무
ELSEIF RAND:3 == 0 && SELECTCOM >= 0 && SELECTCOM <= 19 && INSERT("인서트") && (COND("イきそう") || RAND:5 == 0)
	;플라트なら無し
	SIF TEQUIP:플라트
		RETURN 0

	;ある程度の対応した部分の感度が필요
	SELECTCASE SELECTCOM
	CASE 0
		SIF MAX(ABL:Ｃ감각, ABL:Ｂ감각, ABL:Ｍ감각) < 3
			RETURN 0
	CASE 1, 7
		SIF ABL:Ｃ감각 < 3
			RETURN 0
	CASE 8
		SIF ABL:Ｖ감각 < 3
			RETURN 0
	CASE 2, 9
		SIF ABL:Ａ감각 < 3
			RETURN 0
	CASE 5
		SIF ABL:Ｂ감각 < 3
			RETURN 0
	CASE 6
		SIF ABL:Ｍ감각 < 3
			RETURN 0
	CASE 12
		SIF PENIS(TARGET) == 0 && ABL:Ｃ감각 < 3
			RETURN 0
	CASE 15
		SIF TALENT:요도성감 == 0
			RETURN 0
	ENDSELECT

	IF RAND:4 == 0
		PRINT 성감대를 찔렸다
	ELSEIF RAND:3 == 0
		PRINT 기분이 좋은 장소를 꾸짖을 수 있었다
	ELSEIF RAND:2
		PRINT 민감한 장소를 만지작거려졌다
	ELSE
		PRINT 삽입되면서의애무에
	ENDIF
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	IF RAND:3 == 0
		PRINT 오슬오슬 스며들고 건너는 쾌락에 전신을 진동시켜
	ELSEIF RAND:2
		PRINT 흠칫 떨린 후에 신체를 굳어지게 해
	ELSE
		PRINT 꿈틀 지체를 뛰게 해
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%의 것을
	IF RAND:3 == 0
		PRINTW 웅성거리는 점막으로 의도하지 않고 핥아대 왔다…
	ELSEIF RAND:2
		PRINTW 무의식 중에 단단히 조여 버린다…
	ELSE
		PRINT 한층 더 강하게 단단히 조여 온다…
	ENDIF
	CALL SETFLAG, "締め付け플래그", TARGET
ENDIF


@TRAIN_MESSAGE_A
#DIM MEMO_LINECOUNT
SIF FLAG:6 == 0
	RETURN 0

;ウェイトの発生判定
MEMO_LINECOUNT = LINECOUNT

;절정
IF NOWEX:절정수합계
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	;浴尿
	IF COND("飲尿플래그")
		PRINTFORM %CALLNAME:PLAYER%의 %TEXTR("오줌을 맛보는", "소변기의 역활을")%
	ELSEIF COND("자궁内욕뇨플래그")
		PRINTFORM %TEXTR("자궁", "소중한 부분")%%TEXTR("이 화장실 대신 사용되고", "소변을 뒤집어 받고")%
	ELSEIF COND("질내욕뇨플래그")
		IF RAND:3 == 0
			PRINTFORM 자궁구를 소변으로 자극 받아
		ELSEIF RAND:2
			PRINTFORM 질에서 소변으로 자극 받아
		ELSE
			PRINTFORM %TEXTR("질", "소중한 부분")%이 화장실로 되어
		ENDIF
	ELSEIF COND("온천관장플래그")
		IF RAND:3 == 0
			PRINTFORM 대장에서 소변이 흐릅니다
		ELSEIF RAND:2
			PRINTFORM 배설하는 장소에서 반대로 배설 당합니다
		ELSE
			PRINTFORM 장내부가 화장실이 된
		ENDIF
	ELSEIF COND("유内욕뇨플래그")
		IF RAND:3 == 0
			PRINTFORM 유방을 의 내부가 소변으로 자극
		ELSEIF RAND:2
			PRINTFORM 가슴을 에 소변 냄새가 난다
		ELSE
			PRINTFORM 가슴을 이 화장실이 되었다
		ENDIF
	ELSEIF COND("방광内욕뇨플래그")
		IF RAND:3 == 0
			PRINTFORM 로부터 반대로 소변을 흘려 넣어지고
		ELSEIF RAND:2
			PRINTFORM 배설하는 장소에서 반대로 배설 당합니다
		ELSE
			PRINTFORM 방광을 화장실로 되고
		ENDIF
	ELSEIF NOWEX:요도절정 && (NOWEX:요도절정 == EX:요도절정 || RAND:5 == 0)
		SELECTCASE SELECTCOM
		CASE 10
			;インサート中
			IF INSERT("요도인서트", TARGET, PLAYER)
				IF RAND:3 == 0
					PRINTFORM 성 처리의도구(와)과 같이 다루어지고
				ELSEIF RAND:2
					PRINTFORM 페니스를 %CALLNAME:PLAYER%의 오나홀로 되고
				ELSE
					PRINTFORM 페니스를 내외로부터 기분 좋게 여겨지고
				ENDIF
			ELSE
				PRINTFORM 페니스를 잡아당겨지고
			ENDIF
		CASE 15
			PRINTFORM 를 손가락으로 책망할 수 있고
		CASE 83
			IF TEQUIP:넘어뜨리기
				IF TALENT:요도광
					PRINTFORM 로부터 솟구치는 쾌락으로%TEXTR("頭を真っ白にし", "涎を垂らし", "夢中になって腰を振りたて")%
				ELSEIF PENIS(TARGET)
					PRINTFORM 페니스의 내부에서%CALLNAME:PLAYER%를 범해
				ELSE
					PRINTFORM 를 사용해섹스그래서%CALLNAME:PLAYER%를 범해
				ENDIF
			ELSE
				IF PENIS(TARGET)
					PRINTFORM 페니스를 격렬하다허리놀림그리고 범해지고
				ELSE
					PRINTFORM 를 격렬하다허리놀림그리고 범해지고
				ENDIF
			ENDIF
		CASE 151
			PRINTFORM 민감한 점막을 브지로 책망할 수 있고
		CASEELSE
			PRINTFORM 를 책망할 수 있고
		ENDSELECT
		;삽입ボーナス
		SIF INSERT("요도인서트") && PENIS(PLAYER)
			BASE:PLAYER:절정 += ABL:요도명기도 * 50
	ELSEIF INSERT("인서트") && PENIS(PLAYER) && RAND:3 == 0
		IF INSERT("대면좌위") && RAND:2
			PRINTFORM %조사처리(CALLNAME:PLAYER,"를")% 꼬옥 껴안고 
		ELSE
			IF INSERT("양구멍삽입")
				PRINT 양쪽의 구멍을 
				LOCAL = MIN(ABL:Ｖ명기도, ABL:Ａ명기도)
			ELSEIF INSERT("Ｖ인서트")
				PRINT 질을 
				LOCAL = ABL:Ｖ명기도
			ELSEIF INSERT("Ａ인서트")
				PRINT 애널을 
				LOCAL = ABL:Ａ명기도
			ELSEIF INSERT("요도인서트")
				PRINT 를
				LOCAL = ABL:요도명기도
			ENDIF
			IF LOCAL >= 80 && RAND:3 == 0
				PRINT 절묘한 힘조절로 
			ELSEIF LOCAL >= 60 && RAND:2
				PRINT 굉장한 힘으로 
			ELSEIF LOCAL >= 40
				PRINT 꽈아아악 
			ENDIF
			PRINT 조이
			BASE:PLAYER:절정 += LOCAL * 50
		ENDIF
	ELSEIF NOWEX:사정
		SELECTCASE SELECTCOM
		CASE 4
			PRINTFORM %CALLNAME:PLAYER%의 입에 \@ NOWEX:사정 >= 2 ? 한가득 # \@ 사정하
		CASE 11
			PRINTFORM %CALLNAME:PLAYER%의 가슴에 \@ NOWEX:사정 >= 2 ? 한가득 # \@ 사정하
		CASE 12
			PRINTFORM %CALLNAME:PLAYER%의 발에 \@ NOWEX:사정 >= 2 ? 한가득 # \@ 사정하
		CASE 37
			PRINTFORM 오나홀 안에 \@ NOWEX:사정 >= 2 ? 잔뜩 사정하 # 사정하 \@
		CASEELSE
			IF INSERT("Ｖ인서트", PLAYER, TARGET)
				PRINTFORM %CALLNAME:PLAYER%의 질에 \@ NOWEX:사정 >= 2 ? 대량으로 # \@ 사정하
			ELSEIF INSERT("Ａ인서트", PLAYER, TARGET)
				PRINTFORM %CALLNAME:PLAYER%의 장내에 정액을 \@ NOWEX:사정 >= 2 ? 대량으로 # \@ 토해내
			ELSE
				PRINTFORM 페니스에서 \@ NOWEX:사정 >= 2 ? 한가득 # \@ 정액을 분출하
			ENDIF
		ENDSELECT
	ELSEIF NOWEX:분유 && (NOWEX:분유 == EX:분유 || RAND:5 == 0)
		PRINT 가슴 끝에서 모유를 늘어뜨리
	ELSEIF TALENT:남자 == 0
		;潮噴き
		IF COND("潮噴き가능") && RAND:3 == 0
			IF TEQUIP:카테터
				PRINTFORM 요도에 삽입한 관에서 
			ELSEIF PENIS(TARGET)
				PRINTFORM 보지로부터
			ELSE
				PRINTFORM %TEXTR("보지", "요도구", "オシッコの穴")%로부터
			ENDIF
			PRINTFORM %TEXTR("투명한 액체", "투명한 조수", "뜨거운 물보라")%를 
			;요의게이지を減らす
			IF MAX(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:요도절정) + TALENT:오줌싸개버릇 >= 5 && BASE:요의 >= 10000 && RAND:3
				PRINT 화려하게 
				BASE:요의 -= RAND:5000 + 3000
			ELSEIF MAX(NOWEX:Ｃ절정, NOWEX:Ｖ절정, NOWEX:요도절정) + TALENT:오줌싸개버릇 >= 3 && BASE:요의 >= 6000 && RAND:3
				PRINT 기세좋게 
				BASE:요의 -= RAND:3000 + 2000
			ELSEIF RAND:2
				PRINT 퓻퓻 
				BASE:요의 -= RAND:2000 + 1000
			ELSE
				PRINT 퓻퓻 귀엽게 
				BASE:요의 -= RAND:1000 + 500
			ENDIF
			IF RAND:2
				PRINTFORM 내뿜으
			ELSE
				PRINTFORM %TEXTR("터뜨리", "분출하")%
			ENDIF
		ELSE
			IF NOWEX:절정수합계 >= 4 && CHECK_ECST(TARGET) >= 3 && RAND:2
				PRINT 전신을 누비는 쾌감에 
			ELSEIF NOWEX:절정수합계 >= 3 && RAND:2
				PRINT 격렬한 쾌감에 
			ELSEIF NOWEX:절정수합계 >= 2 && RAND:2
				PRINT 지나친 쾌감에 
			ELSEIF RAND:2
				IF COND("ながら자위플래그") || SELECTCOM == 3
					PRINT 쾌감에 
				ELSE
					PRINT 들이닥치는 쾌감에 
				ENDIF
			ENDIF
			IF RAND:3 == 0 && COND("입의 사용") && ORAL_PLAY() == 0
				IF RAND:2
					IF TALENT:강기 && NOWEX:절정수합계 == 1
						PRINT 이를 악물고 견디
					ELSE
						PRINT 입을 단정치 못하게 벌리
					ENDIF
				ELSE
					PRINT 입가에서 군침을 흘리
				ENDIF
			ELSEIF RAND:2 && COND("입의 사용") && ORAL_PLAY() == 0
				IF NOWEX:절정수합계 >= 3 && RAND:3
					PRINT 정신없이 절규하
				ELSEIF NOWEX:절정수합계 >= 2 && RAND:3
					IF TALENT:부끄럼쟁이
						PRINT 소리를 지를 것같은 입을 손으로 막으
					ELSE
						PRINT 외치듯이 소리를 내
					ENDIF
				ELSE
					IF RAND:2 && TALENT:호색
						PRINT 괴로운 신음 소리를 흘리
					ELSEIF TALENT:부끄럼쟁이 || RAND:3 == 0
						IF NOWEX:절정수합계 >= 2
							PRINT 무심코 교성을 지르
						ELSE
							PRINT 작게 신음을 흘리
						ENDIF
					ELSE
						PRINT 큰 소리를 내
					ENDIF
				ENDIF
			ELSE
				IF RAND:3 == 0
					IF ORAL_PLAY() == 0 && RAND:2
						PRINT 격렬하게 고개를 저으
					ELSE
						PRINTFORM \@ NOWEX:절정수합계 <= 2 ? 반쯤  # \@정신을 놓으
					ENDIF
				ELSEIF RAND:2 || NOWEX:Ｍ절정
					IF TALENT:강기 && NOWEX:절정수합계 == 1
						PRINT 미간을 모으
					ELSEIF TALENT:부끄럼쟁이 && RAND:2
						PRINT 눈을 꾹 감으
					ELSE
						PRINT 눈동자를 촉촉히 적시
					ENDIF
				ELSE
					IF NOWEX:Ｃ절정 + NOWEX:Ｖ절정 + NOWEX:Ａ절정 + NOWEX:요도절정 && RAND:2 == 0
						PRINT 배를 구불거리
					ELSE
						PRINT 등을 뒤로 크게 젖히
					ENDIF
				ENDIF
			ENDIF
		ENDIF
	ELSE
		PRINT 등을 뒤로 크게 젖히
	ENDIF
	PRINT 면서, 
	LOCAL = 0
	IF TALENT:거유 && RAND:3 == 0
		IF RAND:3 == 0
			PRINT 찰진 거유를 
		ELSEIF RAND:2
			PRINT 무거워보이는 가슴을 
		ELSE
			PRINT 풍만한 가슴을 
		ENDIF
		LOCAL = TALENT:거유
	ELSEIF RAND:2
		PRINT 전신을 
	ELSE
		SIF TALENT:작은몸집 && TALENT:유치
			PRINT 자그마한 
		PRINT 몸을 
	ENDIF

	IF LOCAL && RAND:2
		IF RAND:2
			IF INSERT("기승위")
				PRINT 출렁 
			ELSE
				PRINT 탱글탱글 
			ENDIF
		ELSE
			PRINT 화려하게 
		ENDIF
	ELSEIF RAND:3 == 0 || U
		PRINT 조금씩 
	ELSEIF RAND:2 == 0
		PRINT 바르르 
	ELSE
		PRINT 털썩털썩 
	ENDIF
	IF LOCAL && RAND:2
		PRINT 흔들어 
	ELSEIF RAND:3 == 0 || U
		PRINT 떨어 
	ELSEIF RAND:2 == 0
		PRINT 경련시켜 
	ELSE
		PRINT 튕겨
	ENDIF
	IF RAND:2
		PRINTL 절정에 달했다
	ELSE
		PRINTL 절정을 맞이했다 
	ENDIF
ENDIF

;역레이프で사정
IF INSERT("인서트", PLAYER, TARGET) && NOWEX:사정
	PRINTFORM %CALLNAME:PLAYER%의 
	IF INSERT("Ｖ인서트", PLAYER, TARGET)
		PRINT 육벽은, 
	ELSEIF INSERT("Ａ인서트", PLAYER, TARGET)
		PRINT 애널은, 
	ELSEIF INSERT("요도인서트", PLAYER, TARGET)
		PRINT 오줌의 구멍은,
	ENDIF
	SIF ABL:욕망 >= 3 && NOWEX:사정 >= 2
		PRINT 황홀한 표정을 띄우는 
	PRINTFORML %CALLNAME:TARGET%에게서 정액을 쥐어짜고있다…
;절정
ELSEIF NOWEX:절정수합계
	LOCAL = 0
	IF SELECTCOM == 8 && TEQUIP:바이브 == 0
		SIF RAND:(EXP_GAIN(71)*2 + 1) == 0
			LOCAL = 1
	ELSEIF INSERT("인서트")
		SIF RAND:(EXP_GAIN(71)*3 + 6) == 0
			LOCAL = 1
	ENDIF
	;손가락ちゅぱ(손가락넣기or인서트)
	IF COND("입의 사용") && ABL:순종 >= 3 && MARK:반발각인 == 0 && TEQUIP:넘어뜨리기 == 0 && LOCAL
		;LOCAL == 0 なら입に손가락を入れる. それ以外なら입元に差し出す. 
		LOCAL = RAND:3
		IF NOWEX:절정수합계 >= 4 && RAND:4 == 0
			PRINTFORM 녹을 듯한 쾌감에 빠져있는 
		ELSEIF NOWEX:절정수합계 >= 2 && RAND:3 == 0
			PRINTFORM 황홀해하고 있는 
		ELSEIF RAND:2
			PRINTFORM 만족스러운 
		ELSE
			PRINTFORM 숨을 고르고 있는 
		ENDIF
		PRINTFORM %CALLNAME:TARGET%의 
		IF LOCAL == 0
			PRINT 입에 
		ELSE
			PRINT 입가에 
		ENDIF
		IF CHECK_STAIN(STAIN:PLAYER:손, "정액")
			PRINT 정액투성이인 
		ELSEIF CHECK_STAIN(STAIN:PLAYER:손, "Ｖ")
			PRINT 애액으로 얼룩진 
		ELSEIF STAIN:PLAYER:손
			PRINT 체액으로 얼룩진 
		ENDIF
		IF RAND:2
			PRINT 손가락을 
		ELSE
			PRINT 집게 손가락을 
		ENDIF
		IF LOCAL == 0
			IF RAND:2
				PRINT 입에 물리
				LOCALS = る
			ELSE
				PRINT 밀어넣
				LOCALS = む
			ENDIF
		ELSE
			IF RAND:2
				PRINT 내밀
				LOCALS = す
			ELSE
				PRINT 갖다대
				LOCALS = る
			ENDIF
		ENDIF
		IF LOCAL == 0 && RAND:3 == 0
			PRINTFORM %TEXT_CONJ(LOCALS)% 점막을 간질이자, 
		ELSEIF STAIN:PLAYER:손 && ABL:봉사정신 >= 3
			PRINTFORM %TEXT_CONJ(LOCALS)% 깨끗하게 하도록 재촉하자, 
		ELSE
			PRINTFORM 자, 
		ENDIF
		IF TALENT:겁쟁이 + TALENT:청초 && RANDIF(2, TALENT:호색, TALENT:양기, TALENT:부끄럼쟁이, TALENT:헌신)
			PRINT 머뭇머뭇 
		ELSEIF TALENT:호색 && RANDIF(2, TALENT:양기, TALENT:부끄럼쟁이, TALENT:헌신)
			PRINT 즐거운듯이 
		ELSEIF TALENT:양기 && RANDIF(2, TALENT:부끄럼쟁이, TALENT:헌신)
			IF LOCAL == 0
				PRINT 덥석 
				LOCAL = 1
			ELSE
				PRINT 답례로 
			ENDIF
		ELSEIF TALENT:헌신 && RANDIF(2, TALENT:부끄럼쟁이)
			PRINT 부드럽게 
		ELSEIF TALENT:부끄럼쟁이 && RAND:2
			PRINT 부끄러운 듯이 
		ELSEIF MARK:굴복각인 >= 2 && RAND:3 == 0
			;펠라경험豊富
			IF LOCAL == 0 && ABL:욕망 >= 3 && INSERT("인서트") && EXP:펠라경험 >= 50 && PENIS(PLAYER) && RAND:3
				PRINT 흥분한 모습으로 
			ELSE
				PRINT 익숙해진 모습으로 
			ENDIF
		ELSEIF RAND:2
			PRINT 얌전하게 
		ELSE
			IF TALENT:헌신
				PRINT 부드럽게 
			ELSEIF TALENT:부끄럼쟁이
				PRINT 부끄러운 듯이 
			ELSE
				PRINT 나른하게 
			ENDIF
		ENDIF
		;손가락を咥える
		IF LOCAL
			PRINT 입에 물고
			IF COND("視力") && RAND:3 == 0
				PRINTFORM , %조사처리(CALLNAME:PLAYER,"를")% 
				IF MARK:굴복각인 >= 2
					PRINT 바라보면서 
				ELSE
					PRINT 흘끗흘끗 보면서 
				ENDIF
			ENDIF
		ENDIF
		;대상の입⇔조교자の손가락の汚れが移動
		CALL EQ_STAIN, TARGET, PLAYER, 0, 1
		;お청소ルート
		IF STAIN:PLAYER:손 && ABL:봉사정신 >= 3
			IF CHECK_STAIN(STAIN:PLAYER:손, "정액")
				PRINTL 달라붙은 정액을 깨끗하게 핥아냈다…
				EXP:정액경험 += 1
				PRINTFORML 정액경험+1
			ELSE
				PRINTL 할짝할짝 핥아서 닦았다…
			ENDIF
			STAIN:PLAYER:손 = 0
		ELSEIF RAND:3 == 0
			PRINTL 가볍게 깨물었다…
		ELSEIF RAND:2
			PRINTL 빨기 시작했다…
		ELSE
			PRINTL 혀를 기게했다…
		ENDIF
		;손가락ちゅぱ経験+1
		EXP:손가락핥기경험 += 1
	ELSEIF NOWEX:절정수합계 && (NOWEX:절정수합계 >= 2 || BASE:성욕 >= MAXBASE:성욕)
		PRINTFORM %CALLNAME:TARGET%
		IF COND("손가락 사용") && TALENT:남자 == 0 && RAND:3 == 0
			IF RAND:2
				PRINTFORM  %조사만처리(CALLNAME:TARGET,"는")% 얼굴을 
				IF EX:절정수합계 >= MAXBASE:성욕 && RAND:2
					PRINT 거듭되는 절정으로 
					SIF TALENT:쾌감에솔직
						PRINT 칠칠치못한 
				ELSE
					PRINT 쾌감으로 
				ENDIF
				IF NOWEX:절정수합계 >= 3 && RAND:2
					PRINT 무너뜨리고, 
				ELSE
					PRINT 풀고, 
				ENDIF
			ELSE
				IF TALENT:연모 && RAND:2
					PRINTFORM  %조사만처리(CALLNAME:TARGET,"는")% 행복한 표정으로, 
				ELSEIF NOWEX:절정수합계 >= 2 && RAND:2
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 황홀한 표정으로, 
				ELSE
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 기분좋은듯한 표정으로, 
				ENDIF
			ENDIF
			IF COND("입淫中")
				PRINTFORM 혀를 움직이는 일도 잊어
			ELSEIF PENIS(TARGET) && RANDIF(3, TALENT:빈유 == 0, NOWEX:Ｖ절정, NOWEX:Ｃ절정 +  NOWEX:요도절정)
				IF ABL:정액중독 >= 3 && NOWEX:사정 && COND("입의 사용") && INSERT("굴곡위") && RAND:2
					PRINT 커다랗게 입을 벌리고 스스로 정액을 받아들이면서 
					EXP:정액경험 += 1
					STAIN:Ｍ = SET_STAIN(STAIN:Ｍ, "정액")
				ELSEIF RAND:2
					PRINT 페니스를 만지작거리면서 
				ELSE
					PRINT 페니스를 천천히 훑으면서 
				ENDIF
			ELSEIF NOWEX:Ｃ절정 + NOWEX:Ｖ절정 + NOWEX:요도절정 && RANDIF(3, TALENT:빈유 == 0, NOWEX:Ｖ절정)
				;안면기승
				IF TEQUIP:넘어뜨리기 && TEQUIP:안면기승
					IF ABL:새드끼 >= 3 && RANDIF(3, ABL:자위중독 >= 3)
						IF RAND:2
							PRINTFORM %CALLNAME:PLAYER%에게 혀로 뒷처리를 시키면서 
						ELSE
							PRINTFORM %CALLNAME:PLAYER%에게 혀로 후희를 시키면서 
						ENDIF
					ELSEIF ABL:자위중독 >= 3 && RAND:3 == 0
						PRINTFORM %CALLNAME:PLAYER%의 얼굴에 기분 좋은 곳을 문지르면서
					ELSEIF TALENT:호색 && RAND:3 == 0
						PRINTFORM %CALLNAME:PLAYER%에게 벌름거리는 그곳을 과시하면서 
					ELSEIF RAND:2
						PRINTFORM %CALLNAME:PLAYER%의 얼굴 위에서 다리를 쫙 벌린채로 
					ELSE
						PRINTFORM %CALLNAME:PLAYER%의 얼굴에 걸터앉은 채로 
					ENDIF
				ELSE
					IF PENIS(TARGET) && RAND:2
						PRINTFORM 페니스를 
					ELSE
						PRINTFORM 가랑이를 
					ENDIF
					IF ABL:자위중독 && RAND:2
						IF TALENT:호색 + TALENT:음란 && RAND:2
							PRINT 만지작거리는 것을 과시하면서 
						ELSE
							PRINT 만지작거리면서 
						ENDIF
					ELSE
						PRINT 어루만지면서 
					ENDIF
				ENDIF
			ELSEIF NOWEX:Ｖ절정 && RANDIF(3, TALENT:빈유 == 0)
				IF RAND:2 && TALENT:연모
					PRINT 자궁 부근을 어루만지면서 
				ELSE
					PRINT 배를 어루만지면서 
				ENDIF
			ELSEIF TALENT:빈유 == 0 && RAND:3 == 0
				PRINT 가슴을 부드럽게 비비면서 
			ELSEIF RAND:2
				PRINT 손가락을 빨면서 
			ELSE
				PRINT 몸을 어루만지면서 
			ENDIF
		ELSEIF RAND:2
			IF INSERT("인서트") && COND("다리 사용") && RAND:3 == 0
				IF TEQUIP:넘어뜨리기
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%에게 걸터앉아 몸을 맡기면서, 
				ELSE
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 느슨하게 다리를 크게 벌린 채, 
				ENDIF
			ELSEIF RAND:2
				PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 단속적으로 절정을 반복하면서, 
			ELSE
				PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 때때로 몸을 펄떡펄떡 튕기면서, 
			ENDIF
		ELSE
			IF INSERT("인서트") && RAND:3 == 0
				IF COND("다리 사용") && RAND:2
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 천천히 허리를 움직이면서, 
				ELSE
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 육벽에서 끈적거리는 액체를 흘리면서, 허덕이듯이 음순을 벌름거리며, 
				ENDIF
			ELSEIF NOWEX:절정수합계 >= 3 && RAND:3 == 0
				IF RAND:2
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 흥분이 완전히 식지않은 모습으로 크게 숨을 쉬며, 
				ELSE
					PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 거친 숨을 몰아쉬면서, 
				ENDIF
			ELSEIF RAND:2
				PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 깊은 호흡으로 숨을 정돈하면서, 
			ELSE
				PRINTFORM %조사만처리(CALLNAME:TARGET,"는")% 만족스러운 달콤한 한숨을 내쉬면서, 
			ENDIF
		ENDIF
		IF RAND:3 == 0
			PRINTFORML \@ RAND:2 ? 추욱 # 녹초가 되어 \@ 처져있다…
		ELSE
			PRINTFORML \@ RAND:2 ? 쾌감 # 절정 \@의 여운에 \@ RAND:2 ? 잠겨 # 취해 \@있다…
		ENDIF
	ENDIF
ENDIF

IF NOWEX:절정수합계 + NOWEX:사정 >= RAND:3 + 1 && STATE("바인드：Ｐ", PLAYER)
	PRINTFORML …%CALLNAME:TARGET%의 절정으로 인해, 페니스의 구속이 약간 느슨해졌다! 
	CALL SETFLAG, "Ｐ바인드弱体플래그", PLAYER
ENDIF

;절정おもらし…이뇨제が無いと, まず실금はしない. 실금癖があると, しやすくなる. 
VARSET LOCAL
LOCAL = MAX(RAND:(CALCF("실금", TARGET) + 1) - 2, 0)
SIF CALCF("실금", TARGET) >= 8
	LOCAL = CALCF("실금", TARGET)

SIF LOCAL > 0
	PRINTL 

IF LOCAL >= 4 && ABL:욕망 >= 4 && RAND:2
	PRINTFORML %CALLNAME:TARGET%는 삐걱삐걱 신체를 경련시키면서 끝 없고\@ EXP_GAIN(80) ? 정액混じりの # \@오줌을 흘려 보냈다
	PRINTFORML 아무래도 배뇨행위 그 자체에 쾌락을 느기고 있는 것 같다…
;보지が노출していない
ELSEIF LOCAL >= 1 && !CHECK_CLO("Ｖ노출")
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 지나친 쾌감의 크기에 
	;겉옷(블루머とか)をはいている+ずらしていない+가랑이が노출していない
	IF CHECK_CLO("겉옷노출")
		PRINTFORM %조사처리(OUTER(TARGET, 1),"를")% 입은 채 
	;보디슈츠+가랑이をずらしていない+바텀을脱いでない+가랑이が노출していない
	ELSEIF CHECK_CLO("보디슈츠노출")
		PRINTFORM %조사처리(BODYS(TARGET, 1),"를")% 입은 채 
	;팬티스타킹+破ってない
	ELSEIF CHECK_CLO("팬티스타킹노출")
		PRINTFORM %조사처리(OUTER(TARGET, 1),"를")% 입은 채 
	;팬티+팬티をずらしていない+팬티を脱いでない+가랑이が노출していない
	ELSEIF CHECK_CLO("팬티노출")
		PRINTFORM %조사처리(PANTIES(TARGET, 1),"를")% 입은 채 
	ENDIF
	PRINTL 오줌을 지려버렸다
	SIF !CHECK_CLO("팬티스타킹노출")
		PRINTFORML 천의 옆으로부터 흘러넘친다\@ EXP_GAIN(80) ? 정액混じりの # \@오줌으로, 사타구니로부터 아래는 심한 상태가 되어 있다…
ELSEIF LOCAL >= 1 && TEQUIP:안면기승 && TALENT:남자 == 0
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	IF ABL:새드끼 >= 5
		IF NOWEX:절정수합계 >= 2 && RAND:2
			PRINT 쾌락에 떨면서, 
		ELSEIF TEQUIP:안면기승 == 1
			PRINT 가학적인 미소를 띄우면서, 
		ENDIF
	ELSEIF NOWEX:절정수합계 >= 2 && RAND:2
		PRINT 쾌락이 지나친 나머지, 
	ENDIF
	IF TEQUIP:안면기승 == 1 && ABL:새드끼 >= 5
		PRINTFORM %CALLNAME:PLAYER%의 입에 직접 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%의 얼굴에 올라탄 자세 그대로 
	ENDIF
	IF ABL:새드끼 >= 5
		PRINTL 방뇨하기 시작했다…
	ELSE
		PRINTL 오줌을 지려버렸다…
	ENDIF
ELSEIF LOCAL >= 3
	IF NOWEX:절정수합계 >= 8
		PRINT 경련하는 
	ELSEIF NOWEX:절정수합계 >= 4
		PRINT 축 늘어진 
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 요도구에서 분수처럼 오줌이 분출되었다…
ELSEIF LOCAL >= 2
	IF GETBIT(TEQUIP:구속, 21)
		PRINT 구속당한 다리를 크게 벌린 자세인 
	ELSEIF NOWEX:절정수합계 >= 4
		PRINT 축 늘어진 
	ENDIF
	IF TALENT:남자 || RAND:2 == 0
		PRINTFORML %CALLNAME:TARGET%의 요도구에서 물줄기가 방울지며 떨어져, 웅덩이를 만들었다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 오줌구멍에서 기세좋게 물줄기가 내뿜어져, 호를 그렸다…
	ENDIF
ELSEIF LOCAL >= 1
	IF RAND:3 == 0
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 지나친 쾌락에 실금하고 말았다…
	ELSEIF RAND:2
		PRINTFORML 절정에 떨고있는 %조사처리(CALLNAME:TARGET,"는")%, 요의를 억누르지 못하고 오줌을 싸버렸다…
	ELSE
		PRINTFORML 절정의 반동인지, 쾌락으로 느슨해진 작은 구멍에서 오줌이 조르르 흘러나왔다…
	ENDIF
ELSEIF RAND:3 == 0
	IF PENIS(TARGET) == 0 && CALCF("실금", TARGET) >= 5 && EXP_GAIN(31) + EXP_GAIN(33) == 0
		PRINTL 
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 무언가를 참고 있는 듯이 허벅지 안쪽을 긴장시키고있다. 
		PRINTFORML 아무래도, 소변이 새는것을 참고 있는 것같다…
	ENDIF
	LOCAL = 0
ENDIF

SIF LOCAL
	CALL EVENT_OMORASHI, LOCAL

;텍스트を読むためのウエイト（表示があるときのみ）
SIF LINECOUNT > MEMO_LINECOUNT
	WAIT

IF COND("액체관장") + COND("정액관장") && COND("애널栓") == 0
	;限界値を越えているかどうか
	IF COND("배설：限界")
		CALL SELF_KOJO, "플러그無し관장이벤트"
		;애널がふさがれていれば漏れない
		IF INSERT("Ａ인서트") == 0
			PRINTFORML 
			PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
			SELECTCASE SELECTCOM
			CASE 14
				PRINTFORM %CALLNAME:PLAYER%을 향해 애널을 넓히는 자세 인 채 견디지 못하고
			CASE 33
				PRINTFORM 애널을 막고 있던 물건이 움직이는데로 이끌려
			CASE 142, 143
				PRINTFORM 애널에서 엄청난 자극을 받은
			CASEELSE
				IF NOWEX:절정수합계
					PRINTFORM 절정의 쾌락에 견디지 못하고
				ELSE
					IF CUP:쾌Ａ >= 1000
						PRINTFORM 애널 자극을 견디지 못하고
					ELSE
						PRINTFORM 변의를 참지 못하고
					ENDIF
				ENDIF
			ENDSELECT
			SELECTCASE BASE:배설물
			CASE 0
				PRINTFORML 장내의 오염 액을\@ COND("대량관장") ? 성대하게 털어 놓아 # 흘리고 \@아차…
			CASE IS <= 2
				IF COND("정액관장")
					PRINTFORM 정액 범벅이 된
				ELSEIF COND("온천관장")
					PRINTFORM 소변이 섞인
				ELSE
					PRINTFORM 약물로 연화 된
				ENDIF
				PRINTFORMW 장내의 오물을 항문에서\@ COND("대량관장") ? 성대히 # \@뿌려 버렸다…
			CASE IS <= 6
				PRINTFORMW 장에 모아져 있던 몇 일 분이라고 여겨지는 오물 을\@ COND("정액관장") ? 쏟아 지고 정액과 함께 # \@굉장한 기세로 쏟아 버린다…
			CASEELSE
				PRINTFORMW \@ COND("정액관장") ? 정액 투성이가 된 # \@\@ RAND:2 ? 배설물 # 장내의 오물 \@을 성대하게 털어 버린
				PRINTFORMW 잠시 변비가 있었던 날이 있었는지, 처음에 부드러운 느낌의 물건이 굉장한 기세로 날라 간 후, \@ RAND:2 ? 그야말로 뭉쳐진 # 하나로 이어진 \@굵은 초코바 같은 오물이 천천히 시간이 지나면서 항문을 누르며 나오고 있다…
			ENDSELECT

			CALL SETFLAG, "배설"
			
			;배설した時のSOURCEを加算
			CALL SOURCE_COM136, 100

			CALL SELF_KOJO, "플러그無し관장이벤트"
		ENDIF
	ELSEIF COND("액체관장")
		PRINTFORML 
		SELECTCASE COND("액체관장")
		CASE 4
			IF ABL:마조끼 >= 5
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 고통과 쾌락에 풀어져 버린 얼굴로 배설감을 즐기고있다…
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 절망적인 양의 관장액을 간신히 버티고 있는 것 같다…
			ENDIF
		CASE 3
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 대량의\@ COND("온천관장") ? 소변을 받아버린 것 때문에 나오는 배설감 # 관장액 \@을 어떻게든 버티고 있는 것 같다…
		CASE 2
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 필사적으로 배설감을 버티고있다…
		CASE 1
			IF ABL:마조끼 >= 5
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 배설감에 견대면서도 때때로 부족한 듯한 눈으로 당신에게 향한다…
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 새어나오지 않도록 변의를 견디고있다…
			ENDIF
		ENDSELECT
		CALL SELF_KOJO, "플러그無し관장이벤트"
	ENDIF
ENDIF


;-------------------------------------------------
;ボイス停止
;-------------------------------------------------
SIF CONFIG("EmueraTunes")
	TRYCALL VSTOP


@EVENT_OMORASHI, ARG
IF ARG >= 2
	PRINTFORML %EXPNAME:33%＋2
	EXP:실금경험 += 2
	BASE:요의 = 0
ELSE
	PRINTFORML %EXPNAME:33%＋1
	EXP:실금경험 += 1
	BASE:요의 = MAX(BASE:요의 - 20000, 0)
ENDIF
TEQUIP:이뇨제 = 0

;방뇨のソース獲得
CALL SOURCE_COM144, 80

;절정방뇨
NOWEX:절정실금 = ARG
EX:절정실금 += ARG
;尿の汚れ発生
IF TALENT:남자 == 0
	CALL ADD_STAIN, TARGET, "Ｖ", "尿"
	STAIN:Ｖ = SET_STAIN(STAIN:Ｖ, "尿")
	;성수플레이
	SIF TEQUIP:안면기승
		STAIN:PLAYER:Ｍ = SET_STAIN(STAIN:PLAYER:Ｍ, "尿")
ENDIF

;-------------------------------------------------
;럭셔리어스히트と황홀判定
;ARGSは今のところ"커맨드"と"정액"がある
;PLAYERかTARGETかどうかで別物だけどここで一括
;-------------------------------------------------
@LUXURIOUS_HIT, ARGS, ARG
#DIM LCOUNT
#DIM WEEKNESS

SIF ARG == 0 && TARGET
	ARG = TARGET

WEEKNESS = 0
VARSET LOCAL

SELECTCASE ARGS
CASE "커맨드"
	SELECTCASE ARG
	CASE PLAYER
		;今のところ플레이ヤーで判定が起きるのは주인だけ
		SIF PLAYER != MASTER
			RETURN 0

		;성감대の処理
		SELECTCASE SELECTCOM
		;손에약함
		CASE 90
			SETBIT WEEKNESS, 0
		CASE 97
			SETBIT WEEKNESS, 0
			SIF TALENT:남자 == 0
				SETBIT WEEKNESS, 4
		;입에약함
		CASE 91, 96, 98, 99, 140
			SETBIT WEEKNESS, 1
		;가슴에약함
		CASE 5, 81, 92, 104
			SETBIT WEEKNESS, 2
		;다리에약함
		CASE 95
			SETBIT WEEKNESS, 3
		;♀에약함
		CASE 1
			SIF TEQUIP:넘어뜨리기 || TEQUIP:안면기승 || TEQUIP:식스나인
				SETBIT WEEKNESS, 4
		CASE 13, 65, 99
			SETBIT WEEKNESS, 4
		;키스에약함
		CASE 6
			SIF NUM("턴수")
				SETBIT WEEKNESS, 5
		CASE 63, 73
			SIF CHECK_COM("키스") && TALENT:연모
				SETBIT WEEKNESS, 5
		;애널에약함
		CASE 9
			SIF TEQUIP:넘어뜨리기 || TEQUIP:안면기승 || TEQUIP:식스나인
				SETBIT WEEKNESS, 6
		CASE 14, 75
			SETBIT WEEKNESS, 6
		;♂에약함
		CASE 4
			SIF TEQUIP:넘어뜨리기 || TEQUIP:안면기승 || TEQUIP:식스나인
				SETBIT WEEKNESS, 7
		CASE 120 TO 129
			SETBIT WEEKNESS, 7
		ENDSELECT

		;자위中
		SELECTCASE TEQUIP:PLAYER:자위한다
		;컨닐링구스
		CASE 1
			SETBIT WEEKNESS, 4
		;자위
		CASE 3

		;펠라한다
		CASE 4
			SETBIT WEEKNESS, 7
		;애널핥기
		CASE 9
			SETBIT WEEKNESS, 6
		;조개벌리기
		CASE 13
			SETBIT WEEKNESS, 4
		;Ａ조개벌리기
		CASE 14
			SETBIT WEEKNESS, 6
		ENDSELECT

		LOCAL = TCVAR:PLAYER:101

		VARSET LOCALS
		FOR LCOUNT, 0, NUM("성감대")
			SIF GETBIT(TALENT:MASTER:성감대, LCOUNT) == 0 || GETBIT(WEEKNESS, LCOUNT) == 0
				CONTINUE
			LOCAL:1 += 1
			SIF RAND:(LOCAL:1)
				CONTINUE
			LOCALS = %NAMES("성감대", LCOUNT)%
			LOCAL += 1000 * LOCAL:1
		NEXT

		SIF LOCALS == ""
			RETURN 0

		;모유を飲んでいると황홀度合い上昇
		LOCAL = MULTIPLY(LOCAL, 100 + EXP_GAIN(74, PLAYER)*10)

		;성벽合致度
		FOR LCOUNT, 0, NUM("속성")
			SIF GETBIT(BASE:속성, LCOUNT) && GETBIT(TALENT:PLAYER:성벽, LCOUNT)
				LOCAL += LOCAL*20/100 + 1000
		NEXT
		;마음에듬or상사상애
		IF PLAYER == MASTER
			IF TALENT:상사상애
				LOCAL += LOCAL*30/100 + 2000
			ELSEIF TALENT:마음에듬
				LOCAL += LOCAL*20/100 + 1000
			ENDIF
		ENDIF

		SIF LOCAL < 1000 || LOCAL + BASE:PLAYER:절정 < MAX(MAXBASE:PLAYER:절정/4, 5000) || RAND:3
			RETURN 0

		SETCOLOR DEF_COLOR("하트핑크")

		PRINTFORML ＜럭셔리어스 히트! （%CALLNAME:PLAYER%）＞
		PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% %CALLNAME:TARGET%에게 빠져버리고 말았다! 

		NOWEX:PLAYER:럭셔리어스히트 = 1
		EX:PLAYER:럭셔리어스히트 += 1
		BASE:PLAYER:절정 += LOCAL
		TCVAR:PLAYER:101 += LOCAL

		IF LOCAL >= MAX(MAXBASE:PLAYER:절정/3, 5000) && RAND:2 == 0
			PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% 황홀한 기분이 되어버렸다……
			TEQUIP:넘어뜨리기 = 1
			CALL ADD_STATE, PLAYER, "황홀"
			FORCEWAIT
		ENDIF
		RESETCOLOR
	CASE TARGET
		;성감대等を用意してから설정する（PALAM:연속절정COMBO수でも判定）
		;まずは성감대を突かれたかで判定
		;GET_PALAMLV(ARG)の値は、順に1:100,2:500,3:3000,4:1万,5:3万,6:6万,7:10万,8:15万,9:25万

		;快感での범용判定
		SIF TALENT:Ｃ민감 || TALENT:음핵
			LOCAL = MAX(GET_PALAMLV(CUP:쾌Ｃ), LOCAL)
		SIF TALENT:Ｖ민감 || TALENT:음호
			LOCAL = MAX(GET_PALAMLV(CUP:쾌Ｖ), LOCAL)
		SIF TALENT:Ａ민감 || TALENT:애널광
			LOCAL = MAX(GET_PALAMLV(CUP:쾌Ａ), LOCAL)
		SIF TALENT:Ｂ민감 || TALENT:음유
			LOCAL = MAX(GET_PALAMLV(CUP:쾌Ｂ), LOCAL)
		SIF TALENT:Ｍ민감 || TALENT:음탕한입술
			LOCAL = MAX(GET_PALAMLV(CUP:쾌Ｍ), LOCAL)


		;ラグジャラス判定
		SIF LOCAL <= RAND:10 || RAND:3
			RETURN 0

		SETCOLOR DEF_COLOR("하트핑크")

		PRINTFORML ＜럭셔리어스 히트!＞
		PRINTFORML %조사처리(CALLNAME:PLAYER,"는")% %CALLNAME:TARGET%의 약점을 찔렀다! 

		NOWEX:럭셔리어스히트 = 1
		EX:럭셔리어스히트 += 1

		SIF TALENT:Ｃ민감 || TALENT:음핵
			TIMES CUP:쾌Ｃ, 2.00
		SIF TALENT:Ｖ민감 || TALENT:음호
			TIMES CUP:쾌Ｖ, 2.00
		SIF TALENT:Ａ민감 || TALENT:애널광
			TIMES CUP:쾌Ａ, 2.00
		SIF TALENT:Ｂ민감 || TALENT:음유
			TIMES CUP:쾌Ｂ, 2.00
		SIF TALENT:Ｍ민감 || TALENT:음탕한입술
			TIMES CUP:쾌Ｍ, 2.00

		;IF LOCAL*10 + PALAM:연속절정COMBO수*5 > RAND:100 && RAND:2 == 0
		;	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 황홀としている……
		;	CALL ADD_STATE, TARGET, "황홀"
		;	FORCEWAIT
		;ENDIF
		RESETCOLOR
	ENDSELECT
CASE "정액"
	;가는것을참았다状態で無いor既に절정済み
	SIF NOWEX:절정수합계 || NOWEX:가는것을참았다 == 0
		RETURN 0
	;정액に弱くないなら、정액が竜속성でない限り、ここで終了
	SIF TALENT:정액에약함 == 0 && TALENT:PLAYER:용의혈통 == 0
		RETURN 0

	IF SHOOT("입")
		SIF COND("정음에약함") == 0 && TALENT:PLAYER:용의혈통 == 0
			RETURN 0
		SIF EXP_GAIN(19) && RAND:3
			RETURN 0
		TIMES CUP:쾌Ｍ, 2.00
	ELSEIF SHOOT("Ｖ내사정")
		SIF COND("질내사정에약함") == 0 && TALENT:PLAYER:용의혈통 == 0
			RETURN 0
		SIF EXP_GAIN(17) && RAND:3
			RETURN 0
		TIMES CUP:쾌Ｖ, 2.00
	ELSEIF SHOOT("Ａ내사정")
		SIF COND("장내사정에약함") == 0 && TALENT:PLAYER:용의혈통 == 0
			RETURN 0
		SIF EXP_GAIN(18) && RAND:3
			RETURN 0
		TIMES CUP:쾌Ａ, 2.00
	ELSEIF SHOOT("니플퍽")
		SIF COND("유内사정에약함") == 0 && TALENT:PLAYER:용의혈통 == 0
			RETURN 0
		SIF EXP_GAIN(47) && RAND:3
			RETURN 0
		TIMES CUP:쾌Ｂ, 2.00
	ELSEIF SHOOT("요도中出し")
		SIF COND("요도内사정에약함") == 0 && TALENT:PLAYER:용의혈통 == 0
			RETURN 0
		SIF EXP_GAIN(80) && RAND:3
			RETURN 0
		NOWEX:요도절정 = 1
		EX:요도절정 += 1

		TIMES CUP:Ｖ윤, 2.00
		TIMES CUP:욕정, 2.00
		TIMES CUP:굴복, 2.00
		TIMES CUP:치정, 2.00
	ENDIF

	NOWEX:정액절정 = 1
	EX:정액절정 += 1
	NOWEX:럭셔리어스히트 = 1
	EX:럭셔리어스히트 += 1
	NOWEX:가는것을참았다 = 0
	EX:가는것을참았다 = 0

	CALL ORGASM_ADD_A
	CALL ORGASM_ADD_C

	SETCOLOR DEF_COLOR("하트핑크")

	PRINTFORML ＜럭셔리어스 히트! ＞

	IF SHOOT("입")
		IF COND("절정", PLAYER) >= 2 && RAND:2 
			PRINTFORM 대량으로 정액을 먹게 된
		ELSE
			IF COND("절정", PLAYER) >= 2 && RAND:2
				PRINTFORM %TEXTR("대량의", "벅찬", "굉장한 양의")%
			ELSE
				PRINTFORM %TEXTR("힘차게", "뜨거운", "짙은")%
			ENDIF
			IF SELECTCOM == 140
				PRINTFORM 정액%TEXTR("을 식도로 넘기게 된", "을 목구멍에 덮어 쓰게 된")%
			ELSE
				PRINTFORM 정액%TEXTR("으로 입안이 가득 했다", "을 입에 흘려 넣어 지게 된", "을 목구멍에 덮어 쓰게 된")%
			ENDIF
		ENDIF
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 
		PRINTFORM %TEXTR("기분이 너무 좋아", "참지 않고", "흥분한 나머지", "신체를 떨며", "코에서 뜨거운 한숨을 내쉬며", "참지 못하고")%
		PRINTFORMW 절정했다! 
	ELSEIF SHOOT("Ｖ내사정")
		IF BASE:자궁정자량 < 100 && NOWEX:Ｖ내사정 == EX:Ｖ내사정 && RAND:2
			PRINTFORM 굶주린 자궁에 정액을 받게된
		ELSEIF RAND:2
			IF COND("절정", PLAYER) >= 2 && RAND:2
				PRINTFORM %TEXTR("대량의", "벅찬", "굉장한 양의")%
			ELSE
				PRINTFORM %TEXTR("힘차게", "뜨거운", "짙은")%
			ENDIF
			PRINTFORM 정액%TEXTR("이 자궁을 두드렸다", "을 질내에 흘리게 되어", "을 성감대에 덮어 씌워 진")%
		ELSEIF COND("절정", PLAYER) >= 2 && RAND:2
			PRINTFORM 대량으로 정액을 안에 싼
		ELSE
			PRINTFORM 좋아하는 질의 안에 싼
		ENDIF
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 
		PRINTFORM %TEXTR("気持ちよさのあまり", "堪えきれずに", "興奮のあまり", "신체를 震わせて", "熱い吐息を漏らしながら", "我慢できずに")%
		PRINTFORMW 절정해 버렸다! 
	ELSEIF SHOOT("Ａ내사정")
		IF RAND:2
			IF COND("절정", PLAYER) >= 2 && RAND:2
				PRINTFORM %TEXTR("大量の", "溢れんばかりの", "凄い量の")%
			ELSE
				PRINTFORM %TEXTR("勢いよく", "熱い", "濃厚な")%
			ENDIF
			PRINTFORM 정액%TEXTR("で直장の奥を叩かれた", "を애널に流し込まれた", "を성감대に浴びせかけられた")%
		ELSEIF COND("절정", PLAYER) >= 2 && RAND:2
			PRINTFORM 대량으로애널에내사정를 되었다
		ELSE
			PRINTFORM 정말 좋아하는 항내사정를 되었다
		ENDIF
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 
		PRINTFORM %TEXTR("気持ちよさのあまり", "堪えきれずに", "興奮のあまり", "신체를 震わせて", "熱い吐息を漏らしながら", "我慢できずに")%
		PRINTFORMW 절정해 버렸다! 
	ELSEIF SHOOT("니플퍽")
		IF RAND:2
			IF COND("절정", PLAYER) >= 2 && RAND:2
				PRINTFORM %TEXTR("大量の", "溢れんばかりの", "凄い量の")%
			ELSE
				PRINTFORM %TEXTR("勢いよく", "熱い", "濃厚な")%
			ENDIF
			PRINTFORM 정액를%TEXTR("ニプルにご馳走された", "유방の中に流し込まれた", "성감대に注がれた")%
		ELSEIF COND("절정", PLAYER) >= 2 && RAND:2
			PRINTFORM 대량으로유방에내사정를 되었다
		ELSE
			PRINTFORM 정말 좋아하는유내사정를 되었다
		ENDIF
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 
		PRINTFORM %TEXTR("気持ちよさのあまり", "堪えきれずに", "興奮のあまり", "신체를 震わせて", "熱い吐息を漏らしながら", "我慢できずに")%
		PRINTFORMW 절정해 버렸다! 
	ELSEIF SHOOT("요도中出し")
		IF RAND:2
			IF COND("절정", PLAYER) >= 2 && RAND:2
				PRINTFORM %TEXTR("大量の", "溢れんばかりの", "凄い量の")%
			ELSE
				PRINTFORM %TEXTR("勢いよく", "熱い", "濃厚な")%
			ENDIF
			PRINTFORM 정액를%TEXTR("大好きな場所にご馳走された", "オシッコの穴に流し込まれた", "성감대に注がれた")%
		ELSEIF COND("절정", PLAYER) >= 2 && RAND:2
			PRINTFORM 대량으로방광에의내사정를 되었다
		ELSE
			PRINTFORM 정말 좋아하는요도에의사정를 되었다
		ENDIF
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")%, 
		PRINTFORM %TEXTR("気持ちよさのあまり", "堪えきれずに", "興奮のあまり", "신체를 震わせて", "熱い吐息を漏らしながら", "我慢できずに")%
		PRINTFORMW 절정해 버렸다! 
	ENDIF
	RESETCOLOR
	CALL ORGASM_ADD_TEXT, TARGET
ENDSELECT


@EVENT_EXSTART
TEQUIP:역조교 = 1
TEQUIP:PLAYER:역조교 = 1
PRINTFORML 
DRAWLINE
PRINTFORML 
ALIGNMENT CENTER
TRYCALLFORM KOJO_EVENT_EXSTART_{NO:TARGET}_{CFLAG:209}, "タイトル"
ALIGNMENT LEFT
PRINTFORML 
DRAWLINE
;ループ開始
$EX_LOOP
NOWEX:폭발 = 0
CALL EVENT_EXPLAY
TRYCALLFORM KOJO_EVENT_EXSTART_{NO:TARGET}_{CFLAG:209}, "플레이直後"
TRYCALLFORM KOJO_EVENT_EXSTART_{NO:TARGET}_{CFLAG:209}, "선택지"

SIF GETBIT(TEQUIP:역조교, 1)
	GOTO EX_END

;정력が無くなり, かつ스톡が有るなら정력補充
IF BASE:PLAYER:정력 <= 0 && BASE:PLAYER:스톡
	BASE:PLAYER:스톡 -= 1
	BASE:PLAYER:정력 = MAXBASE:PLAYER:정력
	PRINTFORML %CALLNAME:PLAYER%의 정력이 회복되었다! （시켜졌다! ）
	PRINTFORML 
	SETCOLOR DEF_COLOR("노랑")
	PRINTFORML %CALLNAME:PLAYER%의 정력잔량 …… {BASE:PLAYER:정력}! 
	PRINTFORMW 스톡 잔량 …… {BASE:PLAYER:스톡}! 
	RESETCOLOR
	PRINTFORML 
ENDIF
TRYCALLFORM KOJO_EVENT_EXSTART_{NO:TARGET}_{CFLAG:209}, "플레이一区切り"

SIF BASE:PLAYER:정력 > 0
	GOTO EX_LOOP

$EX_END
;스톡が0（かつ정력が0이하）
IF BASE:PLAYER:정력 <= 0 && BASE:PLAYER:스톡 == 0 && RAND:3
	;쾌락각인・음란を取得
	DRAWLINE
	PRINTFORML 
	TRYCALLFORM KOJO_KAIRAKU_{NO:TARGET}_{CFLAG:209}
	PRINTFORML 
	DRAWLINE
ENDIF


;-------------------------------------------------
;快楽조교関数
;몸을맡김からの역조교で使用
;봉사系メイン
;-------------------------------------------------
@EVENT_EXPLAY
PRINTFORML 
;조교メニュー일람
VARSET LOCALS
LOCAL = ABL:기교 * 1000 + MAX(ABL:ＬＶ - ABL:PLAYER:ＬＶ, 0) * 1000 + ABL:ＬＶ * 500 + (MARK:PLAYER:쾌락각인 + TALENT:PLAYER:음란) * 1500

TRYCALLFORM SET_EVENT_EXPLAY_{NO:TARGET}_{CFLAG:209}
LOCALS = %STR:100%

SELECTCASE LOCALS
CASE "손가락挿れ", "바이브責め", "자위"
	LOCAL += ABL:손가락놀림 * 1000 + EXP:봉사쾌락경험 + RAND:10
	EXP:수음경험 += ABL:기교/2 + 1
CASE "快楽레이프"
	LOCAL += ABL:허리놀림 * 1000 + EXP:봉사쾌락경험 + RAND:10
	EXP:Ｖ삽입경험 += ABL:기교/2 + 1
	EXP:PLAYER:Ｖ성교경험 += ABL:TARGET:기교/2 + 1
	EXP:PLAYER:Ｖ경험 += ABL:TARGET:기교/2 + 1
CASE "컨닐링구스"
	LOCAL += ABL:혀놀림 * 1000 + EXP:봉사쾌락경험 + RAND:10
	EXP:커닐링구스경험 += ABL:기교/2 + 1
CASE "펠라치오"
	LOCAL += ABL:혀놀림 * 1000 + EXP:봉사쾌락경험 + RAND:100 + RAND:10
	EXP:펠라경험 += ABL:기교/2 + 1
CASE "파이즈리"
	LOCAL += (ABL:혀놀림 + ABL:마유) * 1000 +  EXP:봉사쾌락경험 + RAND:10
	EXP:파이즈리경험 += ABL:기교/2 + 1
CASE "수음", "오나홀훑기"
	LOCAL += ABL:손가락놀림 * 1000 + EXP:봉사쾌락경험 + RAND:10
	EXP:수음경험 += ABL:기교/2 + 1
CASE "키스"
	LOCAL += ABL:혀놀림 * 1000 + EXP:봉사쾌락경험 + RAND:10
	EXP:키스경험 += ABL:기교/2 + 1
	EXP:PLAYER:키스경험 += ABL:기교/2 + 1
CASE "가슴애무", "搾乳플레이"
	LOCAL += (ABL:손가락놀림 + ABL:마유) * 1000 + EXP:봉사쾌락경험 + RAND:10
	EXP:수음경험 += ABL:기교/2 + 1
CASE "꼬리페니스자극", "꼬리責め"
	LOCAL += ABL:기교 * 1000 + MIN(EXP:봉사쾌락경험 + CFLAG:2, MAXBASE:PLAYER:절정) + RAND:1000
	LOCAL *= 9
	EXP:가학쾌락경험 += ABL:기교*9
ENDSELECT
;その他経験
IF TALENT:남자 == 0 && TALENT:PLAYER:남자 == 0
	EXP:레즈경험 += RAND:5 + 1
	EXP:PLAYER:레즈경험 += RAND:5 + 1
ELSEIF TALENT:남자 && TALENT:PLAYER:남자
	EXP:ＢＬ경험 += RAND:5 + 1
	EXP:PLAYER:ＢＬ경험 += RAND:5 + 1
ENDIF
EXP:가학쾌락경험 += ABL:기교

TRYCALLFORM KOJO_EVENT_EXPLAY_{NO:TARGET}_{CFLAG:209}, LOCALS

LOCAL = MULTIPLY(CALCF("基礎보정", LOCAL), CALCF("快楽보정", 100) )

CALL TEXT, "절정計算", LOCAL
;절정できなかった
IF CALCF("절정", PLAYER) == 0
	RETURN 0
;절정した
ELSE
	LOCALS:1 = 절정：%LOCALS%
	TRYCALLFORM KOJO_EVENT_EXPLAY_{NO:TARGET}_{CFLAG:209}, LOCALS:1

	CALL BOUHATSU
	;追撃判定
	;残정력20이하だと一定확률で追撃, それ이상だとより低확률で追撃. それとは別に스톡でも判定
	IF (BASE:PLAYER:정력 <= 20 && RAND:2) || (BASE:PLAYER:정력 > 20 && RAND:4 == 0) || (BASE:PLAYER:스톡 && RAND:5 == 0)
		IF BASE:PLAYER:스톡 <= 0 && BASE:PLAYER:정력 <= 0
			PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 이미 한계지만, %조사처리(CALLNAME:TARGET,"는")% 그 손을 풀지 않는다! 
		ELSEIF BASE:PLAYER:정력 <= 0
			PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 정력을 남김없이 %CALLNAME:TARGET%에가 바쳤다! 
		ELSE
			PRINTFORMW %CALLNAME:PLAYER%의 정력잔량 …… {BASE:PLAYER:정력}! 
		ENDIF
		PRINTFORML 

		LOCALS:1 = 追撃：%LOCALS%
		TRYCALLFORM KOJO_EVENT_EXPLAY_{NO:TARGET}_{CFLAG:209}, LOCALS:1

		;追撃された場合は必殺
		LOCAL = 100000 + EXP:봉사쾌락경험 + RAND:10000
		CALL TEXT, "절정計算", LOCAL
		CALL BOUHATSU

		;余力がある場合はさらに追撃判定(CALL BOUHATUなどはこの台詞の中で行う)
		IF BASE:PLAYER:정력 >= 10
			LOCALS:1 = ＥＸ追撃：%LOCALS%
			TRYCALLFORM KOJO_EVENT_EXPLAY_{NO:TARGET}_{CFLAG:209}, LOCALS:1
		ENDIF

		IF BASE:PLAYER:정력 <= 0 && BASE:PLAYER:스톡 == 0 
			IF PENIS(PLAYER) && RAND:4 == 0
				PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 완전히 바닥을 드러낼 때까지 %CALLNAME:TARGET%에게 빨아먹혔다! 
			ELSEIF RAND:3 == 0
				PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 정력을 송두리째 %CALLNAME:TARGET%에게 짜여져버렸다! 
			ELSEIF RAND:2 == 0
				PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 모든 정력을 %CALLNAME:TARGET%에게 맛있게 먹히고 말았다! 
			ELSE
				PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 완전히 새하얗게 불타버렸다! 
			ENDIF
		ELSEIF BASE:PLAYER:정력 <= 0
			PRINTFORMW %조사처리(CALLNAME:PLAYER,"는")% 정력을 남김없이 %CALLNAME:TARGET%에가 바쳤다! 
		ELSE
			PRINTFORMW %CALLNAME:PLAYER%의 정력잔량 …… {BASE:PLAYER:정력}! 
		ENDIF
		PRINTFORML 
	ENDIF
ENDIF
